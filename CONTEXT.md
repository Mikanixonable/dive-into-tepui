# Dive into Tepui Context

船体モジュール、ドッキング、建造、分離に関するゲーム上の用語を一つに揃える。

## 船体と能力

**ship**:
健全な cockpit を1個以上持つ接続グラフの、ユーザー向け分類。`vessel` は一般文でのみ使い、正規のドメイン語にはしない。
_Avoid_: vessel（正規のドメイン語として）

**cockpit**:
操縦能力を与える船体モジュール。健全な cockpit を1個以上持つ接続グラフだけが、ユーザー向けに「船」と分類され、操縦命令を受けられる。
cockpit が不健全または不在でも、残ったモジュールは「物資」として存続する。既存の名称を維持し、船体モジュール名としての `command` は使わない。
_Avoid_: command（船体モジュール名として）

**tank**:
両端に接続点を持つ直径6mの円筒船体モジュール。長さは3m・6m・12mで、内容物は `fuelType: main | rcs` で通常燃料またはRCS燃料を選ぶ。文献調査に基づくMVP仮採用値は有効容量80・160・320 m³相当で、長さに比例する。thrusterはmain燃料を、RCSはrcs燃料を消費し、boosterはtankを持たず内部固体燃料を消費する。

**thruster**:
主推進を担う船体モジュール。旧称・同義語の `engine` は船体モジュール名として使わない。
_Avoid_: engine（船体モジュール名として）

**booster**:
タンクを持たず固体燃料を内部に持つ、ノズル付き円筒モジュール。点火後は停止・再点火せず燃料切れまで燃焼する。役割名ではなく部品名である。

**物資**:
健全な cockpit を持たない接続グラフ。船ではないため操縦できないが、船と同じ物理・保存単位として漂流・ドッキングでき、健全な船から操作された dock を介して建造・修理を受けられる。

**ShipAssembly**:
モジュールと接続 edge からなる一つの接続グラフ。健全な cockpit の有無にかかわらず、船と物資の物理・保存状態を表す。

**module definition / module instance**:
`module definition` はカタログに登録された設計定義、`module instance` は船体へ取り付けられた個体。HP、燃料、点火などの可変状態は module instance が持つ。

**dock**:
船または物資を接続する通常の `docking_port` の上位互換。接続機能に加えて、健全な cockpit を持つ船から操作された場合に建造・更新・修理を行える。
`docking_port` 同士、`docking_port` と `dock`、`dock` 同士の全組み合わせで接続できる。

**docking_port**:
船または物資を接続するための端部モジュール。建造・修理機能は持たない。
接続後は他の船体モジュール列と一つの船体へ統合される。

**ドッキング中の船体**:
ドッキングによって複数の船体モジュール列が接続され、複数のcockpit、dock、構造材を含む一つの巨大な船体になった状態。
接続前の船という単位は保持せず、docking portを分離した時に独立した船体へ戻る。

**部品集合**:
接続グラフで結ばれたモジュールからなる構成単位。健全な cockpit があれば船、なければ物資として分類する。
物資は構造上の完成条件を満たせば cockpit なしでも分離でき、分離・発進の確定前に操縦不能であることを警告する。

**構造上の完成**:
操縦能力の有無とは独立した、部品集合をドックから分離できる状態。少なくとも1個の module instance と有効な接続グラフがあれば成立し、cockpit、tank、thruster、docking_port は必須ではない。

**統合船体の建造**:
ドッキング後の一つのShipAssemblyに含まれる任意の健全なdockから、その接続軸に沿って接続グラフへモジュールを直列追加・撤去・修理する操作。撤去は各分岐の自由端から行い、一回の建造セッションでは選択 dock から伸びる一枝だけを編集する。物資側の dock は、健全な船から操作されている場合に使える。

**統合船体の能力集計**:
統合船体内のすべての健全なモジュールを対象に、推進・燃料・武装・放熱・発電・dockなどを集計する。cockpitは操作基準の候補であり、選択されていないcockpitや他の能力モジュールも船体に残る。

**操縦不能警告**:
健全な cockpit を含まない物資を建造終了または分離・発進する際に表示する確認付き警告。キャンセルすると物資は現在のドックまたは統合船体に残る。

**操作基準cockpit**:
船体に一つだけ存在する、操縦の基準となるcockpit。建造中を含め、ユーザー操作で健全なcockpitへ変更できる。デフォルトでは最初のcockpitが操作基準となり、後から追加したcockpitは自動では操作基準にならない。操作基準cockpitが破壊された場合は、別の健全なcockpitへ自動切替する。健全なcockpitがなければ操縦不能になる。
二つの船体をドッキングするときは、乾燥質量と残資源質量を合算した現在質量が大きい方をドッキング先と認識し、その船体の操作基準cockpitを統合後の操作基準cockpitにする。ドッキング先に健全なcockpitがなければ、反対側の操作基準cockpitを採用する。操作基準cockpitが失われても選択状態は維持し、健全な cockpit が復帰すれば同じ assembly ID の船へ戻る。

**操縦不能化**:
すべての cockpit が不健全になった状態。assembly に module instance が残る限り船体喪失ではなく、物資として存続する。健全な cockpit が復帰すれば同じ assembly ID の船へ再分類する。

**統合船体の損傷**:
弾・高速接触では統合船体の接続グラフ全体から健全なHPモジュールを一様抽選する。展開中のラジエーターへの直接命中だけは対象を固定する。モジュール損傷でdocking接続は自動解除しない。

**船体喪失**:
assembly からすべての module instance が除去・破壊された状態。cockpit、tank、燃料の有無だけでは船体喪失にならず、HP 0 の module instance も残存モジュールとして数える。

**所属判定**:
このMVPでは新しいowner概念を導入せず、既存のplayer／enemy判定を使う。複数cockpitは複数の所属を意味せず、一つの船体内の操作起点候補を意味する。

**接続分離**:
docking_portまたはdockの接続関係だけを解除する操作。接続に使ったポート部品は各分離後船体に残り、再接続に使える。1本の接続を解除すると接続グラフは必ず二つの部品集合へ分かれる。

**decoupler**:
円柱の長軸方向にある任意の直接構造接続位置へ置ける、爆砕ボルトからなる使用後消費の分離用火工品。作動時に接続edgeを切り、本体は分離後の船体に残らない。先端の多角形円柱状の覆いは面ごとの物理debrisとして飛散する。

**一時debris**:
decouplerの分離イベントなどで生成される物理オブジェクト。ランタイム中は飛行・接触するが、通常snapshotには保存しない。

**接続グラフ**:
モジュールをノード、docking_port・dock・構造材による接続をedgeとして表す船体構造。edge は軸方向・側面・
ドッキング・建造の種別を持つ。建造UIは直列追加に制限するが、ドッキング統合後の横方向接続と分岐を表現できる。

**構造接続edge**:
建造時に部品を配置して作る直接接続。MVPでは近い直径の円柱である船体・ブースター・docking_port・dockを長軸方向に接続し、太陽電池パネルやラジエーターなどの側面設備は船体モジュールが定義する側面アンカーへ垂直に接続する。通常のドッキング解除では分離しない。

**側面アンカー**:
すべてのcockpitとtankの中央部にある4方向の離散スロット。ドッキングポート、ドック、太陽電池パネル、ラジエーターなどを取り付け、1スロットにつき1モジュールだけを許可する。docking_portとdockは長軸に対して垂直なスロット外向きに接続する。MVPでは自由配置せず、3方向・6方向は将来仕様とする。

**ドッキング接続edge**:
docking_portまたはdock同士を向かい合わせて作る、ユーザー操作で分離可能な接続。船体グラフでは「船体－ポート－ポート－船体」の構成になる。

**建造接続edge**:
建造ドックから追加した枝を、建造完了後もドックと枝の関係として保持する接続。ドッキング接続とは異なり、
子側が docking_port であることを要求しない。建造接続の分離では、接続したドックと追加枝の関係だけを解除し、
両側のモジュールをそれぞれの部品集合へ残す。

**基地**:
健全な cockpitと健全なdockをそれぞれ1個以上持つ船。表示上の役割は「基地」とする。tank、solar_panel、radiatorは基地presetの標準設備だが、基地判定の必須条件ではない。デフォルトpresetはcockpitを1個、main tankとrcs tankを1個ずつ、dock・solar_panel・radiatorを2個ずつ持つ。dock_ringは廃止する。
_Avoid_: Base（役割を表す具象クラス名として）

## 接続状態

**ドッキング可能条件**:
ドッキング確定前に満たすべき許容条件。接続点間距離、接続軸の角度誤差、接続点の相対速度、接続点の空き、少なくとも一方のShipAssemblyに健全なcockpitがあることを判定する。両方がcockpitなしの部品集合の場合はドッキングできない。
これは確定後の船体状態を表さない。

**ドッキング後状態**:
ドッキング確定時に、接続点・接続軸・相対運動のずれをなくして一つのShipAssemblyの一体剛体へ統合された状態。
許容条件に含まれる距離・角度・速度の誤差は、確定後へ持ち越さない。現在質量が大きい側を固定し、小さい側を正規位置へスナップする。同質量の場合はドッキング操作を開始した側を固定する。
