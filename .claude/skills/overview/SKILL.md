---
name: overview
description: 実装の全体像・影響範囲を掴む手順。「どこで何が起きているか」「誰がその状態を持っているか」「いつ走るか」を調べるときは、src/ を読み始める前に DEVELOP/OWNERSHIP.md・CALLSTACK.md・SPEC.md と CLAUDE.md を一次情報として使う
---

# 全体像の掴み方

**いきなり `src/` を読み始めない。** 全体像は既に文書として管理されていて、そちらの方が速く正確。
コードを読むのは、文書で当たりを付けた後の**最後の一手**。

## 1. 問いから入口を選ぶ

| 問い | 見る文書 |
| --- | --- |
| 誰がその状態を持っていて、誰が書き換えるのか / どこで `new` されるのか | `DEVELOP/OWNERSHIP.md` |
| いつ・どの順で・どんな条件で走るのか(per-frame) | `DEVELOP/CALLSTACK.md` |
| 本来どう振舞うべきなのか(仕様・数値・操作) / 今後実装される可能性があるか | `DEVELOP/SPEC.md` |
| なぜその責務配置なのか / このモジュールは何者か | `CLAUDE.md` の Architecture・Controls |
| これから直すことになっている問題 / 進行中のリファクタリングの現況 | `refactoring_plan/refactoring_todo.md`、同フォルダの各文書、`/refactor-fixed` |
| 設計の方向性・未実装の構想 | `dev.md`(人間専用。**読むだけ、編集しない**) |

`DEVELOP/README.md` がこの対応表の元。CALLSTACK / OWNERSHIP は**機械的な事実**(順序と所有)、
CLAUDE.md は**散文の解説**(責務と理由)。

## 2. 当たりを付けてから、必要な分だけコードを読む

- 文書から関係するモジュールを 1〜3 個に絞り、そのファイルだけを読む。
- 広域 grep やファイル一覧の総なめは、文書に載っていないもの(特定の識別子の全参照箇所など)を
  探すときだけ使う。「全体像を掴むため」に使わない。
- 探索用のサブエージェントも同じ理由で不要。一次情報が既にある。

## 3. 文書と実装が食い違っていたら

- **実装を正として扱う**が、勝手に文書を書き換えて先へ進まない。食い違いを見つけたことを報告し、
  `/develop-docs` の手順で同じ変更セットのうちに直す。
- CLAUDE.md と DEVELOP/ が食い違っている場合は DEVELOP/ を正として CLAUDE.md を直す。
