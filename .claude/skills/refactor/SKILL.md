---
name: refactor
description: 作業範囲のコードが DEVELOP/CODING-RULE.md から逸脱していないかを点検し、是正する手順。責務の移動・分割・統合、モジュールの新設・削除、重複実装の解消を伴う変更を終えた後や、ユーザーが整理を求めたときに通す。判断基準そのものは持たず、すべて CODING-RULE.md を参照する
---

# 規約からの逸脱を点検して直す

**判定基準は `DEVELOP/CODING-RULE.md` が正本。** ここにあるのは作業手順だけで、「何が違反か」は
必ずそちらを読んで決める。この手順書に規則そのものを書き足さない — 規則が変わったなら
`CODING-RULE.md` を直す。

**いつ通すか**: 責務の移動・モジュールの分割/統合・大きな機能追加を終えた後と、ユーザーが整理を
求めたとき。個々の変更で規約に従うのは常時の義務であって、この手順を毎回通す必要はない。

## 1. 点検範囲を決める

**リポジトリ全体を一度に点検しない。** 直前の変更セットが触ったファイルと、そこから1段だけ
辿れる範囲を既定にする。

```
git status --short
git diff --stat HEAD -- src/
```

ユーザーが範囲を指定していて、それが広い(数十ファイル以上)なら、**どこを対象にどの深さまで
見るかを問う。** 勝手に全体へ広げない。

## 2. 機械的に見つかるものから当たる

**lint を最初に通す。** 構文だけで判定できる規則は `eslint.config.mjs` が当てている。何を当てて
いるかはそのファイルで確かめ、**3 ではそれらを読んで確かめ直さない。**

```
npx eslint --fix --prune-suppressions <点検範囲の .ts。同じ変更セットが触った tests/ tools/ も含める>
npm run check:boundaries
```

- `--fix` が自動で直せるもの(`import type` など)を直し、`--prune-suppressions` が直って要らなく
  なった suppressions を消す。**両方を付ける** — `--fix` だけだと、次の `npm run lint` が
  「使われていない suppressions」で落ちる。
- 残った error は手で直す。**`eslint-suppressions.json` へ載せても、`eslint-disable` を書いても
  通さない。** 件数は減らすだけで、増やしてよいのは規則を足したときだけ(`eslint.config.mjs` の冒頭)。
- **移動・改名したファイルは、lint を走らせる前に `eslint-suppressions.json` の鍵を新しいパスへ
  書き換える(件数はそのまま)。** 移動・改名は、1 で範囲を決めたのと同じ比較を `git diff -M
  --name-status` で取った `R` 行で分かる。先に走らせると旧パスの鍵が消え、移動しただけの既存の
  違反が新しい違反として浮く。
- warning(`max-lines` / `max-lines-per-function`)は違反ではなく診断の入口。当たったら 1.2「長い
  モジュールは、まず原因を診断する」で診断し、分ける線が無ければそのままにする。
- `check:boundaries` は層と境界の判定。`src/**/*.ts` を編集するたびに PostToolUse フック
  (`.claude/hooks/check-boundaries.mjs`)も位相の混線・`physics/` の依存汚染・配線で占められた
  モジュールへの実装の混入を指摘するが、点検時はまとめて通す。

lint の外で機械的に当たれるもの:

- 同じ形の式・同じ形の処理が複数のモジュールに散っていないか、特徴的な識別子・演算・定数で grep する。
- 旧名の残骸が無いか。改名した名前をリポジトリ全文検索して 0 件を確認する。

## 3. `CODING-RULE.md` の各節を当てる

範囲内のコードに、順に当てていく。**該当した箇所だけ**を直す。

| 見るもの | 節 |
| --- | --- |
| 責務の有無、たらい回し、2 で長さの warning が出たものの診断 | 1.2 モジュール化 |
| 層をまたぐ依存(math/ physics/ render/ marker/ audio/ input/ hud/ game/ settings/ launcher/)、状態の置き場と書き手、直列化と構築、`entity` / `motion` / `view` の責務、接触の帰結の置き場 | `ARCHITECTURE.md` |
| 置き場所が適切か(依存関係だけを見る) | 1.4 責務分割の見つけ方 |
| 重複実装か、早急な一般化か | 1.5 重複実装の禁止と早急な一般化の分かれ目 |
| 状態の重複、導出可能なステート、整合性責務の漏洩、フラグの合成、`?:` で表した不在(`\| undefined` は lint が見る) | 1.6 データ構造 |
| 中身を差し替えて転用しているオブジェクト | 1.7 オブジェクトの流用の禁止 |
| 座標系の取り違え、生の Vec3 への非 ECI 値の混入 | 1.8 座標系の境界 |
| シミュレーション時刻と実時刻の取り違え | 1.9 時刻軸の境界 |
| フレームの位相(入力の解釈・進行・導出と同期・描画)の混線 | `ARCHITECTURE.md` R8 |
| 旧名・互換エイリアスの残骸 | 1.10 改名・移動 |
| TypeScript の書き方のうち lint が当てていないもの(状態を更新して値を返す関数、クロージャ注入、受け手の面、`readonly`、`as` の前提、import の順、公開範囲の選び方)、二段初期化 | 1.11 |
| CSS のリテラル直書き | 1.12 |
| 命名 | 2. 命名規則 |
| コメント | 3. コメント規約(一括点検は `/comment-cleanup`) |
| 現状を写しただけのテスト、要求されていないデバッグ用の仕掛け | 4. テスト・デバッグ |

## 4. 直す — ただし勝手に広げない

- **好ましくないパターンを好ましいパターンへ書き換えることは常に歓迎される。** 見つけたら直す。
- ただし、**プレイヤーから見える挙動を変えない。仕様に書かれていない挙動も同じで、ここでは
  直さない。** 変える必要があると判断したら、直さずに報告する — 仕様を決め直すなら
  `/modify-feature`、何を保存して何を直すかの切り分けが要る規模なら `/write-plan`
  (`DEVELOP/SPEC/README.md`「いつ読むか」B)へ回す。
- 共通化するかどうかで迷ったら、推測で一般化せず `DEVELOP/SPEC/` の該当ファイル末尾にある
  「未確定の案」節を見る。そこで決まらなければユーザーに問う。
- 範囲外のファイルに違反を見つけたら、直さずに報告する。

## 5. 仕上げ

- `npx eslint --prune-suppressions <点検範囲>` を error 0 で通し、`npm run typecheck`。触った層の回帰テスト(`npm run test:physics` / `test:math` / `test:game` / `test:render`)も。
- **何を直したかを、規則の節番号つきで報告する。** 「見つけたが直さなかったもの」と、
  その理由も述べる。
- **リファクタリングでは `DEVELOP/SPEC/` を更新しない。** 挙動を変える必要が出たなら、それは
  リファクタリングではないので、コードを直す前に `/modify-feature` へ回す。
- **`CODING-RULE.md` 自体を書き換えるのは、規則そのものが変わったときだけ。** 現状を正当化する
  ための例外節を足さない。違反を見つけたらコードを直す。
