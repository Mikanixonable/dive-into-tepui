# 開発方針

## 基本原則

可能な限り単純で簡潔な実装を求める。長大なファイル、非自明なコードは人間にもエージェントにも読めないため、簡潔になるように書き直すべきである。

要求した仕様を超えて実装が複雑になるような判断を勝手に追加すべきでない。実装が自明であることは、仕様が自明であることと相関する。非自明な実装は非自明な挙動を生み、悪いUXに繋がる。

いついかなる時も、好ましくないパターンを好ましいパターンに書き換えることは歓迎される。好ましくないものは作るべきでないし、残すべきでない。

## モジュール化

### 良いモジュール化

- それぞれが明確に分割された単一の責務を持った、疎結合なモジュール群。

### 悪いモジュール化

- 責務が大きすぎるモジュール、関数。
- 長大なファイル、モジュール、関数。モジュールは200行、関数は100行を基準とする。
- 責務がないモジュール。例えば不要なたらい回しをするだけの薄すぎるラッパーなど。
- 実装の重複。同じ処理が複数箇所に分散して書かれているもの。
- 早急な一般化。

責務の有無とコードの規模には多少の相関はあるが、絶対ではない。どんなに行数が少なくても、責務を持っているのであれば単独のモジュールとして維持すべきである。行数が長くても無駄なたらい回しをしているだけであれば、削除すべきである。

## データ構造

### 良いデータ構造

- 少ないステート設計。
- 適切に構造化されたデータ。

### 悪いデータ構造

- 正データが複数箇所に分散、重複しているデータ。
- 軽微な計算で求まるものをステートとして持っているデータ。
- 複数箇所が一定の整合性を保つことが要求されるデータ。メモ化アルゴリズムなどでどうしてもそのように扱わなければならない場合は、適切に隠蔽しなければならない。

## 命名

### 良い命名

- 振る舞いと扱い方が明快な命名。
- 混同の恐れない命名。
- 適切な長さの命名。

### 悪い命名

- 類義語の混雑。同じものを異なる言葉で指している命名。
- 曖昧な区別。区別すべきものを同じ名前で指す命名。
- 漠然とした命名。`process` や `data` などの命名は意味を持たない。
- スコープの狭さ、混同のおそれのなさに対して、不必要に長大な命名。スコープが十分に狭ければ、前述の漠然とした命名も許される。一行のラムダ式規模であれば一文字変数名も許される。

## コメント

### 良いコメント

- 大局的な理解を必要とするコードに対するコメント。
- そのモジュール／関数を「どのように扱うべきか」が書かれたコメント。
- 極めて非自明な実装について実装の意図を記述するコメント（例えばboolean判定やインデックス操作、視覚的な調整、物理的な計算式などは非自明になりやすい）

### 悪いコメント

- その場に書かれた実装と同じことを言っているコメント。
- 関数やモジュールが「どのように振舞うか」を書いたコメント。「どのように振舞うか」はコードに書かれていることが唯一であるべきで、コメントに書くべきでない。コードを変更したときに矛盾する。
- 実装と矛盾したコメント。古くなったコメント。
- 従来実装との対比を思わせるコメント。
- 否定形のコメント。「何をしないか」を書きだしたらキリがない。

十分に良い命名、良い設計で書かれたコードは自然言語のように読むことができ、コメントが不要になる。コメントが必要な非自明なコードは、命名や設計が悪さをしていると考えた方がよい。

## 仕様書

### 良い仕様書

- 適切な粒度で「どのように振舞うべきか」が書かれたもの。

仕様書はコメントとは分離される。コードが「どのように振舞うか」を記述し、仕様書が「どのように振舞うべきか」を記述する。これは将来的に一致すべきものであり、開発途中においては一致していなくてよい。

## TypeScript のコーディング規則

- `const` を原則使用し、どうしても破壊的な更新が必要なときだけ `let` を使う。`var` は絶対に使うべきではない。
- `camelCase` を原則とする。
- 配列likeなものの処理については、イテレーターが存在すれば `for...of` を使い、存在しない場合のみ `for (;;)` を使う。`filter` や `map` が適している可能性があればそちらを使う。`reduce` は総和を求めるなどの単純な場合を除き、使わない。

## CSS のコーディング規則

- CSS は CSS ファイルに書き、tsx からはクラス名を指定するのみとする。
- CSS では細部までクラス名で指定するのではなく、変更されにくい文書構造に対しては子要素セレクタによって指定する。

## 責務分割の手順

ポイントは、関数の名前やコメントの自然言語的な関連性には注目せず、依存関係だけに注目することです。

クラス外から情報をもらいまくっていて、クラス内の情報に全然手を付けていない関数は、置き場所が適切でない懸念がある。

特定のクラスが常にある情報を外部からもらっているとき、その情報はそのクラスに参照コピーなどで事前に共有しておくべきである懸念がある。特に、その情報が外部で使用される頻度が低いときには、その情報の正所有権をそのクラスに移動するべきである懸念がある。

異なるクラスにまたがって存在する複数の情報が頻繁にセットで受け渡されているとき、その２つの情報は同じクラスに配置されるべきである可能性がある。

漠然とした多数の情報がひとつのクラスに入っていて、関数ごとに一定のパターンが見られないとき、そもそもそのクラスの責務が定まっていない可能性がある。

外部から一度しか呼ばれていない関数は、本当にそこで処理を区切るべきだったのか熟考する必要がある。形式的には、多数の引数やCtx引数を受け取っている関数は、I/Fの切り方に失敗している可能性がある。意味的には、クラスオブジェクトとして強い独立性を主張できない限りにおいて、疑いの余地がある。

類似した形の式、類似した形の処理が異なるモジュールに分散して書かれているときは、重複実装の疑いがある。