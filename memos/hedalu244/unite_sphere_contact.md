# 2球体の接触判定を1つの判定器へ

## この計画の前提

- **`Asteroid` 型(`GameEntity` の派生クラス)は廃止予定であり、この計画に着手する時点では
  既に存在しない。** 本文書では `Asteroid` を扱わない。
- **大気は天体ごとのパラメータになっており、焼失(連続量の状態判定)と衝突(離散事象の掃引判定)は
  既に別の判定として分かれている。** この計画が扱うのは**衝突側の幾何だけ**で、焼失には触れない。
- 着手時点で、天体接触の判定は `ContactPhysics` / `GameEntity.checkLoss` / `PredictedArc.checkImpact`
  の3箇所に分かれたままでよい。**この計画は判定を呼ぶ箇所を減らさない** — 呼ばれる先の実装を
  1つにするだけ。

## この計画は2段階に分かれ、間に別の作業が入る

| 段階 | 内容 | いつ |
|---|---|---|
| **第一段階** | 純リファクタ。判定器の入口を1つにし、`reachedBody` から幾何を抜く。**挙動不変** | 判定箇所の統合よりも**前** |
| **第二段階** | 判定器の内部の最適化。解法の選択を判定器が自律的に行う | 判定箇所の統合よりも**後**。単一モジュール内で閉じるので独立に着手できる |

第一段階を先に置くのは、**判定箇所の統合が「1つの判定器」を前提にする**ため。第二段階を後ろに
置くのは、**判定回数が確定してから精度と費用の釣り合いを決める**ため。

---

# 第一段階: 判定器の集約(純リファクタ)

## 1. 目的

`physics/sphere-contact.ts` に、**同じ問い**に答える関数が2つ入っている。

| | `sweptSphereToi` | `sweptHermiteSphereToi` |
|---|---|---|
| 経路の近似 | 端点を結ぶ**線分** | 端点の速度を接線に取る**三次エルミート** |
| 解き方 | 2次方程式の閉形式 | 相対位置を3次ベジエへ変換 → 制御点凸包の箱判定 → 左右再帰細分(深さ32)→ 24 反復の二分法 |
| 返り値 | `{ toi, normal }` | `toi` のみ |
| 開始時の重なり | null(呼び出し側の離散 solver へ委譲) | null(同上) |
| 呼ぶ側 | `collision-response.ts` の `resolveSphereCollision`(= `ContactPhysics` の全経路) | `attractor.ts` の `reachedBody`(= `GameEntity.checkLoss` と `PredictedArc.checkImpact`) |

**同じ「移動する球どうしがこの区間で触れたか」を、別の近似で2回書いている。**
しかも呼び出し口が揃っていない — 片方は法線を返し、片方は返さない。片方は1ペアを解き、
片方は天体一覧を回して最小 TOI を選ぶところまでを1つの関数(`reachedBody`)でやっている。

さらに `reachedBody` は幾何(`sweptHermiteSphereToi`)と探索(天体一覧の走査)と
フォールバック(`containingBody` による点判定)を1つの関数に抱えている。

## 2. 判定器の責務(達成すべき契約)

> **2球体の接触判定器は1つだけ存在し、2球体の半径と移動前後の状態を受け取り、その区間で
> 接触するか否か・接触の瞬間の状態・法線を(できるだけ正確に近似して)返す。**

- **どう解くか(線分近似で足りるか、より高次で解くべきか)は判定器の内部に隠蔽される。**
  接触の種別(剛体どうし / 天体)でも、時間加速帯でも、呼び出し元でもない — 精度と費用の
  釣り合いは判定器自身が持つ問題。
- **外から渡すのは解くのに必要なパラメータだけ。** 加えてよいのは、費用最適化のために必要な
  場合の**おおよその要求精度**まで。
- **どのペアを判定にかけるか(broad phase)は判定器の外側の責務。** 呼び出し側が空間グリッド
  なり成員判定なりで絞ってから渡す。判定器自身が持ってよいのは、1ペアに対する安価な早期棄却
  (narrow phase の入口)だけ。

## 3. 達成目標(第一段階)

**実施後、次を1つずつ当てる。**

1. **`src/` に「移動する2球体が触れたか」を解く実装が1つしかない。** `sphere-contact.ts` の
   外に掃引の幾何が書かれていない — `attractor.ts` に2次方程式もベジエ細分も残っていない。
2. `reachedBody` は「判定器を天体一覧に対して回し、最小 TOI のものを返す」だけの関数になっている
   (幾何ゼロ、探索とフォールバックのみ)。
3. `collision-response.ts` と `reachedBody` が**同じ入口**を呼んでいる。
4. **挙動が完全に不変。** `tests/physics/sphere-contact.test.ts` の既存 14 件、
   `tests/physics/attractor.test.ts` の `reachedBody` 7 件、
   `tests/physics/collision-response.test.ts` の既存 10 件が、**1件も書き換わらずに通る**。
   書き換えが必要になった時点で挙動を変えてしまっている。

## 4. 変更が必要な箇所(第一段階)

| ファイル | 変更内容 |
|---|---|
| `src/physics/sphere-contact.ts` | **判定器の入口を1つ作る。** 半径と移動前後の状態を受け取り、接触の有無・接触時刻(区間内の割合)・法線を返す。線形とエルミートの両方を内部に持ち、**暫定的に判定モードの引数で選ばせる**。`sweptSphereToi` / `sweptHermiteSphereToi` は内部実装として残る(export をやめてよいかはテストの参照しだい)。`containingBody` はそのまま残す |
| `src/physics/attractor.ts` | `reachedBody` を、判定器を天体一覧に対して回して最小 TOI を返すだけのラッパにする。**幾何をこの関数から完全に抜く。** 呼ぶモードはエルミート(現状と同じ)。返す状態は `hermiteInterpolate` のまま — 判定の次数と、報告する位置の精度は別の問題 |
| `src/physics/collision-response.ts` | `resolveSphereCollision` を新しい入口へ通す。呼ぶモードは線形(現状と同じ) |
| `tests/physics/sphere-contact.test.ts` | **書き換えない。** `sweptHermiteSphereToi` / `sweptSphereToi` を名指ししている件があるので、そのために内部実装の export を残す |
| `DEVELOP/SPEC/ORBIT.md`「天体表面への到達判定」 | 「1ステップの区間全体を曲線として掃引する」という記述は、判定器がどの近似で解くかに依らない書き方になっているか確認する。第一段階では解法が変わらないので、**書き換えが必要ないことの確認**で足りる |

**触らないもの**: broad phase(空間グリッド・`ArcBodies` の成員判定)は判定器の外に置いたままにする。

## 5. 手順(第一段階、着手順)

1. `sphere-contact.ts` に判定器の入口を1つ作る。判定モードを引数に取り、線形なら
   `sweptSphereToi` の閉形式へ、エルミートならベジエ細分へ分岐する。**返り値の形を揃える**
   (どちらも `{ toi, normal }`)— エルミート側は接触時刻の相対位置から法線を出す。
2. `reachedBody` を新しい入口へ通し、幾何を抜く。呼ぶモードはエルミート。
3. `resolveSphereCollision` を新しい入口へ通す。呼ぶモードは線形。
4. `typecheck` + `test:physics`。**両方の解法が現状と同じ相手に対して現状と同じモードで
   呼ばれるので、挙動は完全に不変。** 既存テストがそのまま通ることが合格条件。commit する。

## 6. 見積り(第一段階)

- 変更ファイル 3(`sphere-contact.ts` / `attractor.ts` / `collision-response.ts`)。
- 入口の統合 +15、`reachedBody` から幾何が消えて −20、エルミート側の法線の算出 +6。
  → **正味 ≈ +1 行。**
- **削除するものは無い。** `sweptHermiteSphereToi` の中身は判定器のエルミートモードとして残る。
- 費用は不変(同じ相手に同じ解法を呼ぶ)。

## 7. リスクと落とし穴(第一段階)

| リスク | 影響 | それが露見する場所 |
|---|---|---|
| 入口を揃えるときに、**エルミート側にも法線を返させる**過程で接触時刻の相対位置を取り違える | 法線の向きが反転する。天体接触では撃力の符号が反転し、**天体から離れる向きに弾かれるのではなく吸い込まれる** | `reachedBody` は法線を使わないので**テストで見えない**。第一段階では `reachedBody` 側の法線を誰も読まないことを確認してから足す。読み手ができるまで返さない選択も採れる |
| **開始時点で既に重なっている**ときの扱いが2実装で微妙に違う | `sweptSphereToi` は `c > 0` を最初に見て null、`sweptHermiteSphereToi` は凸包の箱判定の**後**に開始距離を見て null。統合時にどちらかへ寄せると、片方の呼び出し側のフォールバック経路が変わる | `collision-response.ts` の重なり押し戻しフォールバックと、`reachedBody` の `containingBody` フォールバック。**両方の早期 return の順序をモードごとに保つ** |
| 非有限入力のガードが `!(x > 0)` の否定形で書かれていることを知らずに `x <= 0` へ書き換える | NaN がどの比較でも false になる性質が失われ、**非有限な入力が判定を通り抜けて NaN が伝播する** | `sphere-contact.ts` と `collision-response.ts` のコメントが明示している。`tests/physics/sphere-contact.test.ts` の非有限 3 件と `collision-response.test.ts` の非有限 4 件が押さえている |
| 「純リファクタなのだから」とテストを1件でも書き換える | 挙動を変えたことに気づかないまま次の段へ進む | **合格条件は「既存テストが1件も書き換わらずに通ること」。** 書き換えたくなったら、それは挙動が変わった証拠として扱う |

---

# 第二段階: 解法の自律選択(最適化)

**単一モジュール(`sphere-contact.ts`)の内側で閉じるので、他の作業とは独立に着手できる。**
ただし**判定回数が確定してから**着手する — 何回呼ばれるかが決まる前に、1回あたりの費用を
削る意味がない。

## 8. 目的(第二段階)

第一段階で外に出した「判定モード」の引数は、**責務の置き場所として間違っている。**
どの近似で解くかは、呼び出し元が知っていることではなく、判定器がその1歩の幾何から決めること。

さらに、線形近似は粗い刻みで急速に悪化する(9-1)。呼び出し元が固定のモードを選ぶ形では、
「通常の刻みでは線形で足りるが、粗い刻みでは足りない」という**同じ呼び出し元の中の差**を
表現できない。

## 9. 誤差の構造(第二段階の判断材料)

### 9-1. 線分化の誤差は両側に出る — 支配的なのは偽陽性

線分と真の曲線のずれには符号が2通りあり、**どちらも大きさは同じオーダー**である。
1歩でその天体まわりに掃く角を θ = ω·h(ω = |r × v| / |r|²)とすると、

```
弦と弧の最大乖離 ≈ r · θ² / 8
```

- **区間が近点を跨がないとき**(周回している通常の状態): 円弧の弦は中心に近いので、弦は弧より
  **天体側**にある。→ **偽陽性**。本当は逸れているのに交差と判定する。
- **区間が近点を跨ぐとき**: 真の最接近点は弦より天体側にある。→ **偽陰性**。既存テスト
  `Hermite swept sphere: 弦は外れていても曲線が膨らんで届く通過を捕まえる` が押さえている配置。

**物体はたいてい重力源のまわりを周回しているので、実地で支配的なのは前者(偽陽性)。**

### 9-2. 「半径を膨らませて偽陰性を消す」案は採らない

膨張量 `|Δv|·h/8 = a·h²/8 = ω²r·h²/8 = r·θ²/8` は 9-1 の乖離と**同じ大きさ**なので、
支配的な偽陽性側の誤差をちょうど**2倍にするだけ**になる。片側へ寄せる補正では解決しない。
**誤差そのものを小さくするしかない。**

(broad phase で安全側に半径を膨らませるのは別物 — あちらは通す候補が増えるだけで判定器の
答えを変えない。ここで棄却しているのは**答えそのものを歪める**膨張である。)

### 9-3. 近似の次数と誤差

| 近似 | 誤差のオーダー | 備考 |
|---|---|---|
| 線分(弦) | **O(h²)** = `r·θ²/8` | サジッタ項がそのまま残る。いま `collision-response.ts` が使っている |
| 二次 | O(h³) | サジッタ項を落とす。摂動・空気抵抗を無視すれば軌道は二次曲線なので、ここで大半が取れる |
| 三次エルミート | O(h⁴) | いま `reachedBody` が使っている。二次からの上積みは小さい |

### 9-4. θ の実測値(どの近似で足りるかを決める量)

| 場面 | h | θ = ωh | `r·θ²/8`(r = 6791 km) |
|---|---|---|---|
| 実シミュレーション 通常(`SUBSTEP_MAX_DT`) | 20 s | 0.0225 rad | **430 m** |
| 実シミュレーション 最高ワープ(131072 ÷ 60fps ÷ `SUBSTEP_MAX_COUNT` 64) | 34.1 s | 0.0384 rad | **1.25 km** |
| 弧 最小刻み(`ARC_MIN_STEP_DT`) | 40 s | 0.045 rad | **1.7 km** |
| 弧 28日表示の粗化刻み | 121 s | 0.136 rad | **15.7 km** |

θ は `|r × v|·h / |r|²` で数演算で出せるので、**判定器が自分で「線分で足りるか」を決める基準として
使える**。いずれの値も地球半径 6371 km に対しては小さいが、`r·θ²` が二乗で効くため、
**粗い刻みほど急に悪化する**。判定器の内部で刻みに応じて次数を上げる余地があるのはこの非線形性のため。

### 9-5. 解法1回あたりの費用(棄却される遠い相手の経路、演算数で数える)

| | 演算数 |
|---|---|
| エルミート解法の棄却経路 | `isFinite` 9 + 制御点構築 36 + `axisDistanceSq` × 3 = 30 + 合計と比較 4 ≈ **70** |
| 線形解法の棄却経路 | `c` 6 + `aa` 5 + `bb` 5 + 判別式 4 + 比較 ≈ **21** |

**解法を線形へ倒すだけでは 70 → 21(3.3×)** にしかならず、しかも 9-3 のとおり精度を落とす
取引になる。**軽量化の主因は解法の選択ではない** — この計画で解法を触るのは、責務の置き場所を
正すため(8章)であって、性能のためではない。

## 10. 達成目標(第二段階)

1. **判定器の入口から判定モードの引数が消えている。** 呼び出し元が解法を選ぶ箇所が `src/` に 0 件。
2. 判定器が、その1歩の θ から `r·θ²/8` を出し、要求精度と比べて解法を選ぶ。要求精度は既定値を
   持ち、必要なら呼び出し側が上書きできる引数にする。
3. **モード指定なしで、第一段階の時点の両モードそれぞれと同等以上の精度が出る。**
   要の回帰は `Hermite swept sphere: 弦は外れていても曲線が膨らんで届く通過を捕まえる` が
   **モード指定なしで通ること**。
4. `tests/physics/sphere-contact.test.ts` で `sweptHermiteSphereToi` / `sweptSphereToi` を
   名指ししている件が、新しい入口へ向け直されている。

## 11. 手順(第二段階、着手順)

1. 判定器の内部で、その1歩がその天体まわりに掃く角 `θ = |r × v|·h / |r|²` を出す。
2. `r·θ²/8`(9-1 の乖離)が要求精度を下回るなら線形の閉形式で答え、上回るなら次数を上げる。
3. 上げる先の候補は**二次**(9-3 — 誤差 O(h³)、摂動を無視すれば軌道は二次曲線なのでここで
   大半が取れる)。三次エルミートを残すか置き換えるかは実測で決める。
   **具体的な解法は未定** — 二次の場合、球中心との距離が t の4次式になるので、制御点凸包での
   棄却 + 数回の反復で根を詰める形が素直。
4. 判定モードの引数を消す。テストを新しい入口へ向け直す。
5. `typecheck` + `test:physics` + 実行時確認。

## 12. リスクと落とし穴(第二段階)

| リスク | 影響 | それが露見する場所 |
|---|---|---|
| 精度を**下げる**方向に判定器が線形を選ぶ | いま三次で解いている `reachedBody` の経路が線形に落ち、**周回中の物体が偽陽性で天体に当たったことになる**(9-1 の支配的な誤差) | 9-3 の表を基準に、**上げる方向にしか動かさない**。要求精度の既定値は、いまエルミートで解いている経路の精度を下回らない値にする |
| θ の算出に**その天体の中心からの相対位置**ではなく ECI 位置を使う | 地球以外を周回している物体で θ が桁違いに小さく出て、粗い刻みでも線形が選ばれる | 月周回・小天体周回での掃引。判定器は既に2球体の相対位置を持っているので、そこから出せば取り違えようがない |
| 二次近似の根の探索が、**エルミート実装が持っている「区間の途中だけ球を通過する」拾い上げ**を落とす | 端点が両方とも表面外の通過を見逃す。すり抜けが復活する | `Hermite swept sphere: 弦は外れていても曲線が膨らんで届く通過を捕まえる` と `Hermite swept sphere: 制御点の箱ごと球から離れた天体は null` の2件。**モード指定なしで通ることが合格条件** |
| 「軽くするために」解法を線形へ倒す | 9-5 のとおり 3.3× にしかならず、精度を落とす取引になる。**軽量化の主因は解法ではない** | この計画の目的は責務の置き場所であって性能ではない。性能の判断材料は判定回数の側にある |
| 要求精度を呼び出し側が上書きできる引数にしたとき、**誰も渡さない引数**になる | 使われない一般化。読み手が「何を渡すべきか」を判断できないまま残る | 上書きの必要が実際に生じるまで**引数を作らない**。既定値だけで始める |

---

# 13. 検証

- `npm run typecheck` — 毎段階。
- `npm run test:physics` — 毎段階(`src/physics/` を直接触る)。
- **第一段階の合格条件は「既存のテストが1件も書き換わらずに通ること」。**
- 第二段階の実行時確認(ヘッドレス):
  - 高ワープ(×131072)で、天体をすり抜ける個体が出ないこと。
  - 通常ワープ(×1)で、天体の近傍を通過するだけの軌道が誤って接触と判定されないこと
    (9-1 の偽陽性の回帰)。
  - 28日表示の粗い刻みの弧が、天体表面の手前で不当に打ち切られないこと。
