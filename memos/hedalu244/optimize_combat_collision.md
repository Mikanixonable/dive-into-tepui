# Dynamic 物体どうしの衝突の軽量化 — 階層グリッドと交戦圏

対象コミット: `9802a677`(行番号・実測はこの時点)。

## 目的

物体どうしの接触(`EntityContactPhysics`)の broad phase は 27 近傍の一様グリッドで、セル一辺を
「全参加者の平均変位 Δ̄ を引いた到達量 半径+|Δ−Δ̄| の最大値の 2 倍」に取っている。
**一辺が全参加者の最大値で決まる**ため、到達量の大きい個体が1つ混じるだけで、密集した薬莢の
絞り込みが丸ごと効かなくなる。到達量を大きくする個体は3種類あり、どれも普通の戦闘で現れる。

- **弾。** 初速 1,000 m/s なので ×4(substep 幅 1/15 s)で変位 67 m → 一辺 140 m。
- **基地。** 当たり半径 715 m(`src/assets/models/baseCollision.json` の外接球)→ 一辺 1.4 km。
- **遠方の物体。** 半周先の薬莢は相対 −15 km/s → 一辺 0.4〜1.7 km。しかも数がいれば平均 Δ̄ を
  引きずり、近くの薬莢の到達量まで押し上げる。

node 実測(下表。`tests/dist` の `SpatialGrid` と `resolveSphereCollision` を直に呼び、
直線で 1 substep 進めた集団に対する 1 substep の費用。集団の中身は「見積り」節)では、
連射中に ×4 へ上げると候補ペアが **417 → 38,470 件/substep**、費用は 0.3 → **5.9 ms** になる。
基地が 5 km 以内にいれば ×1 でも **58,000 件・8.8 ms**。破片(fragment)は `collides=false` なので
参加者ではなく、この退化は破片の数ではなく**薬莢の密集 × 到達量の大きい個体**の組で起きる。

修正後は、到達量の大きい個体が何体いても密集した薬莢どうしの候補は薬莢の到達量だけで決まり、
交戦圏(後述)の外にある物体は接触の参加者にならない状態にする。

## 決めたこと

**1. 交戦圏は「自機と基地を中心とする半径 30 km の球の和」で、重なる球は1つにまとめる。**
30 km は既に弾の消滅(`bullet.ts:20`)・ステージ00 の敵の離脱(`enemy.ts:60`)・敵の射撃可否
(`enemy.ts:342`)が同じ値で使っている距離で、SPEC も「交戦圏」の語をこの意味で使いながら定義を
持っていなかった。中心を自機と基地にするのは、この2種だけが操作対象(`Controllable`)であり、
遊ぶ人が観測するのはその周囲だけだから。敵は自機しか撃たず、弾は自機から 30 km で消えるので、
中心から 30 km 超の物体どうしの接触は観測されない。
重なる球を1つにまとめるのは、2つの自機が互いの交戦圏に入っているとき、両者の接触を
どちらか一方の交戦圏で 1 回だけ解くため — 物体を最寄りの中心へ割り当てる形だと、境界を挟んだ
2体(自機どうしを含む)の接触が永久に解かれない。
**覆すなら:** 中心を自機だけにする → 手順1 の文面と手順4 の `base.ts:207` の行を落とす。
半径を変える → 手順4 の定数だけ。

**2. 交戦圏は座標系ではなく、「所属の判定」と「到達量の基準変位」だけを供給する。**
グリッドの近傍関係は同一時刻の位置の差で決まり、慣性系を平行移動しても変わらない。
座標系に依存するのは到達量 半径+|Δ−Δ̄| の Δ̄(区間の基準変位)だけで、これはベクトル1本。
ゆえに座標変換関数もブランド型も要らず、位置は今までどおり ECI の `state.r` のままグリッドへ
入れる。提案2の「交戦圏との相対速度で評価する」は、**Δ̄ を参加者の平均から交戦圏の中心の変位へ
置き換えること**そのものであり、手順4 に畳む。
**覆すなら**(交戦圏ローカル座標のブランド型を導入する): 増えるのは変換モジュールと型だけで
候補ペア数は 1 件も変わらないので、この計画では採らない。

**3. 実装順は 階層グリッド → 交戦圏。** 提案の「3 だけでは効かない(ECI で薬莢が 8 km/s で
動くため)」は成り立たない — 現行が既に平均変位を引いているので、薬莢どうしの到達量は今も
数 m である。効かないのは逆で、**交戦圏だけでは弾が混じった時点で一様グリッドが退化する** — 弾の到達量
67 m(1,000 m/s × 1/15 s)は基準を自機の変位に変えても変わらないので、一辺は 134 m のまま。両方が要り、階層グリッドは
仕様変更を伴わず node のテストで正しさを閉じられるので先に置く。交戦圏はその上で、
(a) 遠方の物体を参加者から外し、(b) 中心の変位を基準にして平均の引きずりを消す。
実測では (b) が効くのは「遠方の薬莢 100 体」の行だけ(階層+平均 52,830 件 → 階層+自機 8,081 件)。
**覆すなら**(交戦圏を先にする): 手順の並びを 1 → 4 → 2 → 3 にする。手順4 の到達量の式は
そのまま `contactCellSize` の Δ̄ の置き換えになる。

**4. 基準変位は交戦圏の先頭の中心(entities の並びで最初の自機/基地)の変位。** 複数中心の
平均にすると、逆行ですれ違う2つの自機の交戦圏で平均が 0 になり両方の雲が粗い段へ落ちる。
先頭に固定すれば少なくとも一方の雲は細かい段に残る。

**5. 30 km の持ち主は交戦圏モジュールにし、`bullet.ts:20` と `enemy.ts:60` の定数を消す。**
CODING-RULE 1.6「同じ値へ入口を2つ作らない」。弾の消滅が「操作対象の自機からの距離」で
測られている(`simulator.ts` が `player?.state.r` を渡す)ことは変えない。
**覆すなら:** 手順4 の `bullet.ts` `enemy.ts` `wave-attack.ts` の行を落とす。

**6. 交戦圏の所属は substep 終端の位置(`state.r`)で決める。** 境界をまたぐ substep の接触は
その回は解かれないが、30 km を横切る物体は弾(消える)か敵(ステージ00 では消える)であり、
どちらも SPEC の「交戦圏の外へ出た」規則に従う側。

**7. 物体どうしの接触のワープ倍率ゲートは消し、「交戦できる倍率でなければ交戦圏が無い」に
一本化する。** 「天体との接触は常に解くが物体どうしは ×4 以下でだけ解く」という規定は、交戦圏の
存在条件に倍率を含めれば SPEC からもコードからも要らなくなる — 物体どうしの接触は交戦圏の中で
だけ解かれ、交戦圏が無ければ参加者も接触代理も無い。高倍率での挙動は変わらない(解かれないのは
同じ)。ベルトの鎖の運動そのものは `belt.update`(`player.ts:312`)が毎フレーム別に進めていて
接触のゲートに乗っていないので、影響を受けない。敵の射撃のゲート(`enemy.ts:334` の
`canShipAct`)はそのまま — 射撃を交戦圏の所属で決めるかは別の判断。
**覆すなら:** 手順4 の `sim-speed-manager.ts` `game.ts` `simulator.ts:70-79` の行と
`engagementZones` の `canEngage` 引数を落とし、`if (canResolveEntityContacts)` を残す。

**8. 弾の消滅は「自機からの距離」のままにし、交戦圏の所属には寄せない。** 所属で消すと、
高倍率へ上げた瞬間に飛翔中の弾が全部消える。弾は交戦の層のものなので消えてよい挙動ではあるが、
`checkLoss` の腕(`playerPos`)を交戦圏へ差し替える変更が全個体に及ぶので、この計画では触らない。
**覆すなら:** `bullet.ts:109` の距離判定を交戦圏の所属に替え、`DynamicSystem.cleanup` と
各 `checkLoss` に交戦圏を渡す手順を手順4 の後ろに足す。

## 達成目標

全手順の実施後、次がすべて満たされること。数値の基準は**手順3 の着手前に同じ条件で控える**
(基準値の控えは手順3 の検証に含める)。条件 A〜C はどれも負荷確認ウィンドウ(F3)の「衝突」群を読む。

- **条件 A(連射・×4):** ステージ1(低軌道実戦)で敵へ向けて連射し、薬莢 200 体以上・
  弾 300 体前後がいる状態で ×4 にする。
- **条件 B(基地):** クリエイティブで基地から 5 km 以内に自機を置き、条件 A と同じ連射。
- **条件 C(置き去り):** 連射で薬莢を作ってから 50 m/s 噴射し、×4 で薬莢が 30 km 以上後方に
  離れるまで待つ。

1. 条件 A で「候補ペア」が着手前の **1/5 以下**、「物体接触」の区間時間が **1/3 以下**。
2. 条件 B で「候補ペア」が条件 A の **2 倍以内**(着手前は 10 倍超)。
3. 条件 C で「参加者」(手順4 で足す)が自機 + 接触代理の数まで落ち、薬莢の数を含まない。
4. 挙動不変: ×1 と ×4 の両方で、弾が敵に当たって被弾の演出が出る・排出直後の薬莢が艦体や
   放熱板に当たって弾かれる(目視)。
5. 条件 A のまま ×16 へ上げると「参加者」「候補ペア」が 0 になり、×4 へ戻すと元の値に戻る。
6. `grep -rn "contactCellSize\|STAGE00_MAX_RANGE\|BULLET_MAX_DIST\|canResolvePhysicalCollisions\|canResolveEntityContacts" src tests`
   が 0 件。`grep -n "j <= i" src/game/dynamic/entity-contact-physics.ts` が 0 件。
7. `npm run typecheck`、`npm run test:math`、`npm run test:game` が通る。

## 手順

## 見積り

node 実測(9802a677、`tests/dist` の `SpatialGrid` / `resolveSphereCollision` を直に呼ぶ。
1 substep を直線で進め、候補ペアには `resolveSphereCollision` まで通す。5 回の最小値)。
集団は自機(半径 2.6 m)+ 放熱板の折り 12(0.77 m)+ ベルトの節 18(0.8 m)+ 薬莢 260
(0.2 m、自機から 200 m 以内、相対 2 m/s)を基本に、弾 300(0.02 m、15 km 以内、相対 1,000 m/s)、
敵 3(10 m、5〜10 km、相対 300 m/s)、基地 1(715 m、5 km)、半周先の薬莢 100(相対 −15 km/s)を
足したもの。**実機の候補ペア 1 件の値段は `contactsWith` ×2 + `entityContactResponse` + 候補
スロットの分だけ高く**、この表の ms は下限。実機での比は候補ペア数の比で見る。

| 構成(N) | 倍率 | 現行: 一辺 / 候補 / ms | 階層 + 平均基準: 候補 / ms | 階層 + 自機基準: 候補 / ms |
| --- | --- | --- | --- | --- |
| 薬莢のみ(291) | ×1 | 5.2 m / 400 / 0.35 | 68 / 0.34 | 68 / 0.34 |
| 薬莢のみ(291) | ×4 | 5.2 m / 417 / 0.32 | 70 / 0.35 | 70 / 0.36 |
| + 弾 300(591) | ×1 | 34.9 m / 5,904 / 1.42 | 1,013 / 0.95 | 832 / 0.83 |
| + 弾 300(591) | ×4 | 139.6 m / **38,470** / **5.84** | 3,808 / 1.36 | 3,131 / 1.15 |
| + 弾 300 + 敵 3(594) | ×4 | 139.5 m / 38,081 / 5.91 | 3,808 / 1.41 | 3,131 / 1.21 |
| + 弾 300 + 敵 3 + 基地(595) | ×1 | 1,431 m / **57,961** / **8.75** | 1,170 / 1.06 | 1,006 / 0.99 |
| + 弾 300 + 敵 3 + 基地(595) | ×4 | 1,436 m / 58,807 / 9.23 | 3,980 / 1.55 | 3,303 / 1.39 |
| + 弾 300 + 敵 3 + 半周先の薬莢 100(694) | ×1 | 437 m / 52,431 / 7.84 | **40,842** / 7.10 | 5,782 / 1.70 |
| + 弾 300 + 敵 3 + 半周先の薬莢 100(694) | ×4 | 1,747 m / 67,431 / 10.64 | **52,830** / 7.95 | 8,081 / 1.98 |
| 薬莢が 2 km に散った + 弾 300 + 敵 3(594) | ×4 | 139.5 m / 1,930 / 0.82 | 1,194 / 1.03 | 1,096 / 0.94 |

| 手順 | 導出 | 効果 |
| --- | --- | --- |
| 手順2・3(階層化) | 弾が混じった ×4 で候補 38,470 → 3,808(1/10)。1 substep/フレーム(×4 の simDt 0.067 s < 20 s)なので、フレームあたりも同じ比。実機の 1 件 ≈ 0.3 µs なら 11.5 ms → 1.1 ms | 条件 A で **−10 ms/frame 級**、条件 B で 58,807 → 3,980 |
| 手順2・3 の底値 | 段の数だけ 27 近傍を引く。占有段 5〜6 で N × 6 × 27 = 600 × 162 ≈ 97,000 回の Map 参照 ≈ 1 ms。実測 1.4 ms(候補 3,808 件の narrow 込み) | 薬莢だけの状態では現行と同じ 0.35 ms |
| 手順4(基準変位) | 半周先の薬莢 100 が平均を 15 km/s × 100/694 ≈ 2.2 km/s 引きずり、近くの薬莢の到達量が 2.2 km/s × 1/15 s ≈ 145 m になって粗い段へ落ちる。中心基準なら 0 → 52,830 → 8,081 | 遠方の物体が多いときだけ **−7 ms/frame 級** |
| 手順4(所属) | 半周先の薬莢 100 が参加者から外れ、8,081 → 3,131(「+ 弾 300 + 敵 3」と同じ) | 残り 5,000 件は半周先の薬莢どうし(1 つの粗いセルに 100 体 → 100²/2)。**この分は階層化でも基準変位でも消えない** |
| 手順4(所属判定の値段) | N × 交戦圏数 × 距離比較 = 700 × 1 × ≈10 ns | 7 µs |
| 手順4(ゲートの一本化) | ×4 以下では今までどおり組み、それより上では組まない。参加者も候補ペアも変わらない | 0 |
| 合計(条件 A、×4) | 現行 5.9 ms(下限)→ 1.2 ms | **1/5** |

### 着手前の実測(ヘッドレス Chrome、9802a677 のビルド)

`npm run build` の成果物を `tools/chrome-session.mjs` で駆動し、負荷ウィンドウの行を 8 回読んだ中央値。
ヘッドレスは描画がソフトウェアで fps 3〜4 になり、1 フレーム = 1 substep(×1 で simDt ≈ 0.3 s)なので、
弾の substep 変位が実機の 60 fps より 1 桁大きく、一様グリッドの退化は実機より強く出る。**前後の比較は
同じ環境・同じ手順で行う。** 条件 A はステージ1 ではなくクリエイティブで組んだ(ステージ1 は弾薬無しで
始まり、ヘッドレスでは補給を回収できない): 地球 400 km 円軌道に自機 1 隻、「敵をスポーン」×3、
「弾薬を満タンにする」を 6 秒おきに押しながら連射。薬莢は上限 260 で頭打ちになる。

| 条件 | 倍率 | 薬莢 / 弾 / 敵 / 基地 | 候補ペア [件/frame = substep] | 物体接触 [ms/frame] |
| --- | --- | --- | --- | --- |
| A | ×1 | 260 / 268 / 1 / 0 | 32,447 | 138 |
| A | ×4 | 260 / 226 / 1 / 0 | 37,570 | 144 |
| A | ×16 | 260 / 10 / 1 / 0 | 0 | 0 |
| A | ×4(×16 から戻す) | 260 / 43 / 1 / 0 | 31,900 | 132 |
| B | ×1 | 260 / 260 / 0 / 1 | 45,136 | 117 |
| B | ×4 | 260 / 225 / 0 / 1 | 41,597 | 96 |

条件 B は、クリエイティブで月 400 km 円軌道に基地(真近点角 0°)と自機(真近点角 0.1°、基地の約 3.7 km 先)を
置き、敵は湧かせずに条件 A と同じ連射をしたもの(基地は月基準の軌道にしか置けない)。

### 手順3 後の実測(同じ環境・同じ手順、コミット `perf(contact): 物体どうしの接触の broad phase を階層グリッドへ替える`)

| 条件 | 倍率 | 薬莢 / 弾 / 敵 / 基地 | 候補ペア [件/frame = substep] | 物体接触 [ms/frame] |
| --- | --- | --- | --- | --- |
| A | ×1 | 238 / 258 / 2 / 0 | 25,761 | 42 |
| A | ×4 | 260 / 223 / 2 / 0 | 37,120 | 54 |
| A | ×16 | 260 / 18 / 2 / 0 | 0 | 0 |
| A | ×4(×16 から戻す) | 260 / 94 / 2 / 0 | 9,544 | 15 |
| B | ×1 | 260 / 265 / 0 / 1 | 41,018 | 104 |
| B | ×4 | 260 / 224 / 0 / 1 | 41,694 | 103 |

物体接触の時間は条件 A の ×4 で 144 → 54 ms(1/2.7)に落ちたが、**候補ペアは 37,570 → 37,120 でほぼ
変わらない。** 原因は基準変位が参加者の平均であること: ヘッドレスは substep が ×4 で ≈ 1.3 s あり、
参加者の半分近くを占める弾(+1,000 m/s)が平均変位を ≈ 600 m 引きずるので、薬莢の到達量が ≈ 600 m
(一辺 2 km の段)になり、薬莢 260 体が数セルに収まって 260²/2 件を作る。弾が少ない「×4(×16 から
戻す)」の行(弾 94)で 9,544 件まで落ちるのがその証拠。実機の 60 fps(substep 1/15 s)では同じ引きずりが
≈ 30 m にとどまるので、計画の node 見積り(38,470 → 3,808)はこの環境では手順3 単独で再現しない。
**手順4 の基準変位の置き換え(平均 → 交戦圏の中心の変位)がこの経路を断つ**ので、達成目標 1 は手順4 の
後に当てる。

### 手順4 後の実測(同じ環境・同じ手順、コミット `feat(contact): 交戦圏を組み、物体どうしの接触をその内側でだけ解く`)

| 条件 | 倍率 | 薬莢 / 弾 / 敵 / 基地 | 参加者 | 候補ペア [件/frame = substep] | 物体接触 [ms/frame] |
| --- | --- | --- | --- | --- | --- |
| A | ×1 | 249 / 267 / 2 / 0 | 545 | 4,602 | 10.1 |
| A | ×4 | 260 / 151 / 1 / 0 | 444 | 3,560 | 7.5 |
| A | ×16 | 260 / 6 / 1 / 0 | 0 | 0 | 0 |
| A | ×4(×16 から戻す) | 260 / 75 / 1 / 0 | 346 | 2,918 | 6.7 |
| B | ×1 | 260 / 271 / 0 / 1 | 559 | 4,292 | 8.8 |
| B | ×4 | 260 / 188 / 0 / 1 | 483 | 6,616 | 10.3 |
| C | ×1(連射後・噴射前) | 156 / 156 / 0 / 0 | 334 | 845 | 14.1 |
| C | ×1(前進 15 s 噴射の直後) | 156 / 156 / 0 / 0 | 335 | 8,519 | 52.6 |
| C | ×4 +30 s → +150 s | 156 / 0 / 0 / 0 | 177 | 9,102 → 2,579 | 56 → 13 |
| C | ×4 +180 s 以降 | 156 / 0 / 0 / 0 | **19** | 0 | 0.8 |

条件 C はクリエイティブで敵を出さず、薬莢 156 を作ってから前進を 15 s(並進出力「中」20 m/s² で
約 300 m/s)噴射し、×4 で待ったもの。噴射直後は薬莢の到達量(自機基準の相対変位 ≈ 300 m/s × substep)が
大きくなって候補ペアが一時的に増えるが、薬莢が 30 km の外へ出た +180 s で参加者は自機 1 + ベルトの節 18 =
19 まで落ちる。5 s(≈ 100 m/s)の噴射では 150 s(sim ≈ 600 s)のあいだに 30 km を越えなかった。

達成目標の当て込み(着手前 → 手順4 後):

1. 条件 A ×4 の候補ペア 37,570 → 3,560(1/10.6)、物体接触 144 → 7.5 ms(1/19)。**達成。**
2. 条件 B ×4 の候補ペア 6,616 は条件 A ×4 の 3,560 の 1.86 倍(着手前は 41,597 で 1.1 倍だが、これは
   ヘッドレスでは条件 A も同じ退化を起こしていたため)。**達成。**
3. 条件 C で参加者が 177 → 19(自機 1 + ベルトの節 18)まで落ち、薬莢 156 を含まない。**達成。**
4. 挙動不変の目視(弾が敵に当たる演出・排出直後の薬莢が艦体に弾かれる)は、ヘッドレスでは行えて
   いない。接触の答えを固定する回帰テスト(test:game 192 件)は通っている。**実機での目視は未実施。**
5. 条件 A のまま ×16 で参加者 0・候補ペア 0、×4 へ戻すと非 0 に戻る。**達成。**
6. 旧名の grep は `src` `tests` で 0 件(`.claude/worktrees/orbit-line-nocache/` は別ブランチの
   worktree で対象外)。`j <= i` も 0 件。**達成。**
7. typecheck、test:math(60/60)、test:game(192/192)。**達成。**

## リスクと落とし穴

| リスク | 影響 | 露見する場所 |
| --- | --- | --- |
| 段の選び方で一辺 < 2·到達量 になる(`log2` の丸め、到達量 0 の扱い) | 隣の段に落ちた相手を取りこぼす。接触が無言で抜ける | 手順2。テストで到達量を 4 桁に散らし、2 の冪ちょうどの到達量も含める。到達量 0 は最も細かい段 |
| 同じ段のペア列挙で「前方 13 セル」の軸を取り違える | 同じペアを二度返す(反発が二度当たる)か、片側の方向を落とす | 手順2。テストの「二度返さない」と全数探索との一致 |
| 粗い段を「隣の段」だけ見る | 2 段以上離れたペア(薬莢と基地)を取りこぼす | 手順2。テストで段を 3 つ以上作る |
| 手順3 で `j <= i` のガードを残す | 段をまたぐペアの半分が落ちる。候補が「減った」ように見えて喜ぶ | 手順3。grep 0 件。node 実測の比(1/10)より大きく減っていたら疑う |
| 段の器を毎 substep 作り捨てる | 占有セル数ぶんの `Map` と配列が毎 substep 死に、ブラウザの GC が周期的に跳ねる | 手順2。`reset` で段ごとの `SpatialGrid` を使い回す。手順3 の負荷ウィンドウでフレーム時間の分布を見る |
| 到達量の基準変位を `state` 側でなく `working` 側で取る/取らない | 解決の途中で書き戻された参加者の到達量が区間と合わない | 手順3・4。到達量は `working[i].r − prevState.r` で取る(現行 `contactCellSize` と同じ) |
| 交戦圏の境界をまたぐペアは解かれない | 30 km の縁で薬莢が敵を素通りする。仕様どおりだが目には「すり抜け」 | 手順1 の文面に明記。手順4 の `contains` は終端位置 |
| 自機が失われて中心が無くなると接触が止まる | 観戦中の薬莢・敵・弾が互いに素通りする | 手順1 の文面に明記(「交戦圏が1つも無ければ起こらない」) |
| 同じ交戦圏に相対速度の大きい 2 中心(逆行ですれ違う 2 自機) | 後ろの中心の雲が粗い段へ落ちて候補が増える。正しさは変わらない | 手順4。「候補ペア」が一時的に増えるだけで、すれ違えば戻る |
| 定数の付け替えで敵の射程・離脱距離が変わる | 両方とも 30,000 m なので値は不変のはずだが、単位(m と km)の書き間違いで 30 m になりうる | 手順4。ステージ00 で敵が 30 km 以内で撃ち、越えたら「交戦圏を離脱」と出る |
| 接触代理の所属判定 | 代理は艦から数 m なので常に内側。ただし `contains` は代理の `state.r` を読むので、`contactProxies` が返す代理の state が生成時点の world 状態で始まっている前提 | 手順4。条件 C で「参加者」が代理の数を含む |
| 交戦圏が空のときに接触代理の収集・書き戻しまで飛ばし忘れる、または ×4 以下でも飛ばす | 高倍率で `contactProxies(simTime, subDt)` を呼ぶと、ベルトの節点が最大 20 s の刻みで解かれて発散する。逆に ×4 以下で飛ばすと、ベルトが物に触れても弾かれない | 手順4。×16 で「参加者」が 0、×4 で「参加者」が放熱板の折りとベルトの節の数を含む |
| 敵の射撃のゲート(`enemy.ts:334` の `canShipAct`)と交戦圏の存在(`canEngage`)が別の getter | 同じ `MAX_PHYS_SIM_SPEED` から出ているうちは一致するが、片方だけ変えると敵が交戦圏の無いところで撃つ | 手順4。両 getter が同じ定数を読むことを確認する。射撃を交戦圏の所属へ寄せるのはこの計画の外 |
| 効果を DEBUG(高負荷)ステージの破片で測る | fragment は `collides=false`。何も変わらず「効かない」と誤判定する | 手順3・4。条件 A〜C は連射で薬莢を作る |
| 効果を ×1 だけで測る | ×1 では弾の変位が 17 m で退化が弱い(5,904 件)。効くのは ×4 | 手順3。条件 A は ×4 |
| 「候補ペア」の意味が階層化で変わる | 着手前後の比較が同じ量の比較でなくなる | 手順3。着手前に同条件で控えて表に書く(候補の定義は「`contactsWith` を通ったペア」で変えない) |
| 手順3 の中で到達量の基準まで自機の変位へ変えてしまう | 階層化の効果と基準変位の効果が 1 つの commit に混ざり、どちらが効いたか分からなくなる | 手順3。平均基準のまま commit してから手順4 |

### 却下したが、条件が変われば戻ってくるもの

- **交戦圏ローカル座標のブランド型。** 近傍関係は平行移動で変わらないので、候補ペアは 1 件も
  減らない。交戦圏の中で位置そのものを回転系で扱う必要が出たとき(例: 基地固定の座標で
  ドッキングの接触を解く)だけ戻る。
- **参加者の速度による自動クラスタリング(中心を置かない交戦圏)。** 遠方の密集(半周先の薬莢
  100 体で残る 5,000 件)まで細かい段に置けるが、substep ごとのクラスタリングの値段と、
  クラスタの割れ方が substep ごとに変わる不安定さがある。「参加者」が中心の無い場所に
  数百体たまり続ける構図(複数の自機を別々の軌道に置いて長時間戦う)が実際に起きたら戻る。
- **到達量に三次掃引の弦からの膨らみを足す。** LEO の 1/15 s では g·dt²/8 ≈ 5 mm で、最小一辺
  1 m に埋もれる。substep 幅が伸びる高倍率で物体どうしの接触を解くようになったら要る。
