## 目標
これは物理屋さんと地学屋さんの考え方の違いかもしれないが、まず太陽と地球と月を考え、次に木星を考え、次に土星を考え…という順より、まず任意天体の万有引力を考え、次に任意天体の大気を考え、次に任意天体の磁気を考え…という順で進めたい。万有引力と衝突のシミュレーションが十全に整っていない段階で、大気抵抗や地磁気、その他、太陽系に個別の事象を実装してしまうと大変なことになる。

私の提案（memos/hedalu244/フォルダ側）はそのような思想から、まずは一般的化された（正確で、拡張性があり、パフォーマンス的にも問題のない）万有引力シミュレーターとして、まず基礎を整えることを主眼としている。具体的には、複雑な軌道のための積分軌道と、比較的安定した軌道のためのケプラー軌道+摂動による解析的なものに二分する。万有引力は物体の質量のオーダーによって、常に無視する/近傍に近づいたときだけ計算する/常に計算する、の3種を使い分ける、といった理想像を立てている。

しかし、現時点では万有引力シミュレーターが不完全であるにも関わらず、大気抵抗や地磁気などを超えたものがどんどん実装されてしまっている。これらの挙動を極力保ちつつも、それらは「拡張性」の部分に切り出し、万有引力シミュレーターを理想形に近づけないといけない。

すでに実装されている万有引力以外の要素（空気抵抗、地磁気、恒星の輻射圧など、あるいはゲームとして/意思のある人工衛星としての軌道制御や弾の攻撃力やダメージ判定など）、およびこれから追加されうる要素(memos/フォルダ全体を参考)については、各物体に（万有引力のシミュレーションが数値積分的であるか解析的であるかに関わらず独立に）継承などによって追加しうるという形にする。万有引力シミュレーター側はそのような拡張性を残しつつも、個別の要素が密結合に絡まっていないクリアなものとして保ちたい。

衝突シミュレーションについては、現時点では弾であるか、天体であるか、大気再突入であるかといった区別になっているが、基本的には弾にせよデブリにせよ小惑星にせよ、剛体シミュレーションとして一般化してから、大気やガス惑星、プラズマ弾といった非剛体を後から考慮する形にしたい。

現在進めているfor_agent.mdやbetter_simulation_todo.mdはこのような大局的な思想に基づいていることを強調し、明言する。

## 実装計画素案

### step2残タスク
1. 分点歳差(指示書が報告を要求している独立検討事項)
1000年で赤道の向きが約5.6°動き、判断基準を満たしますが、これは軌道ではなく地球の自転軸の問題で、ECI の定義そのもの(J2 対称軸・大気の共回転・地球メッシュの自転軸・天球グリッド)を巻き込みます。指示どおり固定のままとし ecliptic.ts に明記しました。別ステップとして扱うかご判断ください。

2. perf 実測ができていません
Phase 0 の基準値・Phase 2-1b の比較とも headless では高負荷状態まで駆動できず測定不能。方針どおりメモ化は入れていません。参考値として Phase 7 完了後にステージ00・敵60体で sim 0.79ms を観測していますが、比較対象がないため判断材料になりません。重力源が3→4に増えているので、実機での確認をお願いします。

3. Phase 7-4 の受入項目 3・4 が未確認
headless CDP から右クリックのコンテキストメニューを開けず、「木星周回軌道に艦を置いて軌道線が木星中心に描かれる」「木星を航法ターゲットにできる」を実地確認できていません。型と経路はコード上確認済みですが、動作確認は実機でお願いします。

4. 月理論の数値表に残る不確実性
遠地点距離が文献値(406,700 km)を最大 187 km(0.05%)超えます。13項への切り詰めの限界と説明していますが、出典の実物と突き合わせた検証はできていません。

木星の e/inc の永年変化率も指示書に無い値を記憶から補っており、こちらは未検証のままです。

### Step3 引力がありかつ重力を受ける存在の追加

実装済み(`memos/hedalu244/for_agent.md` §4 が実行手順、実装結果は `CLAUDE.md`/`DEVELOP/OWNERSHIP.md`/`DEVELOP/CALLSTACK.md`/`DEVELOP/SPEC.md` に反映済み)。

- **無数の小惑星を追加してもパフォーマンス的に爆発しない**: `game/game-entity/asteroid.ts` の `Asteroid`(質量1つから重力定数と衝突半径を導く)が既存の積分経路にそのまま乗り、`game/simulation/gravity-attractors.ts` が解析天体の重力窓と生存中の重力天体を毎ステップ1回だけ合流させ、`physics/attractor.ts` の `relevantAttractors` が実際の寄与(静的な影響半径の見積りではない)でその位置に無視できる天体を落とす、という位置依存の絞り込みまでを実装した。**空間インデックス(spatial hash)は実測していないため要否が判断できておらず、`physics/spatial-grid.ts` は存在しない** — `for_agent.md` §2-11・Phase 8 が判断手順(絞り込み導入後に大量配置で実測 → 悪化があれば実装、なければ打ち切り)を持つ。
- **太陽が存在しない3連星系といった自由な星系**: 質量が比較可能な複数天体が相互に複雑な軌道を描く状況(閉じた解析解を持たない真の多体問題)は `Asteroid` どうしの相互重力がそのまま担い、現実の太陽系とは異なる天体構成・原点で進行するステージは `Ephemeris` が天体レジストリ・ECI 原点・主星の解決をインスタンスごとに持てるようになったことで表現する(`game/stages/stage-debug-alt-system.ts` が両方を実演するデバッグ専用ステージ)。既定レジストリでの既存挙動・GUI の選択肢は一切変わらない。
- **命名の再検討**(`game/celestial/` の見た目クラス群を `CelestialEntity` 系へ寄せるか)は見送った — 統合の前提(解析天体を状態を持つ側へ作り替えること)自体を採らなかったため、改名が意味を持つ前提が発生しなかった。
