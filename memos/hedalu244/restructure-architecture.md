# ゲーム全体の再設計 — 状態の種類から層と依存方向を決め直す

**段 1〜4 は main へ入っている(PR #80・#81・#84・#85)。段 5 は PR #86 で送った。残りは臨時の段 5.5(段 5 までの規則の未達と残件)と段 6〜8。**

**段 5 は 2026-09-18 に新設した**(K8-7)。当初の段 4 は R4(モデル層を進行と視点に分ける)と R11(セーブの規格化)を載せていたが、後者が繋ぎ方の設計(K8)で7手順に膨らんだので、そこで段を切った。以前の段 5〜7 は、番号が1つずつ繰り下がって段 6〜8 になっている。

**規則の書き込み先は `DEVELOP/ARCHITECTURE.md`**(2026-09-19 に CODING-RULE から分けた)。R1〜R13 と、CODING-RULE の 1.3・1.10・1.6「多態を保存し、復元する」・2.2 の `entity` / `motion` / `view` がそちらへ移り、CODING-RULE の 1.11〜1.13 は 1.10〜1.12 へ繰り上がった。`physics/` の正確さは CODING-RULE 1.1、「下位が自決できるものは下位が決める」は 1.4 に残る。**済んだ段の記録にある節番号とファイル名は、当時の CODING-RULE のもの。**

調査時点: 当初の洗い出しは `workspace4` @ `3ee0aeb3`。**文書中の `path:line` は、`restructure-architecture` @ `57fba6fa`(段 3 の終わり)で引き直してある。ただし K8 と手順 5-1〜5-6 の `path:line` は `65146de8`(4-3 の終わり)で引いた。段 4 の規約点検(`0e92d4ca`)で、`focus-camera-selection.ts` と `camera-*.ts` の行番号は少しずれている。**
指す先が変わっていたものは、行番号だけでなく本文も直した。
実施時はコードで引き直す。

## 目的

**immutableな領域を大きくしてmutableな領域を縮小する**。
**層ごとの責務と境界を再定義し、具体への依存を減らしていく**。
**層と層・持ち主と部品の繋ぎ方(直列化と復元、部品を誰が作るか、層の根を誰が組むか)を決める**(K8)。

現行の `DEVELOP/CODING-RULE.md` の層境界(1.3)、配列の対応付けの禁止(1.6)、フレーム位相(1.10)、
`entity` / `motion` / `view` の定義(2.2)は、「依存の向きはともかく、とりあえず分離する」ために
置いたものである。ここに適合しているかどうかでコードを評価すると、いまの形を肯定して固定するだけになる。

この計画は、**状態の種類と、その値を誰が読むかという事実だけから規則を設計し直し**、コードをその規則へ
移す。目標とする規則は「従えば、immutable と mutable が分かれ、責務も分かれる」ものである。

いまのコードの問題は、次の3つが `src/game/` に同居していることにある。

- **1ランの mutable な正本。** シミュレーション、ステージの進行、操作状態、表示の選択。
- **表示の導出。** 状態から表示の宣言を作る処理、1体ごとの View・マーカー・軌道線、HUD のパネル、ピック判定、入力の解釈。
- **immutable な天体。** 構築時の値と時刻だけで状態が決まる。

同居していることの結果として、次が起きている。

- `src/game/` から `render/` への import が 82 ファイル・207 行ある。`hud/`・`game/hud`・`game/marker` を除く `src/game/` のうち、`audio/` を import するものが 28 ファイル、`hud/notifier` が 23 ファイル、`input/` が 22 ファイル。
- 実体の構築経路のすべてに `scene` / `Notifier` / `WorldSfx` / `FlashEffects` / `MarkerSlots` が通されている。
- 表示の選択が規則へ漏れている。
  - 計画ノードの消化は、戦闘ビューを表示しているときだけ走る。
  - 表示期間と軌道線の表示トグルが予測弧の有無と長さを決め、実シミュレーションは弧が届く範囲で積分せず弧をなぞる。
  - 主星の決定を描画 View の `stellarLight` から引いている。
- ループ音の停止が呼び出し側に散らばっている。そのうち決着時の停止は、次の sync で上書きされて効いていない。

修正後に期待する状態は、以下の規則が `DEVELOP/ARCHITECTURE.md` の正本になり、コードがそれに従っていること。

## 進め方 — 規則は段の中でだけ先行させ、段ごとに main へ送る

ファイルが大きく動くので、**段ごとに main へ送って同期する。** 他の作業との衝突を、段の区切りより長く抱えない。

そのため、**規則を最初に全部書かない。** 全部書くと、段の終わりごとに「規則に書いてあるのに従っていないコード」が main に入る。読み手はそれを規則どおりだと思い込むか、逆に規則へ合わせる修正を計画の外で始める。後者は、次の段が触る場所を先に別の形へ動かすので、計画が破綻する。

代わりに、**規則は段の中でだけコードに先行させる。**

1. 段の最初の2手順(N-1・N-2)で、**その段の終わりに満たせる分だけ**規則を書き、機械的検査へ判定を足す。**判定は形で行い、名前で代用しない。判定できない規則は、判定できないと対応表に書く**(達成目標の「規則と判定の対応」、K10)。
2. 段の途中の残りは許可リストに載せる。許可リストは減ることしか許さない。
3. 段の終わりに許可リストを空にし、**判定の無い規則を対応表のとおり洗い出してから** PR を出す。**main に入る時点で、書かれている規則はコードが満たしている。**

段と段の間に他の作業が入っても、その作業が見るのは「書かれている規則」だけでよい。次の段の着手条件は「main が段 N までの規則を満たしている」ことだけになる。`npm run check:boundaries` が判定するのは、そのうち判定のある規則だけである — **段 3 は R3 に判定を足さず、許可リストが空であることを R3 を満たした根拠にしたので、R3 に反するコードが main に入った**(K10)。検査は規則の代理であって規則ではない。検査を通すために名前を替えない。

**中間の規則(暫定)。** 最終形の規則をその段でそのまま書けないときは、その段で満たせる形に狭めて書く。最終形と違う文面には `(暫定 — 段 N で外す)` を付け、段 N の N-1 の手順で外す。狭め方は次の3つだけにする。

- **範囲を狭める。** 例: R6 を dynamic の族とステージにだけ当て、天体の族は段 7 で足す。
- **置き場を仮に認める。** 例: 表示の導出は当面 `src/game/` の中に置く。
- **層の代わりにパスで禁止する。** 層の対応表がまだ割れていない場所を、禁止パターンで代用する。

`rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が段ごとに変わり(段 1 で 3、段 2 で 4、段 3 で 7、段 4 で 6、段 5 で 4、段 5.5 で 10、段 6 で 10、段 7 で 8)、段 8 で 0 になる。**いまは 10**(段 5.5 の 5.5-3 の後)。

### 書き込む規則(最終形)

段 8 の終わりに、`DEVELOP/ARCHITECTURE.md` が下の R1〜R13 を正本として持つ。これから置き換わるのは、ARCHITECTURE の「`launcher/` が `game/` を見てよいのは…」と `entity` / `motion` / `view` の節、CODING-RULE 1.6「配列に対して外部から対応付けを行う設計を避ける」の具体例の段落。ARCHITECTURE の `physics/` の調整値・接触の各節と、CODING-RULE 1.1 の `physics/` の正確さは、層の中身の規則としてそのまま残す。R1〜R11 は層の切り方、R12・R13 は繋ぎ方(K8)を決める。

**どの段でどれを書くかは「規則を段へ振り分ける」の表**で、段ごとの文面の指定は各段の N-1 の手順にある。

**R1. 状態の種類で層を決める。**

| 層 | 持ってよい mutable な値 | その層に入る条件 |
| --- | --- | --- |
| 定義層 | なし | 実行中に値が変わらない(定数・データ表・型・純関数) |
| 時刻層 | 結果を変えないメモ化キャッシュだけ | 構築時に決めた immutable な値と時刻 t だけで答えが決まる |
| モデル層 | 正本 | 捨てたら、ほかの層からは作り直せない |
| 導出層 | R5 の5種だけ | 捨てても、ほかの層から毎フレーム作り直せる |

- 値の置き場は「その値を捨てたら作り直せるか」で判定する。作り直せないならモデル層(セーブを跨いで共通の設定なら、アプリ寿命の正本、R10)。作り直せるなら導出層のキャッシュ。
- 乱数で決めた値は作り直せないのでモデル層に置き、結果を保存する。乱数の系列そのものは保存しない。ランの再現性は目標にしない。

**R2. import は不変な側へだけ向く。**

- 定義層と時刻層は、どこから import してもよい。不変な値は、誰に読まれても整合性を壊さない。
- モデル層を import してよいのは、表示の導出とアプリの組み立て(`main.ts` / `launcher/`)だけ。
- 表示の導出を import してよいのは、アプリの組み立てだけ。
- 装置(`render/` / `hud/` / `marker/` / `audio/` / `input/`)が import してよいのは、定義層・時刻層と自分自身だけ。装置どうしは import し合わない。装置を import するのは表示の導出とアプリの組み立てだけ。
- 時刻層が import してよいのは定義層と時刻層だけ、定義層は定義層だけ。
- import を1本も持たないモジュール(union や定数だけの語彙)は、どのフォルダにあっても定義層として扱う。
- フォルダは層に揃える。

| フォルダ | 層 |
| --- | --- |
| `math/` | 定義 |
| `physics/` | 時刻(法則・暦・時刻系) |
| `celestial/`(新設) | 時刻(この宇宙の天体系: 天体の定義データ・系の構築・索引) |
| `theme.ts` | 定義(全配色の表とトークン。どれを使うかは settings の値) |
| `entity-shapes/`(新設) | 定義(実体の形状の正本: 寸法・取付位置・当たり判定の近似。入れてよい条件は K11) |
| `game/` | モデル(進行と視点。ゲーム内容の定義も持つ) |
| `game/viewer/`(新設) | モデルのうち視点(R4) |
| `presentation/`(新設) | 導出のうち表示の導出(意味を知る側、入力の解釈、表示担当) |
| `render/` / `hud/` / `marker/` / `audio/` / `input/` | 導出のうち装置 |
| `settings/` | アプリ寿命の正本(設定。視点ではない) |
| `launcher/` / `run/`(新設) / `main.ts` | アプリの組み立て |

**R3. 正本には書き手が1つあり、書くのは進行の位相だけ。**

- モデル層の値を書くのは、その値を所有するモジュールだけ。所有者は、その欄を宣言したクラスである(所有の木の中でも例外にしない)。外から変えるときは、所有者が公開する命令(メソッド)を呼ぶ。
- モデル層の口は、命令・読み取り面・出来事の3つ。命令の名前の形は問わない。整合性は所有者が命令の中で保ち、呼び手に世話の口を公開しない。
- モデル層のクラスは可変な public 欄と `set` アクセサを持たず、export する型も可変な欄を持たない。
- 命令は受け付けた時点では状態を変えない。次の進行の位相の先頭で、受け付けた順に適用する。
- 外から読むときは、所有者が公開する読み取り専用の面を通す。可変オブジェクトそのものを外へ渡さない。

**R4. モデル層は「進行」と「視点」に分け、進行は視点を読まない。**

- **進行**: 規則が読む値。積分、接触、AI、ステージの進行と勝敗、計画の実行、spawn、操作対象、計画、ワープ段。
- **視点**: 遊ぶ人の選択のうち、**セーブごとに持つもの**。別のセーブを読み込めばそのセーブの値になり、新しく始めたゲームでは既定から始まる。カメラ、ビュー、航法ターゲット、軌道要素の基準、計画軌道の座標系、表示期間と表示時刻、軌道ガイド、実体ごとの表示設定。
- **視点を設定と取り違えない。** 「何をどう見ているか」は視点の条件にならない。描画品質・見せ方・配色・表示トグル・パネルの畳みとタブも見え方を決めるが、セーブを跨いで共通なので設定(`settings/`、R10)に置く。設定は別のセーブを読み込んでも新しいゲームを始めても引き継がれ、セーブに入らない。**どちらに置くかは SPEC が決める**(`INVARIANTS.md` §6: そのゲームの空間と時間に依拠する選択はセーブ)。**SPEC が決めていない値は、1つの所有者の状態をセーブと設定へ割らない側に置く**(例: 画角と投影は、カメラの他の値と一緒に視点)。
- 進行は視点を import しない。視点が進行に効いてよいのは、表示の導出がフレームごとに渡す**需要**(予測を伸ばす対象と長さ、履歴を残す長さ)だけ。
- **需要が変えてよいのは計算量だけ。** 同じ命令の列からは、需要によらず同じ進行の状態にならなければならない。
- 視点は進行を読んでよい(例: 注視対象が消えたら戻す)。

**R5. 導出層は、捨てても作り直せるものだけを持つ。**

- 導出層が持ってよい mutable な値は次の5種だけ。
  1. 結果に効く入力をすべてキーにしたキャッシュ。
  2. GPU・DOM・音声の資源。
  3. 外界の写し(押下中のキー、ポインタ、画面寸法、タブの可視状態)。
  4. 捨ててよい操作途中の状態(ドラッグ中の量、入力途中の文字、開いているメニューやプロパティ窓)。
  5. 表示を安定させるための前フレームの記憶(ヒステリシス、猶予)。
- 4 と 5 は、表示と入力の解釈以外に影響してはならない。影響するなら視点に置く。
- どれにも当たらない値は、視点か `settings/` に置く。ユーザーが選んだもの、時間をかけて積み上がったもの、他へ影響するものがこれに当たる。セーブごとに持つなら視点、セーブを跨いで共通なら `settings/`(R4)。
- 導出層のアニメーション(遷移・寿命・フェード)は dt で積まず、開始時刻とそのフレームの実時刻から計算する。実時刻はフレームの先頭で1度だけ読み、入力として配る。

**R6. 1体ぶんの表示物は、表示の導出の中で1つの表示担当オブジェクトが持つ。**

- モデル層の実体は、表示物への参照を持たない。
- 表示の導出は、実体の族ごとに突き合わせ役を1つ置く。突き合わせ役はモデル層の実体の集合とその改版番号から、実体1つにつき1つの表示担当を作り、消えた実体の表示担当を捨てる。鍵は実体オブジェクトの同一性。突き合わせ役以外のモジュールは、表示担当を作らず、捨てない。
- 実体1つぶんの表示物は、すべてその表示担当のフィールドであり、表示担当自身の sync の中で更新する。3D の View、マーカー、軌道線、一覧の行、メニュー、プロパティ行がこれに当たる。管理者ごとに id で別々の表を持つ形にしない。
- 表示担当の具象は、実体の種別をタグとするクラス辞書で引く(セーブの復元と同じ形)。
- 集合が構築後に変わらない族(天体)は突き合わせず、構築時に1度だけ作る。
- 実体ごとの視点の値(軌道線の表示トグルなど)は、視点が同じ突き合わせで持つ記録に置く。実体のフィールドには置かない。

**R7. 装置は意味を知らず、宣言だけを受ける。**

- 装置の入力は、そのフレームに見せる(鳴らす)べきものの全体の宣言である。装置は内部で前回との差分を取る。`show` / `hide` / `set` / `play` / `stop` の類の命令 API を外へ公開しない。
- 装置の入力型は、装置が自分の語彙で定義する。実体の種別、ステージ、選択といったモデルの意味を持ち込まない。意味から装置の語彙への写しは、表示の導出が行う。
- 一回きりの出来事(効果音、通知、閃光)は、モデル層が進行の位相で記録する「直近の進行で起きた出来事」を使う。出来事には通し番号が付く。表示の導出がそれを読んで装置へ宣言として渡し、装置は同じ番号を二度扱わない。
- 装置の出力(描いた線の点列、マーカーを出したか、投影)を読んでよいのは、表示の導出の入力解釈(ピック判定)だけ。
- ブラウザの制約(音声のロック解除、タブの可視状態)は、その制約を受ける装置が自分で扱う。

**R8. フレームの位相はデータの流れに合わせる。**

1. **入力の解釈**(表示の導出): 生の入力と前フレームの装置の出力を読み、命令とそのフレームの操作量を作る。
2. **進行**(モデル層): 命令を適用し、操作量と dt で進め、出来事を記録する。dt を受け取るのはこの位相だけ。一時停止中も命令は適用するが、シミュレーションは進めない。**進行の末尾で、表示の導出が進行の結果から作った材料 — 視点の追従の材料(`CameraFrameSamples`)と需要 — を受け、視点を追従させ、予測と履歴を伸ばす。** 材料を作る処理はモデル層を書かない。
3. **導出と同期**(表示の導出 → 装置): 読み取り面と表示時刻から表示の宣言を作り、装置へ渡す。モデル層を書かない。
4. **描画**(装置): 描画命令を発行するだけ。

- DOM イベントはフレームの外で起きるが、そこで作ってよいのは命令と R5-4 の状態だけ。
- セーブは位相 2 と 3 の間で、モデル層だけを直列化する。
- **位相の順序はランの組み立て(`src/run/`)が持つ**(R10)。

**R9. 型と定数は、その値の正本の所有者に置く。**

- 値の型は、その値の正本を持つモジュールに置く。装置の入力型は装置に置き、表示の導出が写す。
- 同じ意味・同じ情報量の型を層ごとに作らない。装置が同じ語彙を要るなら、import を持たない語彙モジュールとして所有者の側に置き(R2 により誰でも読める)、装置はそれを読む。

**R10. アプリ寿命の正本と組み立て。**

- `settings/` は、セーブを跨いで共通のユーザー設定の正本である。パネルの折り畳みやタブ選択のように、どのゲームにも共通する UI の選択もここに含む。特定の物体・天体・位置を指す選択は、ページを読み込み直しても残す値であってもここに置かず、視点としてセーブで運ぶ(R4)。表示の導出へは、読み取り専用の面と変更の callback で渡す。モデル層がラン跨ぎの設定を要するなら、ランの構築時の値として受ける。値の型・妥当性の判定・保存文字列との変換は、その値を解釈する層が持つ。`settings/` がそれを import してよいのは、定義層・時刻層だけを import する値の定義モジュールに限り、その範囲なら層を問わない。
- `launcher/` は、ランの外の記録(セーブスロット、解放記録)の正本を持ち、ランを起こし畳む判断(新しく始めるか、直列化された記録から再開するか)を持つ。
- **ランの組み立て(`src/run/`)は、1ランのモデル層の根と表示の導出の根を組み、R8 の位相の順序を持つ。**
- `main.ts` は、ページの寿命のもの(装置、HUD の外枠、設定、launcher)の組み立てと、rAF の登録を持つ。位相の順序は持たない。
- モデル層は `settings/`・`launcher/`・`run/` を import しない。

**R11. セーブはモデル層そのものである。**

- スナップショットは、モデル層(進行と視点)の直列化である。表示の導出と設定は含めない。読み込みはモデル層を組み直すだけで、表示の導出は突き合わせ(R6)で作り直され、設定は読み込みの前後で変わらない。
- **モデル層の値は、例外なくすべてセーブする。** セーブしないのは、正本から計算し直すキャッシュ(予測の弧など)だけで、所有者のコメントにキャッシュであることを書く。作り直すと数値計算の近似を超えて保存前と違う値になるもの(復元した id から数え直す採番など)は、キャッシュではなく正本である。
- 時刻層の天体系は、構築に使った immutable な値だけを保存する。元期、暦プロファイル、暦パック、自転初期位相、位相オフセット、系の選択がそれに当たる。

**R12. 直列化は所有者が書き、復元は静的ファクトリが行う。**

- モデル層が知るのは「直列化」だけである。セーブ(スロット、記録、自動セーブ、形式の版の照合)は `launcher/` の語彙で、モデル層の型・メソッド・変数の名前に `save` / `restore` を使わない。
- `T` の直列化された形は `SerializedT` と呼び、`T` のモジュールに置く(R9)。JSON の素の値だけで組む。**不変で、JSON の素の値だけから成る値の型は、そのまま直列化の形に使い、`SerializedT` を別に作らない**(1.6 の同じ情報量の型の禁止)。
- `serialize(): SerializedT` は引数を受けない。逆は `static deserialize(serialized: SerializedT, …): T` で、`serialized` の後ろに受けてよいのは、直列化に含まれない協力者(R13)と復元時刻、どの種類として組むかを呼び手が決める不変な定義(カメラの設定・種別ごとの慣性など)だけ。
- **コンストラクタは直列化された形を受けず、新しく作るか復元するかで分岐しない。** 受けるのは、復元済みの部品と状態、部品を作る材料になる復元済みの値、協力者である。構築した後で状態を流し込むメソッドを作らない。
- **記録に無い項目は、新しく作ったときの初期値で補う。** 初期値はコンストラクタの既定引数に1か所だけ書き、`deserialize` は欠けた項目を `undefined` のまま渡す。`deserialize` に書くのは、初期値と違う値で補うものと、ほかの項目から導くものだけ。
- 子を持つなら、親の `deserialize` が子の `deserialize` を呼び、できた子をコンストラクタへ渡す。多態の子は、タグ辞書が具象の `deserialize` を引く(1.6)。
- **1つの記録に、2つの所有者の値を混ぜない。** 所有者の `serialize` へ、ほかの所有者の値を渡さない。形式が混ぜているなら、直すのは形式のほうである(K9)。

**R13. 部品は持ち主が作り、協力者は受ける。**

- **部品**(持ち主だけが参照し、持ち主と寿命が同じで、持ち主の直列化に含まれるもの)は、持ち主のモジュールが作る。新しく作るときはコンストラクタ(既定引数を含む)か静的ファクトリ `create`、復元するときは `deserialize` が作る。上位から注入しない。部品が持ち主への参照を要するなら、持ち主のコンストラクタが復元済みの値から作ってよい。
- **協力者**(複数の持ち主が参照するもの、持ち主より長く生きるもの、ほかの所有者が直列化するもの)は、コンストラクタか `deserialize` で受ける。受けた側は作らず、直列化せず、破棄しない。**同じ協力者を「構築時に受けて持つ」と「呼ぶたびに受ける」の両方で受けない。**
- 協力者を作るのは、その寿命の根である。ランの寿命ならモデル層の根と表示の導出の根、ページの寿命なら `main.ts`。
- 種別で具象が変わる部品は、新しく作るときは種別を知る規則(spawn など)が選び、復元するときはタグ辞書が選ぶ。持ち主は狭い型で受ける。
- **構築を上へ集めるのは層の根までである。** 層の根は自分の層の部品を作る。ランの組み立ては2つの根を作り、ページの寿命の装置を渡すだけ。
- **層を跨いで渡すのは、所有者ごとの読み取り面を所有の木のまま辿れる根である。** 層の入力として値を平らに並べた型を作らない(1.6)。
- テストのために注入しない。テストは本物の部品で組む。

### 規則を段へ振り分ける

| 段 | 書く規則 | 置き換える既存の節 | この段で置く暫定 | 検査に足す判定 |
| --- | --- | --- | --- | --- |
| ~~1~~ 済 | R1、R2 の不変方向・装置・時刻層・語彙の各項、R7(出来事の段落を除く) | 1.3 の `math/`・`render/`・`input/`・`hud/`・`theme.ts`・`game/` の各節 | `hud/` を表示の導出として扱う(ただし `game/`・`settings/`・`launcher/` への import は禁じる。段 8)、`game/` の中を割らない(段 6)、装置は実時刻を sync の入力で受ける(段 2 で R5 へ吸収) | 装置の出ていく import、装置どうしの相互 import、時刻層の出ていく import、`src/hud/` の出ていく import、命令 API の禁止パターン |
| ~~2~~ 済 | R5、R10 の `settings/` の項、R2 の定義層の項 | 1.3「設定の正本を consumer へ渡さない」 | 表示の導出の置き場は `src/game/` の中(段 8)、R5 が「視点へ置く」と言う値は `game/` の所有者が持つ(段 4) | 定義層の出ていく import、`localStorage`・`RunSetting`・二段初期化の禁止パターン、`game/hud` が `Game` を import しないこと |
| ~~3~~ 済 | R3、R7 の出来事の段落、R8、R4 の需要の2項 | 1.10 の全体 | 位相の順序は `game/game.ts` が持つ(段 8)、モデル層の禁止は層でなくパスで当てる(段 6)、視点への書き込みは列を通さなくてよい(段 4) | 表示の選択が進行へ漏れる禁止、モデル層が生の入力を読む禁止、モデル層が出来事の装置を持つ禁止(どれもパス指定) |
| ~~4~~ 済 | R4 の全文、R11 の1。R1・R5・R10 の「ラン跨ぎ」を「セーブを跨いで共通」へ言い直した。**4-1 で先に書いた R11 の2は、4-4 で落として段 5 へ回した**(K8-7) | — | **`game.ts` は段 5 まで分割しない**(4-4。段 5 で外す) | `game/viewer/` への片方向、`settings/` と `game/viewer/` の相互 import、セーブの型から表示の導出への import(以上 4-2 で済。落とす判定は無い) |
| ~~5~~ 済 | **R12、R13、R8 の言い直し(進行の末尾の追従・位相の順序の置き場)、R10 のランの組み立ての項、R2 の表の `run/`、段 4 で落とした R11 の2**(5-1) | 1.6「多態を保存し、復元する」の3点目、1.10 の暫定「位相の順序を組み立てるのは `game.ts`」、4-4 の `game.ts` の暫定 | (R11 の3は段 7 で足す) | `run/` を組み立てとして対応表へ、直列化の語彙・復元の流し込み・直列化された形を受けるコンストラクタの禁止パターン、モデル層の根が表示の導出を持つ禁止(5-1) |
| 5.5(臨時) | 新しい規則は無い。**R3 の言い直し**(所有者は欄を宣言したクラス、モデル層の口は命令・読み取り面・出来事、整合性は所有者が命令の中で保つ、可変な public 欄と `set` を持たない)。`motion` の基準(K10-6)・軌跡の置き場(K10-7)・形状の正本のフォルダ(K11)は段 6 で書く | ARCHITECTURE の層の中身の `physics/` の「状態を持つクラスを置いてもよい」、CODING-RULE 1.11 の「mutable な public フィールドは禁止しない」のモデル層への適用 | 軌跡の記録を `physics/` に置き、運動が読み手へそのまま渡す(段 6。K10-7)。5.5-3 の洗い出しで残すと決めた5つ — 表示の導出がモデル層の所有者を具象のまま受ける(段 8)、被選択物の猶予から視点を動かす(段 8)、カメラの投影を `render/` が返す(段 8)、入力の解釈が押し続ける入力を dt で積む(段 8)、実体が同じ協力者を2経路で受ける(段 6) | **R3 を構文木で判定**。「命令 API の禁止」の名前の列を外す。コンストラクタの判定を引数の型(`Serialized*`)へ |
| 6 | R6(dynamic の族とステージ)、R2 のモデル層の項、`motion` の基準(K10-6)、軌跡の置き場(K10-7)、R2 の層の表の `entity-shapes/` とその入れてよい条件(K11)。K10-6・K10-7 と K11-2 は段 6 の着手前に決める | CODING-RULE 1.6 の具体例の段落、ARCHITECTURE の `entity` / `motion` / `view` | R6 を天体の族に当てない(段 7)、`game/celestial/` は未分類(段 7)、モデル層の根は天体系の描画のために `GameScene` を受けてよい(段 7)、`render/` の語彙の型 import を許す(段 8) | 対応表を `game/` の中まで割り、モデル層の出ていく import と `three` を判定 |
| 7 | R6 の全域、R11 の3 | — | 時刻層は `game/celestial/` にある(段 8) | 時刻層の規則を `game/celestial/` へ当てる |
| 8 | R2 の最終の対応表、R9、R10 の全文 | ARCHITECTURE「`launcher/` が `game/` を見てよいのは…」 | なし(残る8つをすべて外す) | 最終の対応表、`hud/` を装置として判定、`settings/` の import 規則、許可リストを空にする |

**段の切り方は「規則が完結する単位」で決めた。** R7 は装置3つ(`render/`・`marker/`・`audio/`)が揃って宣言的になるまで書けないので、段 1 はその3つを含む。R6 は表示担当が族ごとに揃うまで書けないので、dynamic の族(段 6)と天体の族(段 7)に分かれる。R12・R13 は段 5 に入れた — 手順 5-5 で足す直列化のコードを、新しい形で一度だけ書くためである。同じ理由で、`game.ts` の分割(5-3)も段 5 で済ませる(K8-5)。**手順を段の間で動かすなら、その手順が満たす規則も一緒に動かす。**

## 決めたこと

判断材料は事実だけにした。事実とは、状態の書き手と読み手、書く位相、寿命、セーブの有無、import の辺である。**現行の CODING-RULE に適合するかは判断材料にしていない。** 以下はどれもユーザーが覆せる。覆したときに変わる手順も書く。

### K1. 方針の評価と修正点

「フロントを純粋にし、mutable な正本をバックエンドへ集め、依存を逆転する」という方針は、R1〜R11 と一致する。そのうえで、事実から次の4点を修正した。

1. **逆転する相手は `render/` ではない。**
   - 直近で純粋化した `render/` は、自分の語彙で入力を定義し、game を知らない装置として正しい形にある(R7)。
   - 逆向きになっているのは、`game/` に混ざった表示の導出とモデル層の間である。
   - したがって「import 方向の逆転」は、表示の導出を `presentation/` へ出し、`presentation → game` の向きにすることで実現する。`render/` が `game/` を読む形にはしない。
   - 覆して2層(render/hud がモデルを直接読む)にする場合: 手順 8-3〜8-6 が変わり、render の入力型をモデルの読み取り面へ置き換える作業が加わる。
2. **「mutable ≈ セーブされる領域」は、R11「モデル層 = セーブ対象」として規則にできる。** mutable な値は、操作途中の状態を除いて、ほとんどすべてが何らかの形で残るべきである。残し方は2つあり、取り違えない(ユーザー判断 2026-09-17)。
   - **セーブごとに持つもの(視点と進行。モデル層)と、セーブを跨いで共通のもの(設定。`settings/`)に分かれる。** 別のセーブを読み込んだり新しいゲームを始めたりしたとき、前者は入れ替わる・既定へ戻り、後者は引き継がれる。「何をどう見ているか」はどちらにも当てはまるので、分ける基準にならない。
   - **どちらに置くかは SPEC が決める。** `1e908f1e` は「物体・天体・空間上の位置に依拠する選択はセーブ」とした。この基準では、表示期間やスクラブ位置が設定に入ってしまう。セーブごとに組んでいる軌道の時間尺度は違うので、引き継がれると不便である。そこで、**時間に依拠する選択もセーブ側に寄せる**よう SPEC を直す(K3-5、手順 4-0)。軌道ガイドは、空間の基準によって `962db128` ですでにセーブへ移っている。
   - **SPEC が決めきれない値は、依存関係が単純になる側に置く。** 画角と投影は何にも依拠しないが、注視距離・正射影の半高さと換算し合うので、カメラと一緒に視点に置く。予測パネルの目盛りの表示・相対表記・通過時刻の併記・カメラの基準に追随も、同じパネルの表示期間と一緒に視点に置く。**決めきれない基準は SPEC に書かず、実装者の解釈に委ねる。**
   - **進行には例外を置かない。** 進行の値は無条件でセーブに入り、TODO として残さない。セーブしないのは、正本から計算し直すキャッシュ(予測の弧など)だけ。
   - **操作途中の状態(R5-4)は、いまの挙動のままでよい。** 開いている窓とその位置・中のタブ、選択中の計画ノード、物体一覧の展開・並び順、予測パネルの入力欄の単位など。残してもよいし残さなくてもよい。基準を言語化しきれないので、SPEC には書かない。

   段 4 では、いまの値を次のように扱う。どれも省略可能な項目として足すので、SAVE.md「形式の版」により版は据え置き、段 1 の前のセーブも読める。
   - **視点なのにセーブされていない値を載せる**(手順 5-5)。軌道要素の基準と、予測パネルの状態(計画軌道の座標系、未来側・過去側の表示期間と任意期間、スクラブ位置、目盛りの表示と相対表記、通過時刻の併記、カメラの基準に追随)。
   - **進行なのにセーブされていない値を載せる**(手順 5-5)。ワープ段と `autoWarpUntil`、基地の計画・計画実行モード・`fineAttitude`、creative の手動 spawn の距離と表示設定、デバッグステージの状態、採番器の次の番号。
3. **天体系は「パック + 元期 + 時刻」だけでは決まらない。** 自転初期位相・位相オフセット・系の選択(ステージが決める原点と星系)も要る。これらを1つの immutable な構築値として扱えば、時刻層として救出できる(手順 7-4)。
4. **1体の表示物の持ち方は「実体に分散」でも「全知の層 + id の対応表」でもない。**
   - R6 のとおり、表示の導出の側で**実体ごとに1つの表示担当へ分散**させ、生成と破棄だけを族ごとに1つの突き合わせ役が行う。
   - 実体側に表示物の参照(不透明な型のフィールドへ具象を注入する形)を置く案は採らない。理由は次の3つ。
     - 表示の導出側で具象へ戻すのに、型アサーションか全体に及ぶ型引数が要る。
     - update 中に生まれる弾・破片の構築経路のすべてに、工場を通すことになる。これは今の `scene` / `WorldSfx` / `MarkerSlots` の貫通と同じ形である。
     - モデル層が表示物の寿命を持ち続ける。
   - 管理者ごとの id 対応表(いまの軌道線、マーカーの文字列キー、赤道交点の Map)は、別々の表が別々に整合を取る形なので採らない。

### K2. 規則を当てた結果(置き場の判定)

| 状態・モジュール | 置き場 | 根拠(事実) |
| --- | --- | --- |
| 操作対象(`control-selection.ts`) | 進行 | AI の追尾(`enemy-fire-controller.ts:56-69`)、ステージの補給・勝敗(`stage.ts:221`)が読む。交戦範囲外の弾の消滅は、`c3a58b30` で全実体から組む交戦圏で決めるようになり(`bullet-reaction.ts:66-67`)、操作対象を読まなくなった |
| 計画・計画実行モード・`fineAttitude` | 進行 | 'instant' の実行(`creative-stage.ts:228-249`)と、角加速度の上限(`throttle.ts:205`)が読む |
| ワープ段・`autoWarpUntil` | 進行(手順 5-5 でセーブへ載せる) | 積分の刻み(`simulator.ts:80-91`)、AI の射撃可否(`enemy-fire-controller.ts:62`)、接触(`engagement-zone.ts:51`)、補給(`logistics.ts:124-134`)が読む |
| 一時停止 | 進行への入力(値を持たない) | 開いているオーバーレイから毎フレーム導き(`game.ts:156`)、`advanceSimulation` を止める(`game.ts:533-534`)。モデル層に値が無いので、セーブの対象にもならない。停止中の自動セーブは意図して抑止している(`autosave.ts:26-28`) |
| 敵の攻撃グループ `attackGroupId` | 進行の不変な属性 | 同じグループの敵を1集団として、同時に攻撃する数を制限している(`enemy-attack-group.ts:14`)。当初はこれを識別色 `accent` で決めていたが、`0ed8b1ac` で切り離され、`accent` はマーカー色になった(`enemy.ts:76`) |
| `orbitLineColor`・名前 | 実体の不変な属性 | 表示とセーブだけが読む。不変なので実体が持ってよい(R2) |
| 航法ターゲット(id と名前) | 視点 | 読むのは表示・予測の需要・ピック判定だけ。AI・ステージ・simulator からの参照は無い |
| `Targeter.aliveTarget` | 導出 | NavTarget と roster から毎回計算している(`nav-target.ts:134-137`) |
| `Targeter.boardMarks` | 導出(出来事から作る) | 読むのは的のマーカーだけ(`targeter.ts:310-320`)。段 3 で、弾が的面を通過したという出来事から作る形になった(`targeter.ts:142-160`) |
| カメラ(注視・offset/pan/up・FOV・投影・基準面・回転モード・追従) | 視点 | 規則・予測・音は読まない。セーブはされる(`focus-camera.ts:625-651`)。FOV と投影は何にも依拠しないが、注視距離・正射影の半高さと換算し合うので、カメラの他の項目とまとめてセーブに置く(ユーザー判断 2026-09-16・2026-09-17。カメラの状態をセーブと設定へ割らない)。SPEC はこの2つの置き場を決めない(手順 4-0) |
| ズームの遷移・猶予カウンタ・`viewpoint` | 導出(R5-5 と計算値) | 状態と入力から毎フレーム求まる |
| ビュー(戦闘/マップ) | 視点 | 予測の範囲に効く経路が4本ある。規則に効く2本は後述の SPEC 判断で断つ |
| 予測パネルの状態: 計画軌道の座標系(中心と回転)、未来側・過去側の表示期間と任意期間、スクラブ位置(`sliderT`)、目盛りの表示と相対表記、通過時刻の併記、`followCamera`(カメラの基準に追随) | 視点(手順 5-5 でセーブへ載せる) | 座標系は天体・機体・役割を指し、表示期間とスクラブ位置はそのゲームの時間を指すので、§6 によりセーブに残る(時間の側は手順 4-0 で SPEC に足す)。残りの3つは SPEC が決めないので、同じパネルの状態と一緒に置く(K1-2)。いまは `display-window-manager.ts:90-98` が持つが、目盛りの表示だけは正本を持たず DOM の `hidden` を直に書いている(`predict-panel.ts:368-373`)。どれもセーブされず、ページの読み込み直しで既定へ戻る。表示期間は予測の horizon と履歴の保持長を決めるが、進行へは需要として届く(`display-window-manager.ts:63-71`)。`followCamera` の正本は `711c6611` で HUD の public フィールドからここへ移った |
| 軌道要素の基準モード | 視点(手順 5-5 でセーブへ載せる) | 地球・月・ターゲットを指す選択。表示だけが読む(`orbit-reference.ts:54`)。いまはセーブされない |
| 軌道ガイドの選択 | 視点 | 系と参照軌道が天体を指すので、`962db128` でブラウザからセーブへ移った。いまは `Game` が持ち(`game.ts:165,247,290-293,715`)、型と読み直しは `game/celestial/orbit-guide/orbit-guide-settings.ts` にある。**セーブの形式の一部なので、手順 7-3 で表示の導出へ出さない** |
| 軌道分析ウィンドウの開閉 | 導出(R5-4。操作途中の状態として、いまの挙動のまま) | 何も指さないので、視点ではない。当初は、分析窓が予測を読む者のフラグを書いていたので予測の需要に効いた。フラグは `9bef624f` で消え、段 3 の需要(`display-window-manager.ts:63-71` の `trajectoryDemandOf`)は表示期間・履歴・計画の弧だけから組む。いまはほかに影響しない |
| プロパティ窓・部品窓・軌道線窓の開閉 | 導出(R5-4) | ほかに影響しない。中の軌道線トグルは視点の値を書く命令にする |
| PlanEditor の選択ノード | 導出(R5-4) | 読むのは入力の解釈と表示だけ |
| 実体ごとの軌道線表示トグル、タンパク質の表示設定 | 視点の記録(R6) | 読むのは表示とセーブ(予測の需要には `9bef624f` 以降効かない)。当たり判定は表示設定によらない(`protein-enemy.ts:109-112`) |
| 予測(`Predictor`・`predictedArc`) | 進行が持つメモ化 | 実シミュレーションは弧が届く歩で積分をせず弧をなぞる(`dynamic-motion.ts:318, 460-465`)。R4 の「需要は結果を変えない」は段 3 で `tests/game/trajectory-demand.test.ts` が固定した。直接積分との差は K3-4 |
| 予測を読む者のフラグ3種 | ~~廃止し、需要の入力にする~~ 済 | 導出側が進行のフラグを書いていた。`9bef624f` で消え、段 3 で需要(`dynamic/trajectory-demand.ts`)になった |
| 閃光 | 導出(出来事から作る)— 段 3 で済 | 段 3 で `flash-presenter.ts` が出来事から作る形になり、`FlashEffectsView` だけが読む(`game.ts:743`)。寿命は、発生した時刻と表示時刻から計算する |
| ループ音(推進・RCS) | 導出(状態から導く) | 操作対象・生存・推力とトルク・一時停止・決着から決まる |
| 効果音・ヒント・トースト | 出来事 → 導出 | 文言は表示の導出が組む。出来事が持つのは種別と値だけ |
| BGM | 装置が「ランの状態」の宣言から導く | どの曲を流すかは Conductor が決め、開始と停止だけを launcher が決めている |
| 音声のロック解除 | 装置(audio の内部) | ブラウザの制約(R7) |
| `FrameAnchors` の猶予、`NearbySystemTracker`、ラベル間引きのヒステリシス | 導出(R5-5) | 積分と予測には効かない。表示とピック判定だけに効く |
| 自転初期位相・位相オフセット・元期・暦 | 時刻層の構築値(R11) | 自転初期位相を読むのは表示とセーブだけ(地球は `c22: 0`)。位相オフセットはケプラー軌道の初期位相に効く |
| 主星の決定 | 時刻層(天体の分類から決める) | 主星の同一性は `2050c60c` で天体の分類 `isStar` から決まるようになった(`celestial-system.ts:141`)。自機と敵の散布はそこから太陽方向を引く(`combat/sun-glare-spread.ts:10,13`)。光源だけは、まだ描画 View の `stellarLight` から引いている(`celestial-system.ts:144-146`、手順 7-3) |
| 描いた線の点列、マーカーを出したか、計画折れ線が抱える射影 | 導出の入力解釈が装置の出力を読む(R7) | 読み手はピック判定と計画の編集だけ |
| タンパク質の当たり判定の幾何 | 定義層(純粋な幾何) | 幾何は `687c9a3f` で装置の中から `physics/protein-site-geometry.ts` へ移った。ただし規則は、いまも描画 View のメソッド越しに部位の位置を引いている(`protein-enemy.ts:190,207,211`)。手順 5.5-9 で外し、規則は静止構造を読む(監査報告の 4、K10-11)。`physics/` からも出し、変換は読み手の隣へ分ける(K11-6) |
| 実体の寸法・取付位置・当たり判定の近似 | 定義層(`entity-shapes/`、K11。段 6 で移す) | 焼き込みアセットの形と揃っている必要があり、アセットの作り直しと一緒に書き換える。いまは `physics/`・`render/` のファイルと、モデル層に手で写した近似に割れている。どこまで入れるかは未決(K11-2) |
| 配色 | settings の値 → 導出の入力 | `theme.ts` のモジュール変数 `activePalette` は `711c6611` で消え、settings を購読して反映する形になった(`main.ts:188,213`)。設定を切り替えると再読み込みなしで反映される |
| `panel-shell.ts` の `currentView`・折り畳みとタブの localStorage | 視点の読み取り / settings | ビューの写しをモジュール変数に持っている。ランを跨ぐ UI の選択は R10 により settings |
| エンティティの採番(`EntityIdAllocator` の static) | 進行(ランのインスタンスが持つ) | モジュール寿命の可変状態で、ランを跨いで残る |
| creative の spawn 距離・表示設定・連番 | 進行(手順 5-5 でセーブへ載せる) | spawn の規則が読む。距離と表示設定は保存されない。連番は構築時に復元済みの敵から数え直す(`66896940`、`manual-spawn.ts:35-45`)ので衝突はしないが、破棄済みの番号が再び出うるので、保存前と同じ番号にはならない |
| 採番器の次の番号(`EntityIdAllocators`) | 進行(手順 5-5 でセーブへ載せる) | 復元した id の最大値の次から数え直す(`entity-id.ts:21-25`)。破棄済みの実体(弾など)があると、保存前より小さい番号へ戻る |
| 基地の計画・計画実行モード・`fineAttitude`、デバッグステージの `enemyFireEnabled`・`waveCount` | 進行(手順 5-5 でセーブへ載せる) | `base.ts:204-219`(`BaseSaveData` に計画が無い)、`stage-debug.ts:23-24`。SAVE.md は基地について所持金しか挙げず、ワープ段とデバッグステージには触れない(手順 4-0 で「進行はすべて保存する」と書く) |
| ステージの StatusPanel・ステージごとの UI・結果とブリーフィングの文面 | 表示の導出(ステージクラスごとの表示) | ステージの挙動は一般化しない。表示も同じく並列に置く |
| 入力の連打判定(実時刻のラッチ) | 導出の入力解釈 | 入力の解釈そのもの。段 3 で `throttle.ts` から入力の解釈へ移した(`game/input/pilot-input.ts:162-164`) |
| `Hud`(ページの寿命)と game 用パネル(ランの寿命) | 前者は装置、後者は表示の導出 | 寿命が違うものを1つにまとめている |

### K3. 規則が強いる仕様判断

規則に従うと挙動が変わる箇所がある。**先に SPEC を直す(`/modify-feature`)。**

1. ~~**計画ノードの消化**をビューによらず進行の規則として行う。~~ **済**(`a01d9c36`)。PLAN.md はどちらの規則にもビューの条件を付けていなかったので、仕様の変更ではなく実装の是正として扱い、SPEC は触らなかった。
2. ~~**マップを閉じたときの空ノード削除**は、「計画の編集を閉じる」という命令にする。~~ **済**(段 3、`e8b695a8`)。専用の「閉じる」命令は作らず、編集側(`plan-editor.ts` の `onMapClosed`)が空のノードを判定し、削除を `PlanCommands.removeNode` として列へ積む形にした。挙動は変えていない。
3. ~~**音**(`DEVELOP/SPEC/AUDIO.md` への追記)。~~ **済**(段 1)。書いたのは次の5つ。
   - 一時停止中と勝敗確定後は、エンジン音と RCS の連続音を鳴らさない。
   - タブが非表示のあいだは、効果音も BGM も止め、表示に戻ったら再開する。
   - BGM はランの中(開始から勝敗確定まで)でだけ鳴り、タイトル画面と結果画面では鳴らない。
   - 勝敗確定後は、音量を変えても BGM は鳴り始めない。
   - 最初のユーザー操作による解禁は、タイトル画面や HUD のボタンの操作でも起きる。

   このうち「勝敗確定後に音量で BGM が蘇る」は、既存の文面(勝敗確定で BGM はフェードアウトする、確定後は設定画面を閉じても無音のまま)に反する不具合なので、仕様は変えない。
4. ~~**需要と積分の結果**(R4)。~~ **済**(段 3)。
   - なぞった結果と直接積分した結果は**ビット単位では一致しない**。`StateQueue.at` はサンプル時刻に乗らなければ Hermite 補間を返す。
   - **ユーザーの判断で、これは正当な近似として受け入れた。** なぞりはやめない。
   - したがって R4 が要求するのは「需要によらず同じ状態になる(弧の刻みが需要に依存しない)」ことだけで、直接積分との一致は要求しない(`61968c33`)。この条件は `tests/game/trajectory-demand.test.ts` が固定している。
5. **何がセーブに残るか**(段 4、手順 4-0。ユーザー判断 2026-09-17、K1-2)。
   - `INVARIANTS.md` §6: セーブに残る選択の基準を、「空間に依拠する」から「そのゲームの空間と時間に依拠する」へ広げる。時間の側の例は、表示期間と未来ゴーストスライダーの位置。
   - 同じ §6 の「依拠しない選択はブラウザに残る」は、全称をやめて例示にする(描画品質・見せ方・配色・音量・表示トグル・天球グリッド・パネルの畳みとタブ)。全称のままだと画角と投影がブラウザ側に決まってしまうが、ここは決めきれないので書かない。
   - `SAVE.md`「保存される内容」: ゲームの進み具合を決める状態は例外なくすべて保存される、と書く。いまの箇条はその例として残し、基地の計画・時間加速の段・クリエイティブとデバッグステージの出現の設定を足す。
   - 書かないもの: 操作途中の状態(開いている窓・選択中の項目・一覧の展開)を残すか。§6 の「残らないものだけを例外として書く」にも足さない。

### K4. 置き場の名前

新設するのは `src/presentation/`・`src/celestial/`・`src/game/viewer/`・`src/marker/`・`src/run/`・`src/entity-shapes/` の6つ。**`src/marker/` は段 1、`src/game/viewer/` は 4-3、`src/run/` は 5-3 で作って実在する。** `src/entity-shapes/` は 6-6 で作る(K11)。残るものは、名前だけの変更なら手順 6-1・6-2・6-3・6-6・8-3・8-4 のパスが変わるだけ。

**`src/run/`(ランの組み立て)は `launcher/` にも `main.ts` にも入れない**(K8-5)。`launcher/` はランの外(いつ・どのランを起こし畳むか、ランの外の記録)、`main.ts` はページの寿命、`run/` は1ランの内側の組み立てと位相の順序で、寿命も読むものも違う。計測(`frame-sections`・`perf-counts`)も組み立ての関心なので、手順 8-6 でここへ移す。

**`src/marker/` は `hud/` にも `render/` にも入れない**(当初は `hud/marker/` としていたのを、次の事実で覆した)。

- **入力の語彙が別物である。** HUD の語彙はパネル・行・窓、マーカーの語彙は「投影された点・優先度・間引き」である。R7 が装置を分ける基準は入力の語彙であって、出力が DOM かどうかではない。
- **`render/` へ入れる根拠が無い。** マーカーが使う投影は render ではなく `game/camera/camera-system.ts:138` が `math/projection`(純粋なピンホール)から作っている。render に置くと、いま存在しない「render のカメラ・canvas を読む」依存を新たに作ることになる。マーカーは DOM で、`#hud` の重なり順とモーダルの遮蔽幕に従い、クリックでピックされる — GPU の装置とは資源も寿命も別である。
- **`hud/` の中でも独立できる。** マーカーが要る DOM は `HudShell`(`src/hud/hud-shell.ts`)が作る `layers.marker` だけで、`HudShell` は `main.ts:147` が組み立てて `Hud`・`Launcher`・`ResultScreen` へ配っている**組み立て所有の画面の器**である。`Launcher` と同じ形で装置へ渡せるので、`hud/` を import する必要はない。
- **引き出し線の SVG も共有ではない。** `svgOverlay` を読むのは `LabelLayout` だけで、装置が自分の層の中に自分で作れば共有は消える。いまはそうなっている(`src/marker/marker-device.ts:34`)。
- **これが決め手**: この計画ではフォルダ = 装置 = `check:boundaries` が1つの塊として扱う単位である。`hud/marker/` に置くと、いま実在する `marker → hud/windows/property-window-content`・`hud/utils` の辺(`apsis-marker.ts:10`、`lagrange-point-marker.ts:7` ほか計8本)が装置の内部 import になり、検査が二度と捕まえられない。兄弟フォルダにすれば違反として出る。

### K5. 並行している作業との調整

- **未配線の足場。** `src/game/input/{game-actions,game-commands,game-input-router}.ts`、`src/game/pickable/entity-inspection.ts`、`src/game/dynamic/{dynamic-presenter,entity-lifecycle}.ts` は、mikanixonable が 2026-09-11 に追加したもので、**`1772c935` の時点では互いを import し合うだけで外から1本も参照が無かった**(計 229 行)。
  - **入力の3つは採用済み。** PR #82(`1649cc99`)で `game.ts` から配線され、段 3 の入力の解釈(`game/input/pilot-input.ts`)もその上に載せた。
  - **残る3つ(`entity-inspection.ts`・`dynamic-presenter.ts`・`entity-lifecycle.ts`)は段 3 の終わりでも参照が0本。** 表示担当の手順(6-3・6-4)で採否を決める。
  - 採るのは、R2 と R6 に合う形に直せる場合だけ。消すときは作者の合意を得てから消す。
- **マージで消えた作業(2026-09-13 に調査した)。** mikanixonable は「HUD の Game 依存の除去、保存境界、フレーム Coordinator の分割を実施済み」と `memos/mikanixonable/suspended/game-dependency-decoupling-plan_2026-09-10.md` に記録し、実装コミットとして `0a41653e`・`a4de2a7a`・`e8fad089` を挙げている。**この3つの hash は rebase 前のもので、object としては存在しない。** 作業自体は別 hash で main へ入っており、**そのうち2つはその後のマージで消えている。**
  - **消えたのは `39c813c0`(2026-09-10、`Merge remote-tracking branch 'origin/main' into workspace3`)。** 親のうち `cbd61ab1` 側だけが作業を持っていたのに、`src/game/` を origin/main 側(`b9b6f9d8`)で解決したため、次の2つが差分ごと消えた。
    - **HUD の Game 依存の除去。** `src/game/hud/orbit/orbit-analysis-source.ts` が消え、`import type { Game }` が `hud.ts`・`orbit/orbit-panel.ts`・`orbit/orbit-analysis-window.ts` の3本とも復活した(`7ec154c2` の木では 0 本だった)。
    - **フレーム Coordinator の分割。** `src/game/{input,simulation,presentation}-coordinator.ts` が消え、`game.ts` は `update` / `sync` / `render` を直に持つ形へ戻った。
  - **残っているのは保存境界だけ。** `src/launcher/save/snapshot-service.ts` の `capture` は `RunSummary` と `GameSaveData` を受ける形のままで、`launcher/save/` から `Game` の import は無い(`launcher.ts:1` の import は R10 の組み立てとして正しい)。
  - **手順 2-4(HUD の繋ぎ)は段 2 で実施し、main へ入った。** 残るのは `Game` の分解(手順 5-3)で、こちらも重複ではなく必要な作業である。作者への確認は済んだものとして扱う。消えた木は `cbd61ab1` に残っているので**形の参考にはする**が、以後の main のずれが大きいので cherry-pick はしない。
  - **作者へ伝えること。** この記録はいまも手順1〜3を `[x]` としたまま `suspended/` へ移されている。`memos/` は書き手が管理しているので、こちらからは直さない。
- **雲。** 雲の描画(`src/render/cloud/`)は別の作業が進んでいる。この計画が雲に触れる手順は、段 1 で済ませた `R_EARTH` の注入だけだったので、**段 3 以降は雲と衝突しない。**


### K6. 横断検査で確定した構造の判断

`reduce_implement.md` の洗い出しで出た指摘のうち、**挙動ではなく依存の向きと mutable の置き場の問題**をここへ移した。R3・R5・R6 の類例を機械検査で全量へ絞り、1件ずつ現地で判断してある。

**擬陽性と確定したものは、同じ場所を通る段で再調査しない。** 保留したものは、決める手順をここで指す。

**使った検査(段の N-2 で禁止パターンへ足す候補)**

| 類 | 検査 | 全量 |
| --- | --- | --- |
| ECI 原点を物理の基準に使う | ゼロベクトルのフォールバック(`?? v3()` / `?? V3_ZERO`)と、`len`/`lenSq`/`norm`/`dot`/`cross` が絶対 ECI 位置を `sub()` 抜きで食う行 | 5件 / 14件 |
| 見た目で物理を決める | 描画層以外からの `.view.` 読み / 表示属性で実体を選ぶ `find`・`filter` / `game`・`physics` から `render/` への import | `.view.` 77件、実体選択は1件 |
| 派生値をステートに持つ | `normalize`/`refresh`/`recompute`/`derive` が保存済みフィールドを他フィールドから上書きする形 + 全ミュータブルフィールドの分野別走査 | 候補48件 |
| 実時刻タイマー | `performance.now()` / `Date.now()` / `setTimeout` / `setInterval` の層別 | 16ファイル |

**擬陽性(再調査しない)**

- **実時刻の16ファイルのうち、時計の取り違えは BGM の Conductor だけだった。** 入力・プロファイル・保存時刻・読み込みのフレーム譲り・通信タイムアウト・BGM のポンプは、どれも実時刻が正しい。連打判定も同じで、時間加速で連打が速まってはいけない。`throttle.ts` が**自分で**実時刻を読む点だけが R5 に反していたが、段 3 で入力の解釈へ移した(`578c97d3`。実時刻はフレームの先頭から配る)。
- **`arc-celestial-bodies.ts:51` の `len(state.r)` は正しい。** ECI 化で入る原点補正項は天体の原点距離だけで決まるので、原点からの距離を見るのが正。
- **描画層の幾何を物理が読んでいる箇所は無い**(手順 7-3 の前提)。当たり判定の半径・大気・アルベドはすべて `def`/`motion` から引いている。`siteMarkers`・`motionMetrics`・`lineSamples` は表示・ピック・性能計測で、手順 6-3 が表示担当から読む形へ移す。
- **`camera-system.ts:233` の `focusVelocity ?? v3()` は位置ではなく速度**で、答えられない対象を注視しているあいだの既定として明記されている。R6 と同型ではない。
- **軌道ガイドの `point` と差分ゲートは消せない**(手順 7-3 の前提)。`source === 'lissajous'` では `familyId` が常に `'lissajous'` なので `point` を復元できない。`displayedSettings`/`displayedStyle` は導出値ではなくメモ鍵(R5-1)で、外すと `buildDisplays`・`styleFor`・`THREE.Color` の生成と `retainOnly` が毎フレーム走る。
- **天体ラベルの `frameScratch` は正当なフレームキャッシュ(R5-1)**(手順 6-4・7-3 の前提)。`opacity` は全天体との遮蔽レイ判定の結果で再計算が安くなく、`labelStateOf(id)` が後から任意の id を引く。

**保留した構造の問題と、決める手順**

1. **設定値の鏡映しと、ウィジェットが現在値を持たないこと(手順 6-4 と段 8)。**
   - **段 2 では鏡映しを消さなかった。** `orbit-guide-tab.ts` の `this.current` は「次の値を決める」だけでなく、patch を重ねる土台(`commitKind` ほか `:156-162,297,327`)、兄弟値のクランプ、未設定 id の既定生成にも使われている。読み口はこの3つを満たす形で切る必要があり、段 2 の範囲では切れなかった。
   - **根は `Button` が点灯の現在値を持たないこと。** `src/hud/widgets/button.ts:57-58` の `setOn` は class と `aria-pressed` を書くだけで `on` を保持しないので、現在値を呼び出し側が持たされている。ウィジェット側へ宣言を持たせれば、パネルの写しは要らなくなる。`hud/` を装置として判定する**段 8** で確定する。
   - `toggle-switch.ts` の `on` と `button.ts` の `enabled` が DOM と二重なのは**違反ではない**。読み手はクラスの中だけで、R7 の「装置は内部で前回との差分を取る」に当たる。`pause-menu.ts` の `minimized`、`draggable-window.ts` の `lastTitle`/`lastSubtitle`/`_clipped` も同じ。
   - 一覧の行だけは**流儀が割れている**。`savedExpanded` を正本と明記した `restoreSavedExpanded`(boolean だけ書いて DOM を後続の sync に任せる、`physical-object-list-row-tree.ts:136-137`)と、自分で `applyRowExpanded` を呼ぶ `setAllRowsExpanded` が併存する。手順 6-4 で行を表示担当へ移すときに一方向へ揃える。
2. **導出層が、同じフレームに引数で来る値を写して持っているもの(手順 4-3・5.5-7)。**
   - `hud.ts:64,144-145` の `chromeView` は**消さない。** `applyView` はクラスの付け替えだけでなく、パネルを左レールへ `insertBefore`/`appendChild` で移すので、毎フレーム行うとボタンのフォーカスとホバーが失われる。R5-5(表示を安定させるための前フレームの記憶)に当たる。段 2 で入れた「ビューを sync の入力で受ける」は、**付け替えの実行条件を前フレームとの差にすること**であって、写しを消すことではなかった。
   - `panel-shell.ts:34,44-45` の `view` も同じ理由で残している。
   - `object-windows.ts:50,140,151` の `lastFocusId` は**挙動を持つ**(マップを離れているあいだ最後のマップ注視を据え置き、戦闘中に開いたウィンドウのバッジに出る)。消すならその挙動を決めるのが先なので、視点を集める手順 4-3 で決める。
   - **`simTime` の二重経路が残っている。** DOM 側の入口は表示時刻の所有者から直接読む形に直った(`object-windows.ts:78,85`)が、`ObjectWindows.open`/`openEmptySpaceMenu`/`sync`(`:90,118,138`)と `MapPicking.handleRightClick`/`handleEmptySpaceRightClick`(`:102,154`)は引数でも受け続けている。手順 5.5-7 で引数側を落とす(`view-frame.ts:28,32` の宣言も一緒に落ちる)。
   - 同型の未着手(これも 5.5-7): `frame-controls.ts:22,68,83` の `lastTime`、`plan-editor.ts:84,600-607` の `simTime`。後者は `DisplayDurationSource` を既に受けているので、受け口の型を広げれば所有者から読める。
3. **所有者以外が書いている値 — K10 へ移した(2026-09-19)。**
   - ここでは実体と behavior が `DynamicMotion` へ代入する形だけを保留にしていたが、R3 には判定が無く、洗い出しも「派生値をステートに持つ」までで止まっていた。全量を洗い直すと、実体の外からの `alive`・`state` の書き込みを含めて 70 件を超えた。判断は K10、手順は段 5.5 にある。
   - 次の2つはそのまま有効: **接続中ブースターの `mass`/`att.inertia` は済**(`921661d5`。所有者の `PlayerMotion` が命令で組み直す)。**放熱板の `wear` は違反ではない**(`RadiatorSystem.update(dt, wear)` が自分のフィールドを引数から書く)。
4. **id と表示名は統合しない(手順 4-3)。**
   `nav-target.ts:68-69,96-97`・`equator-node-marker.ts`・`orbit-point-marker.ts` は id と表示名を両方持つ。**表示名は衝突を許し、id は衝突しない**ので、表示は名前・検索は id と使い分ける。横断検査では、表示名で同一性を判定している箇所は `src/` に無かった(名前での比較3箇所は、既定名との一致・並び替えの差分検出・利用者向けの検索文字列で、どれも同一性の判定ではない)。手順 4-3 でターゲットを視点へ移すときも、名前は表示専用の値として残す。


**単独では扱わないもの。** `field-projection.ts:110` の `cosRadiusValue`、`curve.ts:90` の `appliedStyle`、`orbit-guide-catalog.ts:74` の `loadState`、`slot-data.ts:35,47` の `lastPlayedAtReal` は、どれも1フィールドの導出値。`planet-system.ts:53` の `offsetting` は時刻層のメモ化として R1 に適うが、**性能上の理由がコメントに書かれていない**(`:52` のコメントは何を保持するかだけを述べている) — 段 7 で天体を触るときに書く。`next-event-time.ts:8`・`dynamic-trajectory.ts:25`・セーブ索引(`slot-data.ts:13-30`)は、理由が明記された意図的な保持。


**5.5-3 の洗い出しで擬陽性と確定したもの**(`5f87b1790` で規則ごとに洗った。再調査しない)

- **R8**
  - 表示の導出が進行の結果を見て命令を積む形(`dropStaleRotatingFrame`、`onMapClosed` → `removeNode`)は、段 4 で決めた設計(K3-2)。命令を作るのは入力の解釈だけ、とは R8 は定めていない。
  - 一時停止メニューの手動セーブが DOM のハンドラで直列化するのは、位相 2 と 3 の間と同じ状態を読む。フレームの間に走るハンドラの時点では、モデル層は直前の進行の直後から変わっていない(位相 3・4 と次の位相 1 はモデル層を書かない)。
  - 一時停止を開いているオーバーレイから導いて進行の入力にするのは K2 の決定どおり(オーバーレイは入力の解釈が読み、操作量を作る)。
  - `Run.launch`・`warmUp` が `followProgress`・`followCamera`・`extendPredictions` を呼ぶのは、位相の順序の持ち主が最初のフレームの「進行の末尾 → 導出と同期」を組む形で、外からの命令ではない。`extendPredictions` の計画ノードの消化は、復元した計画が起点より前のノードを持たないので空振りする。
  - 装置の中の壁時計(計測、ダブルタップ判定、`ctx.currentTime`)は外界の写しか計測。
- **R3**
  - `DynamicSystem.events`・`idAllocators` の公開は、実体へ協力者を配る `EntityRegistry` の契約。
  - 創作パネルの `requestProteinAsset` がモジュールの取得キャッシュを書くのは、変わらないアセットの取得(外部資源の I/O)。出現の可否がそれを読むのは、資源が揃ったかという外界の条件。
- **R5**
  - パネルが毎フレームの表示用の値(命令の口を含む)を写して持ち、フレームの外の DOM のハンドラが読む形(`orbit-panel`・`target-panel`・`top-bar`・`vessel-panel`・`enemies-panel`・`view-badge`・`predict-panel`)は R5-5 — 読むのは入力の解釈だけ。
  - 閉じても残る小さな UI の選択(設定面の内側タブ、一時停止メニューの位置、予測パネルの入力欄の単位、物体一覧の並び順と分類フィルタ、デバッグの描画ターゲット)は K7-1 の操作途中の状態。
  - 閃光と的通過の印の寿命を表示時刻で測るのは K2 の決定(世界に貼り付く効果)。
  - `PilotInput` が `beginFrame` の引数を写して router の口が読むのは、口の形(`handleCommand(command)`)に引数を足せず、鮮度は beginFrame → route の順で保たれる。`FrameAnchors._bodiesPivot` も同じ。
- **R7**
  - 入力の装置 `input/` の `update`・`take*`・`setVirtualKey`・`tapKey` は、外界(キーボード・ポインタ・画面の操作パッド)の出来事を受ける口。R7 の「宣言だけを受ける」は見せる・鳴らす装置の入力の規則。
  - `MarkerDevice.resolveOverlaps` は全群の宣言の後にフレームで1度だけ呼ぶ締め。
  - 軌道ガイドの線が運ぶ `familyId` などは、装置が解釈しないピック用の素通し。
  - BGM の `Conductor` の曲の選択と曲送りの起点は、音声資源の再生状態(R5-2)。
- **R11・R12**
  - 敵の `attackGroupId` の補い(`deserializeEnemyPlacement`)は、記録に id が無いときに名前へ落とす、初期値と違う値を含む。
  - `OrbitGuideSettings` の入れ子の欠けを既定の定数へ重ねて補うのは、コンストラクタの既定引数では書けない入れ子の補い。定数は1つ。
  - `CameraOrientation.attitude` を復元で持ち越さず、表に出ていないカメラでは null のまま残るのは段 4 の決定(表示中のカメラにだけ進行を当てる)。
  - `RadiatorSystem`・`FireControl`・`EnemyFireController` が `deserialize` を持たないこと、`Player.deserialize` が作った部品を `Player` のコンストラクタが `PlayerMotion`・`FireControl` へ中継することは、持ち主への参照を要する部品の例外(R13)。
- **R13**
  - `NavTargetSelection` が出来事の書き口を持ちながら直近の出来事を呼ぶたびに受けるのは、協力者ではなく中身の値。`PlanDisplay` が期間の読み口を持ちながら今フレームの表示窓を受けるのも同じ。
  - `Enemy.behave` が `registry`・`celestialBodies` を部品 `fireController` へ渡すのは、持ち主から部品への委譲。
  - `protein-asset-loader` のモジュールのキャッシュは、ページの寿命のアセット取得で、ランの状態ではない。
- **R1**
  - `TimeRing` の `hits`・`misses` は、返す値に効かないメモ化の計数。
  - `hud/inject-style.ts` の注入済みの集合は、import を持たないが語彙ではない(R2 の「import を持たないモジュール」は union や定数だけの語彙を指す)ので、置き場の層(表示の導出)の DOM 資源の登録(R5-2)。import を持たない非純粋なモジュール(`overlay-layer`・`breakpoints` ほか)も同じで、装置どうしを跨いで読まれてはいない。
  - `physics/ephemeris/catalog.ts` の取得(I/O)は、保持する値ではない。
  - 慣性の相対値(破片・薬莢・砲身)は操縦応答用の調整値で、形からの手計算ではない。`protein-enemy`・`metal-enemy` の同じ慣性は、別々に調整する敵の値(1.5)。

### K7. 視点と設定の境界で決めたこと(2026-09-17)

`1e908f1e` の基準を当てたときに言い切れなかった値を、ユーザーが次のように決めた。考え方は K1-2、SPEC の変更は K3-5。

1. **操作途中の状態**(開いている窓とその位置・ピン・中のタブ、選択中の計画ノード、物体一覧の展開・並び順・絞り込み、予測パネルの入力欄の単位)は、R5-4 のまま、いまの挙動を保つ。残してもよいし残さなくてもよい。SPEC には書かず、将来の実装者の解釈に委ねる。
2. **表示期間とスクラブ位置**はセーブごとに持つ(視点)。セーブごとに組んでいる軌道の時間尺度が違うので、引き継がれると不便である。
3. **画角と投影**は、カメラの状態をセーブと設定へ割らないために、カメラと一緒に視点に置く。SPEC は置き場を決めない。
4. **進行の値**は、例外なくセーブに入る。TODO として残さない。

### K8. 繋ぎ方 — 直列化・構築・組み立て(2026-09-18)

R1〜R11 は層の切り方を決めたが、層と層・持ち主と部品をどう繋ぐかは決めていなかった。直列化と復元の形、部品を誰が作るか、層の根を誰が組むか、である。4-3 までで視点と進行の mutable な正本が所有者に集まったので、ここで繋ぎ方を決めて R12・R13 とし、段 4 に書く。あわせて R8・R10 を言い直す。ユーザーの提案1〜5を、次の事実(`65146de8` で調べた)に照らして判断した。

**事実**

- **直列化の型。**
  - 約 40 種あり、大半が `game/save/save-data.ts`(293 行)に集まっている。このファイルが 13 モジュールを import しており、`stages/stage.ts` との型の循環も作っている。
  - 名前は `XxxSaveData` に、不規則なものが混ざる。`WeaponStateData`(`FireSaveData` と同じ形で、`as` で繋いでいる)、`BoosterStackData`、`AnyPart[]`、`number`、`Pick<GameSaveData, …>` がそれに当たる。実行時の型をそのまま直列化の形に使っているものもある(`OrbitGuideSettings`・`EphemerisContext`・`ProteinDisplaySettings`・`GamePhase`)。
  - メソッドの名前も割れている: `serializeEnemyFields`・`serializePlan`・`exportData`/`importData`・`saveState`/`restore`。
- **復元の形。**
  - ほぼ全件が、コンストラクタで保存値を受ける。実体は「新規配置 | `{saved, simTime}`」の和を受け、`'saved' in init` で判別する(Player・Base・Pickup・DetachedBooster・敵・FireControl)。
  - 二段初期化になっているもの: Enemy(`fireController.restore`)、MetalEnemy(`setOverallHp`)、Player(`replaceParts`・計画の `addNode` の繰り返し・`planExecution` の後代入)、RadiatorSystem、AttachedBoosterMotion(親の構築途中に質量を書き戻す)、Stage(`restored` と `begin()`/`init()`)。いずれも、既定で組んでから上書きしている。
  - タグ辞書 `restorationFor`(`entity-dictionary.ts:24-61`)は `switch` で具象ごとの引数の並びを知っている。静的側インターフェースを持つのは敵だけ。
- **記録に無い項目の補い。**
  - コンストラクタの中の約 100 か所に散っている。版の照合のほかに、正規化の層は無い。
  - 欠けたときの値が、新規の初期値と違う箇所がある: `Player.planExecution` は新規で `'instant'`、欠けたときは `'off'`(`player.ts:98,178`)。
  - 不正な値の扱いも所有者ごとに違う。`BoosterStack` は throw し、ほかは黙って既定へ落とす。
- **親子。** 子はすべて、親のコンストラクタの中で `new Child(saved.child)` として作られている。
- **視点の値が実体の記録に混ざっている。** 実体の `serialize` が、視点の値(軌道線のトグル、タンパク質の表示)を引数で受けて書き込む。その結果:
  - タンパク質の表示は、タンパク質の敵が 0 体のセーブで失われる。
  - Pickup と DetachedBooster は、軌道線のトグルを書かない。
- **`game.ts` の形。**
  - 1040 行・import 105 行で、`game.ts` を import するのは `launcher.ts` だけ。コンストラクタが 242 行、「HUD へ渡す値」が 176 行、入力ポートの表が 58 行ある。
  - 進行の後の「update 内の導出」(`game.ts:566-631`)で、モデル層への書き込みが走っている: 視点の追従、履歴の保持長、予測の延長、計画ノードの規則(出来事の記録を含む)。
  - 表示の導出の側も 700 行近くあり、`MapView` は 25 引数、`ObjectWindows` は 19 引数を受ける。
- **中継。**
  - `scene`・`renderer`・`events`・採番器が、使わない持ち主を通って運ばれている。
  - 同じ協力者を「持つ」と「呼ぶたびに受ける」の2経路で受けている所がある: Player・FireControl・AttachedBoosters の `events` と `registry`。
- **テスト。**
  - `tests/game` と `tests/render` の多くは、4.1 の A(期待値の正本がコードの外)と B(正本がコード自身)が混在している。
  - `launcher/save` のテストは1本も無い。
  - 構造の変更で書き直しが要るテストのうち、手順 6-3 の対象から `player-systems`・`stage-rules`・`tests/protein-test-assets.ts` が漏れていた。

**決めたこと**

1. **提案1(語彙)は採る**(R12)。
   - モデル層は「直列化」だけを知る。「セーブ」は launcher と SPEC の語彙にする。
   - 形は `serialize(): SerializedT` と `static deserialize(serialized: SerializedT, …): T`。型は `T` のモジュールに置き、`game/save/` は解体する。
   - 不変で JSON の素の値だけから成る型は、そのまま直列化の形に使う。
   - 保存形式(JSON のキーと入れ子)は変えない。`chase`/`overview` と `combat`/`map` の名前のずれや、WaveAttack の保存値が Stage00 では平坦で Creative では入れ子である点も、`deserialize` が写すだけにする。
2. **提案1(構築)は採る**(R12)。
   - コンストラクタは直列化された形を受けず、新規と復元で分岐しない。復元は `deserialize` だけが行う。
   - 記録に無い項目は、コンストラクタの既定引数(= 新規の初期値)で補い、`deserialize` は `undefined` のまま渡す。JS の既定引数は `undefined` のときだけ働くので、補いの分岐がそのまま消える。
   - `deserialize` に残すのは、欠けたときの値が初期値と違う箇所(`planExecution` の `'off'` など)と、ほかの項目から導く箇所(`attackGroupId ?? formationId ?? …`)だけ。**挙動は変えない。**
   - 不正な値の扱い(型を見て既定へ落とす所、throw する所)も、いまの挙動のまま `deserialize` へ移す。揃えるなら、SAVE.md「壊れている記録は読み込めない」に照らした SPEC の判断が先になる(この計画では揃えない)。
3. **提案2(木)は採る**(R12)。
   - 親の `deserialize` が子の `deserialize` を呼び、できた子をコンストラクタへ渡す。
   - 1.6 の静的側インターフェースは、コンストラクタの「復元の腕」から静的な `deserialize` へ置き換える。新規の構築は具象ごとのコンストラクタか `create` で行い、辞書を通さない。
4. **提案3(組み立ての基準)は採る**(R13)。基準は「部品か協力者か」である。
   - **依存性の注入をすべてに当てる案は採らない。** 注入で減るのは協力者への依存だけで、部品の具象は `deserialize` を呼ぶ持ち主がどのみち値 import する。すべてを上へ上げても、構築がエントリポイントに集まるだけで、結合は減らない。
   - `constructor(bar = new Bar())` は書ける。既定引数は呼ぶたびに評価され、前の引数も参照できる。ただし使うのは、初期値が入力なしで決まる部品と状態に限る。入力の要る部品は、持ち主の `create`・`deserialize` が作る。持ち主への参照が要る部品(運動へ渡す反応など)は、持ち主のコンストラクタが復元済みの値から作る。
   - 層の間を平らな寄せ集めで繋がないために、層を跨いで渡すのはモデル層の根(`Game`)の読み取り面にする。その下は所有の木のまま辿る。受け手が使う面へ狭めるのは、受け手の側の仕事(1.12)である。
5. **提案4(`game.ts`)は、早めに分けることを採る。すべてを `main.ts` へ解く当初の 8-6 は棄却する。** 1ランの根どうしの組み立てと位相の順序は、ページの寿命の組み立てや rAF の登録とは別の責務だからである。
   - `Game`(`src/game/game.ts`)をモデル層の根にする。進行と視点を所有し、`serialize()` の結果がそのままセーブの本体になる(R11)。
   - `GamePresentation`(`src/game/game-presentation.ts`。8-3 で `src/presentation/` へ移す)を表示の導出の根にする。
   - `Run`(`src/run/run.ts`、新設)が1ランの2つの根を組み、位相の順序を持つ。`launcher/` はランを起こし畳む判断、`main.ts` はページの寿命と rAF の登録を持つ。
   - **分けるのは段 5 の中、直列化の形を揃える前(手順 5-3)にする。** 理由は2つある。
     - `Game.deserialize` が復元の木の最上位になる。
     - 5-5 で足す値(時間加速の段・採番器・アセット待ちの実体)が、モデル層の根へ直に入る。
   - モデル層の根は、実体・ステージ・天体系が表示物を持っているあいだ、`GameScene`・`HudLayers`・`FrameSections` を暫定の受け口として受ける。`scene` は 6-3、`HudLayers` は 6-5、`GameScene` は 7-3 で外す。`FrameSections` は、計測の置き場を決める 8-6 で扱う。
6. **提案5(テスト)は採る。**
   - 構造を変える手順では、その構造を固定していたテストを 4.1 に照らして書き直すか消す。B の箇所は、置き場を移さずに消す。
   - 層が分かれて組めるようになった所から、SPEC と R3〜R12 の不変条件を足す(手順 5-6・6-3・7-4・8-3)。
   - この計画が触らない装置のテスト(`tests/render` の地表・雲・タンパク質描画の B の箇所)は、計画の外に置く。
7. **繋ぎ方の作業は、段 4 に混ぜず独立した段 5 にする**(ユーザー判断 2026-09-18)。段 4 は R4(モデル層を進行と視点に分ける)と R11(セーブの規格化)の2つを載せていたが、R11 の側が上の 1〜6 で7手順に膨らんだ。**4-3 までで R4 は満たしているので、ここで段を切って main へ送る。**
   - 段 4 の PR に入るのは、4-0〜4-3 と、規則を段 4 の範囲へ戻す 4-4(下)。**約 3,300 分ぶんの繋ぎ方の作業は段 5 へ回り、段 6〜8 は番号が1つずつ繰り下がる。**
   - **落とすのは R11 の2だけ。** 4-1 で書いた規則のうち、コードが満たしていないのは「モデル層の値は例外なくすべてセーブする」とその例外の3項だけである(載せる作業が段 5 へ回るため)。R4 の全文・R11 の1・R1/R5/R10 の言い直しは満たしている。4-2 の判定も 18 本すべてコードが満たしているので、落とすものは無い(空の許可リストで 0)。
   - **SPEC は落とさない。** 4-0 で SAVE.md へ書いた「進行は例外なくすべて保存される」は、コードより先行してよい(SPEC/README.md)。規則(CODING-RULE)だけがコードに追い越されてはいけない。
   - **`game.ts` は 1041 行のまま main へ入る。** 計画の外で割られないよう、4-4 で次の3つを置く: 1.10 の位相の印の行き先を段 5 のランの組み立てへ直す、1.2 に「段 5 で3つの根へ分けるまで分割しない」暫定を置く、`game.ts` の冒頭に同じことを1行書く。段 4 の PR 本文にも書く。

**覆したときに変わる手順**

- **1 を覆す(名前を据え置く)**: 5-2 は型の移動だけになり、5-1 の直列化の語彙の判定を落とす。
- **2・3 を覆す(コンストラクタで保存値を受け続ける)**: 5-4 が消え、5-5 はいまの形で書く。1.6 の復元の腕は据え置き、5-1 の R12 は語彙の項だけになる。
- **5 を覆し、位相を `main.ts` で組む**: `run/` を置かず、8-6 を当初の形に戻す。
- **7 を覆し、繋ぎ方を段 4 に戻す**: 4-4 を落とし(R11 の2をそのまま残す)、5-1〜5-7 を 4-4〜4-10 に戻して段 6〜8 の番号を戻す。段 4 の PR は約 3,300 分重くなるが、`game.ts` の分割が1つ早い PR で main へ入る。

**調査で見つかった別件の割り振り**

- Pickup と DetachedBooster は `alive` を保存しない。DetachedBooster は、保存された名前も読まない。`EnemySaveData.health` を ProteinEnemy は読まず、`ProteinSaveData.schemaVersion` は照合されない。→ 手順 5-5 の「値の性質を確かめてから決めるもの」に足す。
- `PartInventory.serialize` は呼び出し元が無い。`EntityRegistry` のコメント(gate が null なら次のフレームで生む)は、実装(その場で加える)と食い違う。→ 手順 5-4 で消す・直す。
- 復元直後、アセット待ちの陣形の `FORMATION-n` を ManualSpawn が予約できず、新しい陣形の id と衝突しうる(推測)。→ 手順 5-5 で採番器を保存すれば消える。
- `PilotCommandFrame`(`controls`・`dt`・`simDt`・`registry`・`activeStage`・`stageRules`・`celestialBodies`)は寄せ集めである。→ 手順 5.5-4 で寄せ集めをやめ、値を明示的な引数で渡す(CODING-RULE 1.6)。協力者を構築時に受ける形へ移すかは、段 6 の着手前に実体と運動の繋ぎ方(K10-6)と一緒に決める(5.5 の振り分けの表)。
- `MapView` の 25 引数、`ObjectWindows` の 19 引数は、受け手の責務の大きさの問題である。→ 手順 6-4 で、表示担当へ移すときに見直す。
- `tsconfig.test.json` が、存在しない `src/game/camera/focus-target.ts` を名指ししている。`test:settings` が CLAUDE.md のコマンド表に無い。→ 手順 5-6。

### K9. セーブの版を上げ、形式を所有者の語彙へ揃える(2026-09-18)

**ユーザー判断: まだ正式リリースしていない(広く遊ばれていない)ので、セーブの互換は切ってよい。** 歴史的な経緯で溜まった「保存の項目名と入れ子が所有者と食い違う」歪みを、段 5 の手順 5-5 でまとめて直し、形式の版を上げる。

- **直す歪み**(一覧は 5-5 の表): 最上位で進行と視点が平坦に混ざる / カメラの `chase`・`overview` / ビューの選択がカメラの記録の中にある / 実体の記録に視点の値(軌道線のトグル・タンパク質の表示)が混ざる / WaveAttack が平坦と入れ子の二流儀 / 金属敵が総 HP だけを保存する / ProteinEnemy が読まない `health` / 照合されない `schemaVersion` / `FireSaveData` と `WeaponStateData` の二重。
- **版を上げると何が起きるか**(SAVE.md「形式の版」): 版が一致しない記録は読み込めず、変換もしない。**段 4 までに書き出した記録(手動セーブ・自動セーブ・書き出しファイル)は読めなくなる。** 拒まれるだけで、セーブデータ画面の一覧・ページの再読み込み・新しいランの開始は壊れない(5-5 の検証)。
- **SPEC は変えない。** SAVE.md は版の扱いを既に定めていて、版の値そのものは書いていない。「版を跨いだ互換」は未確定の案のままにする(公開した後に上げるときの話)。
- **互換を切るのは段 5 の1回だけ。** 段 6〜8 は挙動不変なので、そこで版は上げない。段 6 以降で形式が動くなら、それは表示物や天体の移動が保存の内容に触れた印なので、手順の設計を見直す。
- **検証の基準が変わる。** 段 4 までの「段 1 に着手する前に書き出したセーブが読める」は、段 5 以降では使えない。代わりに「段 5 で書き出したセーブが読める」を使う(達成目標の 3)。
- **覆したとき**(互換を保つ): 5-5 は「省略可能な項目を足すだけ」に戻り、歪みの是正は落ちる。R12 の「1つの記録に2つの所有者の値を混ぜない」は満たせなくなるので、実体の記録へ視点の値を合成する形を残すことになる。

### K10. 検査は規則の代理であって規則ではない — R3・R7 の検査の是正と、実体・運動・軌跡の責務(2026-09-19)

段 6 の未決事項を洗った報告(`restructure-architecture-step6-audit.md` の 1)が、「`setThrust` は段 1 の禁止パターンに当たるので `applyControl` と名付ける」と提案した。**規則ではなく検査の字面から設計を決めている。** 調べると、計画の方法そのものに穴があった。

**事実**

- **命令 API の禁止は装置の規則(R7)なのに、検査はモデル層にも当てている。** `check-boundaries.mjs` の「命令 API の禁止」は `src/` 全体に名前の列(`setThrust` ほか7語)を当てる。R7 の文面も 2.2 の `view` も装置に限っている。モデル層の口は段 3 で「命令・読み取り面・出来事」に揃えた(R3・R7)ので、モデル層の命令を禁じる理由は無い。
- **名前の判定は形を判定しない。** `setThrust` を `applyControl` と呼び替えても副作用の形は変わらず、検査だけが通る。単なる代入でも、所有者が保つ整合性(予測弧の破棄)でも、呼び手から見れば副作用である点は同じ。
- **過去に、名前を替えて検査を通した例は無かった**(名前で当てる各行について、導入した commit から、当たった名前を消した commit を全部辿った)。
  - 改名だけのものは5件あった。4件は誤検出か、行の目的が命名そのもの(R12 の語彙)。形を残したまま名前を替えたのは、装置の内部の `RenderPipeline.applyGraphics` → `rebuildForGraphics` の1件だけ。
  - `DynamicMotion` の `set thrust` は禁止の導入(09-13)より前(`fd8b3922`、08-19)からあり、回避ではない。ただし CODING-RULE 1.11(副作用があるなら代入でなくメソッドにする)に従って直すときの自然な名前 `setThrust` を、`src/` 全体への禁止が塞いでいた。
  - **害は逆向きに出ていた。名前の列に無い命令は、R7 に反していても素通りしている。** `WorldSfx` の単発音(`fire()`・`playReload()` ほか。`run-event-presenter.ts` が出来事1件ごとに命令する)、`Bgm.setVolume`(`main.ts` が設定の購読から呼ぶ。段 1 が `applyGraphics` で消したのと同じ形)と試聴の `beginAudition`/`playAudition`/`stopAudition`/`endAudition`。R7 も段 1 から main に書かれている規則である。
  - 同じ洗い出しで、R12 の疑いも2件出た。`DynamicSystem.deserialize` が空のまま構築してから、記録を命令 `spawnWhenReady` で流し込む(アセット待ちの関門を理由にできるかは未判定)。版の刻印 `SERIALIZATION_VERSION` がモデル層の根 `game.ts` にある(R12 は版の照合を launcher の語彙としている)。
- **R3 は段 3 で書いたが、判定を1本も足していない**(手順 3-2 が足したのはパス指定の3本だけ)。K6 の横断検査も、「派生値をステートに持つ」は洗ったが「他人のフィールドへの代入」は洗っていない。その結果、
  - 実体の外からの `alive` の書き込み(`DynamicSystem.enforceCaps`・`Logistics` 4か所・メニューの削除)、`state` の setter を通す書き込み(反動・分離・creative の到達・接触解決)、`torque`、`fireEnabled` が、R3 を満たしたことになったまま main へ入っている。
  - 段 5 は `fireEnabled` と creative の `motion.state` を見つけたが、当時の手順(3D の表示担当とステージ)へ回した。**main に入る時点で書かれている規則はコードが満たしている、という計画の前提が、判定の無い規則については成り立っていなかった。** 許可リストが空であることは、判定がある規則を満たしたことしか示さない。
- **呼び手が運動の整合性を世話している。** `invalidatePrediction` は public で、`player.ts`・`attached-booster-motion.ts`(3か所)・`attached-boosters.ts` が呼ぶ。どれも運動の命令(推力・質量の組み直し・状態の置き換え)の中で捨てれば足りるか、既に捨てている。`BoosterHostMotion` の面にまで入っているので、呼ぶのが契約だと読める。
- **`physics/` が可変な正本を持っている。** R2 の表で `physics/` は時刻層で、R1 は時刻層に「結果を変えないメモ化キャッシュだけ」を許す。`DynamicTrajectory` は全実体の実軌跡(履歴に依存し、作り直せない)を持ち、RK4 の積分も呼ぶ。検査は import だけを見るので、層の中の可変な値は判定されない。1.3 の「`physics/` は THREE 非依存であれば状態を持つクラスを置いてもよい」も R1 と食い違っている。
- **軌跡の1クラスが3つの役を持つ。** `DynamicTrajectory` は (1) 積分して先端を進める、(2) 間引いた履歴を記録する、(3) 予測の弧の先を二体軌道で外挿する中心を持つ。実シミュレーションは (3) を使わず(`actual.step` は中心を渡さない)、接触代理(放熱板の折り 12・ベルトの節点 18)は (1)〜(3) のどれも要らないのに、`DynamicMotion` の派生として全部を持つ。代理の毎サブステップの置き直しが `reset` → `StateQueue.push` を通り、履歴が無制限に伸びる(`reduce_implement.md` R20)。
- **実体と運動の分離の設計は、実体が運動へ命令する形だった**(`refactor_entity_separation.md`、`d94268a3` で削除。「AI・入力・射撃・stage への spawn・記録が積分前後に Motion へ命令し、接触の結果を Entity が処理する」)。報告の選択肢 A(実体を運動の所有者とみなす)は、この設計から外れる。

**決めたこと**

1. **R3 の所有者は、その欄を宣言したクラスである(報告の B。ユーザー判断 2026-09-19)。** 実体と運動、運動と behavior、持ち主と部品のような所有の木の中でも、他のクラスの欄へ代入しない。例外を置かない。同じオブジェクトが継承した欄を `this` で書くのは、自分の欄なので当たらない。
2. **モデル層の口は、命令・読み取り面・出来事の3つである。** 命令 API の禁止は R7(装置)の規則で、モデル層には当てない。モデル層の命令は、名前の形(`set*` かどうか)を問わない。
3. **整合性は所有者が命令の中で保つ。** 呼び手に所有者の整合性の世話(キャッシュの破棄など)をさせず、そのための口を公開しない。`invalidatePrediction` は private にし、予測弧を古くする命令(推力の指令・質量の組み直し・状態の置き換え)がそれぞれの中で捨てる。
4. **`alive` は運動が持ち、外からは命令 `kill()` で下ろす。** 実体の死と、シミュレーションからの退場は同じ旗である(Simulator も抗力で下ろす)。顔ぶれから外すのは `prune`、表示資源を捨てるのは `dispose` で、3語を混ぜない。復元で死んだ状態から組むときは、構築の引数で受ける(構築後に下ろさない。R12)。
5. **検査は形で判定する。名前で判定するのは、規則が名前を定めているときだけにする。**
   - **R3 に判定を足す**: モデル層のクラスは、可変な public 欄(コンストラクタ引数の欄を含む)と `set` アクセサを持たない。モデル層から export する interface も可変な欄を持たない(接触の参加者の `state`・`alive` がこれで書かれていた)。判定は TypeScript の構文木で行う。こうすれば、他のクラスの欄への代入は `tsc` が止める — 名前を変えても通らない。
   - **「命令 API の禁止」の名前の列は外す。** R7 の命令 API は、名前でも構文でも判定できない(宣言を受ける `sync` と、名前だけ替えた命令を構文は区別しない)。装置の import の境界は層の規則が判定しているので、R7 の残りは段の終わりの洗い出しで見る。
   - 名前の判定を残すのは、規則が名前そのものを定めている行(R12 の語彙 `SaveData`・`save`/`restore`)と、特定の API の呼び手を限る行(壁時計・`localStorage`・生の入力エッジ・`three`)だけにする。どちらも名前が判定の対象そのものなので、名前を替えれば規則の側から外れる。形の規則を名前で代用している行は、構文の判定に替えるか、代用であることを対応表に書いて洗い出しで補う。
   - **判定できない規則を、判定できたことにしない。** 規則ごとに「判定(検査・`tsc`・テスト)」か「段の終わりの洗い出し」かを決めた対応表を置き、段を送る手順で洗い出しを行う。許可リストが空であることは、判定がある規則を満たしたことしか示さない。
6. **実体と運動の責務の基準 — 保留(ユーザー判断 2026-09-19)。** 境界の設計と、是正をどこで行うかは**段 6 の着手前に決める。** 段 5.5 の手順では是正しない。以下は判断材料として残す案で、決まったことではない。
   - 案: 運動は「シミュレーションの参加者」である。運動が持つのは、シミュレーションの機構(Simulator・接触解決・Predictor)が読み書きする値と、機構が回す処理(軌道・姿勢・熱の積分、接触形状と代理)だけ。機構が読まない値は運動に置かない。機構が求める面は `dynamic-simulation-participant.ts` に列挙されているので、基準は事実で判定できる。
   - 実体が持つのは、識別(id・名前・種別)、ゲームの規則の状態(HP・弾薬・資金・計画)、判断(操作量の解釈・AI)、帰結(被弾・撃破・spawn・記録)、直列化の根。
   - 実体 → 運動は命令(推力とトルク、質量、状態の置き換え、退場)で、運動 → 実体は構築時に注入した反応(接触・焼失・喪失・毎サブステップの環境)で繋ぐ。
   - **運動を実体へ畳まない(展開しない)。** View が抜けても、実体は運動を包むだけの層にならない。自機・敵・基地は規則の状態を持ち、弾・破片・補給・分離ブースターも識別・種別(表示担当のクラス辞書の鍵、R6)・直列化の根・反応を持つ。畳むと、シミュレーションの参加者がゲームの規則を import し、2.2 の「実体の継承で物理処理を差し替えない」が書けなくなる。
   - **案の基準で見ると置き場が揃っていないもの**(未決の1): `PlayerMotion.power` は機構が読まず(読むのは HUD・プロパティ行・命令)、毎サブステップの環境で進むだけ — 同じ形の砲身の熱は、実体側の `FireControl` が反応として進めている。接続中ブースターの段 `PlayerMotion.attachedBoosters` も、機構が読むのは質量と推力だけで、燃焼は実体の `updateControls` が進めている。
   - **天体と対称にする場所は、「運動と表示物を束ねる所」から「モデルと表示の導出の対」へ移る。** 段 6・7 の後、運動と表示物を束ねるのは表示担当で、実体は族を問わず「運動とゲーム上の意味」を束ねる。天体の実体が運動・名前・分類だけの薄い層になるか(=畳むべきか)は、手順 7-1 で同じ基準で判定する。
7. **軌跡の置き場と分け方 — 保留(ユーザー判断 2026-09-19)。** `physics/` の軌跡が R1 に反していること(事実)は変わらない。しかし、どこへ出し、積分・記録・外挿の中心をどう分けるかは、実体・運動・軌跡の関係の設計(6)と一緒に**段 6 の着手前に決める。** 段 5.5 ではコードを動かさず、5.5-1 で `physics/` の文面を R1 に揃えるときに、軌跡だけを `(暫定 — 段 6 で外す)` で認める。
   - **段 5 までの規則からは決まらない。** R1・R2 が決めるのは、履歴に依存する可変な記録を時刻層に置かないことだけである。記録を運動の部品にするか独立の所有者にするか、積分を誰が呼ぶか、外挿の中心を誰が持つかは、書いた規則が決めない。6 の結論次第で行き先が変わりうる。
   - **段 6 が読み手ごと作り直す。** 軌跡を読むのは View(`dynamic-view.ts` が `motion.actual`・`motion.predicted` をそのまま読む)と計画線で、6-3・6-4 で表示担当へ移すときに入力を作り直す。段 5.5 で装置の語彙を定義しても、段 6 でもう一度書き直すことになる。
   - 以下は、当初 5.5-4 で行う予定だった案で、判断材料として残す。
     - `physics/` に残すのは定数と純関数(RK4 の1歩 `stepDynamicsWithSamples`・二体軌道の外挿・エルミート補間)と、意味を持たない容器(`StateQueue`。`math/deque` と同じ扱い — 容器のインスタンスの可変な値は、それを持つ者の層に属する)だけ。**積分は `Vec3` と同じ形 — 新しい状態を返す純関数 — になる。**
     - `DynamicTrajectory` から積分(`step`)を外し、先端と間引いた履歴の**記録**だけにしてモデル層(`game/dynamic/`)へ移す。積分は持ち主(`DynamicMotion`・`PredictedArc`)が純関数を呼んで行い、結果を記録へ積む。外挿の中心は予測の弧だけが使うので `PredictedArc` へ移す。
     - 装置(`render/` の軌跡線・計画線)は軌跡の型を引かず、自分の語彙で入力の面を定義する(R7。記録がモデル層へ移るので、引けば R2 に反する)。
     - **運動と軌跡そのものは immutable にしない。** 値(`KinematicState`・`Attitude`・熱)とその遷移はすでに不変値と純関数で、可変なのは「実体ごとに1つの持ち主が不変値を差し替える」ことと、履歴・予測の記録だけである。持ち主まで不変にすると、刻みごとに顔ぶれ・`attachedTo`・反応・表示担当の鍵(R6 は実体の同一性を鍵にする)をすべて張り替えることになり、K1-4 で退けた id の対応表が要る。履歴を永続構造にしても、過去の版を読む者はいない。
   - **接触代理だけは段 5.5 で行う(5.5-4)。** 放熱板の折りとベルトの節点は `DynamicMotion` の派生をやめ、接触の参加者の面だけを持つ軽いクラスにする。置き直しは履歴を持たないので、R20 の伸びは構造ごと消える。
     - 5.5-4 に置く理由: 置き直しの `proxy.state = world`(`radiator.ts`・`belt-physics.ts`)は、5.5-4 が R3 で消す `state` の setter そのものなので、どのみち書き直す。代理が満たす面(`EntityContactParticipant`)も、5.5-4 が命令へ直す参加者の面そのものである。
     - 軌跡の設計に依らない理由: 代理は軌跡を要らない。段 6 が代理のファイルで触るのは、6-6 で形状の定数の import を直すことだけである。
8. **これらは段 6 の PR に入れず、臨時の段 5.5 で片づける(ユーザー判断 2026-09-19)。** ただし 6(実体と運動の境界)と 7(軌跡の置き場)は保留なので除く(7 のうち接触代理は 5.5-4 で行う)。段 5 までに書いた規則の未達と残件で、段 6 の規則(R6)の作業ではない。段の PR は「その段で書いた規則と、それを満たしたコード」を読む単位なので、前の段の是正を混ぜない。段 5.5 は、同じ種類の未達(R7・R8・R12・R5 と、段 5 の未割り当て)も全部拾い、洗い出し(5.5-3)で残りを足す。段 6 は同じファイルを触るので、段 5.5 を先に main へ入れる。
9. **R12 の疑い2件**(`DynamicSystem.deserialize` の流し込み、`SERIALIZATION_VERSION` の置き場)は、5.5-7 で判定して直す。
10. **R7 の残り(`WorldSfx` の単発音、`Bgm` の音量と試聴)は 5.5-6 で直す。** 単発音は出来事の通し番号を装置が受けて二度鳴らさない宣言へ、音量と試聴は毎フレームの宣言として受ける形へ。
11. **タンパク質の部位(監査報告の 4): 規則は静止構造を読み、揺らぎは表示だけにする(ユーザー判断 2026-09-19)。** 5.5-9 で行う。
    - 銃口と被弾部位の選択は、View を読まずに部位の静止座標から求める。
    - SPEC は削るだけで、書き足さない。INVARIANTS §3「表示のためだけに持っている量(見た目の揺らぎ…)は判定に入らない」が唯一の記述として残る。
    - 見た目の挙動は変わる。銃口と被弾部位が、ズーム・時刻スライダー・描画設定によらなくなる。5i4r では、表示中の部位と静止位置が最大 6.3 m 離れる。
    - **戻すことになる変更**: mikanixonable が 09-16 に PR #82 で入れた「表示中のアンカーで撃ち、被弾部位を選ぶ」。動機は「照準している部位と、実際に発射・命中する部位が見た目で一致しない」だった。
    - その主要因が揺らぎなのかは評価されていない。判定の球が外接半径の 1/4 と粗く、揺らいだ主鎖は判定形状から最大 10 m はみ出す。潜在バグの可能性もあり、当たり判定と銃口を表示するデバッグ機能は無い。
    - **原因の調査と直し方は、この計画の外で独立の PR にする**(`memos/hedalu244/fix_protain_combat.md`)。この段では、アーキテクチャの例外を無くすことを優先する。

**未決(ユーザーが決める)**

1. **実体・運動・軌跡の関係をどうするか。段 6 の着手前に決める**(ユーザー判断 2026-09-19。段 5.5 では是正しない)。
   - 範囲: 実体と運動の境界(6)、案の基準で揃っていない2つ(`power`・接続中ブースターの段)、軌跡の置き場と分け方(7)。実体が構築時に受ける協力者(`PilotCommandFrame` の `activeStage`・`stageRules`・`celestialBodies` と、反応が呼ぶたびに受ける `activeStage`)もここで決める。
   - 決めるのは、`motion` の基準の文面と、是正をどの手順で行うかの2つ。基準は 6-1 で ARCHITECTURE の `motion` に書く(`entity` / `motion` / `view` を 6-1 で書き直すので、そこに合わせる)。
   - 段 5.5 へ回さない理由: 案の基準はまだ規則に書いていないので、`power` が運動にあるのは書いた規則の未達ではない(段 5.5 の片づける範囲の基準の外)。
   - 案のとおり移さないなら、`motion` の基準に「毎サブステップの環境で進む部品は運動に置いてよい」を足すことになり、砲身の熱と流儀が割れたままになる。
2. ~~監査報告の 4 と 5~~ **決めた。** タンパク質の部位の SPEC(監査報告の 4)は 11 で、5.5-9 で実施する。実体の幾何の置き場の流儀(監査報告の 5)は K11 で、段 6 で実施する(入れてよい条件は K11-2 で未決)。
3. **命令の名前**: 退場は `kill()`、状態の置き換えはいまの `reset` を使う。

**覆したときに変わる手順**

- **1 を覆す(実体を運動の所有者とみなす)**: R3 に「所有の木」を書き足し、5.5-2 の判定は所有の木を型で表せないので R3 の代入を判定できなくなる。5.5-4 から推力・トルク・質量の命令化が落ちる。
- **5 の名前の列を残す**: 装置へ範囲を狭めて残す(`targets` を装置のフォルダにする)。モデル層の命令を妨げなければ、害は「判定したつもりになる」ことだけになる。
- **7 で軌跡を physics/ に据え置くと決める**: 5.5-1 の暫定を本文にし、R1 の時刻層の文面に「可変な記録を持つクラスを置いてよい」を足すことになる。`physics/` の意味は「法則・暦・時刻系」から外れる。
- **11 を覆す(揺らぎを進行も読む)**: 5.5-9 は是正でなく機能の変更になる。必要になるのは次の3つ(`fix_protain_combat.md`「直し方の候補」)。
  - 揺らぎを敵と時刻で決まる関数にしてモデル層へ移し、標本化のアルゴリズムを任意時刻で安いものへ替える。
  - 判定形状も揺らがせる。
  - seed を個体 id に揃える。

  SPEC も、INVARIANTS §3 を含む3文書を書き直すことになる。

### K11. 実体の形状の正本は中立のフォルダに置く(2026-09-19)

監査報告の 5(実体の幾何の置き場が5通りに割れている)への判断。

**事実**(`5ef4194d` で調べた)

- **3D モデルはコードで作っている。** 自機・金属敵・銃の部品・弾・薬莢・破片・基地・補給・ブースターの段は、`tools/model-builder/` が組んで `src/assets/models/*.json` へ焼く(`npm run export-assets`)。タンパク質は別の生成系(`/add-protain`)。
- **model-builder は寸法を `src/` の TS から読む。** `importTsDataModule` がファイル単体を transpile して読むので、読まれるファイルは import を持てない。

  | ファイル | 中身 | モデル層の読み手 | 描画側の読み手 |
  | --- | --- | --- | --- |
  | `physics/player-shape.ts` | 銃口2点、放熱板と太陽電池パネルの蛇腹、放熱板のヒンジ、マガジンとベルト | `fire-control`(銃口)、`radiator`(蛇腹とヒンジ)、`belt-physics`(ベルト) | model-builder(`player-ship`・`gun-parts`・`pickups`)、render(`folding-panels-view`・`belt-view`) |
  | `physics/booster-stage-shape.ts` | 段の寸法・取付位置・段間カバー | `attached-boosters` | model-builder(`booster`)、render(`attached-boosters-view`・`detached-booster-view`) |
  | `render/rcs-nozzles.ts` | RCS ノズルの位置と向き | **無い** | model-builder(`player-ship`)、render(`rcs-effects`) |

  幾何でないもの(材質)も model-builder は読んでいる。`render/thermal-emissive.ts` は model-builder(`gun-parts`)と render の6本が読み、`render/dynamic/metal-f0.ts` は model-builder(`materials`)しか読まない。

- **当たり判定の近似は、モデル層に手で写してある。** 描画側のアセットとの一致を固定しているのは金属敵だけ。
  - 金属敵の外接球(`metal-enemy.ts:16-21`)。`ENEMY_MODEL_SCALE` 倍した値で、`tests/render/enemy-model-bounds.test.ts` が描画アセットの外接球と照合する。
  - 薬莢の円柱(`casing-collision.ts:13-15`)。`casing.json` から写した値で、照合は無い。
  - ブースターの接触球 `BOOSTER_COLLISION_RADIUS = 4.2`(`detached-booster-motion.ts:22`)。段の長さ 8 m から手で計算した値で、照合は無い。
  - 実寸に近い半径として置かれている近似: `PLAYER_HULL_RADIUS`(`player.ts:50`)、`PICKUP_PHYSICAL_RADIUS`(`pickup-motion.ts:17`)、`BARREL_PHYS_RADIUS`・`EJECTED_MAG_PHYS_RADIUS`(`fire-control.ts:28-29`)、ベルトの節点の半径(`belt-physics.ts:31`)。
- **基地の判定形状は手で写していない。** `npm run base-collision` が `base.json` から `baseCollision.json` を焼き出し、CI が `--check` で照合する。
- **`physics/protein-site-geometry.ts` は定数ではなく、`math/` を引く変換関数。** 読み手は render の2本(`protein-runtime`・`protein-enemy-view`)とテスト1本で、タンパク質のアセットの生成とは連動しない。

**決めたこと**

1. **流儀は Y(中立のフォルダ)にする**(ユーザー判断)。`src/entity-shapes/` を新設し、層の表で定義層にする。
   - 名前は、既存の `*-shape.ts` に揃えた。`marker-shapes`・`cumulus-shape` と取り違えないように、実体の形であることを名前に入れた。ユーザーの例の `game-geometry` は、`game/`(モデル層)の中にあると読めるので採らなかった。
   - 名前を変えても、パスが変わるだけ。
2. **入れてよい条件 — 未決。段 6 の着手前に決める**(ユーザー判断 2026-09-19。RCS ノズルは入れるべき側、という判断から A を見直した)。どの候補でも、次の2つは共通。
   - ゲームバランスの調整値と、見やすさのための歪めは入れない。条件に当たらない形状の定数は、概念の所有者の隣に置く(1.6)。
   - ゲームが決める大きさ(`ENEMY_MODEL_SCALE`)は入れない。正本はモデル座標(縮尺をかける前)で値を持ち、縮尺をかけるのは利用者にする。定義層は `game/` を import できないため。

   | 候補 | 条件 | 入るもの | 入らないもの | 判定 |
   | --- | --- | --- | --- | --- |
   | A(当初のユーザー案) | モデル層と描画側の両方から見る必要があり、かつアセットの生成と同時に整合のために書き換えることが予想される、モデルの幾何形状。「描画側」は `render/` と model-builder。当たり判定の近似は、描かれる形の近似なので描画側から見るものに数える(当て方は実施のために決めたもので、覆せる) | `player-shape`・`booster-stage-shape`、手で写した当たり判定の近似 | **RCS ノズル**(モデル層が読まない)、`thermal-emissive` | 洗い出し |
   | B(ユーザー案 2026-09-19) | model-builder・render・モデル層のうち2者以上が読む、モデルの幾何形状の共通正本 | `player-shape`・`booster-stage-shape`・**RCS ノズル** | **モデル層しか読まない当たり判定の近似**(金属敵の外接球・薬莢の円柱・ブースターの接触球・実寸に近い半径)。これらは所有者の隣に残り、アセットとの一致はテストだけが守る | 読み手を数えるので、検査で判定できる |
   | C(A から「両方から見る」を外す) | 焼き込みアセットの形と一致していなければならない幾何。読み手は問わない | `player-shape`・`booster-stage-shape`・RCS ノズル・当たり判定の近似 | `thermal-emissive`(幾何でない)、`protein-site-geometry`(アセットの形と連動しない) | 洗い出し |

   - **B の注意点**
     - 「幾何形状」に限らないと、`thermal-emissive`(材質。render と model-builder が読む)も入り、フォルダの意味が「model-builder と描画が共有するもの」に広がる。
     - K10-11 の後も、規則(静止位置)と render(変位を足した位置)は同じ変換の式を使う。そのため、`protein-site-geometry` を分けずに1つの関数として正本に置く形も B の条件に当たり、K11-6 の分け方との選択になる。
     - model-builder と render だけが読むもの(RCS ノズル)は、import の向きの問題を持たない。tools は `render/` を読める。入れる理由は、アセットとの整合を1か所で見られることになる。
   - **推奨は C。** フォルダの目的は、アセットとの整合を取る値を1か所に集め、アセットを作り直すときに一緒に直す(K11-4 の SKILL)ことにある。C はこの目的をそのまま条件にしていて、RCS ノズルも当たり判定の近似も入る。B は検査で判定できるのが利点だが、ユーザーが「大部分」と見た当たり判定の近似が外れる。
3. **手で写した近似は、描画側との一致を確かめられる形にする。** 置き場は 2 で決まる(A・C なら正本、B なら所有者の隣)。
   - 正本の寸法から計算で出せるもの(ブースターの接触球)は、計算で導く。
   - アセットの外形を測って決めるもの(金属敵の外接球・薬莢の円柱)は、一致をテストで固定する。金属敵は既存の `enemy-model-bounds.test.ts` を使い、薬莢には同じ形のテストを足す。
   - 「実寸に近い半径」の近似は、5.5-3 の洗い出しで拾い、2 で決めた条件に当てて振り分ける。
4. **形状を変える手順を SKILL にする**(ユーザー判断)。`/modify-model-shape` を新設し、CLAUDE.md の「SKILL は自分から起動する」の表に「3D モデル(`tools/model-builder/`)の形や寸法を変える」の行を足す。SKILL が持つのは次の手順だけ。どう見せるかは SPEC、置き場の条件は ARCHITECTURE が正本。
   - 正本の寸法を先に直す。
   - `tools/model-builder/` の形を直す。
   - `npm run export-assets` で焼き直す。差分が識別子だけのファイルは戻す(CLAUDE.md)。基地を変えたら `npm run base-collision` も走らせる。
   - アセットの外形から測る近似(外接球・円柱)を測り直して、正本を直す。
   - 一致のテスト(`test:render`)と `render-lab:shot` で確かめる。
5. **反映は段 6 で行う(ユーザー判断 2026-09-19)。** 規則は 6-1、検査は 6-2 で書き、移すのは 6-6 で行う。
   - 段 5.5 は、すでに書いた規則の未達を直す段である。新しいフォルダと、その入れてよい条件を新設することは、その目的から外れる。
   - 意味の上では dynamic の見た目と動作の分離なので、R6 の段 6 に入れる。
   - **段 5.5 の終わりに、`physics/` の形状の定数が書いた規則の未達にならないようにする。**
     - いまの「`physics/` に物理でないものを持ち込まない」は、実体の形を挙げていない。
     - 5.5-1 の `physics/` の書き直しは、可変な値についての文(「定数・純関数と、意味を持たない容器」)にする。
     - 実体の形を `physics/` から締め出す文は 6-1 で書く。
   - 手で写した近似の重複(1.6)は、5.5-3 の洗い出しで拾う。そのうえで振り分けの表で段 6 へ回す。回してよい理由は「後の段が対象ごと作り直す」に当たる。
6. **`protein-site-geometry.ts` は `entity-shapes/` に入れず、ファイルを消して読み手の隣へ分ける**(A・C の条件に当たらない。B を選ぶなら、上の注意点のとおり分け方を選び直す)。段 5.5 の 5.5-9 ではファイルを動かさず、規則も `physics/` の関数を読む(モデル層から時刻層への import は R2 に適う)。分けるのは 6-6 で行う。
   - K10-11 により規則は静止構造を読むので、2つの読み手が要るものは別になる。
     - 規則(`game/protein/`)が要るのは、静止位置の変換と、着弾点を模型座標へ戻す逆変換。静止位置は `closestSite` がすでに組んでいる(`protein-combat-state.ts:243-253`)。
     - View(`render/protein/`)が要るのは、変位を足した位置の変換だけ(`siteMarkers` が読む `ProteinRuntime.siteWorldPositionById`)。
   - 両者が共有するのは、`math/` の回転と加算、アセットの `coordinateScale` だけになる。共有の関数を残す理由は無い。
   - 覆して揺らぎを進行も読む形(11 を覆す)にするなら、両者が同じ関数を読むことになり、置き場を改めて決める。

**覆したときに変わる手順**

- **X(利用者の隣)に戻す**: 6-1 の層の表の行と、6-2 の `LAYER_TABLE` の行を落とす。移す先が所有者のフォルダになる。model-builder が読む定数は import を持たないので、`game/` に置いても定義層として読める(R2)。SKILL は、正本の場所を読み替えるだけで残る。
- **条件を選ぶ(2)**: 6-6 で移すものが変わるだけ。B なら当たり判定の近似は所有者の隣に残り、一致のテストだけを足す。B は読み手の数で判定できるので、6-2 に判定を足せる。

## 達成目標

### どの段でも、main へ送る前に満たすもの

1. **その段までに書いた規則を、コードが満たしている。** 判定のある規則は、`npm run check:boundaries` が**空の許可リスト**で終了コード 0 を返すこと(と `tsc`・テスト)で確かめる。判定の無い規則は、段の終わりに下の表の「洗い出す」列を行って確かめる。**許可リストが空であることを、判定の無い規則まで満たした根拠にしない**(K10)。
2. `DEVELOP/ARCHITECTURE.md` に残る `(暫定 — 段 N で外す)` が、N がその段より後のものだけになっている。
3. **セーブが読める。** 基準は段で変わる(K9)。
   - **段 4 まで**: 保存形式の互換が保たれている。既存の項目は変えず、足すのは省略可能な項目だけ(SAVE.md「形式の版」により版は据え置く)。段 1 に着手する前に書き出したセーブが読め、同じ位置・同じ操作対象・同じカメラで再開する。
   - **段 5**: 形式の版を上げる。段 4 までの記録は読めなくなり、拒まれるだけで画面も起動も壊れない。
   - **段 6 以降**: 版は上げない。**段 5 で書き出したセーブ**が読め、同じ位置・同じ操作対象・同じカメラで再開する。
4. `npm run typecheck`、`npm run test`、`npm run build`、`npm run smoke:browser` が通る(`/send-pr`)。

**規則と判定の対応**(手順 5.5-2 の後。R9・R10 の行は段 8 で足す)。判定できるものは形で判定し、名前で判定するのは規則が名前を定めているときだけにする(K10-5)。

| 規則 | 判定するもの | 段の終わりに洗い出すもの |
| --- | --- | --- |
| R1 | フォルダの層(対応表に無いパスは検査が落ちる) | 定義層・時刻層のクラスが持つ可変な値が、メモ化キャッシュか、意味を持たない容器だけか |
| R2 | import の辺(層の規則) | 段 6 から: `entity-shapes/` の中身が、層の中身に書いた入れてよい条件に当たるか(K11。B を選べば判定へ移せる) |
| R3 | モデル層の可変な public 欄・`set` アクセサ・export する型の可変な欄(構文木で判定。他のクラスの欄への代入は、これで `tsc` が止める) | 可変オブジェクトを外へ渡していないか(読み取り面を通すか)。モデル層の外からの命令が列を通るか |
| R4 | 進行から視点への import、需要が結果を変えないこと(`trajectory-demand.test`) | — |
| R5 | 導出層の壁時計、モジュール直下の可変値(一部のファイルだけ) | 導出層の可変な値が5種に収まるか |
| R6 | モデル層から `three`・装置への import(6-2)、突き合わせ(`dynamic-presenters.test`、6-3) | 実体1体ぶんの表示物が、表示担当の欄になっているか |
| R7 | 装置の import の境界 | 装置の公開メソッドに命令(`show`/`hide`/`set`/`play`/`stop` の類)が無いか。**名前の列では判定しない** |
| R8 | — | 位相の順序(`Run.frame`)。DOM のハンドラが命令と R5-4 の状態しか作らないか |
| R11・R12 | 直列化の語彙(規則が名前を定めているので名前で判定)、往復(`progress-serialization.test` ほか) | コンストラクタが直列化された形を受けないか、構築後に流し込まないか(名前の判定はこの代用にすぎない) |
| R13 | — | 部品を上から注入していないか。同じ協力者を2経路で受けていないか |

### 段ごとに増える目標

| 段 | 段の終わりに満たす規則 | その段だけの達成条件 |
| --- | --- | --- |
| ~~1~~ 済 | R1・R2(装置と時刻層)・R7(出来事を除く) | 装置が命令 API を公開していない(`rg -n "ensureStarted\|\.unlock\(\|setThrust\|setRcs\|applyGraphics\|setFixedBrightnessScale\|MarkerSlots" src` が 0 件)。marker の公開面が「宣言を受ける sync」と「表示したかの問い合わせ」だけになる。`src/marker/` が独立した装置として存在し、ほかの装置との import が双方向とも 0 件。`rg -n "from '.*(game\|settings\|launcher)/" src/hud` が 0 件 |
| ~~2~~ 済 | R5・R10(`settings/`) | `rg -n "localStorage" src/game src/theme.ts` が 0 件。`rg -n "RunSetting\|setInput\(\|setHandlers\(" src` が 0 件。`game/hud` が `Game` を受けない |
| ~~3~~ 済 | R3・R7(出来事)・R8・R4(需要) | 表示側から進行へ届く経路が需要だけになる(`rg -n "predictsFuture\|display-window-duration" src/game/dynamic` が 0 件)。予測弧をなぞる積分について、R4 の検査が通るか、ユーザーの判断が記録されている |
| ~~4~~ 済 | R4(全文)・R11 の1 | 進行のパスから `game/viewer/` への import が 0 件。`settings/` と `game/viewer/` の間の import が双方向とも 0 件。§6 が設定と言う値を `game/` が持っていない。`rg -n "例外なくすべてセーブ" DEVELOP/CODING-RULE.md` が 0 件(規則は段 5 で書き直す)。`game.ts` を段 5 まで分割しないことが、1.10 の位相の印・1.2 の暫定・`game.ts` の冒頭・PR 本文の4か所に書いてある |
| ~~5~~ 済 | R11 の2・R12・R13・R8 と R10 の言い直し | 手順 5-5 で洗い出したモデル層の値が、キャッシュを除いてすべてセーブに載っている。`rg -n "\w+SaveData\b\|WeaponStateData\|BoosterStackData" src` が 0 件で、`src/game/save/` と `src/game/game-host.ts` が無い。`rg -n "'saved' in\|restoreFromSave\|importData\|exportData\|saveState" src/game` が 0 件。位相の順序が `src/run/run.ts` だけにあり、`src/game/game.ts` が `game/{hud,marker,view,pickable,map,lines,input,camera}` と `game-presentation` を import しない。セーブの版が上がり、新しい版で書いた記録が読め、古い版は拒まれるだけで壊れない |
| 5.5(臨時) | 段 5 までの規則(R3 は構文の判定つき) | R3 の判定(モデル層の可変な public 欄・`set`・export する型の可変な欄)が 0 件。`invalidatePrediction` が `dynamic-motion.ts` の外に無い。`rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 10(軌跡の暫定と、5.5-3 で置いた5つを足した)。`rg -n "onDecided" src` と `rg -n "KEY_MAPPING" src/game/dynamic` が 0 件。`rg -n "view\.(siteWorldPositionById\|siteModelPositionById\|localImpactPoint)" src/game` が 0 件。音の装置が単発の命令を公開しない。振り分けの表(5.5-3 で足した行を含む)の全行が「直した」か「残す理由がある」。段 5 で書き出したセーブが読める |
| 6 | R6(dynamic の族とステージ)・`motion` の基準(K10-6)・軌跡の置き場(K10-7)・`entity-shapes/`(K11) | モデル層の実体が表示物・装置を持たない。5.5-1 で置いた軌跡の暫定が K10-7 で決めた形で、5.5-3 で置いた協力者の2経路の暫定が K10-6 で決めた形で外れている。`Game.create`/`Game.deserialize` が `HudLayers` と `THREE.Scene` を受けない。`rg -n "DynamicView\|WorldSfx\|UiSfx\|Notifier\|MarkerSlots\|FlashEffects\|from 'three" src/game/dynamic src/game/player src/game/stages src/game/creative` が 0 件。`src/entity-shapes/` があり、`src/physics/` に実体の形状(`player-shape.ts`・`booster-stage-shape.ts`・`protein-site-geometry.ts`)が無い。`/modify-model-shape` がある |
| 7 | R6(全域)・R11 の3 | `Game.create`/`Game.deserialize` が `GameScene` を受けず、THREE と DOM なしでテストから組める。時刻層に残った天体のファイルから、`render/`・`three`・`hud/`・`game/(camera\|marker\|map\|pickable\|hud\|dynamic)` への import が 0 件。天体系の構築関数の引数が構築値ひとつになる |
| 8 | R2(最終の対応表)・R9・R10(全文) | `src/game/game.ts`(モデル層の根)から `render/`・`three`・`hud/`・`presentation/`・`run/` への import が 0 件。`src/celestial/` が存在し、そこから `physics/` と `math/` 以外への import が 0 件。`rg -n "game/celestial" src tests tools DEVELOP CLAUDE.md .claude/skills` が 0 件。`rg -n "from '.*render/" src/game` が 0 件。`rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 0 |

## 手順

### 段 1〜3 — 済(PR #80・#81・#84)

**手順は実施して main へ送ったので、この文書からは落とした。** 残っているのは、次の段が前提にしてよい結果だけ。

| 段 | 書いた規則 | コードが満たしていること |
| --- | --- | --- |
| 1 | R1、R2 の不変方向・装置・時刻層・語彙の各項、R7(出来事の段落を除く) | 装置(`render/`・`marker/`・`audio/`・`input/`)は宣言だけを受け、命令 API を公開しない。装置は `game/` を import せず、装置どうしも import し合わない。`src/marker/` が独立した装置として存在する |
| 2 | R5、R10 の `settings/` の項、R2 の定義層の項 | 導出層の mutable は R5 の5種だけ。ラン跨ぎの設定と UI の選択は `settings/` が正本で、読み取り専用の面と callback で渡る。`game/`・`theme.ts` から `localStorage` を触らない。`game/hud` は `Game` を受けない |
| 3 | R3、R7 の出来事の段落、R8(1.10 を置換)、R4 の需要の2項 | 進行に効く値への外からの書き込みは、所有者の隣に置いた命令の口(`*-commands.ts`)が `CommandQueue` へ積み、進行の位相の先頭で受け付けた順に適用する。モデル層は音・通知・閃光の装置を持たず、`registry.events`(`RunEventLog`)へ出来事を記録し、表示の導出(`run-event-presenter.ts`・`flash-presenter.ts`)が読んで装置へ渡す。表示の選択から進行へは需要(`dynamic/trajectory-demand.ts`)だけが届く。生の入力を読むのは入力の解釈(`game/input/`)だけで、モデル層へは機体の語彙の操作量と命令(`dynamic-entity/pilot-controls.ts`)が届く |

**段 3 で決まった形のうち、後の段が崩してはいけないもの**(手順 5-3 で位相を `src/run/` へ組み直すときに効く):

- **出来事の記録を空にするのは、進行の位相の先頭で、命令の適用より前。** 命令の適用が出来事を積むため。DOM のハンドラで起きた出来事は `queuedEventSink` で列を通して積む。そうしないと、次の進行の先頭で捨てられて表示されない。
- **操作対象への命令は列へ積まず、そのフレームの操作量の一部として値で渡す。** 列へ積むと時間加速の倍率が決まる前に適用され、`canShipAct` のゲートが前のフレームの倍率で判定してしまう。
- **入力の解釈は、直前の進行が確定させた時間加速の倍率を入力として受ける。** CONTROLS.md は、×4 を超えるワープ中はダブルタップを判定しないと定めている。
- **需要は予測器と履歴要求のどちらよりも前に組む。**
- **何を予測弧で置き換えてよいかは、進行の側の性質**(構築時の `followsPredictedArc`)で決める。需要が決めるのは、どこまで伸ばすかだけ。

**いま生きている装置**: `tools/check-boundaries.mjs` が 17 種を判定し、`tools/boundary-allowlist.json` は空。

**段 3 の終わりに残っていた暫定**(`rg -c "暫定 — 段" DEVELOP/CODING-RULE.md` が 7。段 4 でどう動いたかは下の「段 4 の終わりに残っている暫定」を見る):

- `hud/` を表示の導出として扱う(`game/`・`settings/`・`launcher/` への import は装置と同じく禁止)— 段 8 で外す
- `game/` の中を割らない — 段 6 で外す
- 表示の導出の置き場は `src/game/` の中(`hud`・`marker`・`view`・`pickable`・`map`・`lines`)— 段 8 で外す
- R5 が「視点へ置く」と言う値は `game/` の所有者が持つ — 段 4 で外す
- 列を通すのは進行に効く値への書き込みだけでよく、視点(カメラ・ビュー・航法ターゲット・軌道要素の基準・表示期間)は DOM のハンドラから所有者を直に呼んでよい — 段 4 で外す(段 3 で追加)
- 位相の順序を組み立てるのは `src/game/game.ts` — 段 3 で追加。当初は段 8 で `main.ts` へ移す予定だったが、K8-5 により手順 5-1 でランの組み立て(`src/run/`)が持つ最終の文面へ置き換え、5-3 でコードを移す
- モデル層が装置・生の入力・出来事の装置を import しない規則は、`src/game/` の中のパスで当てる — 段 6 で層の規則へ置き換える(段 3 で追加)

コードの側にも暫定の印がある: `tools/check-boundaries.mjs` の段 3 の判定3本(段 6 で外す)、`src/game/game.ts` の「HUD へ渡す値」の節(当初は段 8。5-3 で表示の導出へ移すときに消す)。

**まだ無いフォルダ**: `src/presentation/`・`src/celestial/`・`src/run/`(`src/game/viewer/` は 4-3 で作った)。

### 段を main へ送る(共通)

各段の最後の手順は、どれも同じ形をしている。

**目的**: その段の規則と、それを満たしたコードを同時に main へ入れる。次の段の着手条件を作る。

**手順**

- `/send-pr` を通す(main の取り込み、変更範囲への `/refactor` と `/comment-cleanup`、CI と同じ全検証)。
- 取り込み直後に `npm run check:boundaries` を走らせ、**他の作業が持ち込んだ違反を自分の段の残りと切り分ける。**
- **規則と判定の対応表(達成目標)の「洗い出す」列を、その段までに書いた規則すべてについて行う。** 判定の無い規則は、許可リストが空でも満たしたことにならない(K10)。見つけた違反はその段の中で直す。直せないなら段を閉じず、規則を狭める(暫定)か、ユーザーに問う。**後の段へ回さない。**
- PR 本文に、**この段で書いた規則と、次の段まで有効な暫定**を列挙する。段の間に入る作業が見るのはこれである。洗い出しの結果も書く。

**達成条件と検証**

- `tools/boundary-allowlist.json` が空で、`npm run check:boundaries` が 0。
- セーブが読める(達成目標の 3。段 4 までは段 1 の前のセーブ、段 5 は新しい版、段 6 以降は段 5 で書き出したセーブ)。同じ位置・操作対象・カメラで再開する。
- `npm run typecheck`、`npm run test`、`npm run build`、`npm run smoke:browser`。


### 段 4 — 済(PR #85)

**手順は実施して main へ送ったので、この文書からは落とした。** 残っているのは、段 5 以降が前提にしてよい結果だけ。

**書いた規則**: R4 の全文、R11 の1。R1・R5・R10 の設定の文面を「セーブを跨いで共通か」で言い直した。その前に `INVARIANTS.md` §6 と `SAVE.md` を更新した(`659ed354`)。**4-1 で先に書いた R11 の2は、満たす作業が段 5 へ移ったので 4-4 で落とした**(K8-7)。`SAVE.md` 側の記述は仕様なので残る。

**コードが満たしていること**: 遊ぶ人の選択のうちセーブごとに持つもの(視点)は `game/viewer/` にあり、セーブを跨いで共通のもの(設定)は `settings/` にある。どちらに置くかは `INVARIANTS.md` §6 が決める。進行は `game/viewer/` を import しない。視点への書き込みも命令の列を通る。セーブはモデル層の直列化で、表示の導出と設定を含めない。**保存形式は段 1 の前から変わっていない**(段 4 の `save-data.ts` の差分は import のパスと `export` の1語だけ)。

**段 5 が前提にしてよい、段 4 で決めた設計**(2026-09-17。覆すなら 段 4 の該当部分をやり直す)

- **視点の形。** 根 `Viewer`(`game/viewer/viewer.ts`)が所有者を組み、セーブとの行き来と、進行の直後に呼ばれる規則 `followProgress(events)` を持つ。所有者は「何の選択か」で名付け(`NavTargetSelection` など)、読み取り面 `*Source` と命令の口 `*Commands`(`xxxCommands(queue, owner)`)を隣に置く。表示の導出・DOM ハンドラ・入力の解釈には、所有者のクラスではなく読み取り面と口だけを渡す。復元は所有者のコンストラクタで行う(二段初期化にしない)— **K8 により、5-4 で静的な `deserialize` へ揃える。**視点のモジュールは、定義層・時刻層・進行・保存形式の型・`command-queue`・`run-events`・自分自身だけを引く(`render/` の語彙の型は `import type` だけ許す)。
- **進行は視点を書かない。** `ControlSelection` は航法ターゲットとカメラを持たず、選び直し(`controlTargetSelected`)と取り除き(`controllableRemoved{id}`)を出来事として記録する。視点は今ステップの出来事を読み、選び直しがターゲットの命令より後(通し番号で比べる)ならターゲットを外し、取り除かれた id を指すターゲットと注視を戻す。これで「選択の後に設定したターゲットは残る」いまの順序が保たれる。構築中の出来事にも当てるため、Game は構築の最後に1回だけ規則を当てる。
- **的面の通過の記録は進行の処理**(`game/dynamic/target-board-passes.ts`)で、どの対象について記録するかを需要(ターゲットの id)として受ける。Game は命令の適用の直後に視点から id を読んで渡す。残った `targeter.ts` は表示の導出なので、手順 6-2 のファイル単位の例外に載せる。
- **ヒント。** 命令の適用結果で文面が変わるもの(ターゲットの設定/解除、カメラのリセット、姿勢追従の切り替え)は、視点が出来事を記録し `run-event-presenter.ts` が文面にする。適用結果に依らないもの(「○○ にフォーカス」)は、表示の導出が操作の時点で直に出してよい。
- **予測パネル。** 状態(座標系・期間のキーと任意期間・スクラブ位置・表記・通過時刻の併記・カメラの基準に追随)に目盛りの表示を足して視点へ移す。戦闘ビューでスクラブ位置を 0 にする規則は視点の `followProgress` に置く。`dropStaleRotatingFrame` は `FrameAnchors`(表示の導出)を読むので、表示の導出が判定して命令を積む(K3-2 と同じ形)。
- **カメラ。** 視点は2台の状態・命令・保存と、姿勢追従の相対値の基準になる姿勢を持つ(姿勢は進行から `followProgress` で更新し、保存時は rotation を絶対値へ畳むので保存しない)。表示の導出は操作係(`camera-operator`)・毎フレームの導出(`camera-rig`・`focus-derivation`)・ガンサイト・画角遷移を持つ。**座標変換に要る材料(表示時刻・座標系の錨・基準の上方向)は `CameraFrameSample` という値で、命令の引数か `followProgress` の引数として受け、視点は表示の導出を参照しない。** 注視の差し替えは世代番号を置かず、表示の導出が `FocusTarget` の同一性で検出する。キー・ホイール・ドラッグは入力の解釈の位相(`handleInput`)で命令にし、同じフレームの進行で適用する(ゼロ入力でも毎フレーム積む — 距離とピッチのクランプがいまも毎フレームかかっているため)。ポインタのピック判定も `handleInput` へ移し、前フレームの `CameraFrame`・候補列で揃える。射影の入口は `CameraFrame.project` の1つにする。画角遷移は dt で積まず実時刻から計算する(R5 の既存の違反を直す)。**進行への追従(姿勢の更新、成立しなくなった回転追従の解除、注視を見失ったときの戻し)は表示中のカメラにだけ当てる**(表示していないカメラの追従を、そのビューに戻るまで保つ。いまの挙動)。注視を差し替えた直後の追従の解除は、2フレームの猶予を置かない。
- **ロード直後にマップのカメラの pan が 0 に戻り、機体・役割への追従が慣性系へ落ちることがある不具合**(`game.ts:402` の `frameControls.setFocus` の副作用)は、§6 と「同じカメラで再開」に反するので、**保存どおり戻すように直す**(ロード時にカメラへ注視の命令を積まない。予測パネルの「カメラの基準に追随」だけは合わせる — 手順 5-5 で座標系を保存したら、保存値があるときは合わせない)。
- **実体ごとの表示設定。** 軌道線のトグルは視点が実体の **id** を鍵に持つ(実施時に同一性から変えた。復元時、アセット待ちで顔ぶれにまだいない個体の分も戻すため。航法ターゲットも id で持っている)。メニューのトグル操作は実体の `runMenu` へ届けず、表示の導出が視点の命令にする。**タンパク質の表示は、いまの UI では全個体と生成時の選択が常に同じ値に揃うので、視点の1つの値にまとめる**(実体の `displaySettings` と `ManualSpawn.display` を消す)。保存形式の各個体の `display` は、セーブを組むときに合成する。読み込みでは保存された最初のタンパク質敵の `display` から戻す(5-5 で専用の省略可能項目を足す)。
- **`lastFocusId` は消し、窓の強調はマップのカメラの注視から毎フレーム導く。** 見た目が変わるのは、戦闘ビューで始めたランでマップを一度も開かないうちから強調が出ることだけ。

**段 4 の終わりに残っている暫定**(`rg -c "暫定 — 段" DEVELOP/CODING-RULE.md` が 6)

- 位相の順序を組み立てるのは `src/game/game.ts` — 段 5 でランの組み立て(`src/run/`)へ移す(4-4 で行き先を `main.ts` から直した)
- `src/game/game.ts` は 500 行を超えたままにし、計画の外で分割しない — 段 5 で外す(4-4 で置いた。`game.ts` の冒頭にも同じ1行がある)
- `game/` の中を割らない / モデル層の禁止はパスで当てる — 段 6 で外す
- `hud/` の扱い / 表示の導出の置き場 — 段 8 で外す

**いま生きている検査**: `tools/check-boundaries.mjs` が 18 種を判定し、`tools/boundary-allowlist.json` は空。進行のフォルダに残る表示の導出は `MISPLACED_PRESENTATION_FILES` に名指しで載っている(手順 6-2 が引き継ぐ)。

### 段 5 — 済(PR #86)

**手順は実施して main へ送ったので、この文書からは落とした。** 残っているのは、段 6 以降が前提にしてよい結果だけ。消したアサーション・洗い出しの表・規約点検で直したものは PR #86 の本文にある。

**書いた規則**: R12・R13(1.3)、R8(1.10)と R10 の言い直し(需要と追従の材料は進行の末尾で受ける、位相の順序は `src/run/` が持つ)、R2 の表の `run/`、R11 の2。1.6 の静的側は「タグと静的な `deserialize`」。**実施中に広げた文面**: `deserialize` が `serialized` の後ろに受けてよいものへ「どの種類として組むかを呼び手が決める不変な定義(カメラの設定・種別ごとの慣性など)」を足した。実体の記録が状態の時刻 `t` を持つので、実体の `deserialize` は復元時刻を受けない(1.6 の揃える引数は「直列化された形と協力者」)。

**コードが満たしていること**
- **3つの根**: `Game`(`src/game/game.ts`)はモデル層の根で、`create`/`deserialize`・`advance`・`followCamera`・`extendPredictions`・`serialize` を持つ。`GamePresentation`(`src/game/game-presentation.ts`)は表示の導出の根。`Run`(`src/run/run.ts`)が2つを組み、`frame()` が位相の順序を持つ。`Run.create`/`Run.resume` を `launcher.startRun` が呼び分ける。暖機(シェーダの事前コンパイル)は進行を通さず、進行の後の導出と同期だけを通す。
- **直列化と復元の形**: 所有者はどれも「既定引数を含む private コンストラクタ(新規の初期値)」「`create`」「`static deserialize`」。持ち主を参照する部品(`FireControl`・`RadiatorSystem`・`EnemyFireController`)は `deserialize` を持たず、持ち主のコンストラクタが復元済みの値から作る。実体は `entity-dictionary.ts` の `ENTITY_CLASSES`(9種)を `findEntityClass(kind)` で引き、`DynamicEntityClass.deserialize(serialized, registry, scene)`。`DynamicEntity.serialize` は抽象。
- **セーブの形式(版 4)**: `{ version, progress, viewer }`。`progress` は `Game` 自身の値と所有者ごとの入れ子(`dynamicSystem`・`controlSelection`・`stage`・`simSpeedManager`・`planNodeRules`)。モデル層の値は、キャッシュ(所有者のコメントに書いてある)・乱数の系列・需要が残させる過去の軌跡を除いてすべて載っている。**段 6 以降は版を上げない**(K9)。段 6 以降の検証は「段 5 で書き出したセーブが読める」を基準にする。
- **アセット待ちの実体**は種別つきの記録 `SpawnRecord`(`entity-registry.ts`)で、実体化は `DynamicSystem` が `ProteinEnemy.create(request, …)` を呼ぶ。出撃数は要求した時点で数え、表示と勝ちの判定は `Stage.enemiesAppeared`(出撃数 − アセット待ちの敵)を読む(PROTEIN.md「出現」)。
- **検査**: `tools/check-boundaries.mjs` に直列化の語彙・復元の流し込み・直列化された形をコンストラクタで受ける・モデル層の根が表示の導出を持つ、の4判定を足した。許可リストは空。`Game` が `HudLayers` の型を受けてよい例外が1つある(`(暫定 — 段 6 の 6-5 で外す)`)。
- **テスト**: `npm run test` は launcher 層(`npm run test:launcher`)を含めて 1003 本。往復の表 `tests/game/progress-serialization.test.ts` は、既定と違う値の記録を入力と期待値にする形(本物を組んで往復させる形は `serialize` 側の取りこぼしを検出しない)。THREE と DOM が要る所有者の往復は 7-4 で扱う。

**段 5 の終わりに残っている暫定**(`rg -c "暫定 — 段" DEVELOP/CODING-RULE.md` が 4)
- `game/` の中を割らない / モデル層の禁止はパスで当てる — 段 6 で外す
- `hud/` の扱い / 表示の導出の置き場 — 段 8 で外す

**段 5 で見つけて後の段へ回したもの**
- 5.5-4・5.5-5(K10): `StageDebug.update` が毎フレーム敵の `fireEnabled` へ代入、creative の `applySimulationEvents` が艦の `motion.state` へ代入(R3)。**R3 は段 3 から main に書かれているので、本来は後の段へ回せなかった**(K10)。
- 6-3・6-5: `player.ts`(約 590 行)は表示物の同居が原因。
- 8-6: `Launcher` が設定と計測を `Run` へ中継するだけの引数、`main.ts` のラン外の入力ポートの表。
- 5.5-7・5.5-8(以前は未割り当て): `SimSpeedManager.handleCommand` が生のキーコードを比べる(入力の解釈が進行へ漏れている、R8)。`DynamicSystem.update` の閉包の注入。`ProteinEnemy.muzzlePosition` が状態を進めて値を返す。敵の `waveId` などが無いことを `undefined` で表す。燃料の関数が状態を更新して値を返す。`focus-camera-selection.ts` が 563 行。
- 計画の外(段 5.5 の振り分けの表)— SPEC と実装の食い違い(テストに書けなかったもの): GAME.md §7「何も無い空間で止まって見えてもならない」に対し、基地の判定形状は p90 で 16 m 浮いている。
- 5.5-8 — launcher の UI: セーブデータ画面が版 3 の手動セーブを読み込めるものとして並べ、ダブルクリックすると何も知らせずに新しいランが始まる(SAVE.md は満たしている)。

**進め方で分かったこと**: 所有者の群ごとにファイルを重ねずに割れば、段の中でも git worktree で並行できた(5-5b の4群、5-6 の2つ、規約点検の4群)。worktree の node_modules はジャンクションで繋ぎ、終わる前にリンクだけを外す。統合は cherry-pick。


### 段 5.5(臨時)— 段 5 までの規則の未達と残件を片づける

**2026-09-19 に新設した**(K10、ユーザー判断)。段 6 の未決事項を洗う途中で、段 1〜5 で書いた規則(R3・R7・R8・R12 ほか)に反するコードが main にあると分かった。後の段へ回した残件にも、書いた規則の未達が混ざっていた。これらは段 6 の規則(R6)の作業ではないので、**段 6 の PR に混ぜず、この段で全部片づけて1本の PR で main へ送る。** 段 6〜8 の番号は変えない。

**この段で書く規則**: 新しい規則は書かない。段 5 までの規則の意図を、判定できる形に書き直す — R3 の言い直し(所有者・口・整合性・可変な public 欄)、CODING-RULE 1.11 のモデル層への適用、ARCHITECTURE の層の中身の `physics/`。**実体と運動の境界(ARCHITECTURE の `motion` の基準、K10-6)、軌跡の置き場と分け方(K10-7)、形状の正本のフォルダ(K11)は書かず、是正もしない** — どれも段 6 で扱う。軌跡は、`physics/` の文面を R1 に揃えるときに暫定で認める。

**段の後、他の作業へ効くこと**: 段 5 までに書いた規則を、main のコードが本当に満たしている。モデル層の欄は宣言したクラスだけが書き、外からは命令で変える(`tsc` が止める)。`physics/` が持つ可変な値は、意味を持たない容器のインスタンスと、暫定で認めた軌跡の記録(段 6 で外す)だけになる(定数と純関数はそのまま)。装置は単発の命令を公開しない。

**片づける範囲の基準**: **段 5 までに書いた規則(R1〜R5・R7・R8・R11〜R13 と、それより前からある 1.x・2.x)に main が反しているものは、全部この段で直す。** 後の段へ残してよいのは次の2つだけで、**黙って後の段へ回さない。**

- 後の段で書く規則(R6・R9・R10 の全文・`hud/` を装置とする)の作業で、その段が対象ごと作り直すもの。書いた規則の未達ではない。
- 書いた規則に反したまま残すと決めたもの。`(暫定 — 段 N で外す)` で規則を狭めて書く。

**段 5 までの残件の振り分け**(5.5-3 の洗い出しで行を足す)

| 残件 | どこで | 理由 |
| --- | --- | --- |
| R3: `DynamicMotion` への代入(推力・トルク・質量・`alive`・`state`)、呼び手による `invalidatePrediction`、`DynamicMotion` の派生の接触代理 | 5.5-4 | K10 |
| R1: `physics/dynamic-trajectory.ts` が可変な正本を持ち、積分も呼ぶ。R3: 運動が可変な軌跡(`actual`・`predicted`)を読み手へそのまま渡す | 段 6(着手前に K10-6 と一緒に設計を決める)。5.5-1 で暫定を置く | K10-7。行き先と分け方は書いた規則から決まらず、実体・運動・軌跡の関係の設計次第で変わりうる。軌跡を読む View の入力は 6-3・6-4 で作り直す |
| 1.6: `PilotCommandFrame` が値を寄せ集めるだけの型になっている(K8 の別件。以前は 6-3) | 5.5-4 | CODING-RULE 1.6「情報をまとめるためだけの型を作らない」。値を明示的な引数で渡す。**協力者(`activeStage`・`stageRules`・`celestialBodies`)を構築時に受ける形へ移すことはしない** — R13 は「構築時に受けて持つ」と「呼ぶたびに受ける」の併用を禁じるだけで、どちらにするかは決めない。同じ `activeStage` を反応も呼ぶたびに受けているので、移すなら反応の経路ごと、段 6 の着手前に実体と運動の繋ぎ方(K10-6)として決める |
| R3: 残りのモデル層の可変な public 欄と `set`(`WeaponState`・`Part`・`fireEnabled`・`planExecution` ほか) | 5.5-5 | K10 |
| R7: `WorldSfx` の単発音、`Bgm` の音量と試聴 | 5.5-6 | K10 |
| R8: `SimSpeedManager` がキーコードを比べる(以前は 8-6) | 5.5-7 | 生の入力の解釈がモデル層にある |
| R12: `Stage.onDecided` を構築後に差す(以前は 8-6)、`DynamicSystem.deserialize` の流し込み、`SERIALIZATION_VERSION` の置き場 | 5.5-7 | K10-9 |
| R5: `simTime` の二重経路、`frame-controls` の `lastTime`、`plan-editor` の `simTime`(K6 の保留 2。以前は 8-6) | 5.5-7 | 同じフレームに引数で来る値を写して持っている |
| 1.11: `ProteinEnemy.muzzlePosition`・`BoosterStack.step` ほか燃料の関数が、状態を進めて値を返す。`DynamicSystem.update` が閉包を注入される | 5.5-8 | 段 5 の未割り当て |
| 1.6: 敵の `waveId` などの不在を `undefined` で表す(記録では `null`) | 5.5-8 | 「不在は `T \| null`」 |
| 1.2: `focus-camera-selection.ts` が 563 行 | 5.5-8 | 原因を診断してから割る |
| 段 5 が上げた版の扱い: セーブデータ画面が版 3 の手動セーブを読み込めるものとして並べ、ダブルクリックすると黙って新しいランが始まる | 5.5-8 | 段 5 が持ち込んだ振る舞い |
| R4: タンパク質の規則が、表示中に揺らいだ部位を読む(監査報告の 4) | 5.5-9 | R4 は段 4 で書いた。規則は静止構造を読み、揺らぎは表示だけにする(K10-11) |
| 実体の幾何の置き場が5通りに割れ、手で写した数値(金属敵の半径・薬莢の円柱・ブースターの接触球)が重複している(監査報告の 5) | 段 6(6-6) | 重複は 1.6 の「定数は概念の所有者が持つ」に反するが、段 6 で K11 の規則を書いて対象ごと置き直すので、後の段が作り直すものに当たる。流儀は Y(K11)、入れてよい条件は未決(K11-2)。5.5-1 の `physics/` の書き直しは定数を締め出さないので、`physics/` の形状の定数はこの段の未達にならない(K11-5) |
| 実体と運動の境界(K10-6)。案の基準では `PlayerMotion.power`・接続中ブースターの段が運動の側にある | 段 6 の着手前に決める | ユーザー判断 2026-09-19。基準をまだ規則に書いていないので、書いた規則の未達ではない。是正をどの手順で行うかも、決めるときに決める |
| `player.ts` が約 590 行 | 段 6(6-3〜6-5) | 原因は表示物の同居で、段 6 が対象ごと解く(1.2 は原因の診断を求めており、診断は済) |
| `MapView` の 25 引数・`ObjectWindows` の 19 引数 | 段 6(6-4) | 実体ごとの表示物が表示担当へ移ると、引数ごと変わる |
| 一覧の行の開閉の二流儀、`Button` が現在値を持たない(K6 の保留 1) | 段 6(6-4)・段 8 | 行は 6-4 で表示担当へ移る。`Button` は `hud/` を装置とする段 8 の規則の作業 |
| 未配線の足場(`dynamic-presenter.ts`・`entity-lifecycle.ts`・`entity-inspection.ts`。監査報告の 2・3) | 段 6(6-3・6-4) | R6 の表示担当と同じ問いへの答えなので、R6 と一緒に決める |
| `Launcher` の中継の引数、`main.ts` のラン外の入力ポートの表、launcher が読む面、計測と読み込みの進捗 | 段 8(8-6) | R10 の全文(段 8)の作業 |
| GAME.md §7 に対して、基地の判定形状が p90 で 16 m 浮いている | 計画の外 | SPEC はコードより先行してよい(未実装は正常)。規則の未達ではない |
| タンパク質の見た目と、判定・発射が一致しない(PR #82 の動機)。判定の球が粗く、揺らいだ主鎖が判定形状から最大 10 m はみ出す | 計画の外(`fix_protain_combat.md`、独立の PR) | 主要因が揺らぎか、判定形状の近似か、潜在バグかが未評価で、原因の調査から要る。規則の未達ではない(5.5-9 の後、規則は View を読まない) |
| **以下は 5.5-3 の洗い出しで足した行**(規則ごとに配って `5f87b1790` で洗った。擬陽性は K6 に足した) | | |
| R3・R4: `PlanPath.update`(表示の導出の材料作り)が、操作艦の予測弧の極値列を `arc.apsides.dropBefore` で切り詰める。戦闘ビューでは走らないので、ビューがモデル層のキャッシュを変える | 5.5-9 | 所有者の外からの書き込み。極値は「時刻 t より後の最初の極値」を問う読み取りにし、列を切り詰めない |
| R3・1.7: `Plan` がノード列をその場で書き換えながら、同じ配列を `nodes`・`displayData()` で外へ渡す | 5.5-9 | 可変オブジェクトを外へ渡している。変更のたびに新しい値へ差し替える |
| 置き場: `plan/plan-path.ts` は `PlanDisplay` が作って持つ作り直せる表示(`PlanPathView` を持つ)で、モデル層ではない | 5.5-9 | `MISPLACED_PRESENTATION_FILES` へ足す(R3 の判定から外れる)。`PlanPath` を `PlanDisplay` の部品のまま他へ配っている件(R13)も同じ手順で、根が作る形にする |
| R3: 表示の導出が、モデル層の所有者を具象のまま受ける(`Game` の public な欄、`ControlSelection`・`SimSpeedManager`・`Stage`・`Player`・`Plan` ほか約 30 か所)。実際の書き込みは列を通るか、既知のものだけ | 段 8(8-3)。**5.5 で暫定を置く** | 読み取り面の型をどこに置き、`Game` が何を公開するかは、表示の導出を `presentation/` へ出すときの境界の設計そのもの。5.5-5 の後は可変な欄が無くなり、残るのは命令を直に呼べる形であることだけ |
| R8: 入力の解釈が dt を受け、押し続ける入力の量を積む(視点のキーの回転・パン `camera-operator.ts:37-46`、Δv の長押しとラッチ `plan-axis-drag.ts:100-115`、長押しの時間 `dvHoldTime += dt`) | 段 8。**5.5 で暫定を置く** | 直し方に設計の選択が残る(操作量として進行へ渡し視点が dt で積むか、命令が dt を受けるか。Δv の編集は選択中ノードという表示の導出の状態に依る) |
| R8: 進行の中で実体を作る・捨てるとき、その View を作り・捨てる(`dynamic-system.ts` の実体化と `prune`) | 段 6(6-3) | 実体が View を持つあいだの帰結で、6-3 が View を表示担当へ移すと消える(後の段が対象ごと作り直す) |
| R8: 列の中で走るメニューの操作が、表示の導出の処理を走らせる(複製で配置パネルを開く、軌道点のワープ・ノード追加で案内と効果音) | 段 6(6-4) | メニューを表示担当へ移し、命令を進行の語彙にする 6-4 が作り直す |
| R5: 前フレームの記憶(被選択物の2フレームの猶予)が視点を変える。`CameraRig.missingFocusFrames` → `lostFocus` → 注視を原点へ戻す、`FrameAnchors` の役割の猶予 → `dropStaleRotatingFrame` の命令。視点の側にも `staleFollowFrames` がある | 段 8。**5.5 で暫定を置く** | 猶予は SPEC(MAP.md)が被選択物に定める表示の挙動で、視点の判断へ効かせるなら視点へ置く。どの猶予をどこへ置くかは設計の選択 |
| R7: 表示の導出が、装置 `CameraView.sync` の出力 `CameraFrame` の投影を読んで、マーカー・線の宣言を作る(K4 の「投影は `camera-system` が作る」は古い) | 段 8。**5.5 で暫定を置く** | 投影は `math/projection` の純関数なので表示の導出でも作れるが、描画原点を含む `CameraFrame` の置き場と `render/` のカメラの境界の設計が要る |
| R13: 同じ協力者を「持つ」と「呼ぶたびに受ける」の両方で受ける — `Player` は `registry` を持ちながら `handleCommand(command, registry)` と `DynamicReactionServices.registry` でも受ける(`Base` は持たない)。`DynamicReactionServices` は束の型 | 段 6(着手前に K10-6 で決める)。**5.5 で暫定を置く** | 実体が協力者を構築時に受けるか呼ぶたびに受けるかは、`PilotCommandFrame` の協力者と一緒に K10-6 で決める(振り分けの表の既存の行と同じ理由) |
| R13: `GamePresentation` がマーカー群を作って表示物へ渡し、受けた側が破棄する(`player-markers`・`equator-node-manager`・`celestial-markers`・`nav-target-presenter`・`plan-display`・`plan-guide`) | 5.5-9 | 受けた側が装置からマーカー群を作る(`Targeter` と同じ形) |
| R13: `CombatView`・`MapView` を `GamePresentation` が作り、`ViewManager` が破棄する | 5.5-9 | 作る者と捨てる者を揃える |
| R13: `DynamicSystem` が持っている天体系の面(`atmosphereMotions`)を、自分の部品 `Simulator` から `cleanup` の引数でも受け、`checkLoss` へ中継する | 5.5-4 | `Simulator` と接触の面を触る手順で、`DynamicSystem` が自分で読む形にする |
| 1.11: `WaveAttack.update` が、同じステージを `activeStage` と閉包 `addEnemy` の2つで受ける | 5.5-9 | ステージを1つの面で受ける |
| 1.6: `EquatorNodeInputs`(`{ displayTime, celestialBodies, frameAnchors, paths }`)を毎フレーム組む | 段 6(6-4) | 赤道交点のマーカーは 6-4 で表示担当へ移る(K1-4 の id の対応表) |
| 1.6: `StageDeps` の束を基底のコンストラクタへ1つの値のまま運ぶ | 段 6(6-3・6-5) | 束の `scene`・`HudLayers` を 6-3・6-5 が外すので、そのとき作り直す |
| 1.6: 名前の禁止 — `DynamicMotion` のコンストラクタ引数 `options`、`shipMotionOptions`、破片の `options` | 5.5-4 | 型 `DynamicMotionProperties` は「物性と初期値」で名前が中身を言っている。引数と関数の名前を揃える |
| 6-4 へ: `HudPanelPresenter` がモデルの木を平らに展開して受ける | 段 6(6-4) | `MapView`・`ObjectWindows` と同じく、受け手の引数を見直す手順で扱う |
| R11: 実体の生死 `alive` を記録するのは敵だけ。一時停止中にメニューから消した補給を保存すると、生きて戻る | 5.5-9 | 実体の共通の記録へ省略可能な `alive` を足し、構築の引数で受ける(版は据え置き) |
| R11: `WeaponState.pendingBarrelJoules` を直列化していない | 5.5-5 | `WeaponState` を命令にする手順で、省略可能な項目として足す |
| R11: キャッシュなのにそうと書いていない — `DynamicSystem.capsUncheckedSinceAdd`・`_collectionRevision`、`Simulator.lastSimDt`・`consecutiveZeroSteps`、フレームごとの計数(`Simulator`・接触・`Predictor`・`ArcCelestialBodies`)、`NanWatchdog.tripped`、接触代理の `prevState` | 5.5-4・5.5-5・5.5-9 | 触る手順で、所有者のコメントにキャッシュであることを書く |
| R11: `RunEventLog` の通し番号と直近の出来事を保存しない | 例外(`例外(ARCHITECTURE R11)` のコメント) | 出来事は進行の位相の先頭で空にする1フレームの通り道で、読み手もランと一緒に作り直す。保存すると、読み込んだフレームに前のランの音と通知が出る |
| R12: `EntityIdAllocators.deserialize` の後で `reserve()` を流し込む(`dynamic-system.ts:118-121`、`attached-boosters.ts:48`) | 5.5-7 | `DynamicSystem.deserialize` の流し込みと同じ手順で、予約する id を材料として組む前に渡す |
| R12: コンストラクタが採番器と省略可能な id を受け、`next(id)` で新規と復元を分ける(`Bullet`・`DebrisPiece`・`Enemy`・`Pickup`・`DetachedBooster`) | 5.5-9 | id は `create`・`deserialize` が採番し、コンストラクタは採番済みの id を受ける(`Base`・`Player` と同じ形) |
| R12: 子の `deserialize` が無い・親が子の値を書く — `BulletReaction`(親が記録を分解して読む)、`ViewSelection`・`OrbitGuideSelection`・`OrbitReferenceSelection`(`Viewer` が読み取り面から書く)、`PartDamageModel`(持ち主が部品を書く) | 5.5-9(部品は 5.5-5) | 所有者が `serialize`・`deserialize` を持つ |
| R12: 保存の形式が2つの所有者の値を1つの記録に混ぜる — 実体の記録に運動の値(状態・姿勢・熱・生死)が平らに入る、`DynamicSystem` の記録に `Simulator` の `simTime` が平らに入る、カメラの記録の `offset`・`rotatingWith` が `CameraOrientation` と自分の値を合成する | 例外(`例外(ARCHITECTURE R12)` のコメント)。**同じ規則に例外が3つ重なるので、報告で再設計(版を上げて形式を直すか)を提案する** | 直すのは形式のほうだが、段 5 の版の記録を読めることが段 5.5 以降の検証の基準で、版は段 5 の1回だけ上げると決めてある(K9)。実体と運動の境界は K10-6 で決まる |
| R12・1.11: `{} as SerializedStage`(`game.ts:135`)で欠けた記録を補う | 5.5-9 | 型アサーションで検査を黙らせている。欠けは `undefined` のまま渡す |
| R12: `PredictPanelSelection.create` が組んだ後に `followCameraFocus()` で座標系を流し込む | 5.5-9 | 初期値をコンストラクタへ渡す |
| R12: `DeployablePanelState.deserialize` が欠けた展開度を 0(収納)で補う。太陽電池の新規の初期値は展開 | 5.5-9 | 補う値を持ち主の初期値に揃える(初期値はコンストラクタの既定引数に1か所) |
| 2.2: `run/` が `AutoSave` 型と `autoSave` という名前を使う(セーブの語は `launcher/` と SPEC だけ) | 5.5-9 | `run/` は、位相 2 と 3 の間に直列化の読み口を受け取る者を自分の語彙で受ける |
| R11・R6: 決着の結果 `Stage._result`(文面)とブリーフィング `_briefing` を保存しない。ブリーフィングは構築後に流し込む | 段 6(6-5) | 結果とブリーフィングの文面は表示の導出(K2)で、6-5 がステージから表示を外すときに、文面の材料(決着の時刻・戦果)を保存する形へ作り直す |
| R11: タンパク質の揺らぎの鍵 `motionSeed` を新規と復元で別の値から導く | 段 6(6-3) | 表示だけが読む値で、View と一緒に表示担当へ移る |
| R7: 単発の命令 — `UiSfx.warp()`(出来事から、と `plan-editor` から直に)、`Bgm.seekAudition`。装置の出力 `Bgm.auditionElapsedSec` を HUD が表示に読む | 5.5-6 | 同じ手順で宣言にする。試聴の経過は、設定画面が宣言した開始時刻と実時刻から求める |
| R5・R10: 消音前の音量 `PauseMenu.lastVol` が表示の導出にしか無い | 5.5-6 | 設定に音量と消音を分けて持つ |
| R7: `FlashKind` の値が出来事の語彙(`bulletImpact`・`destroy1` ほか)で、照準ズームの意味(`dimsInGunsight`)も装置が解釈する | 5.5-9 | 装置は見え方の語彙で受け、出来事からの写しは `flash-presenter` が持つ |
| R7: `CameraFrame` がビュー `mode: ViewMode` を運ぶ(装置の中では誰も読まない) | 5.5-9 | ビューは表示の導出が自分で持って配る |
| R7: `CameraFrame.zoomed`・`PlayerRenderSource.active` を View が解釈して噴射・炎・自機を隠す | 段 6(6-3) | 1体ぶんの View の入力を表示担当が作り直す |
| R7: `InstancedPools` の begin/end と push の即時モードを表示の導出へ公開し、束をモデル層の `DynamicSystem` が組む | 段 6(6-3) | 同上(落とし穴の表の「InstancedPools の begin/end と順序」) |
| R7: タンパク質の `siteMarkers()`(装置の出力)をマーカーの位置に読み、入力 `ProteinSiteStatus` に戦闘の状態が入る | 段 6(6-3・6-6) | 表示担当と部位の変換の分割(K11-6)で作り直す |
| R7・R9: `ProteinPhase` を `render/` が定義しモデル層が import する。`Stage0MetalEnemyView`・`ScaleGridVisibility` の名前にステージ・天体の意味 | 段 8(8-5) | 型の置き場を所有者へ揃える手順 |
| R7・R5: 天体の装置 — `GeostationaryOverlay` の文面、`CelestialIllumination.fixedBrightnessScale` の読み、`bakeClouds` の順序依存、`PointFieldView` の巡回更新(表示時刻が混ざるキャッシュ)、参照軌道線の色を天体の種別で分ける | 段 7(7-3) | 天体の見た目と UI を分ける手順が作り直す |
| R5: `MarkerGroup` が宣言の `cls` を作るときに1度しか貼らない(操作対象を替えても自艦・僚艦の塗り分けが古いまま) | 5.5-9 | 結果に効く入力をキーにする |
| R5: `TrajectoryLine` の焼き直しの鍵に、役割トークンの解決先が入っていない | 段 6(6-4) | 軌道線を表示担当へ移す手順で入力を作り直す |
| R5・R1: 破片の形と暗さを View の構築時に乱数で決める(復元で変わる) | 段 6(6-3) | 表示担当が id から決める形へ作り直す |
| R5: タンパク質の LOD のフェードを表示時刻で測る | 段 6(6-3) | 同上 |
| R5: 表示の不具合 — 窓の行の差分キャッシュの鍵に見出しが無い(`property-window-rows.ts:38`)、天体ラベルの `labelStateOf` が今フレーム出ていないラベルの古い値を返す、相対交点の `update` がマップでしか走らないのに `sync` は両ビューで走る | 5.5-9 | キャッシュの鍵と同期の条件を直す |
| R5・R10: 創作パネルの形態ごとの着色の記憶(視点)、コンパクト表示と敵の種類タブ(設定) | 段 6(6-5) | 創作の UI をステージから外す手順 |
| R5: 物体一覧の順序・行が `viewer`・`displayTime` を引数から写して持ち、呼ぶ順に依る。区画の開閉を構築時に1度だけ設定から写す | 段 6(6-4) | 一覧の行の開閉の二流儀(既存の行)と一緒に扱う |
| R5・R13: ページ寿命のパネルが、ラン寿命の命令の口を毎フレーム受ける(`burnHandlers` ほか) | 段 8(8-6) | `Hud`(ページの寿命)と game 用パネル(ランの寿命)を分ける作業(K2) |
| R7: 列の中で `openObjectPlacer` を直に呼ぶ、`ObjectPlacement` が DOM パネルを持つ、`WaveAttack` が `THREE.Color` を使う | 段 6(6-2・6-5) | ステージと creative から表示を外す手順と、モデル層の `three` の判定 |
| 計測: `FrameSections.enabled`・`GpuTimings.enabled` を HUD が代入で切り替える | 段 8(8-6) | 計測の置き場を決める手順 |
| R1・1.11: 時刻層の天体の運動を構築後に結び直す・積む(`CelestialMotion.bind*`、`addPlanetSystem`・`addSatellite`・`setBody`)。メモ化の鍵が結び直しを含まず、答えが呼ぶ順に依る | 段 7(7-4) | 天体系を構築値から組む手順が、構築の形ごと作り直す |
| R1: `ApsisTrack`(`physics/trajectory-features.ts`)が観測の履歴で極値を溜める記録 | 段 6。5.5 で軌跡の暫定に含めた | 予測の弧の記録の一部で、軌跡の置き場(K10-7)と一緒に決める |
| 1.6: 自機の既定部品の推力・トルクが自機の定数の手写し(`ship-default-parts.ts` の `SHIP_MASS`・`DEFAULT_TORQUE`・`THRUST_LEVEL_MAX`)、`MUZZLE_SPEED` が2か所、`BASE_TORQUE` が手計算、`WeaponState` の砲身の初期温度が `ENV_TEMP` の手写し、軌道ガイドの系の一覧 `ALL_SYSTEMS` が2か所 | 5.5-9(砲身の温度は 5.5-5) | 値を変えずに所有者から引く。既定部品は敵と共有する汎用の構成なので、自機の性能は自機の側から渡す |
| 1.6: 抗力係数 2.2 の手写しと式の重複(`player-motion.ts:145`) | 5.5-4 | 運動を触る手順で、`dynamic-motion.ts` の定数と式を使う |
| 1.6・K11: 形状と焼き込みアセットの手写し — 段の寸法とノズル出口、爆砕ボルトのずれ `+0.08`、排出ポート `v3(-0.9,0,0)`、砲身の放出位置、太陽電池の面積 `7.2`、噴射・再突入の効果の位置、タンパク質のリボンの断面、薬莢の縮尺の2段、model-builder が写した弾数・補給の弾倉数 | 段 6(6-6) | 形状の正本を分ける手順(K11-5)。入れてよい条件は K11-2 で決める |
| 1.6: 天体の定義の `radius` が `shape` の手写し(約 25 件) | 段 7(7-4) | 天体の定義と構築値の手順 |
| 1.6: 天体のテクスチャから手で測った `albedoScale`・`averageHue` | 段 7(7-3) | 天体の見た目の手順。測り直す運用はコメントにある |
| 物理の値の食い違い: `SHIP_BCINV`(質量 8t の前提)と `SHIP_SRP_COEFF`(1t の前提)を `player-motion` が両方 1t の値として扱う、`BULLET_BCINV` が弾の半径・質量から出る値と約 140 倍違う、model-builder の `booster.mjs` の円環の向き | 計画の外 | 物理の値と見た目の判断(CODING-RULE 1.1 はユーザーに問うとする)。規則の未達ではない |

#### 手順 5.5-1. 規則を直す — 済

ARCHITECTURE の R3(所有者・口・整合性・可変な public 欄)、層の中身の `physics/` と軌跡の暫定、CODING-RULE 1.11 を書いた。`rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` は 5。**`physics/` の文面には、計画の「定数・純関数と、意味を持たない容器」に「結果を変えないメモ化キャッシュ(R1)」を足した** — R1 が時刻層に許しているもので、`CelestialMotion`・`PlanetSystem`・`TimeRing` が持つ。締め出すと、R1 に適う値が層の中身の文面の未達になる。

#### 手順 5.5-2. 検査を直す — 済

`tools/check-boundaries.mjs` に構文木の判定2つ(`SYNTAX_RULES`)を足した。「モデル層の可変な公開欄の禁止」(R3。class の可変な public 欄・コンストラクタ引数の欄・`set` アクセサ、export する interface と型の可変な欄・索引・mapped type)と、「直列化された形をコンストラクタで受ける禁止」(R12。コンストラクタ引数の型に `Serialized*` が現れるか。名前の正規表現の行と置き換えた)。「命令 API の禁止」の行は外した。「二段初期化」「復元の流し込み」は代用のまま残し、その旨を行のコメントに書いた。

- **判定の範囲**: `MODEL_ROOTS` から `MISPLACED_PRESENTATION_FILES` を除いたもの。`MODEL_ROOTS` へ、進行と視点の両方が使う `command-queue.ts`・`run-events.ts` と、`control-selection-commands.ts` を足した(当たりは無かった)。
- **着手時の当たり**(許可リストに載せた): R3 が 84 件(計画の見積り 65 件より多いのは、`EllipticPlacementInput` の7欄と `Part` 系の欄を数えたため)、R12 が1件(`NavTargetSelection.constructor(target)`)。
- **R12 の1件は 5.5-7 で直す。** `SerializedNavTargetSelection` は不変な素の値 `{ id, name }` で、選択の状態そのものをそのまま直列化の形に使っている。R12 はこの場合 `SerializedT` を別に作らないとしているので、型を状態の名前(航法ターゲット)で呼び直す。
- 自己検証: `public x = 0`・`set x`(可視性を問わない)・export する interface の `x: number`・export する型の union の中の型リテラル・コンストラクタの `public y`・`Serialized*` の引数で 1、`private x = 0`・`readonly x`・export しない interface で 0 を返した。

#### 手順 5.5-3. 段 5 までの規則を洗い出す — 済

R1(と 1.6 の手写し)・R3・R5(表示の導出)・R5 と R7(装置)・R8・R11 と R12・R13 を、読むだけのサブエージェント 7 体へ規則ごとに配って洗った(`5f87b1790`)。R4 は判定だけで洗い出す列が無く、R2 は段 6 から。結果は振り分けの表の「以下は 5.5-3 の洗い出しで足した行」から下の 60 行と、K6 の擬陽性の節にある。

- **直さずに残すと決めたもの**は、規則の文面を暫定で狭めた(`rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` は 10)。
  - R3: 表示の導出がモデル層の所有者を具象のまま受ける(段 8)
  - R5: 被選択物の2フレームの猶予を通した解決から視点を動かす(段 8)
  - R7: カメラの投影を `render/` が返し、表示の導出が読む(段 8)
  - R8: 入力の解釈が押し続ける入力を実時間 dt で積む(段 8)
  - R13: 実体が同じ協力者を持ちながら呼ぶたびにも受ける(段 6)
  - 軌跡の暫定に `ApsisTrack` を含めた
- **保存の形式が2つの所有者の値を混ぜているもの(R12)と `RunEventLog`(R11)は例外にする。** 違反箇所に `例外(ARCHITECTURE Rn):` のコメントを書く(5.5-9)。R12 に例外が3つ重なるので、段 5.5 の PR 本文と報告で、版を上げて形式を直すかをユーザーに提案する。
- 後の段へ回した行は、その段の手順が対象ごと作り直すもの。各段の手順の「5.5-3 から回した行」を見る。
- 計画の外へ出したのは、物理の値の食い違い(`SHIP_BCINV` と `SHIP_SRP_COEFF` の質量の前提、`BULLET_BCINV`)と model-builder の円環の向き。物理の値の判断はユーザーに問う(CODING-RULE 1.1)。

#### 手順 5.5-4. 運動の値を運動だけが書く — 済

`DynamicMotion` の欄を private と getter にし、命令 `setThrust`・`setTorque`・`kill`・`reset` と、継承先が構成を変えたときの `setMassProperties`(protected)を置いた。`invalidatePrediction` は private。参加者の面は `state`・`alive` を読み取りにして `reset`・`kill` を足し、接触の相手の型を `EntityContactParticipant` へ広げた(`contactKind`・`contactDamageWeight` を足した)。接触代理は `game/dynamic/contact-proxy.ts` の `ContactProxy`(履歴を持たない)。`PilotCommandFrame` は消し、`updateControls` は明示的な引数で受ける。R3 の許可リストから 17 件が消えた(残り 68)。

**実施で決めたこと**
- 分離ブースターの推力は、`updateCommands`(燃焼を進める命令)と `commandedThrust`(直近の燃焼から推力を答える問い合わせ)に分けた。計画の「updateCommands が値を返す」は、状態を進めて値を返す関数(CODING-RULE 1.11)になるので採らなかった。問い合わせは命令の直後に同じ姿勢で呼ぶので、推力の向きは変わらない。質量は behavior の `mass` から引く。
- 予測弧を捨てる条件: 推力は非 null のとき(いまの setter と同じ)。質量と慣性は変わったときだけ(`AttachedBoosterMotion.step` は燃えていないフレームも呼ぶので、無条件に捨てると弧をなぞれなくなる)。点火の切り替えでは捨てない — 点火しても質量は変わらず、燃え始めれば推力で捨てる。
- `DynamicMotion.attachedTo` は常に null の読み取り欄。
- 5.5-3 から回した行(`cleanup` の中継、`options` の名前、抗力係数の手写し、接触代理の `prevState` のキャッシュのコメント)もここで直した。
- 検証: `npm run typecheck`、テストの型検査、`npm run test:game`(255/255)、`npm run check:boundaries`、変更ファイルの lint。**計画の `npm run dev` での目視(噴射・段の分離・被弾ほか)はしていない** — 段の終わりの 5.5-10 でヘッドレスの起動と、ユーザーの目視に回す。

#### 手順 5.5-5. 残りのモデル層の欄を、所有者だけが書く形にする — 済

R3 の判定の許可リストに残るのは `Stage.onDecided`(5.5-7)だけになった(R12 の `NavTargetSelection` は 5.5-7)。

**実施で決めたこと**
- `WeaponState` は遷移を命令として持つ形にした(トリガー・空撃ちの記録・クールダウン・砲身の熱の蓄えと温度の歩み・砲身の交換)。遷移はどれも武装の状態だけに閉じ、`FireControl` は出来事の記録と実体の生成だけを持つ。砲身の熱の蓄え `pendingBarrelJoules` を直列化へ足し、初期温度は `ENV_TEMP` から引く。
- 部品 `Part` の欄は読み取りにし、HP と燃料を書くのは部品を積む `PartInventory`(構築時に受けた部品の写しを持つ)の命令にした。部品の直列化は `PartDamageModel` が持つ。
- 敵の射撃の可否は、ステージの読み取り `Stage.enemiesMayFire`(デバッグステージはトグルの値)を `DynamicSystem.update` の引数で射撃の判断へ届ける。
- `ObjectPlacement` は、持ち主の命令の口を受ける部品として `CreativeStage` のコンストラクタが組む(R13)。`create`・`deserialize` は消えた。
- `plan/plan-path.ts` を `MISPLACED_PRESENTATION_FILES` へ足した(`PlanDisplay` が作って持つ作り直せる表示)。振り分けの表では 5.5-9 としていた行の一部。
- 検証: `npm run typecheck`、テストの型検査、`npm run test:game`(255/255)、`npm run check:boundaries`、変更ファイルの lint。`npm run dev` での目視は 5.5-10 へ回す。

#### 手順 5.5-6. 音の装置を宣言だけにする(R7)— 済

`WorldSfx.sync({ loops, cues })`・`UiSfx.sync(cues)`・`Bgm.sync(BgmDeclaration)` だけが公開の口になった。一回きりの音は `SoundCue`(`audio/sfx/sound-cue.ts`、id と音の種類)の列で受け、装置は鳴らした最新の id より大きいものだけを鳴らす。`Bgm` の `setVolume`・`syncRun`・`beginAudition`・`playAudition`・`seekAudition`・`auditionElapsedSec`・`endAudition` は消えた。

**実施で決めたこと**
- ゲーム世界の効果音の id は出来事の通し番号。出来事から音への写像は `run-event-presenter.ts` の `worldSoundCues`(距離の減衰の値はここで付ける)。UI の効果音(`warp`)は `game/ui-sound-queue.ts` の `UiSoundQueue` が溜めて番号を振り、`GamePresentation.sync` が装置へ渡して空にする。`plan-editor` と出来事の写しはどちらもそこへ積む。
- BGM の宣言は `main.ts` のフレームで毎回組む — 音量(消音を織り込む)、ランの進行(`run.game.activeStage.isPlaying`)、試聴の期間(設定面が開いているか)、試聴の曲。`syncRun` を遷移点で呼ぶ形はこの宣言へまとめたので、`Launcher` は `Bgm` を持たない。決着・周回の開始の反映は最大1フレーム遅れる。
- 最初のタイトル画面でも試聴を鳴らすため、フレームのループは `launcher.start()` を待つ前に回し始める(タイトルへ戻った後と同じ状態)。
- 試聴の選択(曲・選曲の回・シークの回と位置)は `BgmSettingsPanel` が操作途中の状態として持ち、`audition` で宣言する。経過の表示は、選曲・シークの後の最初のフレームの実時刻を起点に求める。
- 消音は `UserSettings.bgmMuted`(鍵 `tepui.settings.bgm_muted`)に音量と分けて持つ。スライダーは消音を織り込んだ音量を見せ、動かせば消音を解く。**変わった挙動**: 消音の前の音量が再読み込みを跨いで残る。手で 0 にした音量から消音ボタンで戻すと、前に消音した音量ではなく既定の 1 になる。
- 検証: `npm run typecheck`、テストの型検査、`npm run test:settings`(6/6)、`npm run test:game`(255/255)、`npm run check:boundaries`、変更ファイルの lint。`npm run dev` での聴き取りは 5.5-10 へ回す。

#### 手順 5.5-7. 位相・直列化・導出の残りを片づける(R8・R12・R5)— 済

境界の検査の許可リストが空になった(`Stage.onDecided` と `NavTargetSelection.constructor(target)` が最後の2件)。

**実施で決めたこと**
- 時間加速の段の上下は `SimSpeedManager.shift(step: -1 | 1)`(命令の口は `SimSpeedCommands.shift`)。キーから段の向きへの解釈は `game-input-ports.ts` が持つ。
- 決着は `Stage.decide` が出来事 `stageDecided` を記録する。`Launcher.followProgress()` を `main.ts` が `Run.frame` を回し終えたフレームごとに呼び、出来事があればクリアを数えて結果画面を出す。BGM は 5.5-6 でフレームごとの宣言にしたので、ここでは触らない。
- `DynamicSystem` はコンストラクタで記録の列を受け、その場で実体化する(揃わないものは待ち行列へ)。`spawnWhenReady` はステージが遊びの最中に敵を足す命令として残る。
- **採番器の予約は、組む前へ移すのではなく消した。** 記録にある id(顔ぶれの個体も、接続中のブースターの段も)はどれも同じ記録に保存した採番器が発番したもので、保存した「次の番号」はそれらを必ず追い越している。予約が守っていた場合は、この形式の記録からは起こらない。`EntityIdAllocators.reserve` は消え、`EntityIdAllocator.reserve` は `next(id)` の中だけで使う private になった。
- `SerializedNavTargetSelection` は `NavTarget` へ改名した(保存形式は同じ)。
- 版の刻印はモデル層から launcher へ移した。`SerializedGame` は `version` を持たず、launcher の `SavedGame`(`save-store.ts`、版 `SAVED_GAME_VERSION`)が版を添えた記録本体。版は `SnapshotService` が書くときに先頭へ添えるので、版の値と位置は変わらない。
- `simTime` の二重経路: `ObjectWindows.open`/`openEmptySpaceMenu`/`sync`、`MapPicking.handleRightClick`/`handleEmptySpaceRightClick`、`ViewFrame.handleCommand`/`handlePointer`、`PlanEditor.handleCommand`/`update` の時刻の引数を落とし、表示窓の所有者 `DisplayWindowManager.current` から読む。`PlanEditor` は写しの `simTime` 欄をやめて所有者から読む。`frame-controls` の `lastTime` は、この手順に着手した時点で既に無かった。
- 検証: `npm run typecheck`、テストの型検査、`npm run test:game`(255/255)、`npm run test:launcher`(2/2)、`npm run check:boundaries`(許可リスト 0 件)、変更ファイルの lint。`npm run dev` での目視(決着から結果画面、時間加速、マップの右クリック、段 5 のセーブの読み込み)は 5.5-10 へ回す。

#### 手順 5.5-8. 段 5 が残した 1.x の未達と、launcher の表示を片づける — 済

`ProteinEnemy.muzzlePosition` の分割は計画どおり 5.5-9 で行う。

**実施で決めたこと**
- 状態を進めて副産物を返す関数(CODING-RULE 1.11)を、命令と問い合わせに分けた。
  - `BoosterStack.step` → 問い合わせ `burnOver(dt)`(燃焼結果。型は `BoosterBurn`)と命令 `burn(dt)`。`toggleIgnition` は操作後の点火状態を返していたので void にし、`ignited` で読む(`AttachedBoosterMotion` も同じ)。
  - 燃料: `consumeFuel`・`refuelFuel` は void。賄えた割合は `throttle.ts` の `suppliedFuelRatio(ship.totalFuel, amount)` で消費の前に求め、補給で入った量は `logistics.ts` が補給の前に残りの容量から求める。
  - `WeaponState.beginShot` → 問い合わせ `nextShot(muzzleCount)` と命令 `fire(muzzleCount)`。`beginShot` は `left` が偽なら null を返していたので、消費の結果の `'empty'` は通らない値だった — 型から消した。計画の表には無かったが、同じ形なのでここで直した。
  - `manualReload(): boolean` は成否の真偽なので 1.11 の許容に入り、残した。
- `DynamicSystem.update` の閉包 `beforeControllables` をやめ、引数 `acceptsCommands`(決着前かつ操作できる倍率か。`Game` が求める)を受けて、自律の推力の後に操縦の命令を自分で適用する。順序は変えていない。
- 不在の `undefined` → `null`(1.6): 敵の `waveId`・`formationId`・`formationRole`(実体の欄と `EnemyPlacement`)、`isFormationEnergyAvailable`、`EnemyContact.waveId`、ウェーブ数の数え上げ。同じ形の欄を洗った結果、記録では null で持ち実体では undefined で持つものはこれだけだった。構築の省略可能な引数(省けば既定)と、`PartDamageModel` の部品参照(記録に出ない内部の参照)は形が違うので対象外。
- `focus-camera-selection.ts`(563 行)は**分けずに残す**。診断: 責務は「1台のカメラの視点と、その変更規則」の1つで、向きは既に `CameraOrientation` へ出ている。各命令は注視・座標系・向き・距離・ずらしをまとめて読み書きする同じ関心の実装で、`config.view` が切り替えるのは初期値と3つの方針だけ(多態で分ける種類ではない)。投影(画角・投影方式・正射影の半高さ)を切り出す案は、距離が視点の置き場(直列化では変位の長さ)と透視のズームを兼ね、下限が注視対象で決まるため、切り出すと各命令へ下限を渡す委譲が並び、行数はほとんど減らない。
- セーブデータ画面: `SnapshotService.isReadable(id)` が本体の形式の版を照合し、読めない手動セーブのカードは選べる見た目にせず、カードの上に理由を書く(タッチでも見える)。ダブルクリックすると理由を状態欄に出し、周回は始めない。カードの `onLoadSnapshot` は真偽の代わりに拒否の文面(読めるなら null)を渡す。
- 検証: `npm run typecheck`、テストの型検査、`npm run test:game`(255/255)、`npm run test:launcher`(2/2)、`npm run check:boundaries`、変更ファイルの lint。`npm run dev` での目視(ブースターの燃焼と分離、敵の射撃、敵の一覧、版の違う記録を置いたセーブデータ画面)は 5.5-10 へ回す。

#### 手順 5.5-9. 洗い出しで出た残りと、監査報告の 4 を片づける

**目的**: 5.5-3 の振り分けの表のうち、5.5-4〜5.5-8 に載らなかった行を直す。あわせて、監査報告の 4(K10-11 で決めた)を実施する。監査報告の 5(K11)は段 6 の 6-6 で行う。

**変えてよい挙動**: タンパク質の銃口と被弾部位の選択だけ。いまはズーム・時刻スライダー・揺らぎの表示設定で変わるが、静止構造から決まるようにする(K10-11)。部位マーカーと結合線は、これまでどおり表示中の揺らぎに追従する。

**変更が必要な箇所**

| 対象 | 何をするか |
| --- | --- |
| 5.5-3 で足した行(5.5-9 と書いた 22 行) | 振り分けの表のとおりに直す。群ごとに分けると次のとおり。<br>・計画の表示: `PlanPath.update` の `dropBefore` をやめ極値を時刻で問う、`Plan` のノード列を差し替えで持つ、`plan-path.ts` を `MISPLACED_PRESENTATION_FILES` へ足し `PlanPath` を根が作る<br>・表示の導出の組み立て(R13): マーカー群を受けた側が作る、`CombatView`・`MapView` の作る者と捨てる者を揃える<br>・直列化(R11・R12): 実体の共通の記録に `alive`、キャッシュのコメント、コンストラクタの `next(id)` を `create`・`deserialize` へ、子の `serialize`・`deserialize`(`BulletReaction`・視点の3つの選択)、`{} as SerializedStage`、`PredictPanelSelection.create`、`DeployablePanelState` の補い、`run/` の `AutoSave` の語、例外のコメント(R12 の形式3件・R11 の `RunEventLog`)<br>・装置の語彙(R7・R5): `FlashKind` を見え方の語彙へ、`CameraFrame.mode` を外す、`MarkerGroup` の `cls` の差分<br>・表示の不具合(R5): 窓の行の鍵、天体ラベルの `labelStateOf`、相対交点の `update`<br>・`WaveAttack` がステージを1つの面で受ける(1.11)<br>・定数の手写し(1.6): 自機の既定部品の性能を自機の側から渡す、`MUZZLE_SPEED`・`BASE_TORQUE`・`ALL_SYSTEMS` |
| `DEVELOP/SPEC/PROTEIN.md`、`DEVELOP/SPEC/RENDERING.md`(監査報告の 4) | **コードより先に `/modify-feature` で削り、`docs(spec):` の単独 commit にする。** 書き足すものは無く、INVARIANTS §3 は触らない。行番号は `5ef4194d3` のもの。<br>・実装上の近似(LOD): PROTEIN 58〜59、RENDERING 629〜631<br>・いまの挙動を固定している文: PROTEIN 60 の後半(発射位置も揺らぎに追従)、62〜63(マーカー・発射位置・命中部位で同じ時点の位置)、RENDERING 627〜628 の「攻撃の起点や活性部位のマーカーは見えているゆらぎに追従するが」<br>・INVARIANTS §3 を個別に言い直している文: PROTEIN 4 の後半、43〜44、66、67〜68 の後半、69〜70 の後半、RENDERING 626<br>どこまで削るかは `/modify-feature` の中でユーザーと決める |
| `src/game/dynamic/dynamic-entity/protein-enemy.ts`(監査報告の 4、R4) | 銃口(`muzzlePosition`。5.5-8 の命令と問い合わせの分割も一緒に)を、部位の静止座標を個体の位置・姿勢で変換して求める。被弾は、着弾点を模型座標へ戻して、部位の位置を渡さずに `applyDamage` を呼ぶ。`view.siteWorldPositionById`・`view.siteModelPositionById`・`view.localImpactPoint` を読まない。コメント「銃口は静止座標で取り…」は実装と一致するようになる |
| `src/game/protein/protein-combat-state.ts` | `applyDamage` と `closestSite` から引数 `sitePositions` を消す。静止位置で選ぶ分岐が唯一の経路になる |
| `src/render/dynamic/dynamic-entity/protein-enemy-view.ts`、`src/render/protein/protein-runtime.ts` | 規則のためだけの問い合わせ(`siteWorldPositionById`・`siteModelPositionById`・`localImpactPoint` と runtime の `siteModelPositionById`)を消す。`siteMarkers` は表示の導出が読むので残す |
| `src/physics/protein-site-geometry.ts` | 規則が要るのは静止位置の変換だけ、View が要るのは変換と変位の加算になる。**この段ではファイルを動かさない。** 規則も `physics/` の関数を読む(モデル層から時刻層への import は R2 に適う)。読み手の隣へ分けるのは段 6 の 6-6(K11-6) |
| `tests/game/protein-combat-state.test.ts` | 「表示中のアンカーで被弾部位を選ぶ」(`a displayed anchor should select the same site on impact`)を、静止位置で選ぶ検査に書き直す |

**達成条件と検証**

- 振り分けの表のすべての行が「直した」か「残す理由がある」になっている。
- `rg -n "view\.(siteWorldPositionById|siteModelPositionById|localImpactPoint)" src/game` が 0 件。`rg -n "dropBefore" src/game` が 0 件。`rg -n "例外\(ARCHITECTURE R1[12]\)" src` が 4 件(R12 の形式3件、R11 の `RunEventLog`)。
- 変更した層の回帰テスト(`npm run test:game`・`npm run test:render` ほか)、`npm run check:boundaries`。
- `npm run dev` で、タンパク質陣形の 5i4r が撃ち、撃たれて部位が壊れることを見る。部位マーカーが揺らぎに追従したまま、揺らぎの表示を切っても部位の HP の減り方が変わらない。

#### 手順 5.5-10. 段 5.5 を main へ送る

**手順と達成条件**: 「段を main へ送る(共通)」のとおり。PR 本文には、新しい規則は書いておらず、段 1〜5 の規則の言い直しと未達の是正の段であることを書く。書き直した規則(ARCHITECTURE の R3・層の中身の `physics/`、CODING-RULE 1.11)、足した判定と外した名前の列、洗い出しの結果(振り分けの表)も書く。暫定の数は 10 — 軌跡の暫定を1つ足した理由(K10-7: 行き先が実体・運動・軌跡の設計で変わりうるので、段 6 の着手前に決める)と、5.5-3 の洗い出しで置いた5つの理由(振り分けの表)。例外にしたもの(R12 の形式3件・R11 の `RunEventLog`)と、R12 に例外が重なるので版を上げて形式を直す再設計の提案も書く。**変えた挙動として、タンパク質の銃口と被弾部位を静止構造へ戻したことを書く**(K10-11)。PR #82 の意図を戻すことと、見た目との一致の調査を `fix_protain_combat.md` で別に行うことも書く。実体と運動の境界(K10-6)、軌跡の置き場(K10-7)、実体の形状の正本(K11)は段 6 で扱うので、この段では書いていないことも書く。


### 段 6 — 1体ぶんの表示物を表示担当へ移す

**この段で書く規則**: R6(対象は dynamic の族とステージ)、R2 のモデル層の項、`motion` の基準(K10-6)、軌跡の置き場(K10-7)、R2 の層の表の `entity-shapes/` とその入れてよい条件(K11)。

**着手の条件**: 段 5.5 が main に入っていること。加えて、次の2つをユーザーが決めてあること。
- **実体・運動・軌跡の関係**(K10-6・K10-7・未決の1)。決めた基準は 6-1 で書き、是正(軌跡を `physics/` から出すことを含む)はそのときに決めた手順へ足す(この段の見積りには、まだ入っていない)。
- **`entity-shapes/` の入れてよい条件**(K11-2 の A・B・C)。6-1 で書き、6-6 で移すものが決まる。

**段の後、他の作業へ効くこと**: 実体に表示物・装置・UI の語彙を持たせない。1体ぶんの表示物は表示担当のフィールドにし、生成と破棄は族ごとの突き合わせ役だけが行う。モデル層から `three` と装置を import しない。実体の形状(寸法・取付位置など)は `entity-shapes/` を正本にし、形を変えるときは `/modify-model-shape` を通す。

#### 手順 6-1. 段 6 の規則を書く

**5.5-3 から回した行**: R13 の暫定(実体が同じ協力者を2経路で受ける)を、K10-6 で決めた繋ぎ方で外す。

**目的**: 1体ぶんの表示物の持ち方と、モデル層の出ていく import を正本にする。コードは変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `DEVELOP/ARCHITECTURE.md` | R6 を新しい節として書く。`entity` / `motion` / `view` を R4・R6・R7 に合わせて書き換える(`entity` はモデル層の「動きとゲーム上の意味」の統合、表示物は表示担当が持つ)。`motion` には、段 6 の着手前に決めた実体と運動の境界(K10-6)と、軌跡の持ち方(K10-7)を書く。R3 の「実体は運動の欄へ代入しない」は 5.5-1 で書いてある(K10-1)。R2 に「モデル層が import してよいのは定義層・時刻層と自分自身だけ」を足す |
| `DEVELOP/CODING-RULE.md` | 1.6「配列に対して外部から対応付けを行う設計を避ける」のうち、物体に付随する表示物が物体自身のフィールドであるという具体例の段落を、ARCHITECTURE の R6 を指す1文へ差し替え、**原則の段落は残す。** |
| `DEVELOP/ARCHITECTURE.md` — R2 の層の表と層の中身(K11) | 定義の行に `entity-shapes/` を足す。層の中身に `entity-shapes/` の項を足し、K11-2 で決めた入れてよい条件と、条件に当たらない形状の定数は概念の所有者の隣に置くことを書く |
| 同 — 「`physics/` に物理でないものを持ち込まない」 | 置かないものに「実体の寸法・取付位置・当たり判定の近似」を足す(ゲーム内容の定義なので、`entity-shapes/` か所有者の隣に置く)。「厳密な幾何なら `physics/`」と読める文面(射影変換の例)が、実体の形まで含むと読まれないようにする |
| 同 — 暫定の文面を直す | 段 8 まで残る「表示の導出の置き場は `src/game/` の中」の列挙に `input`(入力の解釈。段 3 で `game/input/` に集まった)を足す |
| 同 — 外す暫定 | 段 1 で置いた「`game/` の中を割らない」、段 3 で置いた「モデル層の禁止はパスで当てる」、5.5-1 で置いた軌跡の暫定を外す(軌跡は K10-7 で決めた形で書く) |
| 同 — 置く暫定 | **R6 を天体の族に当てない**(段 7)。**`game/celestial/` は未分類**(段 7)。**モデル層の根(`game/game.ts`)は、天体系の描画のために `GameScene` を受けてよい**(段 7)。**`render/` で定義された語彙の型 import だけはモデル層に許す**(段 8) |

**達成条件と検証**

- R6 の見出しがあること。1.6 の原則の段落が残り、具体例だけが変わっていること。
- `entity-shapes/` が R2 の層の表と層の中身にあり、入れてよい条件が書かれていること。
- `rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 6。
- `npm run typecheck`。

#### 手順 6-2. 段 6 の検査を足す

**目的**: 対応表を `game/` の中まで割り、モデル層の出ていく import を層の規則で判定する。段 3 のパス指定の禁止パターンを、ここで層の規則へ置き換える。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `tools/check-boundaries.mjs` | 対応表を割る。`game/{hud,marker,view,pickable,map,lines,input}` = 表示の導出(`input` は段 3 の入力の解釈で、`input/input` を読むので、モデル層に数えると違反になる)、`game/viewer/` = 視点、`game/celestial/` = 未分類(暫定)、`game/game.ts` = モデル層の根(進行と視点の両方を import してよい。`render/scene` の `GameScene` を型で引く辺だけは段 7 まで暫定で通す)、その他の `game/**` = モデル層(進行)。`run/` は 5-1 で組み立てにしてある。モデル層(進行と視点)の出ていく import と `from 'three'`、進行から視点への import を判定する。`render/` で定義された語彙を `import type` で引く辺だけは暫定で通し、値の import は違反にする |
| 同 — ファイル単位の例外 | 対応表はフォルダだけでなく**ファイル単位の例外**も持つ。`game/` の中で表示の導出になったが、置き場がまだ `src/presentation/` に無いものを名指しで列挙する(`game/plan/{plan-editor,node-gizmo,plan-panel,plan-axis-drag,plan-display}.ts`、`game/camera/` の操作係、`game/creative/` のパネル、`game/stages/stage-utils/status-panel.ts`、`game/flash-presenter.ts`・`game/run-event-presenter.ts`(段 3 で置いた出来事の読み手)、`game/game-presentation.ts`(5-3 で置いた表示の導出の根)、`game/controlled-loop-sfx.ts`、`game/orbit-info.ts`、`game/protein/protein-motion-metrics.ts`)。**一覧は手順 8-3 の移動で消える**(暫定であることを検査本体のコメントに書く) |
| 同 — 外すもの | 段 3 で入れたパス指定の禁止パターン3本(「表示の選択が進行へ漏れる禁止」「モデル層が生の入力を読む禁止」「モデル層が出来事の装置を持つ禁止」。どれも本体のコメントに「段 6 で外す」とある)と、手順 4-2 のパス指定のうち、進行から `game/viewer/` への禁止、`game/viewer/` から `settings/` への禁止、根の型の置き場(5-2 で `game/save/` から差し替えた)から表示の導出への禁止、5-1 の「モデル層の根が表示の導出を持つ禁止」を外す(`settings/` から `game/viewer/` への禁止は段 8 まで残す)。**同じ違反を層の規則と禁止パターンの両方で数えない** — 二重に数えると許可リストが空にならない |
| 同 — `LAYER_TABLE` | `['src/entity-shapes/', DEFINITION]` を足す(K11)。対応表に無いパスは検査が落ちるので、6-6 でフォルダを作るより前に足す。K11-2 で B を選んだなら、`entity-shapes/` のファイルを model-builder・render・モデル層のうち2者以上が読むことの判定もここで足す |
| `tools/boundary-allowlist.json` | 手順 6-3〜6-6 が消すあいだの残りを載せる。段 7 で消す `game/celestial/` の違反は、未分類なので載らない |

**達成条件と検証**

- `npm run check:boundaries` が 0 を返す。
- 自己検証: `src/game/stages/` から `render/` の値を1本 import すると 1 を返し、`import type` なら通ること(暫定の範囲であること)を確かめる。
- 段の終わりに許可リストが空。

#### 手順 6-3. 1体ぶんの 3D 表示を表示担当へ移す

**5.5-3 から回した行**: 進行の中の実体化と `prune` が View を作り捨てる(R8)、`CameraFrame.zoomed`・`PlayerRenderSource.active` を View が解釈する(R7)、`InstancedPools` の即時モード(R7)、破片の形の乱数(R5・R1)、タンパク質の LOD のフェードと `motionSeed`(R5・R11)、`siteMarkers`・`ProteinSiteStatus`(R7)、`StageDeps` の束(1.6)。振り分けの表を見る。

**目的**: R6 のとおり、`DynamicEntity` から View を外す。表示の導出の突き合わせ役が、実体ごとの表示担当を作る・捨てるようにする。View の構築は sync で行う(update 中に THREE を作らない)。実体の構築経路から `scene` を消す。見た目は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| (新規) `src/presentation/dynamic/` の突き合わせ役と表示担当のクラス辞書 | roster と改版番号から、実体1つにつき1つの表示担当を作る・捨てる。`InstancedPools` の所有と、`beginFrame` / `endFrame` の駆動もここへ移す(`dynamic-system.ts:331,333`) |
| (新規) 種別ごとの表示担当 | 基地・金属敵・タンパク質敵・補給・自機・弾・破片・分離ブースター |
| `src/game/dynamic/dynamic-entity/dynamic-entity.ts:19,65-97` | `view` フィールドと `renderSource` を消す。表示入力の組み立ては表示担当へ移す |
| View を構築していた箇所 | 構築をやめる: `base.ts:114`、`bullet.ts:22`、`pickup.ts:220,240`、`metal-enemy.ts:52-54`、`protein-enemy.ts:84,113`、`detached-booster.ts:48`、`debris-piece.ts:31-40`、`player/player.ts:165`、`ship.ts:2` |
| `src/game/dynamic/dynamic-system.ts:17-20,58-71,219,324-334`、`src/game/dynamic/dynamic-entity/entity-dictionary.ts` | プールと描画の sync を外す。prune での `view.dispose()` を外す。`scene` の受け渡しを外す(5-4 で `DynamicEntityClass.deserialize` に暫定で残した `scene` 引数も消す) |
| `src/game/game.ts`(モデル層の根) | `DynamicSystem` へ `GameScene.scene` を渡すのをやめる。`GameScene` そのものは天体系のために 7-3 まで受ける |
| `scene` を実体へ渡している spawn 経路 | `scene` を消す: `stages/stage.ts:232-235`、`stage0.ts:54-57`、`stage1.ts:42-46`、`stage2.ts:48-53`、`stage-debug.ts:87-90`、`stage-debug-load.ts:52-55`、`stage-utils/logistics.ts:62-77,95-110`、`stage-utils/wave-attack.ts:78-81,305`、`spawner/{enemy-generator,enemy-spawner}.ts`、`creative/{object-placement.ts:187-204,manual-spawn.ts}`、`player/fire-control.ts:295,341,372`、`player/attached-boosters.ts:120,166,188`、`dynamic-entity/enemy.ts:63` |
| `src/game/stages/stage-utils/wave-attack.ts:3,234-250` | `THREE.Color` による HSL の計算を `math/` の純関数に置き換える |
| View を読み返していた箇所 | 表示担当から読むようにする: `targeter.ts:282`(`enemy.view.siteMarkers`)、`dynamic-entity/protein-enemy.ts:156`(`view.motionMetrics`。`protein/protein-motion-metrics.ts:32` がこの getter を読む)、`pickable/line-pickables.ts:66`(`entity.view.lineSamples`)。規則が View を読んでいた箇所(タンパク質の銃口と被弾位置の部位)は、5.5-9 で外してある |
| `src/game/protein/protein-asset-loader.ts:143-156` | render の表示定義の構築とキャッシュを表示の導出へ分ける |
| `src/game/dynamic/dynamic-presenter.ts`・`entity-lifecycle.ts`(未配線の足場) | K5 のとおり、ここで採否を決める |
| `tests/render/{game-entity-dispose,dynamic-view-source}.test.ts`、`tests/game/{player-systems,stage-rules}.test.ts`、`tests/protein-test-assets.ts` | 実体と View の関係の変更に合わせて直す。`player-systems` は `Ship` に `DynamicView` の派生を渡し、`stage-rules` は `Logistics` に `new THREE.Scene()` を渡し、`protein-test-assets` は `buildProteinAssetBundle` の `.render` を使っている。`dynamic-view-source` が登録時に入れる document の代役は、テストの後で戻す |
| (新規) `tests/render/dynamic-presenters.test.ts` | 突き合わせ役は、顔ぶれの実体1つにつき表示担当を1つだけ作り、顔ぶれから消えた実体の表示担当を1度だけ捨てる。鍵は実体の同一性で、同じ id の別の実体には別の表示担当を作る(R6)。8-3 で `tests/presentation/` へ移す |

**達成条件と検証**

- `rg -n "DynamicView|\bscene\b" src/game/dynamic src/game/player src/game/stages src/game/creative` が 0 件。
- `rg -n "from 'three" src/game/dynamic src/game/player src/game/stages` が 0 件。
- `npm run typecheck`、`npm run test:game`、`npm run test:render`(新しい突き合わせの検査を含む)、`npm run check:boundaries`。
- `npm run dev` で次を見る。
  - 弾・薬莢・破片・分離ブースターが、出たフレームから描かれる。
  - 撃破で消える。
  - セーブ読み込み後に全機体が描かれる。
  - インスタンス描画の弾がちらつかない。

#### 手順 6-4. 1体ぶんのマーカー・軌道線・UI を表示担当へ移す

**5.5-3 から回した行**: 列の中のメニューの操作が表示の導出を走らせる(R8)、`EquatorNodeInputs`(1.6)、`HudPanelPresenter` の平らな引数、`TrajectoryLine` の焼き直しの鍵(R5)、物体一覧の引数の写しと区画の開閉(R5)。振り分けの表を見る。

**目的**: R6 の残りとして、実体に付随するマーカー、軌道線、一覧の行、メニュー、プロパティ行、ピック候補を表示担当のフィールドにする。管理者ごとの id 対応表を消す。実体から UI の語彙を消す。挙動は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `src/game/lines/entity-line-manager.ts` | 表示担当が自分の軌道線を宣言する形へ解体する。ターゲット強調など族を跨ぐ判断は、フレームの入力として渡す |
| `src/game/targeter.ts:196-249`、`src/game/marker/grouped-markers.ts`、`src/game/marker/lead-markers.ts`、`src/game/marker/player-markers.ts` | 実体ごとのマーカーを表示担当が宣言する。`markerKey` の文字列対応をやめる |
| `src/game/marker/{equator-node-manager.ts:15,equator-node-marker-pair.ts,equator-node-marker.ts}` | id → マーカー対の Map をやめ、表示担当のフィールドにする |
| `src/game/dynamic/dynamic-entity/{combat-target.ts:19,vessel.ts:66-72,base.ts,enemy.ts,pickup.ts}`、`src/game/player/player.ts` | `markerItem`・HP マーカーの SVG・`menuItems`・`propertyRows`・`glyph`・一覧の文言・`fmt*`・`MenuCommon`・`orbitRows`・`shows` を表示担当へ移す |
| `src/game/pickable/{inspected-object,object-pickable,listed-object,pickable-listing}.ts` | これらの契約は表示担当が実装する |
| `src/game/hud/panels/physical-object-list-row-tree.ts:137-150`、`physical-object-list-panel.ts:39,42,333-348` | 行の開閉を、`savedExpanded` を正本とする一方向(boolean を書いて DOM は後続の sync)へ揃える。いまは `setAllRowsExpanded` だけが自分で DOM を書く二流儀になっている(K6 の保留 1)|
| `src/game/pickable/entity-inspection.ts`(未配線の足場) | K5 のとおり、ここで採否を決める |
| `src/game/pickable/object-windows.ts:249,257` | `instanceof Player` / `instanceof CelestialEntity` の分岐をやめ、表示担当へ委ねる |
| `src/game/view/map-view.ts`(構築の引数 25 個)、`src/game/pickable/object-windows.ts`(19 個)、`src/game/hud/frame/frame-controls.ts`(13 個) | 実体ごとの表示物を表示担当へ移した後で、受ける引数を見直す。受け手が使う面へ狭め(1.12)、責務が複数あるなら分ける(1.2)。表示の導出の根から平らに配る形を増やさない(R13、K8 の別件) |
| `src/game/pickable/object-menu-commands.ts`、`runMenu` を持つ実体 | 段 3 でメニューの実行を列へ通したとき、命令が表示の導出の編集口(`PlanEditor`・`ObjectAuthoring`)を運んで実体の `runMenu` へ渡す形になった。命令の口も `hud/windows/menu-actions` と `plan/plan-editor` を import している。`menuItems` を表示担当へ移すときに、**実体へ届く命令を進行の語彙だけにする**(編集口を開くのは表示担当の側で済ませる) |

**達成条件と検証**

- `rg -n "MenuItem|PropertyRow|ContextMenu|ObjectPickable|markerItem|GroupedMarkerItem|hud/" src/game/dynamic src/game/player` が 0 件。
- `equator-node-manager.ts` が無い。
- `npm run typecheck`、`npm run test:game`、`npm run test:render`、`npm run check:boundaries`。
- `npm run dev` で次を見る。
  - マーカー、ターゲット強調、軌道線の色と表示トグル、赤道交点
  - 右クリックメニュー、プロパティウィンドウ、一覧パネル
  - 軌道線の右クリック選択

#### 手順 6-5. ステージと creative から表示を外す

**5.5-3 から回した行**: 決着の結果とブリーフィングの文面(R11・K2)、創作パネルの着色の記憶・コンパクト表示・種類タブ(R5・R10)、`openObjectPlacer` を列を通さず呼ぶ・`ObjectPlacement` が DOM パネルを持つ・`WaveAttack` の `THREE.Color`(R7・R8)、`StageDeps` の束(1.6)。振り分けの表を見る。

**目的**: ステージを進行だけにする。ステータスパネル、ブリーフィング・結果・副題の文面、デバッグ用ウィジェット、creative のパネルと配置プレビューは、ステージクラスごとの表示(表示の導出。一般化しない)へ移す。`StageDeps` から表示の型を消す。`stage.id === 'creative'` の分岐を、宣言された能力に置き換える。挙動は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `src/game/stages/stage.ts:46-53,179,205,334-343` | `StageDeps` から `HudLayers`・`THREE.Scene` を消す(`Notifier` と `FlashEffects` は段 3 で消えた。いまの `StageDeps` は `hud`・`scene`・registry・天体系・操作対象・命令の列)。StatusPanel の構築と `sync(camera, displayTime)` を外す。結果の文面(`detailHtml`)を構造化した値にする |
| `src/game/stages/{stage0,stage00,stage1,stage2}.ts` | `briefingHtml` / `hudSubStatus` / 結果の文面を、ステージクラスごとの表示へ移す。これらのファイルの `KEY_MAPPING` の import はブリーフィングの文面のためだけにあるので、一緒に消える。段 3 で、ブリーフィングは出来事の記録へ入れず、ステージが読み取り専用で差し出して `game.ts` がトーストする形になった(`Stage.briefing`)。表示へ移したら、この差し出しも消す |
| `src/game/stages/stage-utils/status-panel.ts` | 表示の導出へ移す |
| `src/game/stages/stage-debug.ts:44-56` | 5-4 で新規と復元の両方で作るようにしたウィジェットを、ステージの表示へ移す |
| `src/game/game.ts`(モデル層の根)、`src/run/run.ts` | ステージへ `HudLayers` と `scene` を渡すのをやめ、`Game.create`/`Game.deserialize` の `hud` 引数を消す |
| `src/game/stages/creative-stage.ts:10,95,185-203,263-272`、`src/game/creative/{object-placer-panel,stage-controls-panel,slider-field,orbit-form-fields}.ts` | パネルと DOM の装着を creative の表示へ移す。検証・状態の生成・採番(`object-placement.ts` の命令部分、`duplicate-form.ts`、`placement-validation.ts`)は進行に残す。`placement-validation.ts:3` が使うラベル名は、表示の導出へ移す |
| `src/game/creative/object-placement.ts:80,104-115` | プレビューの View とマーカーを表示の導出へ移す |
| creative の分岐(`game.ts:306` にあり、5-3 で関心を持つモジュールへ出したもの) | id の分岐を、ステージクラスの静的な宣言に置き換える(`vessel-panel.ts` にあった分岐は `dd827e08` で消えた) |
| `ObjectAuthoring`(`openObjectPlacer`) | 「UI を開け」という面をモデルから外し、表示の導出が持つ |

**達成条件と検証**

- `rg -n "hud/|render/|audio/|from 'three" src/game/stages` が 0 件。
- `rg -n "=== 'creative'" src` が 0 件。
- `npm run typecheck`、`npm run test:game`(`creative-placement-validation`)、`npm run check:boundaries`。
- `npm run dev` で全ステージについて次を見る。
  - ブリーフィング、ステータス、結果画面
  - creative の配置パネル・プレビュー・スポーン・各トグル
  - デバッグステージのウィジェット

#### 手順 6-6. 実体の形状の正本を分ける

**5.5-3 から回した行**: 形状と焼き込みアセットの手写し(段の寸法とノズル出口、爆砕ボルトのずれ、排出ポート、砲身の放出位置、太陽電池の面積、噴射・再突入の効果の位置、タンパク質のリボンの断面、薬莢の縮尺の2段、model-builder が写した弾数・補給の弾倉数)。振り分けの表を見る。

**目的**: 監査報告の 5 を実施する(K11)。実体の形状を `physics/` から出し、`entity-shapes/` を正本にする。手で写した近似は、描画側のアセットとの一致を確かめられる形にする。形を変える手順を SKILL にする。どこまで `entity-shapes/` に入れるかは、段の着手前に決めた K11-2 の条件に従う。**3D モデルの形と、物理の結果は変えない**(焼き直したアセットの差分が識別子だけになる)。

**変更が必要な箇所**(C を選んだ場合。A なら RCS ノズルの行が落ち、B なら当たり判定の近似は所有者の隣に残り、照合のテストだけを足す)

| 対象 | 何をするか |
| --- | --- |
| `src/physics/{player-shape,booster-stage-shape}.ts`、`src/render/rcs-nozzles.ts` → `src/entity-shapes/` | 中身を変えずに移す。ファイル名は据え置く。読み手の import を直す: モデル層4本(`fire-control`・`radiator`・`belt-physics`・`attached-boosters`)、render 5本(`folding-panels-view`・`belt-view`・`attached-boosters-view`・`detached-booster-view`・`rcs-effects`)。model-builder の `importTsDataModule` のパス(`player-ship`・`booster`・`gun-parts`・`pickups`)も直す。冒頭の「import してはならない」の注意は残す |
| 手で写した近似: `metal-enemy.ts:16-21`、`casing-collision.ts:13-15`、`detached-booster-motion.ts:22`、5.5-3 で拾った近似 | K11-2 の条件に当てて振り分ける(K11-3)。<br>・金属敵の外接球は、モデル座標(`ENEMY_MODEL_SCALE` を掛ける前)の値で持つ。縮尺を掛けるのは利用者。<br>・ブースターの接触球は `BOOSTER_STAGE_DIMENSIONS` から導く。いまの 4.2 がどの外形を包む値かを確かめてから式を決め、導いた値と 4.2 の差を記録する |
| (新規)薬莢の照合テスト(`tests/render/`) | 描画アセット `casing.json` の外形と、薬莢の円柱が一致することを固定する。形は `enemy-model-bounds.test.ts` と同じ |
| `src/physics/protein-site-geometry.ts` | K11-6 のとおりファイルを消し、読み手の隣へ分ける。規則の変換と逆変換は `game/protein/`、View の変換は `render/protein/` へ。`tests/game/protein-combat-state.test.ts` の import も直す |
| (新規)`.claude/skills/modify-model-shape/SKILL.md`、`CLAUDE.md` | K11-4 の手順を SKILL に書く。CLAUDE.md の「SKILL は自分から起動する」の表に、「3D モデル(`tools/model-builder/`)の形や寸法を変える」の行を足す。どう見せるか(SPEC)と置き場の条件(ARCHITECTURE)は書き写さず、正本を指すだけにする |

**達成条件と検証**

- `src/physics/` に `player-shape.ts`・`booster-stage-shape.ts`・`protein-site-geometry.ts` が無く、`rg -n "physics/(player-shape|booster-stage-shape|protein-site-geometry)" src tests tools` が 0 件。
- `npm run export-assets` の差分が識別子だけ(形が変わっていない。差分は戻す)。`npm run base-collision:check`。
- `/modify-model-shape` が CLAUDE.md の SKILL の表にある。
- `npm run typecheck`、`npm run test:game`、`npm run test:render`(金属敵と薬莢の照合を含む)、`npm run check:boundaries`。
- `npm run dev` で、放熱板とベルトへの被弾、ブースターの分離と接触、薬莢の跳ね返り、RCS の噴射の位置を見る。

#### 手順 6-7. 段 6 を main へ送る

**手順と達成条件**: 「段を main へ送る(共通)」のとおり。PR 本文には、R6(dynamic の族とステージ)と R2 のモデル層の項、`motion` の基準(K10-6)、`entity-shapes/` とその入れてよい条件(K11)、SKILL `/modify-model-shape`、そしてこの段で置いた暫定4つと、外した暫定(軌跡、協力者の2経路)を書く。

### 段 7 — 天体を時刻層にする

**この段で書く規則**: R6 の全域、R11 の3。

**段の後、他の作業へ効くこと**: 天体の定義・構築・索引に見た目と UI を混ぜない。天体の表示物は構築時に1度だけ作る表示担当が持つ。天体系は1つの immutable な構築値から組み、保存するのはその構築値だけ。

#### 手順 7-1. 段 7 の規則を書く

**目的**: R6 を族の別なく当て、時刻層が保存するものを正本にする。コードは変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `DEVELOP/ARCHITECTURE.md` | R6 の「天体の族に当てない」暫定を外し、「集合が構築後に変わらない族(天体)は突き合わせず、構築時に1度だけ作る」を本文にする。R11 に3(時刻層の天体系は、構築に使った immutable な値だけを保存する)を足す |
| 同 — 外す暫定 | 「`game/celestial/` は未分類」を外し、対応表で時刻層にする。「モデル層の根は、天体系の描画のために `GameScene` を受けてよい」を外す |
| 同 — 置く暫定 | **時刻層の天体は当面 `game/celestial/` にある**(段 8 で `src/celestial/` へ移す) |

**達成条件と検証**

- R6 に天体の項があること。R11 が3項になっていること。
- `rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 4。
- `npm run typecheck`。

#### 手順 7-2. 段 7 の検査を足す

**目的**: 天体が時刻層の規則(定義層・時刻層しか import しない)を満たすことを機械で判定する。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `tools/check-boundaries.mjs` | `game/celestial/` を対応表で時刻層へ移し、時刻層の出ていく import の規則を当てる(`render/`・`three`・`hud/`・`game/` の他フォルダが違反になる)。`orbit-guide-catalog.ts` が読む `assets/` への辺は、データの読み込みとして**恒久の例外**に理由付きで書く。6-2 で `game/game.ts` に通した `GameScene` の暫定の辺を落とす |
| `tools/boundary-allowlist.json` | 手順 7-3〜7-5 が消すあいだの残りを載せる |

**達成条件と検証**

- 自己検証: `src/game/celestial/` から `render/` を1本足すと 1 を返す。
- 段の終わりに許可リストが空。

#### 手順 7-3. 天体の見た目と UI を分ける

**5.5-3 から回した行**: 天体の装置の出力の読み・順序依存・巡回更新(R7・R5)、テクスチャから手で測った `albedoScale`・`averageHue`(1.6)。振り分けの表を見る。

**目的**: 天体のモジュールを時刻層だけにする。対象は天体の定義、系の構築、索引、所属の問い合わせ、重力源の一覧、セーブ。見た目・View・照明と星野・グリッド・軌道ガイドの表示・ラベルの距離帯・近傍系のヒステリシス・点群の分布・地表の配信は表示の導出へ移す。見た目は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `src/game/celestial/celestial-system.ts` | 索引・所属・重力源・セーブと、表示資源の所有(`:85-117,154-168` の build、`:361-444` の sync)を分ける。後者は表示の導出の「天体の場」へ移す。`CameraSystem` を受けるのをやめる(`:16,365,379,410`)。主星の**同一性**は `isStar`(天体の分類)から決まるようになった(`2050c60c`)。残るのは、主星が放つ**光**をまだ描画 View の `stellarLight` から引いている点。参照軌道線の点列は表示の導出から引く(`:343-346,354`) |
| `src/game/celestial/celestial-entity/celestial-entity.ts` | 定義・運動・名前・分類だけを持つ immutable なものにする。View・マーカー・ピック・メニュー・プロパティ行は、天体の表示担当(構築時に1度だけ作る)へ移す |
| `src/game/celestial/solar-system/{earth-system,mars-system,jupiter-system,saturn-system,uranus-system,neptune-system,inner-planets,dwarf-planets,small-bodies,sun,solar-system}.ts` | 物理の宣言と運動の構築を残す。見た目(`CelestialSurface.*`、テクスチャ、光学、View の構築)は、表示の導出の天体 id をキーにした見た目の表へ移す。表に無い id は構築時に例外にする。`solarSystem` の `renderer` 引数(`solar-system.ts:53`)を消す |
| `src/game/celestial/solar-system/{earth-surface-runtime,earth-surface-source,point-field}.ts`、`src/game/celestial/{scale-grid-view,planet-distance,nearby-system-tracker}.ts`、`src/game/celestial/orbit-guide/{orbit-guide-model,zero-velocity-model}.ts` | 表示の導出へ移す。`orbit-guide-catalog.ts`(周期軌道のデータを非同期に読む)は時刻層に残す。`orbit-guide-settings.ts` は視点の語彙として手順 4-3 で `game/viewer/` へ移っている |
| `src/game/stages/stage.ts:97-105`、`src/game/stages/stage-debug-alt-system.ts:78-102` | 天体系の工場から `renderer` を外す。架空の天体の見た目は、見た目の表に登録する |
| `src/game/game.ts`(モデル層の根)、`src/run/run.ts` | 天体系の生成(`renderer`)と `build(scene, pipeline)` が表示の導出へ移るので、`Game.create`/`Game.deserialize` の `GameScene` 引数を消す。天体の表示担当と描画資源は `GamePresentation` が作る |
| `src/hud/utils.ts`(`epochUnixSeconds`) | 時刻の換算なので `physics/time/` へ移す。`tests/game/epoch-display.test.ts` は `tests/physics/` へ移す |
| `tools/render-lab/{cases,lab,main}.ts`、`tools/cloud-lab/{lab,main}.ts` | 本番と同じ工場で組むことを保ったまま、import を直す |

**達成条件と検証**

- 時刻層に残るファイルから、`render/`・`three`・`hud/`・`game/(camera|marker|map|pickable|hud|dynamic)` への import が 0 件になる(`npm run check:boundaries` の時刻層の規則)。
- `rg -n "GameScene|from 'three" src/game/game.ts` が 0 件。
- `npm run typecheck`、`npm run test:physics`、`npm run test:render`(`celestial-illumination`・`orbit-guide-view`)、`npm run test:game`(`earth-system`・`map-visibility`・`point-field`)。
- `npm run render-lab:shot` を手順の前後で撮り、惑星・地球と雲・環を比べる。
- `npm run dev` のマップビューで次を見る。
  - 惑星、環、ラグランジュ点、軌道ガイド、ゼロ速度曲線、スケールグリッド、星野の明るさ
  - デバッグステージの架空の星系

#### 手順 7-4. 天体系を構築値から組む

**5.5-3 から回した行**: 天体の運動を構築後に結び直す・積む形(R1・1.11)、天体の定義の `radius` と `shape` の手写し(1.6)。振り分けの表を見る。

**目的**: R11 のとおり、天体系を1つの immutable な構築値から組む形にする。構築値は、元期・暦プロファイル・暦パック・形式版・自転初期位相・位相オフセット・系の選択。構築時の乱数は `80c63542` で消えたので、構築値に乱数は入らない。保存形式は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `Game.create`・`Game.deserialize` の元期の解決(5-4 で、開始日時とステージの宣言から取る側と、記録から取る側に分けた) | 元期の解決を、構築値を作る1か所へまとめる。**自転初期位相の乱数は `80c63542` で消えた**(位相は元期だけから決まる)ので、構築値に乱数は入らない |
| `src/physics/ephemeris/ephemeris-context.ts:7-14`、`SerializedGame.ephemerisContext`(`src/game/game.ts`)、`src/launcher/save/snapshot-service.ts:70` | 直列化の境界で、構築値と記録の形を相互に変換する(R12)。互換の照合は変えない |
| `src/game/stages/stage.ts:97-105`、`src/game/celestial/solar-system/solar-system.ts:51-57`、`src/game/celestial/celestial-system.ts:48-59,125,137-138` | 構築値だけを受け取って天体系を組む |
| (新規) `tests/physics/` の決定論性の検査 | 同じ構築値から組んだ2つの天体系が、任意の時刻で同じ状態を返す(不変条件) |
| (新規) `tests/game/game-serialization.test.ts` | 7-3 で `Game` を THREE と DOM なしで組めるようになったので、モデル層の根の往復を固定する。ステージごとに `Game.create` から数歩進めた `game` について、`Game.deserialize(game.serialize(), …).serialize()` が `game.serialize()` と等しい(SAVE.md「保存される内容」、R11・R12)。一時停止(`advance(dt, controls, true)`)でも積んだ命令が適用され、`simTime` が進まないことも固定する(R3・R8)。タンパク質のアセットは `tests/protein-test-assets.ts` で読む |

**達成条件と検証**

- 天体系の構築関数の引数が、構築値ひとつになる。
- `npm run typecheck`、`npm run test:physics`(`ephemeris-context` と新しい検査)、`npm run test:game`(モデル層の根の往復)。
- 段 5 で書き出したセーブが読み込めること(版は上げない)。

#### 手順 7-5. 採番器とモジュール寿命の状態をランへ移す — 済

`a566fc45`(採番器をランの持ち物にする)と `66896940`(手動スポーンの採番へ復元 id を予約する)で実施済み。`EntityIdAllocators` はランのインスタンスが持ち、復元した id を `reserve` してから先へ進める。

**残っている `static nextId` は `src/game/hud/windows/{context-menu,object-picker}.ts` の2件だけで、これは DOM 要素の id 採番** — 導出層の資源(R5-2)なので、この手順の対象ではない。当初の達成条件に書いた `rg -n "static .*counter|private static .*next" src/game` はこの2件を拾うので、**モデル層に絞った grep で判定する。**

#### 手順 7-6. 段 7 を main へ送る

**手順と達成条件**: 「段を main へ送る(共通)」のとおり。PR 本文には、R6 の全域と R11 の3、残る暫定(すべて段 8 のもの)を書く。天体系の構築値が1つになり、保存形式が変わっていないこと。

### 段 8 — 置き場と向きを整える

**この段で書く規則**: R2 の最終の対応表、R9、R10 の全文。残る暫定をすべて外す。

**段の後、他の作業へ効くこと**: 規則が最終形になり、フォルダと層が一致する。表示の導出は `src/presentation/`、時刻層の天体は `src/celestial/`、`hud/` も装置である。型はその値の正本の所有者に置く。

#### 手順 8-1. 段 8 の規則を最終形にする

**目的**: 暫定を全部外し、R1〜R13 だけが残る状態にする。コードは変えない(追いつかせるのは手順 8-3〜8-6)。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `DEVELOP/ARCHITECTURE.md` | R2 の対応表を最終形にする(`presentation/`・`celestial/`・`game/viewer/`・`run/` を含み、`hud/` を装置とする)。R9 を新しい節として書く。「`launcher/` が `game/` を見てよいのは、ランを起こすときだけ」を R10 の全文で置き換える(`launcher/`・`main.ts`・モデル層は `settings/`・`launcher/`・`run/` を import しない・`settings/` が import してよい範囲。ランの組み立ての項は 5-1 で書いた) |
| 同 — 外す暫定 | 残る8つ(`hud/` の扱い、表示の導出の置き場、`render/` の語彙の型、天体の置き場と、5.5-3 で置いた R3・R5・R7・R8 の4つ)をすべて外す。後者の4つは設計を決めてから外す(8-3・8-6 の「5.5-3 から回した行」) |
| `.claude/skills/**/SKILL.md`、`CLAUDE.md` | 層の名前とフォルダが最終形になったので、参照を揃える |

**達成条件と検証**

- `rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 0。
- R1〜R13 の見出しが揃っていること: `rg -n "^\*\*R([1-9]|1[0-3])\." DEVELOP/ARCHITECTURE.md` が 13 件。
- `npm run typecheck`。

#### 手順 8-2. 検査を最終形にする

**目的**: 対応表を最終形にし、暫定で通していた辺を落とす。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `tools/check-boundaries.mjs` | 対応表を R2 の最終の表と同じにする。`hud/` を装置として判定し、`settings/` の import 規則(定義層・時刻層だけを import する値の定義モジュールに限る)を足す。暫定で通していた辺(`render/` の語彙の型 import)を落とし、**手順 6-2 のファイル単位の例外の一覧を消す**(フォルダが層に揃うので要らなくなる)。手順 4-2 の `settings/` から `game/viewer/` への禁止は、`settings/` の import 規則が覆うので外す |
| 同 — 狭めたままの判定 | 「モジュール直下の可変値の禁止」は、段 2 で `src/theme.ts` と `panel-shell.ts` の2つだけに当てたまま広げていない(本体のコメントに「段 3 以降で直すぶんまで許可リストへ載るので」とある)。`src/game/`・`src/presentation/`・`src/celestial/` へ広げる。いまの `src/game/` に残る2件は、どちらも R1・R5 が認める形なので、理由を付けて除外する: `base-collision.ts` の `sharedBVH`(不変な幾何の遅延構築)、`node-gizmo.ts` の `styleInjected`(R5-2 の DOM 資源) |
| `tools/boundary-allowlist.json` | 段 8 の着手時点の残りを載せ、手順 8-6 の終わりに空にする |

**達成条件と検証**

- 自己検証: `src/hud/` から `src/audio/` へ1本足すと 1 を返す。
- 段の終わりに空の許可リストで 0 を返す。

#### 手順 8-3. 表示の導出を `src/presentation/` へ出す

**5.5-3 から回した行**: R3 の暫定(表示の導出がモデル層の所有者を具象のまま受ける)を、`Game` が公開する読み取り面と命令の口の設計を決めて外す。R7 の暫定(カメラの投影を `render/` が返す)と R5 の暫定(被選択物の猶予から視点を動かす)も、`CameraFrame` の置き場と猶予の置き場を決めて外す。

**目的**: 段 1〜6 で表示の導出になったものを、R2 の対応表どおり `src/presentation/` へ移す。汎用の部品は `src/hud/` へ移す。移動だけで、挙動は変えない。

**変更が必要な箇所**

| 移すもの | 移し先 |
| --- | --- |
| `src/game/hud/**` のうち game 用のパネル・窓・スタイル | `src/presentation/hud/` |
| `src/game/hud/**` のうち汎用の部品 | `src/hud/`。対象は `windows/{context-menu,object-picker}`、`sync-throttle`、`panel-shell`、`hud-root` の `hudRail`、`orbit/{chart-canvas,orbit-chart,orbit-chart-axes,tick-scale,calendar-ticks}`、`map-scale`、`frame/frame-panel` |
| `src/game/marker/**`(残り)、`view/**`、`pickable/**`、`map/**` | `src/presentation/` |
| `lines/**` の残り | `src/presentation/`。表示担当へ解体済みなら消す |
| 手順 4-3 で表示の導出にしたカメラの操作係、`display-window-manager` の UI 部分、`controlled-loop-sfx.ts`、`orbit-info.ts`、`protein/protein-motion-metrics.ts` | `src/presentation/` |
| 段 3 で置いた入力の解釈と出来事の読み手: `src/game/input/**`、`run-event-presenter.ts`、`flash-presenter.ts` | `src/presentation/`。`tests/game/{pilot-input,input-router,flash-presenter}.test.ts` も `tests/presentation/` へ移す |
| 5-3 で置いた表示の導出の根 `src/game/game-presentation.ts`(と、`game/hud/`・`game/input/` に置いた `hud-panel-view-models.ts`・`game-input-ports.ts`) | `src/presentation/`。6-3 の `tests/render/dynamic-presenters.test.ts` も `tests/presentation/` へ移す |
| `src/game/plan/{plan-editor,node-gizmo,plan-panel,plan-axis-drag,plan-display}.ts`、`plan-path.ts` の射影・ピック部分 | `src/presentation/plan/`。弧の計算とキャッシュは `game/plan/` に残す |
| 手順 6-5・7-3 で表示の導出にしたもの | `src/presentation/` |
| `src/hud/panels/{graphics-panel,bgm-settings-panel}.ts`、`src/hud/windows/{pause-menu,settings-view}.ts`(`render/graphics-settings` と `audio/bgm` を読む) | 読んでいる先が import を持たない語彙モジュールなら `src/hud/` に残す。`Bgm` の試聴のように装置の実体を操るものは `src/presentation/` へ移す。装置どうしの import(R2)を 0 にする |

import を直す外側:

| ファイル | 何をするか |
| --- | --- |
| `src/main.ts` | import を直す |
| `src/settings/user-settings.ts` | 表示トグルとパネルの選択の型は、解釈する表示の導出から import する(軌道ガイドは `962db128` で設定から外れ、視点になった) |
| `tests/game/{camera-rig,focus-target,frame-anchors,map-scale,map-visibility,...}.test.ts` | 移った先を対象にするテストを `tests/presentation/` へ移す。**移す前に 4.1 で判定し、B の箇所は移さずに消す**(K8-6)。`camera-rig`(全体が注視対象の参照の同一性と猶予の回数の固定)と `shortcut-hint`(文言)は、K8 の調査では B だけだった。判定し直して B なら、ファイルごと消す。`focus-target:42,71,74-83`、`frame-anchors:51-58`、`map-scale:10-11,16-17`、`map-visibility:78-79,84-88,110-130,159-168` も判定し直す。`map-scale` は MAP.md の 1・2・5 の丸めと注視深度の基準を不変条件として足す |
| `tests/run.ts`、`tsconfig.test.json`、`package.json` | `presentation` 層(`npm run test:presentation`)を足す |
| `CLAUDE.md` | コマンド表に `test:presentation` を足す |
| `tools/verify-ui-style.mjs`(`src/game/hud` を文字列パスで読む)、`tools/verify-theme-contrast.mjs`(存在しない `src/game/theme.ts` を読んでいる) | パスを直す |

**達成条件と検証**

- `src/game/{hud,marker,view,pickable,map,lines}` が存在しない。
- `rg -n "src/game/(hud|marker|view|pickable|map|lines)" tools tests src .claude/skills DEVELOP CLAUDE.md` が 0 件。
- `npm run typecheck`、`npm run test`、`npm run build`、`npm run verify:ui-style`、`npm run verify:theme`、`npm run check:boundaries`(対応表から該当行を消す)。
- `npm run smoke:browser`。手順の前から落ちるなら、落ち方が変わらないことを比べる。

#### 手順 8-4. 天体を `src/celestial/` へ出す

**目的**: 時刻層に残った `game/celestial/` を `src/celestial/` へ移す。移動だけで、挙動は変えない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `src/game/celestial/**`(時刻層に残ったもの) | `src/celestial/` へ `git mv` |
| import している側 | 123 ファイルを直す。うち tests が 64 か所、`launcher/save-browser/save-browser.ts:3` |
| 文字列パスで読む tools | `rg -n "game/celestial" tools tsconfig*.json` で出る箇所を直す: `tools/export-lagrange-orbits.mjs:50-52`、`tools/export-moon-features.mjs:18`、`tools/compile-source.mjs:45`、`tsconfig.test.json:32`、`tools/render-lab/*`、`tools/cloud-lab/*` |
| `tests/game/earth-system.test.ts` など、対象が `src/celestial/` にあるテスト | `tests/celestial/` へ移し、`test:celestial` を足す(`tests/run.ts`、`package.json`、`CLAUDE.md`)。天体のデータを材料に使う `tests/physics/` のテストは、パスだけ直す |

一括の書き換えは、UTF-8 で書き出す node スクリプトか Edit で行う。PowerShell の `Set-Content` とシェルの sed は、日本語を壊し、置換を黙って空振りする。

**達成条件と検証**

- `rg -n "game/celestial" src tests tools DEVELOP CLAUDE.md .claude/skills` が 0 件(`tests/dist` と `memos/` は除く)。
- `npm run typecheck`、`npm run test`、`npm run build`、`npm run check:boundaries`。
- `npm run render-lab:shot` と `npm run cloud-lab:shot` が起動して撮れる。

#### 手順 8-5. 型の置き場を所有者へ揃える

**5.5-3 から回した行**: `ProteinPhase` を `render/` が定義する、`Stage0MetalEnemyView`・`ScaleGridVisibility` の名前(R7・R9)。

**目的**: R9 のとおり、型をその値の正本の所有者へ移す。対象は、モデル層が `render/` で定義された型を使っている残りと、`hud/` と `settings/` が装置の内部から引いている型・定数である(段 8 で `hud/` が装置になるので、後者も装置どうしの import になる)。挙動は変えない。

**変更が必要な箇所**

| 型 | いまの場所 | 移し先 |
| --- | --- | --- |
| `ViewMode` | `game/view/view-mode.ts`(`1007f4ef` で `render/` から移した) | 視点が正本なので、最終的には `game/viewer/` の語彙モジュール。import を1本も持たないので、いまも定義層として render から読めている(R2) |
| `ProteinDisplaySettings` / `ProteinPhase` | `render/protein/protein-display.ts` | 表示設定は視点、フェーズは進行が正本。どちらも import を持たない語彙として所有者の側に置く。モデル層から使っている側: `save/save-data.ts`、`protein/{protein-combat-state,protein-schema}.ts`、`run-events.ts`(段 3 で増えた。出来事がフェーズを運ぶ)、`dynamic-entity/{protein-enemy,enemy-display-capabilities}.ts`、`stages/{creative-stage,creative-stage-commands}.ts`、`stages/spawner/enemy-generator.ts`、`creative/manual-spawn.ts`。表示の導出の `flash-presenter.ts`・`creative/stage-controls-panel.ts` は装置の語彙を読んでよい側 |
| `ProteinBackboneAsset` | render | 当たり判定が使う側(`protein/protein-sphere-collision.ts`)へ |
| `display-window-duration.ts` | `game/` | 視点の語彙(表示期間は視点。K7-2)なので `game/viewer/` へ。進行からの import は段 3 で消えた(需要として受ける)。残る読み手は視点と表示の導出 |
| `DirectionMarkerMode` | `render/celestial/orbit-guide/direction-markers.ts` | 軌道ガイドの選択(視点)の一部で、`game/viewer/` の語彙が引いている。import を持たない語彙として視点の側に置き、装置はそれを読む |
| `perf-counts.ts` が引いている render の型 | — | 計測は組み立て(`main.ts`)の関心として整理する |
| `settings/user-settings.ts:3,9-10,16-17` が引いている format / parse | `render/graphics-settings`・`render/celestial-grid`・`audio/bgm/bgm`(どれも装置の内部を import している) | 値の型と保存文字列との変換だけを、**import を持たない語彙モジュール**へ切り出す(`render/render-style.ts` と同じ形)。R10 の「`settings/` が import してよいのは、定義層・時刻層だけを import する値の定義モジュール」を満たす |
| `CLICK_MOVE_THRESHOLD`(`hud/windows/draggable-window.ts:15`、`hud/windows/pause-menu.ts:16`) | `input/input.ts` | しきい値を `input/key-mapping.ts`(import を持たない)へ移す。`hud/` が装置になる段 8 で、装置どうしの import を残さない |
| `hud/panels/graphics-panel.ts`、`hud/windows/{pause-menu,settings-view}.ts` が引いている型 | `render/graphics-settings`・`audio/bgm/bgm` | 上で切り出した語彙モジュールへ狭める。`Bgm` の実体を操る試聴は手順 8-3 で `presentation/` へ移る |

**達成条件と検証**

- `rg -n "from '.*render/" src/game` が 0 件。
- 装置どうしの import が残らない: `rg -n "from '.*(render|audio|input|marker)/" src/hud` が、import を持たない語彙モジュールへの辺だけになる。
- `settings/` が装置の内部を読まない: `rg -n "from '../(render|audio|game)/" src/settings` が、切り出した語彙モジュールへの辺だけになる。
- `npm run typecheck`、`npm run test`、`npm run check:boundaries`。

#### 手順 8-6. ランの組み立ての残りを片づける

**5.5-3 から回した行**: R8 の暫定(入力の解釈が押し続ける入力を dt で積む)を、視点とΔv の編集の量を操作量として進行へ渡すかどうかを決めて外す。ページ寿命のパネルがラン寿命の口を毎フレーム受ける(R5・R13)、`FrameSections.enabled`・`GpuTimings.enabled`(計測)。

**目的**: 5-3 で `game.ts` を3つの根に分けた後に残る、組み立ての仕事を片づける。ここに残したのは、R10 の全文と段 8 の置き場が要るもの(launcher が読む面、計測と読み込みの進捗)である。決着の読み方・時間加速の命令・`simTime` の経路は、段 5 までの規則(R12・R8・R5)の未達なので、段 5.5 の 5.5-7 へ移した(2026-09-19)。

- 許可リストを空にする。

挙動は変えない。8-3 の後なので、表示の導出のパスは `src/presentation/` の下にある。

**前提(K5 で調査済み)**: Coordinator 分割は `cbd61ab1` の木に `src/game/{input,simulation,presentation}-coordinator.ts` として残っているが、`39c813c0` のマージで main からは消えている。**復元しない。** 5-3 の分け方(モデル層の根・表示の導出の根・ランの組み立て)は、`Game` を残したまま中身を3つへ配る Coordinator とは切り方が違う。

**前提(段 3・5-3)**: 位相の中の順序は、「段 1〜3 — 済」の節の5項目と、5-3 の `Run.frame` の順序表のとおりである。計測の区切りを動かすときも、これを崩さない。

**変更が必要な箇所**

| ファイル | 何をするか |
| --- | --- |
| `src/launcher/launcher.ts`、`src/launcher/save/*`、`src/game/run-summary.ts` | `run.game.activeStage`・`phase`・`celestialSystem.nameOf` を、狭い面で受ける |
| `src/game/frame-sections.ts`、`src/game/perf-counts.ts`、`src/presentation/hud/windows/debug-info-window.ts`、`src/game/loading-progress.ts` | 計測は `src/run/` へ、読み込みの進捗は launcher へ移す。`Game.create`/`Game.deserialize` と `DynamicSystem` が受けている `FrameSections` は、モデル層が `run/` を import しない形にする(区間を `Run` の側で区切るか、import を持たない語彙の面で受ける) |
| `tools/boundary-allowlist.json` | 空にする |

**達成条件と検証**

- `src/game/game.ts` はモデル層の根で、`rg -n "from '.*(render|hud|audio|input|presentation|settings|launcher|run)/|from 'three" src/game/game.ts` が 0 件。
- `rg -n "onDecided" src` が 0 件。
- `rg -n "KEY_MAPPING" src/game` が 0 件(キーの解釈がモデル層に残っていない)。
- `npm run check:boundaries` が空の許可リストで 0 を返す。
- 次が 0 件(`input/key-mapping.ts` など import を持たない語彙は除く)。
  ```
  rg -n "from '.*(render|hud|audio|input|presentation|settings|launcher|run)/|from 'three" src/game src/celestial
  ```
- `npm run typecheck`、`npm run test`、`npm run build`、`npm run smoke:browser`。
- `node tools/dep-metrics.mjs --file src/game/game.ts` の ctx2 が 38453 行(分割前の `game.ts` の調査時点の値)より小さい。
- `npm run dev` で次を見る。
  - ラン → タイトル → 別ステージの順に進んでも、HUD が重複しない。
  - 決着から結果画面が出る。決着済みの記録から始めても、結果画面が出る。
  - 段 5 で書き出したセーブが読める。

#### 手順 8-7. 段 8 を main へ送る

**手順と達成条件**: 「段を main へ送る(共通)」のとおり。加えて、この段でだけ確かめるものがある。

- `tools/boundary-allowlist.json` が空で、対応表が R2 の最終形と一致している。
- `rg -c "暫定 — 段" DEVELOP/ARCHITECTURE.md` が 0。
- 「達成目標」の最終の行がすべて満たされている。
- PR 本文に、R1〜R13 が正本になったことと、`check:boundaries` が何を判定するかを書く。

## 見積り

作業量は次の式で出す。**段 1〜5 は実施済みなので、残り(段 5.5〜8)だけを載せる。** 段 3 から回した項目の分は、受ける手順の行に足して括弧で示した。

```
意味の変更ファイル数 × 20 分 + 機械的な import 置換ファイル数 × 1 分 + 検証 20 分
```

ファイル数は、各手順の表と、調査時点の import グラフ(629 ファイル・3447 辺)から数えた。規則の手順は節数(1節 15 分)、検査の手順は足す判定の数、main へ送る手順は `/send-pr`(取り込み・点検・全検証・本文)から見た。

| 段 | 手順 | 分 |
| --- | --- | --- |
| 5.5 | ~~5.5-1 規則~~ 済 | 0 |
| | ~~5.5-2 検査~~ 済 | 0 |
| | ~~5.5-3 洗い出し~~ 済 | 0 |
| | ~~5.5-4 運動の書き手~~ 済 | 0 |
| | ~~5.5-5 残りのモデル層の欄~~ 済 | 0 |
| | ~~5.5-6 音の装置~~ 済 | 0 |
| | ~~5.5-7 位相・直列化・導出の残り~~ 済 | 0 |
| | ~~5.5-8 1.x の未達と launcher の表示~~ 済 | 0 |
| | 5.5-9 洗い出しの残りと監査報告の 4(4 の分は SPEC の削除と 6 ファイル × 20 + 検証 20 = 140。洗い出しの 22 行は約 40 ファイル × 20 = 800) | 940 |
| | 5.5-10 main へ送る | 150 |
| | **段 5.5 計(残り)** | **1,090** |
| 6 | 6-1 規則(R6・R2 のモデル層、`entity-shapes/` の層と条件 +15、`physics/` から実体の形を締め出す +15) | 120 |
| | 6-2 検査(対応表を game/ の中まで割る、`LAYER_TABLE` の行 +5。K11-2 で B なら読み手の数の判定 +20) | 95 |
| | 6-3 3D の表示担当(モデル層の根の `scene` +20、漏れていたテスト3本 +30、突き合わせの検査 +40) | 810 |
| | 6-4 マーカー・軌道線・UI(メニューの命令を進行の語彙にする +40。受け手の引数の見直し +60) | 760 |
| | 6-5 ステージと creative(モデル層の根の `hud` +20) | 480 |
| | 6-6 実体の形状の正本(C の場合。import の置換 13 ファイル × 1、近似の移動 3 ファイル × 20、薬莢の照合テスト +20、`protein-site-geometry` の分割 3 ファイル × 20、SKILL と CLAUDE.md +40、焼き直しの差分の確認 20) | 210 |
| | 6-7 main へ送る | 120 |
| | **段 6 計** | **2,595**(K10-6・K10-7 の是正の分を除く) |
| 7 | 7-1 規則(R6 の全域・R11 の3) | 60 |
| | 7-2 検査(時刻層を天体へ当てる) | 40 |
| | 7-3 天体の見た目と UI(モデル層の根の `GameScene` +20) | 740 |
| | 7-4 構築値(構築時の乱数は実施済み。モデル層の根の往復と一時停止の検査 +60) | 210 |
| | ~~7-5 採番器~~ 済 | 0 |
| | 7-6 main へ送る | 120 |
| | **段 7 計** | **1,170** |
| 8 | 8-1 規則(R2 の最終表・R9・R10 の残り・暫定8つの削除) | 120 |
| | 8-2 検査(最終形。可変値の判定を広げる +20) | 100 |
| | 8-3 presentation へ(段 3 の入力の解釈と出来事の読み手 +10、表示の導出の根 +10、移すテストの 4.1 の判定 +30) | 540 |
| | 8-4 celestial へ | 363 |
| | 8-5 型の置き場(語彙モジュールの切り出しを含む。`ViewMode` は移動済み) | 350 |
| | 8-6 ランの組み立ての残り(狭い面 3、計測と進捗 4 — 計 7 × 20 = 140、検証 20。決着の読み方・時間加速・`simTime` の経路は 5.5-7 へ移した) | 160 |
| | 8-7 main へ送る | 120 |
| | **段 8 計** | **1,753** |
| | **残りの合計** | **8,668 分 ≒ 144 時間**(段 6 の前に決める K10-6・K10-7 の是正の分と、段 8 で暫定を外すための設計の分を除く) |

- 残りの commit は約 25 本、PR は 4 本(段 5.5・6・7・8)。段 5 の実測は約 45 commit(規約点検と後始末を含む)。**手順どうしは直列にする**(どの手順も同じファイル群を重ねて触る)。ただし手順の中で、所有者の群ごとにファイルを重ねずに割れるなら、git worktree で並行してよい(段 5 の「進め方で分かったこと」)。
- 手順 5.5-4〜7-3 は `src/game/dynamic/` と `stages/` を重ねて触るので、段を跨いでも直列にする。**段 5.5 を main へ入れてから段 6 に着手する。**
- **規則と検査の手順(N-1・N-2)は、その段の実装の手順と同じ commit にしない。** 規則だけの commit を先に置くと、PR の中で「規則 → コード」の順が読める。
- 段を1本の PR にする。段の中の手順ごとに commit を分け、**段の途中で main へ送らない** — 送ると、規則を満たしていないコードが main に入る。


## リスクと落とし穴

| リスク | 影響 | 露見する場所 |
| --- | --- | --- |
| 規則を段より先に書く(最終形をまとめて書く) | 満たしていないコードが main に入り、他の作業が計画の外で規則へ合わせ始める。次の段が触る場所が先に動く | 各段の N-1 の手順(その段の終わりに満たせる分だけ書く)。狭めた文面に `(暫定 — 段 N で外す)` を付ける |
| 段の終わりに許可リストが空にならない | 次の段の着手条件が崩れ、違反が段を越えて積み上がる | 段の PR の手順。空にできないなら段を閉じず、残りが規則の設計の誤りなら**その段の規則を狭める** |
| 暫定の文面が残る・増える | 規則が読めなくなり、最終形との差が分からなくなる | 各段の N-1 の達成条件の `rg -c "暫定 — 段"`(3 → 4 → 7 → 6 → 4 → 10 → 10 → 8 → 0) |
| 段を切ったのに、先に書いた規則を落とし忘れる | 満たしていない規則(R11 の2)が main に入り、他の作業が「載っていない値がある」ことを違反と読む | 手順 4-4(`rg -n "例外なくすべてセーブ"` が 0 件)、4-5 の PR 本文 |
| 規則を落とすときに、SPEC まで一緒に落とす | 仕様が後退する。SPEC はコードより先行してよい(未実装の記述は正常) | 手順 4-4(触るのは `DEVELOP/CODING-RULE.md` だけ。`DEVELOP/SPEC/SAVE.md` は触らない) |
| 段 4 の PR の後、ほかの作業が `game.ts` を割る | 段 5 の分割と衝突し、計画の切り方(3つの根)が失われる | 手順 4-4(1.10 の位相の印・1.2 の暫定・`game.ts` の冒頭のコメント)、4-5(PR 本文の予告) |
| 段と段の間の作業が、まだ書いていない規則へ向けて先に直す | 次の段が触る場所が別の形へ動き、計画と合わなくなる | 段の PR 本文に「この段で書いた規則」と「暫定」を列挙する。**書いていないものを守らせない** |
| 同じ違反を層の規則と禁止パターンの両方で数える | 許可リストが二重になり、消しても空にならない | 手順 6-2(パス指定のパターンを層の規則へ置き換えるとき外す) |
| 検査の名前の列を規則と読み、名前を替えて通す。名前が塞いでいるので形を歪める | 規則の趣旨と違う設計が入り、検査は通る(`setThrust` を `applyControl` と呼び替える案) | 手順 5.5-2(名前で判定するのは、規則が名前を定めているときと、特定の API の呼び手を限るときだけ)、K10-5 |
| 判定の無い規則を、許可リストが空なので満たしたと読む | 規則に反するコードが main に入り、次の段が前提を誤る(段 3 の R3、段 1 の R7 の単発音) | 達成目標の規則と判定の対応表、「段を main へ送る(共通)」の洗い出し |
| 運動の命令にしたとき、予測弧を捨てる時点が変わる | 噴射中の弧が残る、または噴射していないのに消える。AI が読む予測が変わる | 手順 5.5-4(捨てる条件をいまの setter と同じにする。推力は非 null のときだけ) |
| 接触代理を軽いクラスにしたとき、相手を `DynamicMotion` として受けている反応が代理の値を読めなくなる | 放熱板・ベルトへの被弾で、反応が例外を投げるか黙って外れる | 手順 5.5-4(先に相手の型を参加者の面へ広げてから、代理を差し替える。`npm run dev` で被弾を見る) |
| 軌跡を記録と積分に分けるときに、先端の置き換えと間引きの順序を変える | 弧の刻みが需要に依存し、R4 の不変条件が崩れる | 段 6 の着手前に K10-7 で決めた是正の手順(`trajectory-demand.test` を変えずに通す) |
| 検査のファイル単位の例外の一覧が古くなる(移した / 消したファイルが残る) | 表示の導出になっていないファイルが表示の導出として通り、違反が隠れる | 手順 6-2 で一覧に理由を書き、手順 8-2 で一覧そのものを消す。存在しないパスが一覧にあれば検査がエラーを返すようにする |
| 段の途中で main を取り込んだとき、他の作業が持ち込んだ違反が自分の段の残りに混ざる | 許可リストの差分が読めなくなる | 取り込み直後に `npm run check:boundaries` を走らせて切り分ける(各段の PR の手順) |
| 境界検査へ足した判定が、`export … from`・動的 import・`import type` を取りこぼす | 規則を守ったことにして違反が増える | 各段の N-2 の自己検証(その判定に当たる違反を仮に足して、落ちることを見る) |
| 形状の正本を移すときに、model-builder が読むパスを直し忘れる | `npm run export-assets` が落ちる。直し忘れた読み手だけが古い値を読み続ける | 手順 6-6(移した後に焼き直し、差分が識別子だけであることを見る) |
| `entity-shapes/` に、入れてよい条件に当たらない定数を入れる | 雑多な定数置き場になり、定数が概念の所有者から離れる(監査報告が挙げた Y の欠点) | 手順 6-1(条件を ARCHITECTURE の層の中身に書く)、段 6 以降の終わりの洗い出し(対応表の R2 の行。B なら 6-2 の判定)、`/modify-model-shape` |
| 5.5-1 で `physics/` を書き直すときに、定数まで締め出す文面にする | `physics/` の物理定数と形状の定数が、段 5.5 の終わりに書いた規則の未達になる。形状を段 6 へ回した前提が崩れる | 手順 5.5-1(「定数・純関数と、意味を持たない容器」と書く。K11-5) |
| 「import を持たない語彙は定義層」を抜け道に使い、意味のある型を空の import のファイルへ逃がす | 装置がモデルの意味を知る | 手順 8-5 のレビュー(語彙モジュールを1本ずつ見る) |
| 導出層が持つ写しを一律に消す | 毎フレームの DOM 付け替えでボタンのフォーカスとホバーが飛ぶ。兄弟値のクランプと未設定 id の既定生成が落ちる | 手順 5.5-7(保留 2)、6-4 と段 8(保留 1)。K6 の保留 1・2 のとおり、**残す写しと消す写しを先に分ける** |
| 視点への書き込みを列に積むことで、UI の操作と表示が1フレームずれる | 注視やビューの切り替えが遅れて見える。同じフレームに届いた命令の順序が変わる | 手順 4-3(段 3 と同じく、受け付けた順に適用すること、一時停止中にも適用すること) |
| 位相を `Run` へ組み直すときに、段 3 で決めた位相の中の順序を崩す | 一時停止中に同じ効果音が毎フレーム鳴る。DOM で起きた出来事が表示されない。`canShipAct` が前のフレームの倍率で効く。予測が1フレーム遅れて伸びる | 手順 5-3(「段 1〜3 — 済」の節の5項目と `Run.frame` の順序表を前提として守る)、8-6 |
| 出来事の通し番号をランを跨いで持つ・戻す | ラン開始直後に音が鳴らない、または前のランの音が鳴る。いまの `RunEventLog` はランごとに `game.ts` が作り、番号は 0 から始まる | 手順 5-3(記録を持つ `Game` と読み手の `GamePresentation` を、同じ `Run` の寿命で作り畳む) |
| 表示担当の突き合わせを、同一性ではなく id でする | 読み込み直後に古い表示担当が残る、または作り直しが漏れる | 手順 6-3 |
| 自機の遅延除去(`reclaimedByOwner`)と表示担当の寿命がずれる | 撃墜直後の自機が消えない、または早く消える | 手順 6-3 |
| View を sync で作ったとき、`InstancedPools` の begin/end と順序がずれる | 出たフレームの弾が描かれない、ちらつく | 手順 6-3 |
| 見た目の表の登録漏れを既定の見た目で黙って埋める | 天体の見た目が静かに変わる | 手順 7-3(表に無い id は構築時に例外にする) |
| lab を本番と別の組み立てで直す | lab の撮影が本番を検証しなくなる | 手順 7-3、8-4(lab は本番の工場を呼ぶこと) |
| 文字列パスで src を読む tools を直し忘れる | tools が黙って壊れる、または何も検査しなくなる。`verify-theme-contrast.mjs` はすでに存在しないパスを読んでいる | 手順 8-3、8-4 |
| 一括置換を PowerShell か sed で行う | 日本語が化け、置換が黙って空振りする | 手順 8-3、8-4 |
| 並行するエージェントのテストが `tests/dist` を消し合う | ENOTEMPTY や Cannot find module で落ち合う | 並行させる手順(3・4・6、7・8)。outDir を分ける |
| 保存形式を、意図しない手順で変えてしまう | セーブが読めない、または戻る位置がずれる。版を上げる手順が 5-5 の1つでなくなり、どの記録がどの版かが追えなくなる | 手順 4-3・5-2〜5-4(形式を触らない。段 1 の前のセーブで確かめる)、5-5(ここだけが版を上げる)、7-4・8-6(段 5 で書き出したセーブで確かめる) |
| 版を上げたときに、古い記録が「拒まれる」ではなく「壊す」 | セーブデータ画面が開かない、自動セーブの読み込みでタイトルに戻れない、取り込みが全滅する | 手順 5-5(古い記録を残した状態で、一覧・再読み込み・取り込みを `npm run dev` で見る)、5-6(`tests/launcher` で版の不一致を固定する) |
| 版を上げたのに、古い形式を読む道を残してしまう | 変換の分岐が残り、SAVE.md「旧い版の記録を新しい版へ変換して読むことはしない」に反する | 手順 5-5(`deserialize` に版の分岐を書かない) |
| 設定を視点と取り違える(「見え方を決める値」を `game/viewer/` へ入れる、またはその逆) | 前者: 別のセーブを読み込む・新しいゲームを始めると、描画品質や配色のようなゲームを跨ぐ選択が入れ替わる・既定へ戻る。後者: 地球圏のセーブで選んだ基準や座標系、軌道の時間尺度に合わせた表示期間が、別のセーブへ持ち込まれる | 手順 4-0(§6 に時間を足す)、4-1(R4 に区別と基準を書く)、4-2(`settings/` と `game/viewer/` の間の import を止める)、4-3(値ごとに §6 で振り分ける) |
| SPEC が決めない値を、1つの所有者の中でセーブと設定へ割る | 同じ状態の片方だけが読み込みで戻り、換算し合う値が食い違う(画角と注視距離など) | 手順 4-1(R4 に「割らない側に置く」を書く)、4-3 |
| 別の作業者の未取り込みの作業と重複・競合する | 同じ境界を二度直す。取り込み時に大きく衝突する | 手順 5-3、6-3、6-4、8-6(着手前に作者へ確認する) |
| `Hud`(ページの寿命)とランごとの表示の導出の破棄順がずれる | ランを跨いで DOM やリスナーが残る | 手順 5-3(`GamePresentation.dispose` が、いまの `Game.dispose` の表示の分と同じ順で戻す) |
| launcher が決着をフレームの後で読むように変えたとき、決着したフレームの sync が走る | 結果画面の直前に1フレーム余分に描かれる | 手順 8-6 |
| 規則の上ではモデル層なのにセーブされない値が残る(ワープ段・基地の計画・creative の手動 spawn の設定・デバッグステージ・採番器) | 読み込みで元に戻らず、「セーブ = モデル層」が崩れて見える | 手順 5-5 で洗い出し、キャッシュ以外はすべて載せる(R11) |
| 作り直せない値を「キャッシュ」として載せずに済ませる | 読み込み後に番号や状態が保存前と食い違う | 手順 5-5(作り直すと近似を超えて違う値になるものはキャッシュではない) |
| 洗い出しが所有者の直下で止まり、深い所の値を取りこぼす | 載っていない値が残ったまま、R11 を満たしたことになる | 手順 5-5、5-6(往復の検査で視点と進行の部品を固定)、6-3(表示担当を外すときに実体のフィールドを見直す)、7-4(モデル層の根の往復) |
| 記録に無い項目を既定引数で補うとき、`null` が既定に落ちない | JSON の `null`(保存時の `NaN` も `null` になる)が、既定値ではなくそのまま入る | 手順 5-4(既定引数が働くのは `undefined` のときだけ。いま型を見て既定へ落としている所は、その検証を `deserialize` に残す) |
| 欠けたときの値が新規の初期値と違う箇所を、既定引数へ寄せてしまう | 古い記録を読むと挙動が変わる(`planExecution` が `'off'` でなく `'instant'` になり、古い計画がその場で実行される) | 手順 5-4(違う値は `deserialize` に明示して残す) |
| `deserialize` が、`serialize` の書いた項目を読み落とす | 保存と読み込みを1回挟むと値が既定へ戻る(保存しても読まれない項目が増える) | 手順 5-6・7-4(往復の検査) |
| `create` と `deserialize` の片方にだけ初期化を書く | 新規と復元の片方でだけ部品や表示が欠ける(復元したデバッグステージの操作パネルと同じ形) | 手順 5-4(両方に要るものはコンストラクタで作る) |
| 構築の順序(`DynamicSystem` → `ControlSelection` → `Stage` → `Viewer`)を、`create`・`deserialize` の中で崩す | 操作対象が付かない。ステージの初期配置が操作対象を取らない。視点が復元前の顔ぶれを見る | 手順 5-3、5-4 |
| 型を所有者へ移すときに、型だけの循環や import が増える | 読むための近傍が広がり、層の判定が新しい辺で落ちる | 手順 5-2(`dep-metrics --against` と、移した先の import の差分) |
| 分割しても、表示の導出の根が 500 行を超える、またはコンストラクタに配線以外の実装が残る | 1040 行の塊が別の名前で残るだけになり、計画を知らない作業がまた割り始める | 手順 5-3(「HUD へ渡す値」と入力ポートの表を別モジュールへ出し、配線以外の実装を所有者へ出す) |
| モデル層の根に置いた暫定の受け口(`GameScene`・`HudLayers`)を外し忘れる | モデル層の根が装置の型を持ち続け、段 8 の最終の判定で初めて落ちる | 手順 6-3・6-5・7-3(各手順の達成条件の grep)、6-1・7-1(暫定の文面) |
| `Run.frame` で自動セーブの位置を動かす | 導出と同期の後に保存され、R8 の「位相 2 と 3 の間」から外れる。入力の途中で畳まれたランを保存する | 手順 5-3(順序表の 9・10) |
| 入力の途中でランが畳まれた後も `Run.frame` が続く | 破棄した根に sync や render が走り、例外か描画の残骸が出る | 手順 5-3(再出撃キーの経路を `npm run dev` で見る) |
| テストの B の箇所を、消さずに新しい置き場へ移す | 新しい置き場でも実装の細部を固定し続け、構造を変えるたびに書き直しが要る | 手順 5-6、8-3(4.1 で判定してから移す) |
