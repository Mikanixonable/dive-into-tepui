## 軌道計画モードのデバッグが必要

## 非mapモードでも計画軌道を表示してほしい
現状nodeマーカーだけが表示されているが、Trajectoryも表示してほしい。ノード編集機能は不要。パネルも非表示で良い。Trajectoryだけ描画してほしい。
ArcやTrajectoryの表示非表示を司るフラグは既に存在するはずだが、mapModeTogglerから連動してしまっている（どの経路で連動しているかが深い）この連動を切って。

## plan.tsにおいて、id管理の必要性が怪しい
idは外部ファイルで一度も参照されていなさそう。

sortByTime前後の対応関係をeditor内で保持するためだけに見えるが…
当面、変更は下流ノードをすべて破棄する、という実装になることが確約されている。
もし将来的に変えるとしても、下流ノードがそのまま残るということはあり得ない
（Arcが繋がらなくなるので）補正をするか破棄をするかの二択。

1. 変更箇所の時刻を取得
2. 変更箇所より後ろのノードを破棄
3. 変更操作を行う

みたいな実装であれば問題ないのではないか？

## ノード以外をクリックした時にselectedを解除し、gizmoを非表示にする

## ノードパネルの横幅が不必要に大きすぎる
CSSを調整

## hideGizmoを継続して呼ぶ必要ある？
不要なら削除

## nodeの編集、更新のパフォーマンス評価と積分の分散
以前はPLAN_ARC_THROTTLE_MS = 200msによって毎秒5回だけ再計算していたが、
かえって体感のフレームレートが落ちたように錯覚するため、UXが悪く、除去した。

パフォーマンス上の問題が出るか、問題があるとしたらそれは積分とメッシュ再構築のどちらにあるのか、調査測定する。

積分の問題であれば、predictorがやっているように、フレームごとに積分ステップを分散させる（徐々にメッシュが伸びていくようなアニメーションになるが、カクカク動くよりもUXははるかに良いだろう）

## ノードのドラッグの挙動が怪しい。
外側？にドラッグすると、軌道を2周3周した後のarcにまでドラッグできてしまっているように見える。

i番目のノードとi+1番目のノードの時間間隔は、i番目のノードの軌道周期よりも長くなってはいけないという制約を課し、それがドラッグ中なども守られるようにする。