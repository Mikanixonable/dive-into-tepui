# 影が消える条件の切り分けと、光源・カメラを動かせる描画テスト環境

**この文書のコード上の事実・行番号は、リビジョン `825903d8` 時点のもの。** 描画の作業手順は
`/rendering-workflow`、仕様の書き方は `DEVELOP/SPEC/README.md` が正本。

---

## 目的

3 つある。

1. **何らかの条件でメッシュの影がまるごと消える原因を突き止め、断つ。** 実機でカメラと自機を
   回していると、影が出るときと一切出ないときがある。**「どの深度マップを引いたか」が見えない
   ままでは、遮蔽器が捨てられたのか・スロットを取れなかったのか・引いた先が空だったのかを
   分けられない。**
2. **光源方向とカメラ方向を動かしながら観察できるようにする。** いま `render-lab` の各ケースは
   恒星の向きもカメラも定数で固定されている。**光源が斜め上にあるのか真横にあるのかで、疑うべき
   問題のセットがまるごと入れ替わる**(すれすれの入射角では自己遮蔽の斑と数値精度の話になり、
   高い仰角では影の伸びる向きの話になる)ので、動かせないと切り分けが成立しない。
3. **どの深度マップからその画素の影を引いたのかを、絵として読めるようにする。** スロットは
   4 枚あり、どれが採用されたかは現状どこにも出ない。目的 1 の切り分けはこの表示に乗る。

### `DEVELOP/SPEC/RENDERING.md` へ追記する文面

「デバッグ表示」節の候補は現在 10 種類。これを 11 種類にし、「影」の直後へ次の 1 項目を足す。

> - **影スロット**: その画素へ影を落としうる深度マップがどれだったかを、スロットごとの色で
>   塗り分けて表示する。どのスロットの範囲にも入っていない画素は黒。

---

## 済んだこと

### 深度マップを引く UV の上下反転を直した(`825903d8`)

ライト空間クリップ座標を `xy·0.5+0.5` で UV へ写していたが、WebGPU ではレンダーターゲットへ
描いた NDC y=+1 の画素がテクスチャの 0 行目(v=0)に落ちるので、v は `0.5−0.5·y` でなければ
ならない。参照がライト空間の水平軸で鏡像になっていた。

**修正前後を同じ構図で撮って確認した結果:**

- `leo` — 修正前は艦の上面左寄りとアンテナ根元に黒い塊が乗っていた。修正後は消え、代わりに
  ノズル環の左下側(恒星と反対側)へ細い影が出る。恒星は `(1, 0.35, 0.5)` すなわち右上手前。
- `ship-selfshadow` — 修正前は船体下面を横切る鋸歯状の黒帯があった。修正後は消え、突起の影が
  細い線として出る。
- 「影」表示のスロット 0 の中身は修正前後で変わらない。**深度マップ自体は最初から正しく、
  狂っていたのは引き方だけだった。**

**ユーザーが実機で目視確認し、影の位置が正しくなったことを確認済み。** 同時に、**何らかの条件で
影がまるごと消える**別のバグが残っていることが分かった — それがこの計画の目的 1 になる。

---

## 決めたこと

**ユーザーはここを覆せる。覆したときにどの手順が変わるかまで書く。**

### 1. 影がまるごと消える最有力の候補は、カメラの背後・至近にある遮蔽器がスロットを独占すること

**これは推論であって実測ではない。** 手順 3 の「影スロット」表示で確かめてから手順 4 で直す。

`SunShadowMaps` は遮蔽器を見かけの大きさの降順に並べ、上から順にスロットを与える
(`sun-shadow-maps.ts:262-280`)。見かけの大きさは `metersPerPixelAt`(同 294-303)が返す
「その位置での画面 1 px の実寸」で割って出すが、**この関数はカメラ前方への射影距離を使い、
`metersPerPixelAtDepth` はその距離に `MIN_DEPTH = 1e-6` の床を敷いている**
(`src/physics/projection.ts:61-67`)。

**カメラの背後にある遮蔽器は射影距離が負なので、この床が効く。** 画角 50°・高さ 540 px なら

```text
mpp = 2 × 1e-6 × tan25° / 540 = 1.73e-9 m/px
10 m の艦の見かけ         = 10 / 1.73e-9 = 5.8e9 px
吸収してよい広がりの上限  = 1024 × 1.73e-9 = 1.8e-6 m
```

結果として次が起きる。

- **背後の遮蔽器が並びの先頭へ来て、スロットを 1 枚ずつ占有する。** 上限 1.8e-6 m では何も
  吸収できないので、1 枚に 1 つしか入らない。
- 4 枚が埋まると `clusters.length >= SHADOW_SLOT_COUNT` で打ち切られ、**画面に写っている艦は
  スロットをまったく取れない。** どのスロットの箱にも入らないので、受け手は影を引かない。
- **カメラを回して遮蔽器が背後へ入った瞬間に切り替わるので、「何らかの条件で消える」という
  現れ方になる。**

薬莢や破片のプールは枝ぜんたいで 1 遮蔽器なので、**個体が画面内に見えていても箱の中心が背後に
あればこれに当たる。**

**直し方は、見かけの大きさをカメラ前方への射影距離ではなくカメラからの距離で測ること。**
背後の遮蔽器も「遠い」と正しく評価され、床が効かなくなる。**背後の遮蔽器を捨てるのではない** —
カメラの後ろにある艦の影が画面内へ伸びてくる構図は普通にある。

**覆されたら**: 「背後の遮蔽器は影を落とさなくてよい」と決めるなら、手順 4 は射影距離が負の
遮蔽器を捨てる 1 行になる。カメラの後ろから伸びてくる影が出なくなる。

### 2. スロットは積で合成せず、テクセルが最も細かい 1 枚を選ぶ

**当初この計画は「入っているスロットすべての積を取る」と書いていたが、撤回する。**

**積は物理的に正しくない。** 透過率は「恒星の円盤のうち遮られずに残る面積比」なので、2 つの
遮蔽器が同じ画素へ効くときの正解は **1 −(遮られた領域の和集合の面積比)** であって、積ではない。
両方が円盤の同じ半分を隠す場合、正解は 0.5 なのに積は 0.25 になる。**半影が二重に濃くなる。**
環の帯で積を取るのが正しいのは、あちらが半透明の媒質で Beer–Lambert に従うためで、不透明な
メッシュには当てはまらない。

**したがって排他的に 1 枚を選ぶ。選ぶ基準は `texelWorld` が最小のもの** — 同じ点を複数の深度
マップが覆っているなら、細かいほうが影の形をよく表している(段階的なシャドウマップで細かい
段を選ぶのと同じ理由)。いまの「箱に入った最初の 1 枚」は並び順に依存していて根拠が無い。

**これだけでは「一部の艦に影が出ない」は閉じない。** 塊は空間ではなく遮蔽器を分割するので、
受け手が自分の遮蔽器の入っていないスロットを選ぶ経路は残る。**そこは塊の作り方(手順 4)の
問題であって、選び方の問題ではない。**

**覆されたら**: テクセルではなく「箱の中心がいちばん近いもの」を選ぶなら、手順 5 は比較する量が
変わるだけで形は同じ。

### 3. 底面のすれすれの入射角での斑は、手順 1(実施済み)で解消した。追加の調整は保留する

**`render-lab` の `ship-selfshadow` で底面に余計な影は出ていない**(ユーザー確認済み)。修正前に
出ていた鋸歯状の黒帯は座標変換の誤りが原因だった。

ただし、**その面がそもそも「対向していない」ことは変わらない。** `ship-selfshadow` が使う恒星
方向は `tools/render-lab/cases.ts:126` の `(-0.70, 0.20, 0.68)` を正規化した
`(-0.7027, 0.2008, 0.6826)` で、**仰角は asin(0.2008) = 11.6°** — ほぼ真横である。艦の姿勢は
同 127 行の Euler XYZ `(-0.5, 0.6, 0.12)` で、機体下方向(ローカル −Y)をワールドへ回すと
`(0.0988, -0.9037, 0.4167)`。恒星方向との内積は

```text
0.0988·(-0.7027) + (-0.9037)·0.2008 + 0.4167·0.6826
  = -0.0694 - 0.1814 + 0.2844 = +0.0336
```

**+0.034、終端線より 1.9° だけ日向側。** ライティングパスは `saturate(dot(normal, lightDir))` を
放射照度へ掛けるので、対向する面なら遮蔽の値によらず色は動かない。**この面の色が遮蔽で動くのは
正常である。**

そして N·L = 0.034 では、傾き比例バイアス `min(texel·slope·2, texel·MAX_SLOPE_BIAS_TEXELS)`
(`sun-occlusion.ts:58,269`)の必要量が slope = √(1−N·L²)/N·L ≈ 29.8 から約 60 texel なのに、
上限は 8 texel しかない。**より浅い角度では斑が出うる。**

**優先度は低い。手順 1 のスライダーで仰角を 5° 前後まで下げて観察し、そこで初めて実施するか
決める(手順 6)。**

**覆されたら**: 先に潰すと決めるなら手順 6 を手順 1 の直後へ移す。恒星がほぼ真横のままの構図で
調整することになるので、仰角の高い構図で過剰なバイアス(影が本体から浮く)になっていないかを
別途見る必要が増える。

### 4. `render-lab` の操作 UI は `DEVELOP/SPEC/UI-DESIGN.md` の対象外として扱う

`UI-DESIGN.md` は冒頭で「HUD(画面に重なる操作パネル・ウィンドウ・ボタン・入力欄・タッチ操作)の
見た目・配置・操作の規約の正本」と範囲を定めている。`tools/render-lab/` はゲームの画面ではなく
開発用の器で、`DEVELOP/` のどこからも参照されていない。既にケース選択・表示選択のボタン行を
`index.html` 内の独自のスタイルで持っているので、**スライダーもその並びへ足す。**

**覆されたら**: HUD の規約へ従わせるなら、手順 1 は `SegmentedControl` と同じ場所にあるスライダー
部品を `src/game/hud/` 側へ新設し、それを `render-lab` からも使う形になる。工数は倍以上になる。

### 5. ライトカメラの枠の設計は、手順 3 の絵を見てからユーザーが再検査する

枠がいま「箱の対角を 1 辺とする正方形」なので、細長い遮蔽器では捨てている面積がある。ただし
**その影響の大きさは、手順 3 の「影スロット」表示と「影」表示を並べて初めて読める。** 現状の
把握が先で、実装方針はそのあとユーザーが決める。**手順 7 はその判断待ちの枠として置く。**

参考までに現状の数値。`SHADOW_SLOT_SIZE = 1024` の 1 枚が覆うのは 1 辺 `1.05 ×(箱の対角)` の
正方形なので、texel の実寸は `1.05·D/1024`。カメラから 10 m の艦を画角 50°・高さ 540 px で見る
とき画面 1 px は `2·10·tan25° / 540 = 0.0173 m` なので、**影の texel が画面 1 px を下回る条件は
D ≤ 16.8 m。** 艦 1 隻はこれを大きく下回る。塊の吸収条件(`sun-shadow-maps.ts:288-292`)も
「対角 ≤ 1024 × その位置の m/px」なので、吸収された塊でも 1 texel/px は保たれる。

**覆されたら**: 手順 7 の中身が決まった時点でこの節を書き直す。

---

## 達成目標

**全手順の実施後、次がすべて満たされていること。**

1. **実機(`npm run dev`)でカメラと自機を回し続けても、艦の影が消えるフレームが無い。** 消える
   構図が残るなら、「影スロット」表示でその画素がどのスロットにも入っていないことまで確かめ、
   原因を計画へ書き足す。
2. `render-lab` の画面に、恒星の方位角・仰角と、カメラの方位角・仰角・距離のスライダーがあり、
   動かすとその場で絵が描き直される。**スライダーに触れていない状態の絵は、実施前の
   `.render-lab/shots/<case>.png` と画素で一致する。**
3. **デバッグ表示に「影スロット」があり、`ship-cluster` で 2 色以上に塗り分かれる。** どのスロットの
   範囲にも入らない虚空は黒。
4. **`ship-cluster` で、どの艦にも自己影が出ている**(「一部の艦にだけ一切遮蔽が無い」が消える)。
5. `grep -n "最初のスロット" src/render/pipeline/` が 0 件。
6. `grep -n "影スロット" DEVELOP/SPEC/RENDERING.md` が 1 件で、同節の候補が 11 種類になっている。
7. **浅い角度での斑(手順 6)と、ライトカメラの枠(手順 7)について、実施するか落とすかの判断が
   下されている。** 判断の根拠になった絵をどのケースのどの表示で見たかまで書かれている。
8. `npm run typecheck` が通る。`src/physics/` を触るのは手順 4 が `metersPerPixelAtDepth` 側へ
   踏み込む場合だけで、そのときは `npm run test:physics` も走らせる。

---

## 手順

### 手順 1. `render-lab` に恒星方向とカメラを動かすスライダーを載せる

**目的.** 恒星の仰角とカメラの向きを固定したままでは、問題の切り分けができない。**恒星がほぼ真横
(仰角 11.6°)の構図と、高い仰角の構図では、疑うべき問題のセットがまるごと入れ替わる。** また、
**影が消える条件はカメラを回して初めて現れる**ので、カメラを動かせないと手順 4 の観察ができない。

**この手順ではゲーム本体の描画に触れない。**

**変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `tools/render-lab/cases.ts:63-79` | `LabCase` へ `viewTarget?: THREE.Vector3`(カメラが周回する注視点)を足す。艦のケースは艦の位置を渡す。省略時は原点から視線方向へ既定距離だけ進んだ点 |
| `tools/render-lab/cases.ts:81-88` | `labCamera` は今のまま。**スライダーを触っていないときの位置・姿勢が現在と完全に一致すること**を保つため、既定の方位角・仰角・距離はケースのカメラと注視点から逆算する |
| `tools/render-lab/lab.ts:114-133` | `render()` が、恒星方向を「ケースの `sunDirection` そのまま」ではなく「スライダーの方位角・仰角から組んだ向き」から引くようにする。カメラも同じく、注視点まわりの方位角・仰角・距離から `position` と `lookAt` を組み直す。**恒星の位置とシーン光源の向きを 1 つの向きから引く既存の形は崩さない** |
| `tools/render-lab/lab.ts:96-106` | `show()` がケースを差し替えるとき、スライダーをそのケースの既定値へ戻す(戻さないと前のケースの向きが持ち越されて絵が読めない) |
| `tools/render-lab/main.ts:19-34` | ボタン行を作る `buildButtonRow` と同じ形で、スライダー行を作る下請けを足す。**値を度数で隣へ出す** — 数字が読めないと「仰角 45° で確かめた」と書けない |
| `tools/render-lab/main.ts:36-51` | 恒星の方位角(−180〜180°)・仰角(−90〜90°)、カメラの方位角・仰角・距離(対数)のスライダーを組み、`input` で `view` へ渡す |
| `tools/render-lab/index.html:31-36` | スライダー行の `div` を足す |
| `tools/render-lab/index.html:8-30` | `input[type=range]` の見た目を、既存のボタンと同じ色の変数から組む。**新しい色の直書きをしない** |

**達成条件と検証.**

- `npm run typecheck` が通る。
- `npm run render-lab` で `ship-selfshadow` を開き、恒星の仰角スライダーを 11.6° から 45° へ動かすと、
  **明暗の境界と影の向きが同時に動く**(片方だけ動くなら、恒星の位置とシーン光源のどちらかを
  更新し忘れている)。
- カメラの方位角を 180° 回して**艦をカメラの背後へ回り込ませても、例外が出ずに描き続ける。**
- **スライダーに触れずに `ship-selfshadow` / `ship-cluster` / `leo` を撮り、実施前と画素単位で
  一致する。**(重いケースで撮影が落ちる既知の問題があるので、この 3 つは 1 ケースずつ CDP で撮る。)

---

### 手順 2. 「影スロット」のデバッグ表示を仕様へ足す

**目的.** デバッグ表示はゲーム本体の機能で、`DEVELOP/SPEC/RENDERING.md` に候補が列挙されている。
**仕様はコードより先行するので、実装より先にここを決める。** この手順ではコードに触れない。

**変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `DEVELOP/SPEC/RENDERING.md`(「デバッグ表示」節) | 「候補は次の10種類」を 11 種類へ直し、「影」の項目の直後へ、この文書の目的節に書いた「影スロット」の 2 行を挿入する |

**達成条件と検証.**

- `grep -n "11種類" DEVELOP/SPEC/RENDERING.md` が 1 件。
- `grep -n "影スロット" DEVELOP/SPEC/RENDERING.md` が 1 件。
- `git diff --stat` に `src/` と `tools/` が現れない。

---

### 手順 3. 「影スロット」のデバッグ表示を実装する

**目的.** その画素へ影を落としうる深度マップがどれだったかを絵にする。**手順 4 の切り分けは
この表示に乗る** — 影が消えている画素が「どのスロットの箱にも入っていない(黒)」のか「箱には
入っているが引いた先が空」なのかを、ここで初めて分けられる。

**変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `src/render/pipeline/debug-target.ts:5` | `DebugTargetId` へ `'shadow-slot'` を足し、`DEBUG_TARGETS` の「影」の直後へ `['shadow-slot', '影スロット']` を足す。**HUD 側(`graphics-panel.ts:62`)と `render-lab` 側(`main.ts:44`)はこの表を読むだけなので、どちらも変更不要** |
| `src/render/pipeline/sun-occlusion.ts:233-256` | 「この点がこのスロットの箱の中か」の判定を private メソッドへ切り出す。**遮蔽の合成とデバッグ表示が別々に判定を持つと、片方だけ直したときに嘘の絵になる** |
| `src/render/pipeline/sun-occlusion.ts`(新規メソッド) | 上の判定を使って、その点を覆うスロットの色(スロット 0..3 に赤・緑・青・黄)を返す public メソッドを足す。どれにも入らなければ黒。**手順 5 で選び方を変えたら、この表示も選ばれた 1 枚の色だけを出すよう合わせる** |
| `src/render/pipeline/render-pipeline.ts:126-152` | `compositeMaterials` へ `'shadow-slot'` の 1 枚を足す。G バッファ深度から view 位置を復元し(既存の `viewPositionAt` と `depthDebugProjInv`)、view→描画座標の行列を掛けて上のメソッドへ渡す |
| `src/render/pipeline/render-pipeline.ts:60-63` 付近 | view→描画座標の行列 uniform を 1 本足す(`occlusion.ts` が同じものを持っているのと同じ理由 — composite の `QuadMesh` は固定直交カメラで描かれるので、実カメラの行列は自前で書く) |
| `src/render/pipeline/render-pipeline.ts:236-241` | 毎フレーム `camera.matrixWorld` をその uniform へ写す |

**達成条件と検証.**

- `npm run typecheck` が通る。
- `npm run render-lab` で `ship-cluster` を開き「影スロット」を選ぶ。**艦ごとに違う色で塗られ、
  2 色以上が出る。** 虚空は黒。
- `ship-selfshadow` では 1 色だけが出る。
- 「影」表示に切り替えると 2x2 のスロットが読め、色の数とスロットの埋まり方が一致する。

---

### 手順 4. 影が消える条件を切り分け、遮蔽器の見かけの大きさの測り方を直す

**目的.** 何らかの条件で影がまるごと消える原因を突き止めて断つ。**「決めたこと 1」の候補
(カメラの背後にある遮蔽器がスロットを独占する)は推論なので、直す前に絵で確かめる。**

**着手はまず観察から。** `npm run render-lab` で `ship-cluster` を開き、手順 1 のスライダーで
カメラを回して艦を 1 隻ずつ背後へ送りながら、次の 3 つを同時に読む。

1. **「影スロット」表示** — 手前の艦の色が消え、黒になるか。
2. **「影」表示** — スロットの埋まり方。背後の艦がスロットを占めていないか。
3. **通常表示** — 影が消えるか。

**「影スロット」が黒くなるなら、遮蔽器がスロットを取れていない**ので下の修正へ進む。
**色が残ったまま影だけ消えるなら原因は別** — その場合はこの手順を書き直す。

**変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `src/render/pipeline/sun-shadow-maps.ts:294-303` | `metersPerPixelAt` が、カメラ前方への射影距離ではなくカメラからの距離で測るようにする。**背後の遮蔽器も「遠い」と正しく評価され、`MIN_DEPTH` の床が効かなくなる** |
| `src/render/pipeline/sun-shadow-maps.ts:22-27` | `MIN_CASTER_PX` のコメントが「画面上でこの直径を下回る」と書いているが、距離で測るようになると背後の遮蔽器にも同じ足切りがかかる。**その意味へコメントを合わせる** |
| `src/render/pipeline/sun-shadow-maps.ts:262-280` | 上の変更で並び順と吸収の判定がどう変わるかを確かめる。**背後の遮蔽器が先頭へ来なくなり、吸収の上限も正常な値になる** |
| `src/physics/projection.ts:61-67` | **触らない見込み。** `MIN_DEPTH` の床は透視投影の 1 px の実寸としては正しく、誤っているのは背後の遮蔽器の見かけを射影距離で測っていた呼び出し側。**床そのものを動かすなら `npm run test:physics` を走らせる** |

**達成条件と検証.**

- `npm run typecheck` が通る。`src/physics/` を触ったなら `npm run test:physics` も。
- `npm run render-lab` の `ship-cluster` で、**カメラを 360° 回しても「影スロット」表示に黒く
  抜ける艦が出ない。**
- 同じ操作で、**通常表示の影が消えるフレームが無い。**
- `npm run dev` でカメラと自機を回し、影が消えるフレームが無い。**消えるなら「影スロット」表示で
  その画素を読み、原因を計画へ書き足す。**

---

### 手順 5. スロットの選び方を、テクセルが最も細かい 1 枚へ変える

**目的.** 「箱に入った最初の 1 枚」は並び順に依存していて根拠が無い。**同じ点を複数の深度マップが
覆っているなら、細かいほうが影の形をよく表している。** 積で合成しないのは、不透明な遮蔽器では
透過率の積が半影を二重に濃くするため(決めたこと 2)。

**着手前に、手順 3 の「影スロット」表示で `ship-cluster` を見る。** 複数の色が重なる画素が実際に
あることを確かめてから直す。重なる画素が無いなら、この手順は絵を変えないので落とす。

**変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `src/render/pipeline/sun-occlusion.ts:233-256` | `taken` による「最初の 1 枚で打ち切る」をやめ、箱に入っているスロットのうち `texelWorld` が最小のものを選ぶ。**`If` で囲む形は保つ** — `select` は両辺を評価するので、画面のほとんどを占める虚空の画素からテクスチャフェッチが消えなくなる |
| `src/render/pipeline/sun-occlusion.ts:258-297` | `slotVisibility` へ、UV が 0..1 の外、または深度が 0..1 の外なら 1(遮られない)を返す判定を足す。**箱の判定は法線オフセットぶん(4 texel)だけ枠より広いので、枠の外へわずかに出た点がテクスチャの縁の値を引く経路がある** |
| `src/render/pipeline/sun-shadow-maps.ts:6-7` | 「最初のスロットしか引かない」TODO を、残る限界(受け手が自分の遮蔽器の入っていないスロットを選びうること)へ書き直す。**塊が空間ではなく遮蔽器を分割する以上、選び方だけでは閉じない** |
| `src/render/pipeline/sun-occlusion.ts:230-232` | 同じ趣旨のコメントを、テクセルの最も細かい 1 枚を選ぶ旨へ直す |
| `src/render/pipeline/sun-occlusion.ts`(手順 3 で足したメソッド) | 「影スロット」表示が、覆っているスロットすべてではなく**選ばれた 1 枚**の色を出すよう合わせる |

**達成条件と検証.**

- `npm run typecheck` が通る。
- `grep -n "最初のスロット" src/render/pipeline/` が 0 件。
- `npm run render-lab` の `ship-cluster` 通常表示で、**4 隻すべてに自己影が出ている。**
- 同じケースの「影スロット」表示で、**重なっていた画素が 1 色に決まっている。**
- `ship-in-debris` の絵が `ship-selfshadow` と同じ自己影を保つ(小片群がスロットを奪っていない)。

---

### 手順 6.(保留)すれすれの入射角での自己遮蔽の斑を断つ

**保留の理由.** `render-lab` の `ship-selfshadow` に余計な影は出ておらず、修正前に出ていた斑は
座標変換の誤りが原因だった(決めたこと 3)。**より浅い角度では出うるが、優先度は低い。**

**着手の条件.** 手順 1 のスライダーで `ship-selfshadow` の恒星の仰角を 5° 前後まで下げ、機体の
底面と側面に斑が出ることを確かめる。**出ないならこの手順を落とす。**

**出た場合に変更が必要な箇所.**

| ファイル | 何をするか |
| --- | --- |
| `src/render/pipeline/sun-occlusion.ts:56-58` | 法線オフセット量を定数倍から、入射角に応じて開く形(`texel × 定数 × √(1−N·L²)`)へ直す。**sin を掛けるので、正対する面では 0、すれすれの面でも 1 texel 幅を超えない** — 影が本体から浮かない |
| `src/render/pipeline/sun-occlusion.ts:265-269` | 上を反映する。傾き比例の深度バイアスと上限はそのまま残す |
| `src/render/pipeline/sun-occlusion.ts:233-256` | N·L が 0 へ近づくにつれてメッシュの影の項を 1(遮られない)へ寄せる。**その面の拡散・鏡面はどちらも N·L で 0 へ落ちるので、終端線のごく近傍で影を引くことに絵としての意味が無い。** 寄せ始める角度は定数で直書きし、根拠(1 texel の深度誤差が影の判定を反転させる N·L)をコメントへ書く |

**達成条件と検証.**

- `npm run typecheck` が通る。
- `ship-selfshadow` で、**恒星の仰角を 5°・11.6°・45°・80° と動かしても底面と側面に斑が出ない。**
- 同じ操作で、**突起(アンテナ・放熱板)の影が本体から浮かない** — 影の根元が付け根に接している。

---

### 手順 7.(保留)ライトカメラの枠の設計を見直す

**保留の理由.** 現状の枠の取り方(箱の対角を 1 辺とする正方形)が絵にどれだけ効いているかは、
手順 3 の「影スロット」表示と「影」表示を並べて初めて読める。**現状の把握が先で、実装方針は
そのあとユーザーが決める。**

**着手の条件.** 手順 3・4 を終えた状態で、次を撮ってユーザーへ渡す。

- `ship-selfshadow` の「影」表示 — 1024² の中で艦が占める割合。**枠が余っているほど捨てている。**
- 同じケースの通常表示 — 突起の影の縁の粗さ。
- `ship-cluster` の「影」表示 — 吸収された塊が枠をどれだけ広げているか。

**そのうえでユーザーが実装方針を決める。** 候補として挙がっているのは「ライト空間へ箱の 8 頂点を
射影して枠を密着させる」と「`SHADOW_SLOT_SIZE` を 2048 へ上げる」の 2 つで、**どちらを採るかは
決まっていない。**

---

## 見積り

### 手順 1・2 — GPU への影響

無い。手順 1 はゲーム本体の描画に触れず、手順 2 は文書だけ。

### 手順 3 — 「影スロット」を選んでいる間だけの費用

```text
箱の判定 4 スロット × 6 比較 = 24 命令/画素
1920×1080 = 2.07M 画素       → 50M 命令/フレーム
```

**選んでいないフレームの費用は 0** — 表示ごとに別マテリアルを構築し `quad.material` の差し替えで
切り替える、既存の形に従うため。

### 手順 4 — CPU への影響

`metersPerPixelAt` は塊あたり 1 回。射影距離(内積 1 回)を距離(平方根 1 回)へ替えるだけなので、
フレームあたり最大 4 回の平方根が増える。**測れる差にならない。**

### 手順 5 — 遮蔽パスの増分

いまも変更後も、テクスチャフェッチは選ばれた 1 枚ぶんだけ。

```text
選択後: 1 スロット × (探索 1 + PCF 12) = 13 フェッチ  (現状と同じ)
```

増えるのは箱の判定と `texelWorld` の比較で、スロット 4 枚ぶんの 4 命令程度。

### 手順 7 — 枠を密着させた場合に得られる密度(**採否は未定**)

8×2×2 m の艦なら

```text
現状: (1.05 × 8.49)² = 79 m² を 1024² texel で覆う → 8.7 mm/texel
密着: 約 8.5 × 3.5 = 30 m² の枠
        → 粗いほうの軸で 8.5/1024 = 8.3 mm/texel、細いほうで 3.4 mm/texel
```

**面積で 2.6 倍、細いほうの軸で 2.4 倍。** 立方体に近い遮蔽器(基地)では利得は 1.7 倍程度。

### 手順 7 — `SHADOW_SLOT_SIZE` を 2048 へ上げた場合(**採否は未定**)

```text
深度マップ   2048² × 4 B (r32float) × 4 枚 =  67.1 MB   (現状 16.8 MB)
深度バッファ 2048² × 4 B (float32)  × 4 枚 =  67.1 MB   (現状 16.8 MB)
合計                                         134.2 MB   (現状 33.6 MB)
```

---

## リスクと落とし穴

| リスク | 影響 | 露見する場所 |
| --- | --- | --- |
| **スライダーの既定値がケースの値と一致せず、撮影の絵が変わる** | `.render-lab/shots/` の PNG が全ケースで微妙にずれ、**実施前後の比較がすべて無意味になる** | **手順 1。** スライダーに触れずに撮って、実施前の PNG と画素で比べる |
| **恒星の向きをスライダーへ移したとき、シーン光源の向きを更新し忘れる** | 遮蔽と自己影は新しい向きで出るのに、拡散反射だけが元の向きのまま。**影と陰影の向きが食い違うが、どちらも「それらしく」見えるので気付きにくい** | **手順 1。** 仰角を動かして、影の向きと明暗の境界が同時に動くかを見る |
| **カメラを動かしても、線の分割と球の詳細度が追随しない** | `leo` の円軌道はケースを組んだ時点のカメラで焼かれ、球の詳細度も一度きり。**カメラを大きく動かすと線が粗くなる/球が粗いまま。** 影の観察には支障が無いので許容するが、**それを線の描画の不具合と読み違えない** | **手順 1。** `leo` でカメラを大きく回したときに線が粗くなるのは仕様 |
| **デバッグ表示を足したのに `compositeMaterials` の型が通ってしまう** | `Record<DebugTargetId, ...>` なので新しい ID を足せば型検査が落ちる。**落ちなければ、足した先が間違っている** | **手順 3。** `npm run typecheck` が一度落ちることを確かめてから足す |
| **箱の判定を切り出すとき、遮蔽の合成側とデバッグ表示側で別々の判定が残る** | 塗り分けは正しいのに影が出ない(あるいはその逆)という、**最も読み解きにくい壊れ方**になる | **手順 3。** 判定を書いた場所が 1 か所であることを `grep` で確かめる |
| **「影スロット」が黒くなる画素を「遮蔽器が無い」と読み違える** | 黒は「どのスロットの箱にも入っていない」であって「遮蔽器が居ない」ではない。**この 2 つを混ぜると、手順 4 の切り分けが最初から間違う** | **手順 4。** 黒い画素を見つけたら必ず「影」表示のスロットの中身と突き合わせる |
| **距離で測るようにした結果、カメラのすぐ脇にある遮蔽器が常に最大になる** | 画面に写っていない至近の遮蔽器が並びの先頭を占め続ける。**背後の遮蔽器を捨てないと決めた以上これは仕様どおりでもある** — 影が画面へ伸びてこないなら無駄なスロットを 1 枚使う | **手順 4。** `ship-cluster` でカメラを艦のすぐ脇へ寄せ、「影」表示のスロットが何に使われているかを見る |
| **修正が「消える」条件のうち 1 つしか塞がない** | カメラの背後以外にも、`MIN_CASTER_PX` の足切り・`resolvesInSlot` の棄却・スロット 4 枚の上限という 3 つの経路がある。**どれも「影が消える」という同じ症状になる** | **手順 4。** 消える構図が残ったら、その画素の「影スロット」の色と「影」表示を読み、どの経路かを計画へ書き足す |
| **テクセル最細で選んでも、受け手が自分の遮蔽器の入っていないスロットを選ぶ** | 塊は空間ではなく遮蔽器を分割するので、この経路は選び方では閉じない。**`ship-cluster` で艦の間隔を詰めると片方の影が消える** | **手順 5。** 起きることを確認し、TODO として残す。閉じるには塊の作り方を変える必要がある |
| **枠の外へ出た点がテクスチャの縁の値を引く** | 箱の判定は枠より 4 texel 広いので、縁の 1 列の値が箱の外へ引き伸ばされる。**画面上は「影が枠の外へ帯状に伸びる」** | **手順 5。** `ship-cluster` で、艦の周囲に矩形の縁が見えないかを見る |
| **すれすれの入射角の手当てが、高い仰角の面の影を消す** | N·L で影を弱める処理の効き始める角度が大きすぎると、**斜めから照らされた面の影が薄くなる。** 「柔らかくなった」と読めてしまうので気付きにくい | **手順 6。** 仰角 45° と 80° で突起の影の濃さが同じであることを見る |
| **法線オフセットを増やした結果、影が本体から浮く** | 影の根元が遮蔽器から離れる(ピーターパン現象)。**細い部材ほど出やすい** | **手順 6。** `ship-selfshadow` でアンテナの影の根元が付け根に接しているかを見る |
| **`npm run render-lab:shot` が全ケースを撮り切れない** | 17 番目のタンパク質のケースで CDP の制限時間 60 秒を超えて落ち、**そこで例外が握り潰される。** この計画の変更とは無関係の既知の問題 | **全手順。** 影の観察に使うのは `leo` / `ship-selfshadow` / `ship-cluster` / `ship-in-debris` の 4 つなので、1 ケースずつ撮る |
