# レンズエフェクト

**前提は [`pipeline.md`](pipeline.md)。** とくに §1-5(明るさの目盛り)。
出力段(`render-pipeline.ts`)の中身が担当範囲。

**露出は [`exposure.md`](exposure.md) が持つ。** ここが持つのはレンズ由来の像だけで、
**どちらを先に着手してもよい** — 理由は §1 の末尾。

---

## 1. 順序

```text
露出 → ブルーム → レンズフレア/グレア → トーンマッピング → sRGB
```

**ブルームは最小限。** グローの代用にしない — **グローは大気散乱として実装する**
([`atmosphere.md`](atmosphere.md))。ここが持つのは**レンズ由来のもの**だけで、
**由来で分ける。**

対象は HDR 化済みの発光体。**再突入プラズマは既に動圧という物理量から強度を引いている**
(`REENTRY_GLOW_MIN_Q` / `_FULL_Q`)ので、検証の基準ケースに使える。

**レンズ効果を線形演算に保つ限り、この並びの中で露出とレンズの前後は入れ替えても同じ絵になる**
(`L(e·x) = e·L(x)`)。だから露出([`exposure.md`](exposure.md))とレンズはどちらを先に
着手してもよく、**片方が他方を作り直させることはない。** 閾値(ニー)を1つでも入れた瞬間、
この性質は失われる。

---

## 2. レンズ効果 — 何が欲しいか

**欲しいのはグレア(滲み)・スミア(条)・ゴースト。** どれも実在のレンズ/センサに由来する像で
あって、大気やボリュームの発光とは別物。

- **グレア**: 明るい点の周りの淡いハロー。**広がりは画面上の角度で決まる**(目やレンズの応答なので、
  光源が遠いほど狭くなることはない)。
- **スミア**: 極端に明るい点から伸びる放射状の条。**向きは画面に固定**され、光源に貼り付いて
  回ることはない。
- **ゴースト**: 絞りの反射像。画面中心を挟んだ反対側へ淡く色づいた像を落とす。

**これらは光を増やさない。** レンズへ入った光を画面の中で配り直したものなので、
**滲んだぶんだけ元の光点は暗くなる。**

いま太陽が持っている「実半径の 12.3 倍の板に貼ったグロー」は、**由来で分ければレンズ側のもの**
(仕様も「肉眼で見た太陽が視直径そのものより大きく滲む」と書いている)。しかも
**広がりが太陽の実半径に比例して縮む**ので、角度で決まるべき応答としては向きが逆。
**レンズ段が引き取って廃止するのが筋。**

---

## 3. 太陽が 1px を切る問題

**本作では、遠くから見た太陽が極めて小さな点として描かれうる。**

| 太陽までの距離 | 戦闘視点(55°/1080px)での見かけ径 | `render-lab`(50°/540px)での見かけ径 |
| --- | --- | --- |
| 1 AU | 10.5 px | 5.8 px |
| 5.2 AU(木星) | 2.0 px | 1.1 px |
| 9.6 AU(土星) | 1.1 px | 0.60 px |
| 30 AU(海王星) | 0.35 px | 0.19 px |

ガンサイトズーム(6°)では 9 倍細かくなるので、ちらつく距離もそのぶん遠のく。

### 3-1. 何が起きるか

太陽面の表示値は **4.62e4**(= (AU/R_sun)²)で、画面の白の 4 万倍以上ある。
見かけ径が 1px を切ると、その巨大な値が**乗るか乗らないか**でしか画面に入らない。
4 サンプルの MSAA を掛けても被覆率が 1/4 刻みに量子化されるだけで、
**30 AU では「0」と「4.62e4 の 1/4」を往復する** — サブピクセルの移動に対して総光量が
0 と 2.6 倍(真値比)の間を跳ぶ。

### 3-2. レンズ効果はこの問題を隠さない、**広げる**

**「大気やブルームで既に広がっているから分岐は要らない」は成り立たない。**
広がっているのは**出力**であって、レンズ段の**入力**である太陽の画素値は広がっていない。
しかもレンズ段は線形なので、入力の変動をそのまま出力の変動として運ぶ。

いまは芯の画素がトーンマッピングで白に張り付いていて、**ちらつきが飽和の中に隠れている。**
グレアを入れると、同じ変動が**飽和していないハローの明るさとして画面の広い範囲に出る。**
つまりレンズ効果は、この問題を**見えるようにする側**である。

### 3-3. 論点1 — 効果が二重に掛かることをどう防ぐか

**レンズ段を線形に保てば、防ぐ必要そのものが消える。**

`out = (1−w)·I + w·(K∗I)` の形(K は総和 1 の核)なら、`I = I_太陽 + I_その他` を
分けて計算した結果は分けない結果と厳密に一致する。だから将来「太陽だけ解析合成へ切り出す」を
やるときも、**太陽をバッファから抜いて、抜いたぶんを解析で足し直すだけ**で済み、
他の画素の絵は 1 ビットも変わらない。

- **バッファを分ける必要も、描画順を組み替える必要も、いまは無い。**
- **必要になったときに要る拡張性は 2 つだけ**: レンズ段が線形であることと、太陽が world パスの
  単独の描画物であること(いまも成立している)。
- **閾値(ニー)を 1 つ入れた瞬間にこれが崩れる。** 太陽を抜いた途端、他の物の滲み方まで
  変わってしまう。**閾値を持たないことが、この問題に対する唯一の拡張性の担保。**

### 3-4. 論点2 — つなぎ目で絵が飛ばないようにするには

**両方式が同じ総光量と同じ核を名乗るなら、つなぎ目は原理的に飛ばない。**
飛ぶのは「解析側とラスタ側で明るさの根拠が違う」ときだけ。したがって
**どのサイズで切るかは自由度が高く、そこを詰めるのは本質ではない。**

揃えるなら **2px** — 全天体が既に「見かけ直径 2px 未満では球として描かない」で切っていて、
惑星はそこで輝点へ丸ごと入れ替わる。**太陽だけがこの規則の外にいる**(実装は距離によらず球を
描き続ける)。「常に太陽だけ別方式」も選べるが、そちらは論点4を全距離で自前で解く必要が出る。

### 3-5. 論点3 — 「太陽が小さい」をどう判定するか

**見かけ径では判定しない。**

判定したいのは「ラスタライズが総光量をどれだけ取りこぼすか」であって、それは
**サブピクセル移動に対する画面の総光量の変動**として直接測れる(測り方は実装計画の手順2)。
分岐を作るときの**実行時の条件**は連続量(見かけ径)でよい。**分岐が要るかどうかの判断**は
実測でやる。

### 3-6. 論点4 — 小さな太陽が遮蔽されずに見えることをどう判定するか

**既に答えがある。** 「描画座標の任意の点へ届く恒星光の透過率」を返す関数が 1 本あり
(`pipeline.md` §1-4 の遮蔽の合成規約)、**カメラ位置でそれを評価すればよい。**
天体の球・環の帯・メッシュの影マップが全部同じ規約で効き、半影ぶん連続に変化するので
**遮蔽の判定自体はちらつかない。**

深度バッファを 1 テクセル読む方式は採らない — サブピクセルの太陽に対しては二値でちらつき、
問題を別の場所へ移すだけになる。

### 3-7. いまの立場と、分岐が要ると分かったときの候補

**いまの立場: 分岐は作らない。** まず線形のフラグメント方式を入れ、4-5 の測り方で実測してから
決める。**論点1〜4 はどれも「作る前に決めなければならないこと」ではない**ことが上で片付いた。

分岐が要ると分かったときの候補(優先順):

- **(a) 太陽の点像に角度の下限を与える。** 惑星の輝点(星殻上の固定角の板・全天体一律の表示応答)と
  同じ扱いへ寄せる。滲みの広がりは目/レンズの応答であって光源の大きさではないので、
  **太陽だけが実半径比例で縮んでいる現状のほうが物理的におかしい。**
  実体/輝点の切り替えは全天体が既に 2px で持っている。
  **これはレンズ段の分岐ではなく光源側の表現の切り替えなので、論点1〜4 のどれも発生しない。**
- **(b) 太陽だけ解析合成する。** 論点1〜4 を全部引き受ける。(a) で足りなかったときだけ。
- **(c) SSAA を上げる。** 4 倍でも量子化が 1/16 になるだけで、0.19px の太陽には効かない。**却下。**

### 3-8. 実測 — サブピクセル移動に対する画面平均輝度の変動

`render-lab` の `sun-1au` / `sun-5au` / `sun-30au`(960x540・50°、注視点は手前 10m の艦)で、
カメラ方位を 1px の 1/4(0.02315°)ずつ 4 位相ずらして撮り、sRGB の Rec.709 輝度を画面全体で
平均したもの。**単位は表示値の 0〜255 で、1 LSB = 1.0。**

| ケース | 位相 | 全画素平均 | 飽和(≥250)を除く平均 | 飽和画素数 |
| --- | --- | --- | --- | --- |
| `sun-1au` | 0 | 2.1637 | 1.7463 | 860 |
| | 1/4 | 2.1569 | 1.7428 | 853 |
| | 2/4 | 2.1415 | 1.7264 | 855 |
| | 3/4 | 2.1343 | 1.7173 | 859 |
| | **変動幅** | **0.0294** | **0.0290** | |
| `sun-5au` | 0 | 1.5391 | 1.5250 | 29 |
| | 1/4 | 1.5357 | 1.5212 | 30 |
| | 2/4 | 1.5359 | 1.5214 | 30 |
| | 3/4 | 1.5411 | 1.5275 | 28 |
| | **変動幅** | **0.0053** | **0.0063** | |
| `sun-30au` | 0 | 1.4801 | 1.4791 | 2 |
| | 1/4 | 1.4793 | 1.4788 | 1 |
| | 2/4 | 1.4779 | 1.4774 | 1 |
| | 3/4 | 1.4823 | 1.4818 | 1 |
| | **変動幅** | **0.0044** | **0.0044** | |

**予測どおり、レンズ以前ではどのケースも 1 LSB に遠く届かない。** 変動が飽和した芯の中に
隠れている(§3-2)。

**ただし 5.2 AU と 30 AU の変動幅がほぼ等しいことに注意** — この 0.004〜0.005 は太陽ではなく、
**同じ絵に写っている艦のシルエットの縁**がカメラの回転で動くぶんの床である。30 AU の太陽は
この床の下に埋もれていて、全画素平均では分離できない。**レンズ導入後にこの床を超える変動が
出たら、それが太陽由来のちらつきである。**

---

## 4. その他

- TAA / SSAA は `GraphicsSettings` のトグル配下で任意。
- **AO はここではない** → [`screenspace.md`](screenspace.md)。

---

## 5. 完了条件

- 太陽が 1px を切る距離でフレア/スミアがちらつかない。
- 滲み・条・ゴーストが由来どおりに分かれている — 大気散乱の代用になっていない。

---

# 実装計画 — フラグメントのみのレンズ効果

## 目的

**§2 のレンズ効果を、画面バッファだけを読むフラグメント方式で出力段へ入れる。**
そのうえで **§3-5 の測り方で太陽のちらつきを実測し、解析合成の分岐(§3-7 (b))が要るかどうかを
数値で決められる状態にする。**

**露出は範囲外**([`exposure.md`](exposure.md) が持つ)。レンズ段が線形である限り、露出が
先に入ってもレンズを作り直す必要は無い(§1 末尾)。

## 決めたこと

**どれもユーザーが覆せる。覆されたときにどの手順が変わるかを併記する。**

1. **レンズ効果は線形演算にする。閾値(ニー)を持たない。**
   根拠は §3-3 — 将来の分岐に対する拡張性がこれ 1 つに掛かっている。副次的に、露出との順序が
   自由になる。**覆すなら手順3が変わり、§3-3 の結論も無効になる。**
2. **合成は加算ではなく配り直し** — `out = mix(base, K∗base, w)`、`K` は総和 1 の核、
   `w` はレンズが本来の道から外す光の割合。総光量が保存されるので、解析側とラスタ側が
   同じ量を名乗れる(§3-4)。**覆すなら手順3・4・5 の合成式と、各手順の「較正が動かない
   こと」の検証が変わる。**
3. **太陽の 3D グロービルボードは、レンズ段が引き取って廃止する。**
   根拠は §2 末尾。**残すなら滲みが二重に掛かる。** 覆すなら手順3の `stars.ts` /
   `sun-view.ts` の変更と、手順1の仕様の 1 文が変わる。
   なお **1 AU での見た目の規模はほぼ変わらない** — 現状のグローは幅 129px でほぼ全面が
   白に飽和しており、`w = 0.03` のグレアが作る飽和域は半径 53px(幅 106px)。
4. **解析合成の分岐(§3-7 (b))はこの計画では作らない。** 拡張余地は「線形であること」と
   「太陽が world パスの単独の描画物であること」の 2 つで確保し、仕様の「未確定の案」へ
   1 行だけ残す。
5. **判断の指標は「太陽の見かけ径」ではなく、サブピクセル移動に対する画面平均輝度の変動。**
   閾値は **1/255(表示の 1 LSB)** — これを超えたら目に見える。

## 達成目標

- 戦闘視点で太陽・爆発・噴射炎の周りに滲みと条とゴーストが出る。描画品質設定でオフにすると
  絵から消える。
- デバッグ表示で「レンズ」を選ぶと、配り直された光だけが単独で出る。
- 負荷計測に「レンズ」の行が出て、0 でない値を持つ。
- **較正が動いていない** — `albedo` ケースの最も明るい画素が sRGB (241, 231, 215)、
  `saturn` ケースの環と本体の比が 0.20(どちらも ±1 LSB)。
- **太陽を 1 AU / 5.2 AU / 30 AU に置いた 3 ケース × サブピクセル 4 位相の平均輝度が、
  レンズ導入の前後それぞれについて表になっている。**
- **その数値に基づく「分岐が要るか」の判断が §3-7 へ書かれている。**
- `npm run typecheck` / `npm run test:physics` / `npm run build` が通る。

## 手順

**手順1(仕様を書く)は実施済み** — `DEVELOP/SPEC/RENDERING.md` に「レンズ効果」節があり、
描画パイプラインは 10 段、デバッグ表示は 12 種類、太陽のグローの記述は削除済み。

**手順2(`render-lab` に太陽を出し、レンズ以前のちらつきを測る)は実施済み** — `LabCase` は
`sunDistance` を持ち、`sun-1au` / `sun-5au` / `sun-30au` の 3 ケースがある(グローのビルボードは
組み立て時に 1 回だけ正対させている)。**基準の数値は §3-8。** 測定に使った一時スクリプトは
scratchpad にあり、commit していない — 手順6 で同じものを回す。

**手順3(レンズ段の滲み)は実施済み** — `src/render/pipeline/lens-pass.ts` が半解像度から 5 段の
縮小チェーンと 4 段の拡大チェーンを持ち、**総和 1 の核**(箱縮小 + テント拡大、段ごとの重みは
1 オクターブあたり等エネルギー = 1/角度²)を出す。合成は `out = mix(base, K∗base, 0.03)`。
`lens` トグル・デバッグ表示「レンズ」・GPU 計測の「レンズ」の行・`gpu-timings.ts` の `+=` 化・
太陽のグロービルボードの廃止まで含む。**確かめたことは §6。**

**手順4(条)は実施済み** — `lens-pass.ts` に 1/8 解像度の段から 3 軸 × 両側 28 タップを積む
段がある(条は 6 本)。滲みと条は足し合わせず `mix(glare, streak, 0.2)` で配分を分け合うので、
核の総和は 1 のまま。**向きは画面固定**で、カメラ方位 0°/10°/20° の 3 枚で確かめた。

**手順5(ゴースト)は実施済み** — 1/8 解像度の段から、画面中心を軸に像を縮めて反対側へ置き
直したものを 3 枚重ねる(倍率 −0.30 / −0.55 / −0.80、色みの加重平均は白へ正規化)。
`mix(mix(glare, streak, 0.2), ghosts, 0.08)` で 3 つの核が配分を分け合う。

**切り出し先の名前は計画と違う。** 計画は `lens-flare.ts` に「条とゴーストのフィルタ」を
出すとしていたが、条が使う `sumOf` / `tapAt` を滲みの箱・テントも使うため、その置き場所を
決めると循環 import になる。**核をすべて `lens-kernels.ts` へ出し、`lens-pass.ts` は段の器
だけを持つ**形にした(101 行 / 179 行)。責務の切れ目は「総和 1 の核」と「段の器」。

### 手順6. ちらつきを測り直し、分岐の要否を判断する

**目的**: この計画の本題。**フラグメントのみのレンズ効果を入れた状態で、仕様の要求
(「1ピクセルを切った光点の滲みがちらついたり消えたりしてはならない」)を満たすかを測って
決める。**

**やること**

1. 手順2と同じ一時スクリプトで、3 ケース × 4 位相を測り直す。
2. 位相間の差(全画素平均・飽和を除く平均のそれぞれ)が **1/255** を超えるケースを列挙する。
3. 超えていたら **§3-7 の候補から選び、選んだ理由と測った数値を §3-7 へ書く。**
   **実施はこの計画に含めない**(別の計画になる)。
4. 超えていなければ、その旨と数値を §3-7 へ書く。**分岐は作らない。**
5. 手順2の基準値(レンズ以前)と並べて表にする。**グロービルボードを外したぶんの悪化と、
   レンズ段が広げたぶんの悪化を分けて読む。**

**達成条件と検証**

- §3 の下に、レンズ導入前後 × 3 ケース × 4 位相の表がある。
- §3-7 の「いまの立場」が、測った数値を根拠にした 1 文へ書き換わっている。
- **main へ送るので、変更箇所によらず全部回す**: `npm run typecheck` /
  `npm run test:physics` / `npm run build`。

## 6. 実測(手順3 の時点)

**較正は動いていない。** レンズのオン/オフで同じケースを撮り比べた最も明るい画素:

| ケース | レンズ有り | レンズ無し |
| --- | --- | --- |
| `albedo` | (241, 231, 214) | (241, 231, 215) |
| `leo` | (245, 240, 226) | (246, 240, 226) |
| `saturn` | (167, 144, 104) | (168, 145, 105) |

どれも 1 LSB 以内。`leo` の艦のハイライトの周りに目に見える滲みは出ていない(画面全体でも
最大差 5 LSB で、それは地球のリムの側)。

**CPU** — `sun-1au` でレンズのオン/オフを交互に 8 往復し、`cpuRenderMs.avg` のペア差を取った:
+2.21 / +0.02 / +1.06 / +0.94 / +0.52 / +0.94 / +0.34 / +0.08 ms → **平均 +0.77 ± 0.24 ms**。
計画の規則(0.5ms 超で段数を 5→4)には触れるが、**標準誤差が 0.24ms あって言い切れる差ではなく、
この環境は GPU タイムスタンプが未対応(代替アダプタ)で絶対値そのものが本番より膨らんでいる。**
**ユーザーの判断で 5 段のまま据え置いた。** 実機で測り直してから決める。

**GPU** — **この環境では測れない。** `render-lab` の計測は `gpuSupported: false` を返し、
全パスが 0 になる(レンズに限った話ではない)。見積りの約 51 MB/frame は実測へ置き換えられて
いない。**`gpu-timings.ts` の `+=` 化が効いているかも、ここでは確かめられない** — 実機の
負荷計測ウィンドウで「レンズ」の行を見るのが唯一の確認手段。

**手順4 で踏んだ地雷(2 つとも計画に無かった)**

- **タップの間隔を等比で置くと、条が点線になる。** タップ 1 つ 1 つが光源の複製として見え、
  間が埋まらない。**間隔は 1 テクセル固定にする**しかなく、そのぶん届く距離は段の解像度が
  決める(細かい段からは遠くまで届かない)。1/4 から 1/8 へ落として釣り合わせた。
- **タップの和を左畳みで書くと、WGSL のパーサが再帰の上限に当たってシェーダごと落ちる。**
  `a.add(b).add(c)...` は括弧が項数ぶん深く入れ子になる。168 項で落ちた。**平衡木で畳む**
  (`sumOf`)。例外は出ないが、ブラウザのコンソールへは検証エラーが出る — **`render-lab` の
  撮影スクリプトはページのエラーを拾うので、そこを見ていれば気付ける。**

**手順5 で確かめたこと**

- `sun-1au` で、太陽と画面中心を結ぶ直線上に 3 枚のゴーストが出る。位置は解析どおり
  (太陽 (633, 183)・画面中心 (480, 270) に対し (373, 550) / (202, 428) / (289, 379))。
- カメラ方位を −12° にして太陽を画面中心へ寄せると、3 枚とも中心側へ寄る。
- `albedo` / `leo` / `saturn` の最も明るい画素は条・ゴーストを足しても動かない
  (241,231,214 / 245,240,226 / 167,144,104)。

**その他に確かめたこと**

- `antialias` を切った(MSAA 無し)ビルドでも `sun-1au` / `albedo` が同じに写る。NaN 由来の
  全面黒・全面白は出ない(黒画素の割合 0.838、白画素の割合 0.0002)。
- デバッグ表示「レンズ」に、配り直された光(太陽のハローと、艦のハイライトのごく淡い滲み)
  だけが出る。
- 模式図スタイルではレンズ段を呼ばない — `render()` の `style === 'schematic'` の分岐の中に
  レンズ段が無い(構造上そうなっている)。`render-lab` は表示スタイルを切り替えられないので、
  絵での確認は実機に残る。

---

## 見積り

**GPU** — 1920×1080・rgba16f(8 B/px)で、レンズ段が読み書きするバイト数から:

| 内訳 | バイト数 |
| --- | --- |
| 縮小 5 段(全解像度読み 16.6MB + 半解像度書き 4.1MB、以降 1/4 ずつ) | 約 27 MB |
| 拡大 4 段(各段で下地読み + 書き) | 約 15 MB |
| 条(1/4 解像度の読み書き) | 約 4 MB |
| ゴースト(1/8 解像度) | 約 1 MB |
| 合成が半解像度のレンズを追加で読む | 4.1 MB |

合計 **約 51 MB/frame**。帯域 100 GB/s なら 0.51ms、200 GB/s なら 0.26ms。
**既存の world パスが 2.6ms なので、この見積りなら受け入れられる。**
実測は `render-lab` の計測が出す「レンズ」の行で置き換える。

**CPU** — `renderer.render()` の呼び出しが 11 回増える(縮小 5・拡大 4・条 1・ゴースト 1)。
滲みだけの 9 回で **+0.77 ± 0.24 ms**(§6)。**段数は 5 のまま据え置いた。**

**滲みの規模**(`w = 0.03`、1/r² の広がりを 1〜1000px で正規化した粗い見積り):
1 AU の太陽(表示値 4.62e4・面積 86.6px²)が配る光は 1.2e5 で、半径 53px までが表示値 1 を
超える = 白に飽和する。**現状のグロー(幅 129px・表示値 240)とほぼ同じ規模**なので、
1 AU での見た目は大きく変わらない。

## リスクと落とし穴

| リスク | 影響 | 露見する場所 |
| --- | --- | --- |
| GPU 計測が 1 パスにつき最後の 1 回しか記録しない | レンズの行が実際の 1/10 を表示し、負荷を過小評価したまま先へ進む | **手順3**(`+=` へ変える)。マテリアル/大気のデバッグ表示中の二重描画も足し算になるが、実際に GPU がやった仕事なのでそれが正しい |
| half float の上限 65504 と太陽面の 4.62e4 | 重み和が 1 を超えると Inf → `mix` を通って画面全体が NaN。**例外も警告も出ない** | **手順3**(核を正規化で組む。`antialias` オン/オフ両方で `sun-1au` を撮る) |
| MSAA ターゲットの読み方 | 解決テクスチャを経由しない読み方をすると、three が無言で 1 サンプル目だけを読む | **手順3**(`antialias` オンで縁がざらつくかを見る) |
| 模式図でレンズ段を呼ぶ | 白背景の輪郭図に滲みが乗る、または空のターゲットを読んで真っ黒になる | **手順3**(スタイルを模式図へ切り替えて `leo` を見る) |
| グロービルボードを外したぶんの悪化が、レンズのせいに見える | 手順6の判断を誤る | **手順6**(手順2でグロー有りの基準値を取ってあるので切り分けられる。表を並べる) |
| トーンマップ後の平均輝度は芯が飽和していて鈍い | ちらつきを過小評価する | **手順2・6**(飽和画素を除いた平均も一緒に出す) |
| `tools/render-lab/` に未コミットの変更がある(表示スタイル切り替え) | 手順2の編集が衝突する | **手順2**(着手前に `git status` を見て、衝突するなら先に commit するか退避する) |
| `pipeline.md` §2 のパス表が 9 段のまま古くなる | 次に読む人がパス構成を読み違える | **手順3**(`memos/` は指示があったときだけ書き換えるので、1 行足してよいかユーザーへ確認する) |
| 太陽を画面へ入れると周囲が白く飛び、戦闘中に照準が読めなくなる | 物理的には正しいが遊べない | **手順3**(`npm run dev` で太陽を正面に入れる。遊びのための例外が要るなら、それは仕様なので `SPEC/COMBAT.md` のマズルフラッシュ減光と同じ扱いで書く) |
| タップ数の多いフィルタの和を左畳みで書く | WGSL のパーサが再帰の上限に当たり、そのマテリアルの描画が丸ごと無効になる。**JS の例外は出ない** | **手順4で露見済み。** `sumOf` で平衡木に畳む。手順5 でタップを増やすときも同じ |
| 露出([`exposure.md`](exposure.md))が先に入るとレンズを作り直す羽目になる | — | **起きない。** 線形なので露出とレンズは交換可能(§1 末尾)。**閾値を入れた瞬間に成立しなくなる**ので、そこだけ手順3で守る |
