# グラフィック基盤 — ロードマップ 第四版

第三版(`for_agent_v3.md`)の光と影の扱いを、4 つの独立した軸へ整理し直したもの。
**この版が有効で、第一〜三版は破棄する。**

個々の見た目の仕様は `DEVELOP/EARTH_MOON_GRAPHICS_PROPOSAL.md` /
`DEVELOP/PHYSICAL_RING_RENDERING_PROPOSAL.md` が持つ。この文書はその下に敷く土台だけを扱う。

**適用範囲の前提**: LEO で優れた性能が出ることは最低要件だが、**太陽系全域で破綻しないこと**が
同時に要件である。「LEO でしか使わないから」を実装する/しないの根拠にしてはならない。
カメラは既に太陽系全体を見渡せる(`OverviewCamera`)ので、そこで破綻する設計は採れない。

---

## 0. 第三版からの変更点(要約)

第三版は「エリアライト」と「ソフトシャドウ」を 1 つの話として書いていたが、実際には
**互いに独立した 4 つの軸**が混ざっていた。第四版はこれを分離する。

| 軸 | 問い | 第三版 | 第四版 |
|---|---|---|---|
| **軸 1** | **反射** — 光源の大きさを、受け手はどう積分するか | §1-5/§1-6 に混在 | **§1-5**。太陽=球光源、天体=ランバート球。遠方で点光源へ**滑らかに縮退する単一の仕組み** |
| **軸 2** | **太陽光の遮蔽** — 影をどう作るか | 「解析 3 層 + CSM」 | **§1-6。全面的にシャドウマップへ統一。** 解析的リング影・解析的球影を GPU から廃止 |
| **軸 3** | **地球照の遮蔽** | 言及なし | **§1-7。無視するか AO 近似。** 全天体からシャドウマップを撮るのは原理的に不可能 |
| **軸 4** | **CPU 側の日照率** — 熱・電力・SRP | §1-4 に混在 | **§1-8。描画とは別問題として独立。** 描画へ流用するのはアドホックなので避ける |

**第三版の重大な誤り(訂正)**: 第三版 §1-4 は「**シャドウマップでリングは解けない**」と結論し、
解析的遮蔽器 3 層構成を採った。**この結論は誤りである。** 根拠にした計算(天王星 λ 環の
幅 1.5e3 m / 半径 4.2e7 m = 比 3.6e-5、2.8 万テクセル/軸が必要)は、
**リング系全体を 1 枚のシャドウマップで覆うことを暗に前提していた。**
画面上のスケールでカスケードすれば、λ 環の影の境界が見えるほど寄っているときには
カスケードが覆う範囲もその分狭い。**必要な解像度は「対象の絶対寸法」ではなく
「画面上で見えている範囲」で決まる。** 訂正の詳細は §1-6。

その他の変更:

| # | 第三版 | 第四版 |
|---|---|---|
| 5 | 遮蔽の統一 = 解析 3 層 + `sunVisibility` | インターフェース `sunVisibility` は維持。**中身が解析 3 層からシャドウマップ合成基盤へ変わる** |
| 6 | エリアライトは Phase 8(終盤) | **軸 2(影)を軸 1(反射)より先に置く。** 影はパイプライン構成と密結合、反射は点光源から後で差し替え可能 |
| 7 | `physics/shadow.ts` の修正は Phase 7 に同梱 | **完全に独立させる(軸 4)。** どの時点でやっても計画に影響しない |

**実装順(確定)**: **軸 2 → 軸 1 → 軸 4 → 軸 3**。理由は §4 冒頭。

第三版で確定済みの判断(§1-1 露出 / §1-2 座標境界 / §1-3 深度カスケード / §1-4 ライトプリパス /
Phase 0 の LOD 最優先)は**そのまま引き継ぐ**。

---

## 1. 確定した判断

### 1-1. 露出 — 放射照度を基準に取り、既存の手調整値は捨てる

**方針(確定)**: HDR 線形空間へ移した上で、物理的根拠のある式へ組み替える。視覚的な問題が出たら
**HDR 空間の上で**再調整する。LDR 時代の手調整値へ戻す方向の調整はしない。

```text
E(d) = SOLAR_CONSTANT * (AU / d)^2      [W/m^2]    — 距離 d における放射照度
L_surface = E(d) * albedo / pi          [W/m^2/sr] — 完全拡散反射面の放射輝度
exposure  = 1 / L_ref                              — L_ref を中間グレーへ写す係数
```

必要な量は**すべて既にある**: `SOLAR_CONSTANT = 1361`(`game/const.ts:65`)、
`ASTRONOMICAL_UNIT`(`physics/srp.ts:9`)、逆二乗則(`physics/srp.ts:24`)、
`STEFAN_BOLTZMANN` / `HULL_EMISS`(`game/const.ts:44-45`)、機体温度(`game/const.ts:47-51`)。

**唯一の欠落はアルベドである。** 天体表面は 101 天体ぶんの 16 進直書き色(85 リテラル、ユニーク 63 種、
`celestial-registry.ts:88-195`)しか持たず、アルベド値は 1 つも無い。根拠コメントも無い。
**これが露出フェーズの最大の実作業になる。**
さらに §1-5 により、**アルベドは「表面の見え方」ではなく「他の物体を照らす光源の強さ」を兼ねる。**

`SOLAR_PRESSURE_1AU = 4.56e-6`(`physics/srp.ts:6`)は `SOLAR_CONSTANT / c` と同じ量の二重表現。
片方をもう片方から導出する形へ統一すること。

**既にこの基準で書かれている部分がある。** リングだけが完全な放射伝達モデルになっている —
`RingOpticsDef` の単一散乱アルベド・法線光学的厚さ・HG 非対称因子が全 35 帯に出典コメント付きで入り、
`physics/ring-optics.ts:50-64` の `ringSingleScattering` は明示された単位を持つ。現状はこれを
`RADIANCE_SCALE = 0.72`(`render/ring.ts:30`)という**手調整係数 1 個**で LDR へ押し込んでいる。

> **露出の校正はリングを基準に取る。** `RADIANCE_SCALE` が 1.0 になることを完了条件の一つにする。

**HDR 化で構造変更が要る箇所**: `Billboard` は `MeshBasicMaterial.opacity`(0..1 クランプ)で
明るさを表しており、`transparent + AdditiveBlending` の 5 経路の最大のものがここを通る
(`render/billboard.ts:16-18`)。1 を超える輝度を出せないので、ノードマテリアルへ置き換える。

### 1-2. 座標境界 — 浮動原点を描画から切り離す

**方針(確定)**: 描画はすべてフローティングオリジン座標系(`THREE.Vector3`)で行い、
GPU に ECI 座標(独自 `Vec3`)が渡らないことを徹底する。

- **`render/` の `FloatingOrigin` 依存は 2 箇所だけ** — `orbit-line.ts:119`、`sampled-line.ts:197`。
- **ECI 絶対座標が uniform で GPU へ渡っている箇所は 0 件。現時点で単精度の破綻は無い。**
- ただし**大きな絶対値が f32 バッファに載る系統が 2 つある**:
  `orbit-line.ts:76,261-263` の頂点(中心天体相対、海王星軌道 ~4.5e12 m → f32 量子化 ~3e5 m)と
  `point-field-view.ts:92-96` の `instanceMatrix`(太陽中心、カイパー帯 ~7.5e12 m → ~5e5 m)。
  **引きの絵では画素以下だが、寄ると露見する。Phase 1 で実測する。**
- **本丸は `sampled-line.ts`** — 絶対 ECI を取る関数型 `ScaleAtFn`(`:28`)を `render/` 自身が定義している。

**切り離しの方法**: (1) 頂点列の生成(bake + Hermite 細分。THREE 非依存)を `game/` へ出し、
`render/` は「渡された配列をアップロードする」だけにする。(2) 表示 LOD ポリシー
(サグ `MAX_EDGE_SAG_PX = 0.5`、折れ角上限 5°)は描画の判断なので `render/` に残すが、
ECI を取らない純関数 `chordCountFor(turnRad, chordLen, metersPerPixel)` として公開する。

**完了条件**: `src/render/**` から `Vec3` / `KinematicState` / `ReferenceFrame` / `Attractor` /
`Ephemeris` / `FloatingOrigin` への import が 0 件。

### 1-3. 深度カスケード — 測ってから決める。変換は一か所に閉じる

**方針(確定)**: 距離圧縮の撤去は GPU 上の深度精度が担保できるかが鍵であり、やってみないと分からない。
**スパイク → 測定 → 判断 → 本実装**の順。判断が否なら距離圧縮は残る(影響は §1-6 と §6)。

判断を楽にする事実:

- **`physics/projection.ts` は near/far に一切依存しない**(`:10-16, 19-37, 53-57`)。
  → **fov/aspect を共有する限り `ProjectFn` / `ScaleFn` の全消費者(マーカー約10系統、picking 5系統、
  plan 系、`sampled-line` の頂点密度、`ring-view` の LOD、`point-body` の 2px 判定)は無変更で通る。**
- **投影行列を書く箇所は `camera/camera-system.ts:73` の 1 箇所だけ。**
- **`COMBAT_CAMERA_FAR = 6e7` は `MOON_VIS_DIST = 4.5e7` から逆算された値**(`const.ts:120-123`)。
- **地球だけは既に真位置・実半径**(`earth-body.ts:34`)。真スケール描画の前例が既にある。

**カスケードと浮動原点**: 浮動原点は 1 つのまま、カスケードは near/far だけを持つ。
遠方カスケードでは要求される絶対精度も緩いので釣り合う。**仮説であり測定対象そのもの。**

**変換の切り出し(要件)**: ECI → 描画位置の順変換は **5 種類に分散**し、逆変換は **1 つも無い**。
`FloatingOrigin.RtoThreeV3` / `sphere-body.ts:76-81` / `point-body.ts:141-147` /
`sun-body.ts:54-62` / `point-body.ts:177-181` / `environment-scene.ts:170-171`。
1 モジュール(`render/view-space.ts`)へ集約し `toRender` / `toEci` / `cascadeOf` / `cascadeNearFar` を公開する。
**圧縮を採るにせよ撤去するにせよ、この集約は先にやる。**

**測定項目**: (a) 分割数 2/3/4 での z-fighting、(b) パス増加によるフレーム時間、
(c) `depthTest: false` の 3 箇所(`stars.ts:28` / `earth.ts:92` / `plan-gizmo-3d.ts:32`)が
深度クリアと衝突しないか、(d) `frustumCulled = false` の箇所が多いので `camera.layers` 振り分けが効くか。

**先行して必要な小作業**: `types/three-shims.d.ts` に `clearDepth()` が無い(`:6-19`)。

### 1-4. ライトプリパス(3 パス構成)

**方針(確定)**: 2 パス遅延シェーディングをやめ、**3 パスのライトプリパス**を採る。

```text
Phase A  ジオメトリパス   → 深度 + 法線 + 粗さ            (幾何だけ。マテリアルの色を持たない)
Phase B  ライティングパス → 拡散照度バッファ + 鏡面照度バッファ
                            光源の形の積分・遮蔽問い合わせはここに閉じる
Phase C  レンダリングパス → 再度ジオメトリを描き、照度バッファを読み、材質を掛ける
Phase D  半透明 + ポストプロセス
```

**なぜ 2 パスでは足りないか**: 遅延シェーディングでは「光源の立体角積分」と「BRDF 評価」が
同じシェーダに同居する。§1-5 で光源の種類(球・ランバート球・点への縮退)を増やす以上、
マテリアル分岐との掛け算で膨らむ。ライトプリパスは**照度という中間表現で両者を切る**:

- **ライティングパスは「その点にどの向きからどれだけ光が来ているか」だけを答える。**
- **レンダリングパスは「来た光をこの材質がどう返すか」だけを答える。**

**トレードオフ**: ジオメトリを 2 回描く(ドローコール増)代わりに G-buffer が狭くなり、
マテリアルの多様性と光源の多様性が独立に増やせる。

**既知の限界**: 鏡面反射は「入射方向 × 視線 × 材質」に依存するので照度バッファ 1 枚に畳めない。
標準的な対処は**粗さをジオメトリパスに書き、その粗さで積分した鏡面項を別バッファに出す**こと。
拡散反射が支配的な天体表面 + 少数の金属的な艦船という本作の絵では破綻は小さいと見込むが、
**破綻したら艦船だけを前方描画へ逃がす**(ドローコールは既に 2 回なので構成は壊れない)。

**器を先に作る**: 光源の実装(§1-5)も影の実装(§1-6)も、この器が無いと載せる場所が無い。
したがって**器は序盤に作り、その時点では単一の平行光(現状と同じ絵)を通す。**
絵が変わらないことをもって器の正しさを確認する。

---

### 1-5. 軸 1 — 反射(光源の大きさをどう積分するか)

**方針(確定)**: 光源が十分近く大きいときは面光源、十分遠く立体角が小さいときは点光源。
**これを分岐ではなく、単一の式が滑らかに縮退する形で実現する。**
太陽にも地球にも月にもその他の天体にも、同じ仕組みが適用される。

**なぜ必要か(立体角)**:

| 光源 | 視点 | 視半径 | 立体角 | 全天比 |
|---|---|---|---|---|
| 太陽 | 1 AU | 0.266° | 6.8e-5 sr | 5.4e-6 |
| 太陽 | 水星近日点 0.31 AU | 0.86° | 7.1e-4 sr | 5.6e-5 |
| 地球 | LEO 420 km | **70.0°** | **3.90 sr** | **31%** |
| 地球 | 静止軌道 | 8.6° | 0.071 sr | 0.56% |
| 地球 | 月軌道 | 0.95° | 8.6e-4 sr | 0.007% |
| 木星 | エウロパ軌道 | 11.5° | 0.126 sr | 1.0% |

**太陽ですら完全な点ではなく、地球は LEO で全天の 31% を占める。**
そして**同じ地球が月軌道では点光源と区別がつかない。** 近距離用と遠距離用に別モデルを持つのではなく、
**距離とともに連続に縮退する 1 つのモデル**でなければ、遷移する場所で絵が飛ぶ。

#### 1-5-1. 太陽(恒星) — 一様球光源

最も単純な形。球全体が一様な放射輝度を持つ。**一様球光源からの拡散照度には閉じた解があり、
その式は距離が大きくなると自動的に逆二乗則へ縮退する。** したがって
**「遠いときは点光源に切り替える」という分岐は書かない** — 同じ式が両方を覆う。

分岐が要るのは鏡面項だけ。LTC(Linearly Transformed Cosines)による球光源の積分は
角半径が小さいと数値的に無駄なので、閾値で点光源近似へ落としてよい
(拡散項と違い、鏡面のハイライトは光源が小さければ本当に点と区別がつかない)。

#### 1-5-2. 惑星・衛星 — ランバート球(部位によって照度が違う球)

**太陽と決定的に違うのは、球面の一部しか光っていないこと。** 昼側だけが光り、
艦から見ると三日月形になる。一様球光源では表せない。

**近似(確定)**: **ランバート球**として扱う。球面上の各点の放射輝度が
`L(p) = E_sun * albedo * max(0, dot(n_p, sun_dir)) / pi` で与えられる球。

- **一様球ではないので LTC の標準的な適用範囲外である。** これは承知の上で採る近似であり、
  検証ケース(三日月形に見える位置での地球照)で妥当性を確かめる必要がある。
- 実装の候補は 2 つある。(a) 照らされている半球を**等価な円盤へ代表させ**、その円盤に LTC を適用する
  (位相角に応じて円盤の面積と位置をずらす)。(b) 球光源の閉じた解に**位相係数**を掛ける
  (正しくない — 明るさは合うが、光が来る「向き」の偏りが表現されない)。
  **(a) を採る。** 光が来る向きの偏りこそが軌道上の絵の特徴だから。
- **色を持つ光源になる。** 地球は海の青・雲の白・陸の褐色を持つので、艦の影側が青みを帯びる。
  初版は全球平均色でよく、局所分布(雲がある側が明るい)は将来の改良。

#### 1-5-3. 光源の打ち切り

**打ち切りの基準は距離ではなく立体角。** 立体角が閾値を下回った光源は
点光源として扱う(縮退は式が自動でやるので、実際には「LTC を使うか点光源で済ますか」の判断だけ)。
さらに小さければ照度そのものが無視できるので光源リストから外す。
**太陽系全域で、光源リストは実用上「太陽 + 最も近い 1〜2 天体」に収まる。**

#### 1-5-4. 現状との差

今は `AmbientLight(0x8899bb, 0.25)`(`environment-scene.ts:87`)という
**方向も距離も位相も持たない定数**が、地球照・星明かり・多重散乱の全部を兼ねている。
軸 1 の完成はこの定数を消す作業でもある。

#### 1-5-5. なぜ軸 1 を後回しにできるか

**ライティングパス(Phase B)とレンダリングパス(Phase C)が分離されていれば、
軸 1 の変更は Phase B の中に閉じる。** 最初は太陽も天体も点光源として実装しておき、
後から球光源・ランバート球へ差し替えられる。**受け手側のコードは一行も変わらない。**
これが軸 2 より優先度を下げられる理由である。

---

### 1-6. 軸 2 — 太陽光の遮蔽(シャドウマップへ全面統一)

**方針(確定)**: 太陽光の遮蔽は無視できず、**ソフトシャドウ的な照度変化の再現が要る。**
太陽の立体角が小さくても、**半影のスケールは遮蔽器までの距離に比例して大きくなる**ので無視できない。
そして**以下はすべて同じ現象であり、同じ基盤(シャドウマップ)で表現されるべきである**:

- 地球の影に出入りする艦の照度変化
- 艦のセルフシャドウ
- 月食の境界
- 土星のリング影の境界

**第三版の解析的遮蔽器 3 層構成は破棄する。**

#### 1-6-1. 第三版の「シャドウマップでリングは解けない」は誤りだった

第三版の根拠は「天王星 λ 環(幅 1.5e3 m @ 半径 4.2e7 m、比 3.6e-5)に 1 テクセル割り当てるだけで
2.8 万テクセル/軸が要る」だった。**この計算はリング系全体を 1 枚で覆うことを前提していた。**

**必要な解像度は対象の絶対寸法ではなく、画面上で見えている範囲で決まる。**
λ 環の影の境界が見えるほど寄っているとき、カスケードが覆うべき範囲も同じだけ狭い。
逆に、リング系全体が画面に収まるほど引いているなら、λ 環の影は 1 px 未満なので解像する必要がない。

**この「画面上のスケールで解像度を決める」という原則が、軸 2 の設計全体を貫く。**
そして同じ原則が §1-3 の深度カスケードにもある。**カスケード選択のポリシーは 1 つのモジュールが持つ。**

#### 1-6-2. カスケードの軸は「カメラからの距離(画面上のスケール)」

遮蔽に関わる寸法は **1e0 〜 1e10 m** に開く。

| 遮蔽の対象 | 寸法 |
|---|---|
| 艦の構造 | 数 m(`PLAYER_HULL_RADIUS = 2.6`、全長 5〜6 m) |
| 追跡カメラ距離 | 12〜8000 m |
| 戦闘域 | 10〜30 km |
| 惑星本体 | 1.7e6〜7.2e7 m |
| リング系 | 6.7e7 〜 1.3e10 m(土星フェーベ環) |

1 枚では不可能。しかし**カスケードを「光源からの距離」ではなく「カメラからの距離」で切れば解ける**:

- **艦がカメラから十分遠ければ(画面上で十分小さければ)、そのセルフシャドウが高解像度である必要はない。**
- **ほぼ点にしか見えない惑星の蝕は、そもそも無視できる。**

これで**艦のセルフシャドウ・月食の境界・リング影を統一的に扱える。**

#### 1-6-3. 難所 — 「地球の影に出入りする艦」

**カメラから十分に近い被遮蔽物に対して、無視できない規模の巨大な遮蔽物がある**という構造。
艦(5 m)と地球のシルエット(1.3e7 m)の比は 2.6e6 で、1 枚のマップには載らない。

**解決**: **遮蔽器を 1 枚のマップへ排他的に割り当て、受け手は自分を含む全マップに問い合わせて積を取る。**

- 地球は**粗いカスケード**の遮蔽器になる。艦はそのマップの中では点でよい。
  必要な精度は「終端(明暗境界)の位置」だけであり、これは低周波の量である。
- 艦は**細かいカスケード**の遮蔽器かつ受け手になる。ここで自己影が解像される。
- 艦は**両方の受け手**であり、遮蔽度は積で合成される。

**排他割り当てが必要な理由**: 同じ遮蔽器が 2 枚に入ると、積を取ったときに二重に暗くなる。
「各遮蔽器はちょうど 1 枚のマップに属する」を不変条件にする。

**Z 精度**: マップごとに近接範囲が決まるので、**Z 精度もマップ単位でカスケードする**のが順当。
これに失敗した場合の退路は §1-8 の CPU 側計算を描画へ流用することだが、
**これはアドホックな方法なので可能な限り避ける。**

#### 1-6-4. 太陽は方向光源ではない(全方向へ照らす)

**カメラのスケールによって、太陽が一方向と近似できる場合とできない場合がある。**

- 特定の惑星に寄って惑星や艦を見ている視点では、狭い画角のシャドウマップで足りる。
- **太陽系全体を見ている視点では、太陽は全方向へ光を放っているため、180° を超える画角が要る。**
  しかし太陽系はほとんどが虚空なので、そのほとんどの情報が捨てられる。

**解決**: **1 枚の広画角マップを組まない。オブジェクトが実際に存在する方向だけを狙い撃ちする。**

遮蔽器を空間的にクラスタリング(実質的には「惑星とその衛星と周辺の艦」の単位)し、
**クラスタごとに 1 枚のマップを組む。** 太陽から見たクラスタの角度的な広がりは常に極小
(太陽から見た地球は 4.3e-5 rad)なので、**クラスタごとに平行投影で十分**であり、
広画角の透視投影は最初から要らない。視差誤差はクラスタの角度幅で抑えられる。

さらに、**画面上で 1 px 未満のクラスタはマップを組まない。**
太陽系全体を見ている視点では、ほぼすべてのクラスタがこれに該当する。
「無駄の多い広画角マップ」は、そもそも作られない。

#### 1-6-5. 一段階抽象化した要件

以上をまとめると、必要なのは:

> **1 つの恒星に対して、複数の画角・複数の Z 精度・複数の範囲のシャドウマップを組み合わせ、
> 遮蔽度を合成する基盤。**

```text
ShadowSet
  maps: ShadowMap[]              各マップ: 投影(向き・範囲・near/far)・解像度・所属する遮蔽器
  assign(occluder) -> map        遮蔽器はちょうど 1 枚に属する(二重計上の防止)
  select(cameraDist, screenPx)   画面上のスケールで必要なマップだけを組む
  visibility(worldPos) -> [0,1]  自分を含む全マップの積
```

受け手側の入口は §1-4 で置いた `sunVisibility(worldPos, normal)` のまま変わらない。
**中身がこの合成基盤へ差し替わるだけ**であり、天体表面・艦の BRDF・リング・大気の呼び出しは無変更。

#### 1-6-6. 半影(ソフトシャドウ)

太陽の視半径は 1 AU で 4.65 mrad。**半影の幅は遮蔽器から受け手までの距離 d に対して `d × 9.3e-3`。**

| 状況 | d | 半影幅 | 対象の寸法 | 比 |
|---|---|---|---|---|
| 艦の自己影 | 5 m | 4.7 cm | 5 m | 1% — 「わずかに柔らかい」 |
| 艦が地球の影へ | 1e6 m | 9.3e3 m | 5 m | **艦は完全に階調の中** — 実質オブジェクト単位の連続値 |
| 月食の境界 | 3.8e8 m | 3.5e6 m | 月 3.5e6 m | **半影が月と同じ大きさ** — 境界が極めて柔らかい |

**遠いほど半影が支配的になる。** PCSS(Percentage-Closer Soft Shadows)でブロッカー距離を
推定し、**フィルタ半径を太陽の角半径から導く。チューニング値ではなく物理量である。**
これで `SHADOW_PENUMBRA = 6e4`(`physics/shadow.ts`)の直書きも、
`inBodyShadow`(`render/ring.ts:71-77`)が二値であることも、同時に解消する。

#### 1-6-7. 半透明な遮蔽器 — 深度だけでは足りない

**リングは不透明ではない。** `RingOpticsDef` の法線光学的厚さは 2e-8 〜 8 の範囲にあり、
帯によって透過率が桁で違う。**深度のみのシャドウマップは「遮った / 遮らない」しか表せず、
Beer–Lambert の透過率を表せない。**

したがって、**遮蔽器の種類によってマップの中身が変わる**:

| 遮蔽器 | マップに書く量 |
|---|---|
| 艦・デブリ・基地・天体本体 | 深度(不透明) |
| リング帯・(将来)大気 | **累積光学的厚さ、または透過率** |

これは既知の技法(deep shadow map / Fourier opacity map の系統)であり、
**軸 2 の基盤設計に最初から入れておく必要がある。** 後から足すと合成規則が変わってしまう。

#### 1-6-8. 距離圧縮への依存

**リング影シェーダは現状「真の方向 × 偽の位置」を混ぜている。**
`sunDirNode` は天体の**真の ECI 位置**から引いた方向(`sphere-body.ts:64-65`)なのに、
`positionWorld` と 32 バンドの `center` / `inner` / `outer` は**圧縮された描画位置・描画長**
(`celestial-surface.ts:128-150`)。`ring.ts` の `bodyRadius` にも描画スケールが入る。

**距離圧縮を撤去しない限り、遮蔽を物理的に正しく統一することはできない。**
これが §1-3 のカスケード判断と軸 2 を結びつける決定的な依存関係である。

#### 1-6-9. 廃止されるもの

軸 2 の完成に伴い、以下は GPU から消える:

- `render/celestial-surface.ts:63-91` の**固定長 32 バンド uniform 配列**によるリング影。
- `render/ring.ts:71-77` の解析的 `inBodyShadow`(二値)。

**解析的な球影は CPU 側(軸 4)にのみ残る。** 描画は全面的にシャドウマップへ移る。

---

### 1-7. 軸 3 — 地球照の遮蔽(優先度低)

**方針(確定)**: 太陽光と比べて照度のオーダーが小さいので、**無視するか、AO として近似する。**

**軸 2 と同じ方法は原理的に採れない。** シャドウマップは光源ごとに要る。天体は 101 個あり、
そのすべてから撮ることはできない。§1-5 で光源リストが「太陽 + 最も近い 1〜2 天体」に
収まるとしても、面光源(ランバート球)に対する影は点光源に対する影よりさらに高価である。

**採る手段**:

1. **無視する。** 初版はこれでよい。地球照は太陽光の 1e-2 〜 1e-3 のオーダーであり、
   その中の遮蔽の差はさらに小さい。
2. **AO として近似する。** 幾何的な遮蔽度(周囲がどれだけ開けているか)を
   地球照に掛ける。方向を持たない近似だが、艦の凹部が暗くなるという主要な効果は出る。

**優先度は最も低い。** 他のどの軸にも依存されない。

---

### 1-8. 軸 4 — CPU 側の日照率(熱・電力・SRP)

**方針(確定)**: `physics/shadow.ts` の近似は粗すぎるので修正する。**ただしこれは描画とは別問題である。**

**現状の欠陥**:

- `sunlitFactor`(`physics/shadow.ts:8-13`)は**地球固定の円柱影**で、`R_EARTH` 直書き、
  **他天体の影が存在しない。**
- 読んでいるのは照明(`environment-scene.ts:130-132`)・ラジエータ熱入力(`player.ts:230-233`)・
  電力(`player.ts:235`)・SRP(`dynamics.ts:120-123`)の 4 系統。
- → **月の影に入ってもラジエータは冷えず、SRP は効き続ける。これは描画の問題ではなく物理の欠陥である。**
- **円柱影は「太陽を点光源として扱った近似」そのもの**でもある。本影と半影の区別が無い。

**修正の内容**: **艦を点、惑星と恒星を球と近似したうえで、幾何的に照度を計算する。**
艦から見た太陽の円盤と遮蔽天体の円盤の**重なり面積比**が日照率であり、これは閉じた解を持つ
(2 円の交差面積)。円柱影を円錐影(本影 + 半影)へ置き換えるのと同じことだが、
面積比で書けば本影・半影・完全日照が場合分けなしに 1 つの式で出る。
`Attractor[]` を取る形にして全天体へ一般化する。

**描画への流用は避ける**: §1-6-3 の退路として言及したが、**CPU 側の点近似の結果を
艦の視覚的な照度へ流用するのはアドホックな方法である。** 艦は点ではないので自己影も表せず、
シャドウマップ基盤の外側に別経路を作ることになる。軸 2 が成功すれば不要になる。

**独立性**: この修正は**描画のどのフェーズにも依存せず、どのフェーズからも依存されない。**
どの時点でやっても計画に影響しない。

---

## 2. 事実の訂正(第一〜三版に対して)

| 箇所 | 過去の記述 | 実際 |
|---|---|---|
| **v3 §1-4** | **「シャドウマップでリングは解けない」** | **誤り。** 根拠の計算がリング系全体を 1 枚で覆う前提だった。画面上のスケールでカスケードすれば解ける(§1-6-1) |
| v3 §1-4 | 「遮蔽器は解析 3 層に分ける」 | 破棄。**GPU は全面的にシャドウマップ。解析解は CPU 側(軸 4)にのみ残る** |
| v1 | 「出力色空間 未設定」 | three r0.169 の既定が `SRGBColorSpace`。欠けているのは HDR RT とトーンマッピングのみ |
| v1 | 「sRGB画像の誤用を全テクスチャで点検」 | **点検済み。ほぼ問題なし。** `colorSpace` 未設定は `8k_clouds.jpg` のみで、`.r` をマスクに使うので線形扱いが正しい |
| v1 | 「リング 35バンド → 66描画オブジェクト」 | 帯数 35 は正しい |

---

## 3. 現況の事実

### 3-1. 光源と陰影の二重世界

- シーンライトは **2 つだけ**: `AmbientLight(0x8899bb, 0.25)`(`environment-scene.ts:87`)と
  `DirectionalLight(0xfff4e0, 2.2)`(`:89`)。受けるのは `MeshStandardMaterial` の層
  = 艦船・デブリ・薬莢・基地・小惑星のみ(生成 12 箇所、すべて `render/ships.ts`)。
- 天体は全て `MeshBasic(Node)Material` でライト非依存。手書き Lambert + `NIGHT_AMBIENT = 0.04`
  (`celestial-surface.ts:30`)が地球にも他天体にも同じ値で掛かる。
- **「艦が惑星の影に入る」は現状 `DirectionalLight.intensity` のスカラ変調のみ**
  (`environment-scene.ts:147-148`)。**形状に依存する影は一切無い。`castShadow` / `shadowMap` は src に 0 件。**
- 平行光の向きは描画原点(`fo.r`)から見た恒星方向(`:145-146`)。
- `AMBIENT_INTENSITY = 0.25`(`const.ts:89`)が定義済みなのに `environment-scene.ts:87` は直書き。
- **`AmbientLight` は §1-5 の地球照の代用品として働いてしまっている。** 軸 1 の完成で消える。

### 3-2. 天体の座標系の扱いが不統一

地球のみ真位置・実半径(分割数は見かけ直径で 64×48〜384×288 を選ぶ)。
他天体は単位球をスケールする方式(`celestial-surface.ts:113,119`)で、かつ距離圧縮を受ける。

### 3-3. `renderOrder` の実測(9 段。うち線の族は集約済み)

**線の族は `LINE_RENDER_ORDER`(`game/const.ts:547`)へ既に集約されている**
(reference:0 / shipOrbit:1 / secondaryTarget:2 / target:3 / plan:4 / predicted:5、消費者 9 箇所)。

| 値 | 対象 |
|---|---|
| -10 | 星空シェル(`depthTest:false`) |
| -9 | 太陽ビルボード、惑星輝点ビルボード |
| 0 | 天球グリッド、`OrbitLine` 既定、`LINE_RENDER_ORDER.reference`、天体本体球、点群、艦船モデル |
| 1 | `LINE_RENDER_ORDER.shipOrbit`、リング(本体 `renderOrder + 1`) |
| 2 | 大気リム(`depthTest:false`)、`SampledLine` 既定、`LINE_RENDER_ORDER.secondaryTarget` |
| 3 | オーロラ、`LINE_RENDER_ORDER.target` |
| 4 | `LINE_RENDER_ORDER.plan` |
| 5 | **`LINE_RENDER_ORDER.predicted` と `Billboard` 既定が同値**(どちらも半透明) |
| 999 | Δv ギズモの `Group` — **THREE は親→子へ伝播しないので実質無効** |

**残る問題**: (1) 値 5 の衝突、(2) `LINE_RENDER_ORDER` が `game/const.ts` にあり §5 の規則に反する、
(3) `environment-scene.ts:217-218` の冗長な再代入。

### 3-4. `frustumCulled = false` の箇所

`stars.ts` / `celestial-grid.ts` / `orbit-line.ts` / `sampled-line.ts` / `ring.ts`(4箇所)/
`celestial-surface.ts` / `billboard.ts` / `ships.ts`(2箇所)/ `point-field-view.ts` /
`instanced-pool.ts`。**理由が残っているのは `instanced-pool.ts` のみ。** 残りは意図的か
どうかを見直し、意図的な箇所だけ理由を残す(Phase 0 の残作業)。
シャドウマップのカリング(§1-6)にも同じ問題が効く。

---

## 4. ロードマップ 第四版

**光と影の実装順は 軸 2 → 軸 1 → 軸 4 → 軸 3。** 理由:

- **軸 2(影)が最初** — シャドウマップの合成基盤は**パイプラインの構成そのもの**
  (パス数・バッファ・カリング・カスケード選択)と密結合しており、後から差し込めない。
  特に §1-6-7 の半透明遮蔽器は、合成規則を最初から決めておかないと後で全体が変わる。
- **軸 1(反射)が次** — ライティングパスとレンダリングパスが分離されていれば
  **暫定的に点光源で始めて後からブラッシュアップできる**(§1-5-5)。受け手のコードは変わらない。
- **軸 4(CPU)はいつでも** — 描画と独立。他のどのフェーズにも依存しない。
- **軸 3(地球照の遮蔽)が最後** — 無視してよい。他から依存されない。

第三版との対応:

| v4 | 内容 | v3 |
|---|---|---|
| Phase 0 | LOD とカリング | Phase 0(維持) |
| Phase 1 | 座標境界の確定 | Phase 1 |
| Phase 2 | デッドコード除去・計測・描画設定GUI | Phase 2 |
| Phase 3 | HDR 線形パイプラインと露出基準 | Phase 3 |
| Phase 4 | ライトプリパス基盤の導入(**点光源のみ**) | Phase 4 |
| **Phase 5** | **軸 2a: シャドウマップ合成基盤 + 近傍カスケード** | Phase 7 の一部 → **前倒し・全面書き換え** |
| Phase 6 | 深度カスケードのスパイクと判断 | Phase 5 |
| Phase 7 | 距離圧縮の撤去 | Phase 6 |
| **Phase 8** | **軸 2b: 天体・リングを遮蔽器として同じ基盤へ** | Phase 7 の残り(**解析 3 層を破棄**) |
| **Phase 9** | **軸 1: エリアライト(球光源・ランバート球)** | Phase 8 |
| **Phase X** | **軸 4: CPU 側の日照率(独立・任意の時点)** | Phase 7 に同梱 → **分離** |
| Phase 10 | 半透明パスの統合 | Phase 9 |
| Phase 11 | 大気散乱 | Phase 10 |
| Phase 12 | ポストエフェクト | Phase 11 |
| Phase 13 | ボリュームレンダリング | Phase 12 |
| **Phase 14** | **軸 3: 地球照の遮蔽(任意)** | — (**新設・最低優先**) |

各フェーズは独立に比較可能であること。**一度に全面置換しない。**

### Phase 0 — LOD とカリング(最優先)

**パフォーマンス問題を先に潰す。他のどのフェーズにも依存しない。**
後続フェーズはすべて描画負荷を増やす方向なので、**土台の負荷を下げてから積む。**
特に Phase 4 でジオメトリを 2 回描き、Phase 5 でシャドウマップの分だけさらに描く。

**「画面上のスケールで解像度・詳細度を決める」判断の置き場所は `render/screen-lod.ts` に決めた。**
スカラーだけを引数に取る純関数を置く場所で、THREE にも `game/`・`physics/` の座標型にも依存しない。
§1-6-1 のシャドウカスケード選択と §1-3 の深度カスケードもここへ合流する。

残っている作業:

1. スクリーン投影サイズによる統一的な間引き。現在 8 天体にしか無い
   `PHYSICAL_DIAMETER_THRESHOLD_PX`(`point-body.ts:37`)を全 101 天体へ一般化し、
   `screen-lod.ts` へ合流させる。地球以外の天体球も同モジュールの分割段ラダーへ乗せる。
2. 天球グリッド約 960 本の `THREE.Line` をまとめて 1 描画へ。
   **マップビューのドローコール(約 1020)の主因はここ。**
3. `frustumCulled = false` の各所(§3-4)を見直す。意図的な箇所だけ理由を残す。
4. リングの線↔面クロスフェード(`ring-view.ts:26-39`)を統一 LOD へ吸収し、
   `ring.ts:182-235` の CPU 側光学**二重実装を削除**。

**完了条件**: 最大数の弾・薬莢・デブリが出ている状況でフレーム時間が改善している。見た目は変わらない。

### Phase 1 — 座標境界の確定

§1-2。

1. `render/` から `FloatingOrigin` を剥がす(2 箇所)。呼び出し側が `THREE.Vector3` を渡す。
2. 頂点列の生成と頂点バッファのアップロードを分ける。表示 LOD ポリシーは純関数として `render/` に残す。
   **Phase 0 で LOD の置き場所を決めた直後なので方針が揃っている。**
3. `sampled-line.ts:28` の `ScaleAtFn` を排除する。
4. f32 精度の実測 — 海王星軌道線とカイパー帯点群(§1-2)。

**完了条件**: `src/render/**` から `physics` / `game` の座標型への import が 0 件。

### Phase 2 — デッドコード除去、計測、描画設定GUI

1. `AMBIENT_INTENSITY` の直書き重複を直す。
2. `perf-meter.ts` の計測を完成させる(`sync` と `render` の分離、可能なら GPU タイムスタンプ)。
   **Phase 5 以降はシャドウマップのパス時間を個別に見たいので、パス単位の計測にしておく。**
3. `render/graphics-settings.ts` を新設。品質プリセット(低/中/高)+ 個別トグル:
   解像度スケール(`setPixelRatio` 上限)、AA、ブルーム、大気、オーロラ、雲、ボリューム、
   **影(カスケード枚数・解像度)**、点群、LOD バイアス。
   現状 `setPixelRatio(devicePixelRatio)` は上限なしで、**resize 時に読み直してもいない**(`scene.ts:18,22-24`)。
4. `hud/settings-panel.ts` に「描画」タブ。ローカル保存し `main.ts` 起動時に適用。
5. 画像回帰シーンを撮る。**役割は「一致判定」ではなく「変化の可読化」**(§1-1)。

**完了条件**: 全トグルをオフにしたとき現行と同じ絵が出る。

### Phase 3 — HDR 線形パイプラインと露出基準

§1-1。**すべての後続フェーズの前提。**

1. HDR レンダーターゲット(`rgba16float`)へ描き、出力段で `toneMapping` を経て sRGB へ。
2. 露出基準を実装。`SOLAR_PRESSURE_1AU` を `SOLAR_CONSTANT / c` から導出する形へ統一。
3. **101 天体のアルベドを入れる。** 16 進直書き色(63 種)を幾何アルベド + 色度へ置き換える。
   **本フェーズ最大の実作業。** 出典をコメントに残す(`RingOpticsDef` の帯コメントが手本)。
   **§1-5 により、この値は Phase 9 で「他の物体を照らす光源の強さ」としてそのまま使われる。**
4. `Billboard` をノードマテリアル化。太陽・惑星輝点は等級 → 放射照度へ。
5. `RADIANCE_SCALE = 0.72`(`ring.ts:30`)を 1.0 にする。
6. 砲身赤熱を機体温度 + `STEFAN_BOLTZMANN` + `HULL_EMISS` の黒体放射へ。
7. 手調整値(`NIGHT_AMBIENT`、`SUN_INTENSITY`、各 opacity、`ZOOM_MUZZLE_FLASH_SCALE`)は**捨てる。**

**完了条件**: `RADIANCE_SCALE` が消える。LDR クリップ回避のための係数が不要になる。
露出を 1 段変えると全体が一様に明るく/暗くなる。

### Phase 4 — ライトプリパス基盤の導入(点光源のみ)

§1-4。**このフェーズの目的は絵を変えることではなく、器を作ることである。**

```text
Phase A  render/gbuffer.ts        深度 + 法線(oct符号化, rg16float) + 粗さ(r8unorm)
Phase B  render/light-prepass.ts  拡散照度 (rgb16float) + 鏡面照度 (rgba16float)
Phase C  render/material-pass.ts  ジオメトリ再描画。照度を読み、アルベド/金属性を掛ける
```

1. 上記 3 パスを組み、**光源は現状と同じ単一平行光 1 個だけを通す。**
   `AmbientLight` も現状の定数のまま残す(Phase 9 で地球照に置き換わる)。
2. **絵が Phase 3 の時点と一致することを完了判定にする。** 器の正しさは「何も変わらない」ことで確かめる。
3. 艦船の `MeshStandardMaterial` と天体の自前 Lambert が **Phase C で 1 つの BRDF に統合される。**
   `NIGHT_AMBIENT = 0.04` の天体別複製が消える。
4. 遮蔽の問い合わせ口 `sunVisibility(worldPos, normal)`(§1-6-5)を Phase B に置く。
   **中身は当面 `environment-scene.ts:147-148` の現行スカラ変調を移設しただけでよい。**
   Phase 5 / Phase 8 でこの関数の中身だけが差し替わる。
5. **光源インターフェースを「球光源」の形で定義しておく。** 実装は点光源(角半径 0)でよいが、
   **型として角半径と位相を持たせておく**ことで、Phase 9 の差し替えが Phase B 内に閉じる。
6. `earth.ts` の地表シェーダを分解: BRDF 部分は Phase A/C へ、もや(`:67-71`)・夕焼け(`:59-64`)は
   Phase 11 の大気パスへ移すため**一旦そのまま残す**。
7. **鏡面の近似の限界を実測する**(§1-4)。破綻するなら艦船だけ前方描画へ逃がす経路を用意する。

**完了条件**: 3 パス構成で Phase 3 と同じ絵が出る。光源を 1 個増やすのに
レンダリングパスのシェーダを触らなくてよい。天体を 1 つ追加してもシェーダコードが増えない。

### Phase 5 — 軸 2a: シャドウマップ合成基盤 + 近傍カスケード

§1-6。**軸 2 の器。パイプライン構成と密結合なので、光源の高度化(Phase 9)より先に置く。**
このフェーズでは**遮蔽器を艦・デブリ・基地に限る**(天体とリングは Phase 8)。
天体を遮蔽器にするには真位置が要る(§1-6-8)ので、距離圧縮の撤去を待つ。

1. **`render/shadow-set.ts` を新設** — §1-6-5 の合成基盤。
   複数マップの保持、**遮蔽器のマップへの排他割り当て**(二重計上の防止、§1-6-3)、
   受け手側の積による合成、**画面上のスケールによるマップ選択**(§1-6-1/§1-6-4)。
2. **カスケード選択のポリシーを 1 モジュールに置く。** §1-3 の深度カスケードと同じ原則
   (画面上のスケールで解像度を決める)なので、Phase 6 の `view-space.ts` と置き場所を揃える。
3. **クラスタごとの平行投影**(§1-6-4)。太陽から見たクラスタの角度幅が極小なので平行投影で足り、
   広画角の透視投影は要らない。**画面上で 1 px 未満のクラスタはマップを組まない。**
4. **PCSS による半影**(§1-6-6)。フィルタ半径は太陽の角半径 4.65 mrad から導く。**チューニング値にしない。**
5. **半透明遮蔽器のためのマップ種別を最初から入れる**(§1-6-7)。
   このフェーズでは不透明マップしか使わないが、**合成規則(深度の積ではなく透過率の積)を先に決める。**
   後から足すと合成規則が変わってしまう。
6. Phase 4 で置いた `sunVisibility` の中身を、この基盤へ差し替える。**呼び出し側は無変更。**
7. `castShadow` / `receiveShadow` は現状 0 件なので新規。**Phase 0 のインスタンシングが効く**
   (シャドウマップへも同じジオメトリを描くため)。

**完了条件**: 艦が自分の構造で影を落とし、その影の縁が距離に応じてぼける。
艦をカメラから遠ざけるとシャドウマップの解像度が下がるが、画面上では劣化が見えない。
太陽系全体を見ている視点で、シャドウマップが 1 枚も組まれない(全クラスタが 1 px 未満)。

### Phase 6 — 深度カスケードのスパイクと判断

§1-3。**このフェーズの成果物はコードではなく判断である。**

1. `render/view-space.ts` を新設し、順変換 5 種を集約。逆変換 `toEci` を作る。
   **判断の結果によらず有効**なので先にやる。**Phase 5 のカスケード選択ポリシーと同居させる。**
2. `types/three-shims.d.ts` に `clearDepth()` を追加。
3. カスケード 2 / 3 / 4 枚のスパイクを作り、§1-3 の測定項目 (a)〜(d) を測る。
   **Phase 4 で 3 パス、Phase 5 でシャドウパスが加わっているので、(b) はその上で測る。**
4. **判断**: 通れば Phase 7 へ。通らなければ距離圧縮を維持し、**Phase 8 は
   「圧縮空間の中で幾何を整合させる」限定版に留まる**(§6 に影響を明記)。

### Phase 7 — 距離圧縮の撤去(Phase 6 の判断が可の場合のみ)

1. 天体を真位置・真半径で配置。`sphere-body.ts:76-81` / `point-body.ts:141-147` のスケール計算を削除。
2. `MOON_VIS_DIST` / `PLANET_VIS_DIST` / `POINT_BODY_VIS_DIST` / `SUN_DISTANCE` / `SUN_VISUAL_SIZE` を削除。
   `STAR_SHELL_RADIUS` / `CELESTIAL_SHELL_RADIUS` を「無限遠背景カスケード」へ一本化。
3. `COMBAT_CAMERA_FAR` の根拠が消えるので、カスケード境界から引き直す。
4. `sun-body.ts:39-64` / `point-body.ts:96-186` の overview/combat 分岐を削除。§3-2 の不統一も解消。
5. シーンライトを真の太陽方向へ統一。天体ごとの `sunDirection` uniform の役目が終わる。

**完了条件**: 圧縮定数がゼロ件。ズームを通して天体の見かけサイズが連続。

### Phase 8 — 軸 2b: 天体・リングを遮蔽器として同じ基盤へ

§1-6。**Phase 5 の基盤と Phase 7 の真位置が前提。**

1. **天体本体を遮蔽器として `ShadowSet` へ登録。** 粗いカスケードに属する(§1-6-3)。
   これで「地球の影に出入りする艦」「月食の境界」が同じ基盤に乗る。
2. **リング帯を半透明遮蔽器として登録**(§1-6-7)。深度ではなく累積光学的厚さを書く。
   Beer–Lambert の τ は `RingOpticsDef` に既にある。
3. **`render/celestial-surface.ts:63-91` の固定長 32 バンド uniform 配列を廃止。**
   バンド数の上限がシェーダから消える。
4. **`render/ring.ts:71-77` の解析的 `inBodyShadow`(二値)を廃止。** 半影を持つシャドウマップに置き換わる。
5. Z 精度のカスケード(§1-6-3)を実測する。**失敗した場合の退路は §1-8 の CPU 側計算の流用だが、
   アドホックなので可能な限り避ける。** 退路を採るなら、その旨をコードのコメントに明記すること。

**完了条件**: 艦が地球の影へ入るとき、境界が半影の幅で滑らかに暗くなる。
月食の境界が柔らかい。リング影の境界がぼけ、帯ごとの透過率の差が影の濃さに出る。
**同じ遮蔽現象について GPU に解析実装が残っていない**(CPU 側の軸 4 とは別)。

### Phase 9 — 軸 1: エリアライト(球光源・ランバート球)

§1-5。**Phase 4 の器が前提。変更は Phase B(ライティングパス)の中に閉じる。**

1. **太陽を一様球光源へ**(§1-5-1)。拡散項は距離とともに逆二乗則へ自動で縮退する閉じた解を使い、
   **「遠いときは点光源」という分岐を書かない。** 鏡面項だけ角半径の閾値で LTC / 点光源を切り替える。
2. **天体をランバート球へ**(§1-5-2)。照らされている半球を等価な円盤へ代表させ、LTC を適用。
   **光が来る「向き」の偏りが表現されることを完了判定にする**(単なる明るさの位相変調では不可)。
   強さは Phase 3 で入れたアルベド × その天体が受ける放射照度。
3. **光源の打ち切りは立体角の閾値で**(§1-5-3)。実用上「太陽 + 最も近い 1〜2 天体」に収まる。
4. 光源天体自身の日照は Phase 5/8 の遮蔽基盤を共有(影に入っている天体は光らない)。
5. **`AmbientLight(0x8899bb, 0.25)` を削除。** 地球照が置き換える(§3-1)。
6. 色を持つ光源として扱う。**艦の影側が地球の色を帯びることを検証ケースにする。**

**完了条件**: LEO で艦の地球を向いた面と宇宙を向いた面の明るさが桁で違う。
高度を上げていくと地球照が滑らかに弱まり、月軌道では点光源と区別がつかなくなる
(**その遷移でフレームが飛ばない**)。三日月形に見える位置で地球照が弱く、光の向きが偏る。
`AmbientLight` が src から消える。

### Phase X — 軸 4: CPU 側の日照率(独立・任意の時点)

§1-8。**描画のどのフェーズにも依存せず、どのフェーズからも依存されない。いつやってもよい。**

1. `physics/shadow.ts` の `sunlitFactor` を**全天体へ一般化**(`Attractor[]` を取る形に)。
2. 円柱影を**艦=点、天体・恒星=球とした幾何的な照度計算**へ置き換える。
   太陽の円盤と遮蔽天体の円盤の**重なり面積比**で、本影・半影・完全日照が場合分けなしに出る。
3. `SHADOW_PENUMBRA = 6e4` の直書きを太陽の視半径から導出する形へ。
4. **描画へ流用しない。** 照明(`environment-scene.ts:130-132`)がこれを読んでいる現状の経路は、
   Phase 5/8 の完成をもって切る。残る消費者はラジエータ・電力・SRP の 3 系統。

**完了条件**: 月の影に入るとラジエータの熱入力・電力・SRP が同時に落ちる。
`R_EARTH` の直書きが消える。**描画側がこの関数を読んでいない。**

### Phase 10 — 半透明パスの統合

1. `render/transparent-pass.ts`: 不透明カスケード解決後に描画。`viewportDepthTexture` / `linearDepth`。
2. 「視線に沿って不透明面までメディアを積分する」共通ヘルパ。**大気・オーロラ・プルーム・リングが共有する。**
   **Phase 8 の半透明シャドウマップと同じ光学モデル(Beer–Lambert)を共有できるか検討する。**
3. `earth.ts:86-124` の大気リム球を廃止 — `depthTest:false` + 自前球交差の回避策が不要になる。
4. `aurora.ts` の CPU 頂点更新(`:66-107`)を GPU へ。
5. `depthTest: false` の 3 箇所を規約内へ回収する。
6. **`LINE_RENDER_ORDER` を `render/` へ移し、線以外の半透明物を同じ表へ統合する。**
   値 5 の衝突(§3-3)はこの統合で解消する。
7. 加算合成の上限は Phase 3 の露出基準に従う。

### Phase 11 — 大気散乱(指数密度)

1. 透過・単散乱・多重散乱近似を LUT 化(オフライン生成も可)。カメラが大気圏内外どちらでも同じモデル。
2. Phase 10 のレイ積分枠組みへ載せ、エアリアルパースペクティブを不透明深度に対して積分。
   `earth.ts:67-71` のもや(`ATMO_HAZE_TAU0 = 0.34`)を置換。
3. 夕焼けを光路長から導出し、`vec3(1,0.4,0.1)` の固定補間(`:59-64`)を削除。
4. 雲は当面 Phase 4 の Phase C 内 2 層サンプリング。**雲を半透明遮蔽器として Phase 8 の基盤へ載せれば、
   `earth.ts:55-56` の UV ずらしによる雲影が半影付きの本物に置き換わる。**

### Phase 12 — ポストエフェクト

1. 露出 → ブルーム → レンズフレア/グレア → トーンマッピング → sRGB。
2. ブルーム対象は Phase 3 で HDR 化済みの発光体。**再突入プラズマは既に動圧という物理量から
   強度を引いている**(`REENTRY_GLOW_MIN_Q` / `_FULL_Q`)ので、HDR 化の検証基準ケースに使える。
3. TAA/SSAA は Phase 2 のトグル配下で任意。AO は優先度低。

### Phase 13 — ボリュームレンダリング

Phase 10 のレイ積分枠組みが前提。プルーム・RCS・再突入・オーロラを板ポリゴンから参加媒質へ。

### Phase 14 — 軸 3: 地球照の遮蔽(任意)

§1-7。**優先度最低。無視したままでも計画は完結する。**

1. AO を導入し、地球照(および他の二次反射光)にのみ掛ける。太陽光には掛けない
   (太陽光の遮蔽は Phase 5/8 が正しく解いているので、AO で二重に暗くしてはならない)。
2. **全天体からシャドウマップを撮るという方向は採らない**(原理的に不可能、§1-7)。

---

## 5. 責務境界の規則(全フェーズ横断)

> **`game/` は「なにがあるか」を知り、「どう見えるか」を知らない。**
> **`render/` は「どう見えるか」を知り、「どこにあるか(ECI)」を知らない。**
> **ライティングパスは「光源の形」と「遮蔽」を知り、「材質」を知らない。**
> **レンダリングパスは「材質」を知り、「光源の形」も「遮蔽」も知らない。**

4 行目までが揃うと、軸 1(光源の形)と軸 2(遮蔽)が互いに独立に差し替えられる。
**これが第四版の構成の目的である。** Phase をまたいで少しずつ寄せる。一括改修はしない。

### 移す

| 対象 | 現在 | 移動先 | 対応フェーズ |
|---|---|---|---|
| `ringPixelCoverage`(スクリーン空間量) | `physics/ring-optics.ts:27-30` | `render/` | Phase 0 |
| 頂点列の生成(bake / 細分) | `render/sampled-line.ts`, `orbit-line.ts` | `game/` — 座標は `game/` の責務 | Phase 1 |
| 絶対 ECI を取る `ScaleAtFn` 型定義 | `render/sampled-line.ts:28` | 廃止(純関数 `chordCountFor` へ) | Phase 1 |
| テクスチャURL表・101天体の配色 | `celestial-registry.ts:13-33, 88-195` | `render/`。レジストリには**表示名とidだけ**残す | Phase 3 |
| `RingOpticsDef.color` | `physics/solar-system.ts:107-116, 322` | `render/`。τ・アルベド・位相係数は物理なので残す | Phase 3 |
| エフェクトの size/opacity 式・配色 | `vfx/`, `player/*-effects.ts`, `const.ts:230-247, 499-534` | `render/` | Phase 3 |
| シーンライト・光量変調 | `environment-scene.ts:87-89, 130-132, 147-148` | `render/` のライティングパス | Phase 4 |
| **描画用の日照判定** | `physics/shadow.ts` を `environment-scene.ts:130-132` が読む | **`render/` のシャドウマップ基盤。物理側の関数は残るが描画は読まない** | Phase 5/8 |
| `celestial/{sphere,point,sun,earth}-body.ts`, `ring-view.ts`, `point-field-view.ts` | `game/celestial/` | `render/celestial/` | Phase 7 |
| 参照線の色・不透明度・`renderOrder`・頂点密度方針 | `environment-scene.ts` | `render/` | Phase 10 |

### 残す

- `physics/` の THREE 非依存(**現状ゼロ件、維持する**)。
- `physics/ring-optics.ts` の放射伝達5関数、`physics/projection.ts` — 物理・幾何として妥当。
  **`projection.ts` が near/far 非依存であることは、カスケード導入時の重要な資産**(§1-3)。
- **`physics/shadow.ts` の日照率(軸 4)は物理側に残す。** 熱・電力・SRP の消費者がいる。
  **ただし描画はこれを読まない**(§1-8)。同じ物理現象について CPU と GPU に別実装があることになるが、
  **問いが違う**(点の日照率 vs 画素ごとの遮蔽度)ので二重実装ではない。
- `render/ships.ts:6` の `game/const` 参照 — 読み取り専用の例外。Phase 3 以降は寸法のみ。
- `GameEntity.obj: THREE.Object3D` — 不透明ハンドルとして当面許容(`game/` の 49 ファイルが
  `three/webgpu` を import している)。ただし
  **`game/` 側で material / renderOrder / LOD / 色 / `castShadow` を決めるのは禁止**とする。

### 判断基準

`game/` に書いてよいのは「この天体が存在する」「この位置(ECI)にある」「破壊された」まで。
「何 px だから点で描く」「`renderOrder` は 3」「色は `0x9a8a7a`」「影を落とす」は `render/` の判断。

---

## 6. リスクと未決事項

| 項目 | リスク | 備考 |
|---|---|---|
| **Phase 8 の Z 精度カスケード** | **高** | §1-6-3 の「近い被遮蔽物 × 巨大な遮蔽物」。**失敗すると軸 2 の統一が崩れ、CPU 側の流用というアドホックな退路しか残らない。** Phase 5 の段階で艦の遮蔽器だけで先に精度の当たりを付けておく |
| **Phase 3 のアルベド 101 天体** | **高** | 出典の無い 63 種の直書き色を根拠のある値へ。**Phase 9 の光源強度も兼ねるため重い** |
| **Phase 4 の鏡面近似** | **高** | ライトプリパスは鏡面のマテリアル依存を照度バッファに畳めない(§1-4)。破綻したら艦船だけ前方描画へ。**Phase 4 の完了判定に実測を含めること** |
| Phase 6 の判断が否だった場合 | **高** | 距離圧縮が残ると Phase 8 が「真の方向 × 偽の位置」を解消できず限定版に留まる(§1-6-8)。**カスケードを試す最大の動機** |
| **Phase 9 のランバート球の近似** | 中 | 「照らされた半球を等価な円盤で代表」は LTC の標準的な適用範囲外(§1-5-2)。三日月形の位置での検証ケースが必須 |
| **Phase 5 の半透明シャドウマップ** | 中 | 合成規則(透過率の積)を最初に決める必要がある(§1-6-7)。**後から足すと基盤が変わる** |
| Phase 5 のクラスタリング | 中 | 「どこで区切るか」の基準が未決。惑星系単位が自然だが、艦が惑星間にいる場合の扱いを決める必要がある |
| Phase 4/5 のドローコール増 | 中 | ジオメトリを 2 回 + シャドウマップの枚数だけ描く。**Phase 0 の LOD/インスタンシングが先に済んでいるので吸収余地がある** — LOD 前倒しの二次的効用 |
| Phase 9 の LTC テーブル | 中 | three.js の `RectAreaLight` 用テーブルが流用できるか、球光源用に別途生成するか要調査 |
| Phase 1 の頂点生成の移動 | 中 | `sampled-line` は予測軌道・計画軌道・デバッグ線の 3 消費者を持つ |
| Phase 6 のカスケード分割数 | 中 | 実測で決める。まず 2〜3 枚 |
| G-buffer + 照度 + シャドウのバンド幅 | 中 | 解像度スケールとシャドウ解像度トグル(Phase 2)で逃げ道を確保 |
| ライトプリパス + 半透明 | 中 | 半透明は恩恵を受けない。Phase 10 は前方描画で確定 |
| 低性能端末 | 中 | 全部オフでも動くこと。Phase 2 のプリセットが担保 |
| **既存の絵が崩れること** | — | **リスクではない**(§1-1) |
| ~~未決: 露出の基準~~ | — | §1-1 で決定 |
| ~~未決: 影の基盤~~ | — | §1-6 で決定(**シャドウマップへ全面統一**。v3 の解析 3 層は破棄) |
| ~~未決: CSM と `MeshStandardMaterial` の経路差(v3)~~ | — | **解消**。BRDF 統合(Phase 4)を影(Phase 5)より前に置いた |
| 未決: Phase 5 のクラスタ境界の決め方 | — | 上記「クラスタリング」に依存。Phase 5 のスパイクで決める |

---

## 7. 次の一歩

**Phase 0 の最初の 3 項目は完了した。**

| 済んだこと | 結果 |
|---|---|
| デッドコード 3 系統の削除 | `ringPlanetShadow` / 環テクスチャ経路 / `earth-color.ts` 一式(と `earth.jpg` の重複)を除去 |
| 負荷確認ウィンドウのドローコール数・三角形数 | `PerfMeter` が `WebGPURenderer` を直接受け取り「描画」セクションへ出す |
| 地球のジオメトリ LOD | 三角形数 173万 → 21万。分割段は見かけ直径から選ぶ(`render/screen-lod.ts`) |
| 弾・薬莢のインスタンシング | ドローコール 1683 → 325。`render/instanced-pool.ts` |

計測値(`?stage=1`、戦闘ビュー、連射中): draw calls 約 325 / triangles 約 20万。
マップビュー: draw calls 約 1020 / triangles 約 19万。

**デブリ片(`DebrisKind` の `'fragment'`)はインスタンシングできない** — `buildDebrisMesh` が
個体ごとに乱数でジオメトリを生成し、マテリアルも色ごと個体ごとに new するため。
まとめたければ形状の乱数生成そのものを設計し直す必要があり、それは見た目の仕様の変更にあたる。
排出バレルとマガジンフレームは共有リソースだが同時存在数がごく少なく、割に合わない。

**次に着手するのは Phase 0 の残り**、重い順に:

1. **天球グリッド約 960 本を 1 描画へ。** マップビューのドローコール約 1020 の主因。
   戦闘ビューでは 325 まで下がっているので、いま効くのはこちら。
2. **`PHYSICAL_DIAMETER_THRESHOLD_PX` の全天体一般化と、天体球の分割段の `screen-lod.ts` 合流。**
   地球以外はまだ単位球固定分割のまま。
3. `frustumCulled = false` の見直し。
4. リングの線↔面クロスフェードの統一 LOD への吸収と、`ring.ts` の CPU 側光学二重実装の削除。

その後 Phase 1(座標境界の確定)へ進む。`render/screen-lod.ts` が既に
「表示 LOD ポリシーは純関数として `render/` に残す」を満たしているので、
Phase 1 の項目 2 はそこへ合流させる。

**並行して始められるもの**:

- **Phase 3 のアルベド調査** — 他フェーズと独立。**Phase 9(軸 1)の光源強度の前提でもあるので、
  早く始めるほど効く。**
- **Phase X(軸 4)の CPU 側日照率** — 描画と完全に独立。いつやってもよい。
  **ただし「描画がこれを読まない」状態は Phase 5/8 の完成を待つ。**
