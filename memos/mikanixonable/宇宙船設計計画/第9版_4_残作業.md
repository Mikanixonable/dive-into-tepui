# 第4巻 — 残作業

第1巻 `第9版_1_基盤.md` と第2巻 `第9版_2_設計・建造・資源.md` の段階のうち、実装が残っているものだけを、実際の依存関係に従って並べ直した作業計画である。

## 1. この文書の位置づけ

第1巻と第2巻の段階表は、**何も実装されていない状態**を出発点として依存を引いている。実装が進んだ現在、その依存の一部は既に満たされており、表のうえで前段に見えるものが実際には障壁でなくなっている。逆に、表では独立に見えて実際には別の段階に塞がれているものもある。**この文書は、いまの実装を出発点として依存を引き直し、着手できるものと塞がれているものを分ける。**

第1巻・第2巻・第3巻の段階表を置き換えるものではない。各段階が何を作るかの定義はそれらが持つ。この文書が持つのは、**どれが残っていて、どれから着手できるか**だけである。

`第9版_実装中の追加事項.md` の追加タスクのうち未解決のものは、§5 で残りの段階へ割り当てる。

## 2. 実装されている範囲

**第1巻** — F1 / F2 / F3 / F4 / F5 / F6 / F7 / F11 / R1 / R2 / R3。

**第2巻** — B3 / B5。

F11（自動操縦の可否と圏外の挙動）は、2つの段階の合成として成立している。F4 が `src/game/vessel/capabilities.ts` に `CoverageQuery` と `canAutopilot` を置き、B3 が `src/game/comms/` に通信圏の判定そのものを置いた。`src/game/plan/plan-executor.ts` の `commandLinked` が圏外で燃焼を完遂し、次のノードを読み飛ばし、圏内復帰で再開する。**第1巻 F11 が「常に圏内を返す仮の実装で足りる」としていた部分は、仮ではなく本物で埋まっている。**

R3 の閉包検査は電力の実現可能性まで含む。`tests/unit/resource-closure.test.ts` は、すべての資源が到達可能であること、すべての設備が建設可能であること、太陽電池に至る経路が初期の発電容量だけで歩けることを検査する。

## 3. 残っている段階と、引き直した依存

### 3-1. 第1巻

| 段階 | 内容 | 引き直した依存 | 状態 |
|---|---|---|---|
| **F8** | 接触形状のカプセル化（§11-1）とツリー・カプセルの開発用線画（§13 後段） | なし | **着手できる** |
| **F9** | 姿勢制御系の分離（§14-1〜14-3、§14-9）。減衰最小二乗の配分、フライホイールの飽和、アンローディング | なし | **着手できる** |
| **F10** | 推進剤の物性表とエンジン型式（§15-1〜15-4）、ツィオルコフスキーによる燃焼計算（§15-5）、絞り率スロットル（§14-8） | F9 | F9 に塞がれる |
| **F12** | ロフトメッシュの生成と自機モデルの置き換え（§13） | なし | **着手できる** |
| **F13** | 推力軸と重心のずれ、ジンバル速度、推進系HUDパネル（§14-10）、推進剤とエンジンの劣化（§15-6） | F10 | F10 に塞がれる |

F8・F9・F12 が依存する F7 は済んでいる。**この3つは互いに依存せず、3人が同時に着手できる。**F9 が要求する F3 の純関数（`src/physics/geomagnetic.ts` と `src/physics/gravity-gradient.ts`）も置かれている。

### 3-2. 第2巻

| 段階 | 内容 | 引き直した依存 | 状態 |
|---|---|---|---|
| **B1** | `splitStages` と分離の物理（§7）。分離後のツリー・質量特性の再計算。段を跨ぐ燃焼の判定とノードの分割提案 | 分離の物理はなし／段を跨ぐ燃焼の判定は F10 | **分離の物理は着手できる** |
| **B2** | ドッキングポートによる結合（§8） | B1 | B1 に塞がれる |
| **B4** | ペイロード倉庫と放出（§9）、弾薬の搭載要素化と段による制約（§12） | B1 | B1 に塞がれる |
| **B6** | 生産と生産可能性の統合（§5）。設計から資源・設備・電力の要求を求め、消費して実機を得る | なし | **着手できる** |
| **B7** | 組立モードの入口と骨格モード（§6-1〜§6-4）。基地を操作対象にしたときの戦闘ビューに組立モードの開閉が現れ、開いている間だけ左右のレールへ編集パネルが出入りする | F12 | F12 に塞がれる |
| **B8** | 搭載モードと塗装モード（§6-5、§6-7） | B6, B7 | B7 に塞がれる |
| **B9** | 配管の搭載要素化、`autoRouteFeed` と `resolveFeed`（§10） | B1 | B1 に塞がれる |
| **B10** | `pressurant.ts` と与圧低下による推力低下（§11） | B9, F10 | B1・F10 に塞がれる |
| **B11** | 配管モード、右レールの段一覧、飛行中の段・ペイロード・通信パネル（§6-6、§6-8、§6-11） | 通信パネルはなし／他は B1〜B10 | **通信パネルは着手できる** |
| **B12** | 基地モジュールの区画（§14）。コンテナをツリーを持たない `Vessel` として実装する | B6, B8 | B7 に塞がれる |
| **B13** | 敵のドロップ、生産設備の稼働、送電網と月面の電力（§16-1、§16-2） | B12 | B7 に塞がれる |
| **B14** | 磁気トルカ、熱シールドと大気掠め（§15-3）、大気捕集による産地、永久影の放熱（§16-3） | 磁気トルカは F9／大気掠め・大気捕集・永久影は B13 | **磁気トルカは F9 の直後に着手できる** |
| **B15** | 軌道上での結合（§6-9） | B2, B9, B12 | B1 に塞がれる |
| **B17** | 戦闘ビューでのモジュールのプロパティーウィンドウ（§19）。`pickModule` の判定、種別ごとのプロパティ行と操作項目、操作モジュールのウィンドウが機体全体のウィンドウであること | 判定の純関数はなし／戦闘ビューへの配線は F12／分離機構の操作項目は B1 | **判定の純関数は着手できる。配線と実行時検証は F12 に塞がれる** |
| **B16** | 核熱・電気・核融合のエンジン型式、ヘリウム3の同位体分離（§17-3〜§17-5） | F10, B14 | F10 に塞がれる |

### 3-3. 元の表と依存が変わった箇所

**B1 の依存が2つに割れる。**分離の物理（連結成分の分割、運動量保存、分離後の質量特性の再計算）は、F6 のツリーと F7 の質量特性の導出があれば通る。一方、段を跨ぐ燃焼の判定は `propellantForDv` を要求し、これは F10 のツィオルコフスキーの式そのものである。**分離だけを先に通し、燃焼の分割は F10 の後に足す。**

**B3 は B1 と B2 より先に済んでいる。**したがって B4（ペイロード放出）を塞ぐのは B1 だけである。

**B14 の依存が3つに割れる。**磁気トルカは、`src/physics/geomagnetic.ts` の純関数（`geomagneticField` と `magneticTorque`）が置かれていながら `src/game/` からの参照を1つも持たない状態にある。これを配線する先は F9 のアンローディングであり、B13 ではない。熱シールドの本体は F7 が実装済みであり、B14 に残るのは大気掠めによる減速（§15-3）と、大気捕集による産地、永久影の放熱の3つで、これらは B13 の設備の稼働に依存する。

**B17 の依存が3つに割れる。**第2巻 §20 は B17 の依存を第1巻 F5・第1巻 F12・B1 と定める。F5（搭載要素の一覧と性能値）は済んでいるため、モジュールの判定（`src/game/vessel/module-pick.ts`）と種別ごとのプロパティ行の組み立て（`src/game/hud/module-property-rows.ts`）は、どちらも DOM にも THREE にも依存しない純関数として今すぐ書け、`tests/unit/module-pick.test.ts` だけで検証できる。**しかしそれだけでは画面に何も現れない。**戦闘ビューの右クリックへ配線して答えが正しいことを確かめるには、自機が設計から生成した外皮で描かれていることが要る——現在の自機は事前生成のモデルで描かれており、搭載要素の取り付け位置と画面上の見え方が対応しないため、指した先が合っているかどうかを判断できない。これを与えるのは第1巻 F12 である。分離機構の「ここで分離」（§19-4）は、分離の物理そのものが無ければ実行できる操作を持たないため B1 を要する。**したがって B17 は、純関数だけを先に書くことはできるが、確かめられる状態になるのは F12 の後である。**

**B16 を塞ぐのは B14 ではなく F10 である。**核熱・電気・核融合はエンジンの型式であり、型式を表すデータ構造とツィオルコフスキーの計算は F10 が持つ。B14 の大気掠めは B16 の前提ではない。

**B11 の通信パネルは単独で先に置ける。**通信圏の判定は B3 が実装済みで、`CommNetwork` は `Game` が毎フレーム更新し、実際に無人機の計画実行を止めている。パネルはその結果を読むだけであり、段の一覧やペイロードのパネルが要求する B1〜B10 とは何も共有しない。

## 4. 実装済みでありながら、操作・確認の口を持たないもの

**プレイヤーが触れないものは、正しく動いているかどうかを確かめられない。**現在その状態にあるものは7つである。

| 実装済みのもの | 現在の状態 | 口を与える段階 |
|---|---|---|
| 設計データ `src/game/vessel/blueprint*.ts` | `BlueprintLibrary` を生成する箇所が無く、`VesselBlueprint` を設計層の外から参照する箇所も無い。`assemblyOf` は検証器の内部からしか呼ばれない。**設計から実機を作る経路が存在しない** | **B6**（設計から実機を得る経路そのもの）／編集は **B7** |
| 資源と設備 `src/game/economy/` | `ResourceLedger` を生成する箇所が無く、`producibility()` を呼ぶ箇所も無い。他モジュールからは型の import のみである | **B6**（在庫の消費と不足の表示）／設備の稼働は **B13** |
| 通信圏 `src/game/comms/` | `CommNetwork` は毎フレーム更新され、計画実行を実際に止めている。**圏内か圏外かをプレイヤーが見る手段が無い** | **B11 の通信パネル**／操作モジュールのウィンドウの圏内外の行は **B17** |
| ツリーと質量特性 | 実機に適用済みであり、慣性テンソルも投影面積も形状から導かれている。**ツリーを編集する手段が無い** | **B7**（骨格モード）／搭載は **B8** |
| 配管 `src/game/vessel/feed-network.ts` | 手動で敷いた区間を表す型だけがあり、敷設も解決も無い | **B9**（敷設と解決）／編集は **B11 の配管モード** |
| 熱シールド | アブレータが加熱に応じて減り、尽きれば閾値が戻る。**残量を見る手段が無い** | **B11**（第2巻 §6-11 は「大気に入る軌道を持つ機体に対して常時表示する」と定める）／モジュールごとの残量は **B17**（第2巻 §19-4） |
| 搭載要素の取り付け位置と要素ごとの装甲 | 要素は機体ローカルの取り付け位置（`src/game/vessel/tree.ts` の `MountPoint`、`assembly.ts` の `VesselAssembly`）を持ち、装甲は要素ごとに減る。機体の性能値もすべて要素の合成である。**飛行中に、要素ごとの装甲・状態・性能値を見る手段が無い。**全画面の `BaseView` の部品タブは格納中の機体しか扱わない | **B17**（戦闘ビューでモジュールを右クリックして開くプロパティーウィンドウ） |

第2巻 §20 の B5 は「**UI は持たない。**設計ファイルを手で書けば新しい機体が作れる状態にする」と定めている。UI を持たないこと自体は計画どおりである。**ただし現在は「手で書けば作れる」にも達していない。**設計を実機へ変える経路が無いためであり、それを置くのが B6 である。

**B7 が第1巻 F12 に塞がれることは、第2巻 §20 が理由まで書いている**——「組んだ形が外皮として見えなければ設計の操作を確かめられないため、F12 が先に要る」。骨格モードはノードとエッジを組む操作であり、組んだ結果が実寸の外皮として見えなければ、断面の指定が意図した形になっているかを判断できない。開発用の線画（F8 後段）は輪郭を示すだけで、外皮の見え方の代わりにはならない。

第2巻 §6 が定める組立モードは、**`hasBaseModule` を満たす機体を操作対象にしたときの戦闘ビューの中の操作モード**であり、専用のビューを持たない。したがって B7 が作るものには、骨格モードそのものに加えて**入口**——基地を操作対象にすると組立モードの開閉が現れ、開いている間だけ戦闘ビューの左右のレールへ編集パネルが出入りし、ドックモジュールの定める作業領域の枠が線として空間に現れること——が含まれる。この入口が無ければ、後続のどのモードも開く手段を持たない。

4つのモードのうち、この文書が扱う範囲で担当が分かれるのは3つである。**B7 が骨格モード**（ノード・断面・エッジ、接続口からのノードの生成、ミラー編集、元に戻す／やり直す）、**B8 が搭載モードと塗装モード**（要素パレットへの生産可能性の反映、トラス上の任意位置への外装要素の取り付け、`hull` 外表面への RCS の取り付け、外皮の色と質感）、**B11 が配管モードと右レールの段の一覧**を担当する。右レールの性能パネル（乾燥質量・Δv・推力重量比・電力収支・生産の不足）は骨格モードと同時に要るため B7 に含む。

**全画面の `BaseView` は、格納した艦艇・搭載部品の付け替え・在庫と生産キューを扱う画面であり、船体の作成をそこへ置かない。**したがって B7 は既存の `BaseView` を拡張する作業ではなく、戦闘ビューの側に新しい操作モードを足す作業である。

**飛行中の実機のモジュールを選ぶ経路（§19、B17）は、組立モードとは別の常設の仕組みである。**組立モードが扱うのは設計データであり、B17 が扱うのは既に飛んでいる実機である。同じ「モジュールを指して触る」操作に見えるが、対象が違い、依存も共有しない。

## 5. 未解決の追加事項の割り当て

`第9版_実装中の追加事項.md` の追加タスクのうち、T-1・T-2・T-3・T-6 は解決している。T-1 は F7 が姿勢積分を対称テンソル（`src/physics/inertia-tensor.ts` の `InertiaTensor`）へ一般化して解決した。T-3 は R3 が電力の実現可能性の検査を実装して解決した。T-2 は超伝導材に超伝導材製造炉が、塩素に溶融塩電解炉の陽極出力が、キセノンに火星と木星の大気捕集の産地が与えられて解決し、R3 の閉包検査がそれを保証している。T-6 はケロシンを資源の一覧から外すことで解決した。

残るものの割り当ては次のとおりである。

| 項目 | 内容 | 割り当て |
|---|---|---|
| **T-4** | 搭載要素ごとの建造費の表。`BlueprintPart.buildCost` は経路だけがあり、`src/game/vessel/vessel-parts.ts` の要素は資源量を申告しない | **B6**。生産が要求する当のものであり、表が空では在庫を消費できない |
| **T-5** | ヒドラジンの触媒床に要る白金族の質量。現在は `needed: 0` として「量ではなく所持そのものの要求」で表している | **B6**。生産の要求を集計する段階で、ふつうの資源要求へ変える |
| **T-7** | `Vessel.planExecution` を代入からメソッドへ閉じる | **F9**。姿勢制御の要求元を一元化する段階であり、`PlanExecutorShip` インターフェースの変更をまとめて行える |
| **T-8** | `CLAUDE.md` に残る経緯の記述の一掃 | **単独の作業**。段階に含めると、その文が担っている理由の説明を失う危険がある |
| **T-9** | `DEVELOP/CONTROL_SYSTEM_REDESIGN_PROPOSAL.md` の識別子の読み替え | **単独の作業**。提案を実施すると決めた時点で、着手の前に行う |
| **T-10** | 投影面積に外装要素と、横並びのエッジを算入する | **F8**。接触形状を形状から導く段階であり、投影面積の畳み方も同じ幾何の扱いである |
| **T-11** | アブレータの減少分を機体質量から差し引く | **B1**。分離が `Vessel.massProperties` を再計算する経路を作るため、質量が構築時に一度きり決まる形が崩れる |
| **T-12** | 敵対機に質量特性の導出を適用するか決める | **F12**。自機のモデルをロフト生成へ移す段階で、敵対機を導出へ移すか `hostileAssembly` を消すかを決める |
| **T-13** | 合成した消費電力と廃熱を熱収支へ繋ぐ | **F13**。推進系HUDパネルが電力収支と放熱余裕を表示するため、繋ぎ先が同時に要る |
| **T-14** | 通信圏の判定をフレーム内で記憶し、間引きを実時刻にする | **単独の作業**。時間加速中に毎フレーム全再構築になるため、後続の段階を待たずに直す |
| **T-15** | 設計ファイルの入出力を既存のセーブと共通化する | **B6**。生産が設計ファイルの読み込みを実際に使う最初の場所である |

## 6. 着手順

**F12・B6・F9 を並行して始める。**この3つは互いに依存せず、それぞれが別の壁を崩す。

**F12（ロフトメッシュの生成）を最優先に置く。**現在の実装で最も大きな欠落は、設計を編集する手段が無いことである。編集の入口は B7 の組立モードであり、B7 を塞いでいるのは F12 だけである。F12 は F7 の質量特性が済んでいるため今すぐ着手でき、通れば B7 → B8 → B12 → B13 という第2巻の最長の連鎖の先頭が開く。**さらに B17（モジュールのプロパティーウィンドウ）を確かめられる状態にするのも F12 である。**機体が設計から生成した外皮で描かれて初めて、画面上で指した位置と搭載要素の取り付け位置が対応する。**残っている段階のうち、後続を最も多く抱えているのが F12 であり、この点は B17 が加わったことでさらに強まっている。**

**B6（生産）を同時に始める。**B6 は `VesselBlueprint` から `Vessel` を得る経路そのものであり、これが無いために設計データと資源の帳簿の両方が到達不能なコードとして置かれている。F12 とは何も共有せず、依存も互いに無い。B6 が通れば、設計ファイルを手で書いて機体を作るという第2巻 §20 が B5 に定めた状態に初めて到達する。**T-4 の建造費の表と T-5 の白金族の質量は、この段階が要求して初めて埋まる。**

**F9（姿勢制御系の分離）を3本目に置く。**F10 → F13 と F10 → B10・B16 を塞いでいるのは F9 だけであり、機体の系列でこれ以上先へ進む道は F9 を通る以外に無い。F9 を通せば磁気トルカの配線（B14 の一部）も同時に片付き、置かれたまま参照されていない `src/physics/geomagnetic.ts` に読み手ができる。

**B1（分離の物理）と F8（接触形状のカプセル化）は、上の3本と並行できる。**B1 の分離の物理は F10 を要さず、通れば B2・B4・B9 が開く。F8 は他の何も塞いでいないが、細長い機体の貫通が捕まらない現状を直すものであり、機体が長くなる多段（B1）と同時期に要る。

**B17（モジュールのプロパティーウィンドウ）は F12 の直後に置く。**判定の純関数と種別ごとのプロパティ行の組み立ては F5 が済んでいるため今すぐ書けるが、それだけでは画面に何も現れず、正しさを単体テスト以上には確かめられない。F12 が通った直後に配線すれば、同じ機体を組み立てるのに使った外皮が、そのままモジュールを指すための当たりになる。B17 は設計と生産の系列（B6〜B11）と何も共有せず、B7 の後を待つ理由も無い。**現在、機体は搭載要素の合成として性能を持ち、装甲も要素ごとに減っているのに、飛行中にそれを見る手段が1つも無い。**B17 はその口そのものである。分離機構の「ここで分離」の項目だけは B1 を待つが、それ以外の種別の行と操作項目は B1 と無関係に揃う。

**通信パネルは、上のどれとも独立に、いつでも置ける。**通信圏の判定は動いていて無人機の計画実行を実際に止めているのに、プレイヤーには**なぜ無人機が動かないのかが分からない**。パネル1枚で、既に払った実装の費用が初めて見えるようになる。B11 の他の内容（段の一覧、ペイロード）は B1〜B10 を待つが、通信パネルだけを先に出すことに障害は無い。

**T-14（通信圏の判定のフレーム内記憶）も待たない。**時間加速中に毎フレーム 101 天体の走査を機体ごと・サブステップごとに行う形は、フレーム予算が最も苦しいときに最も重くなる。

**着手の先頭は F12・B6・F9 の3本のままである。**B17 が加わっても、それ自身が誰かを塞ぐわけではなく、確かめられる状態になるのが F12 の後だからである。B7 の内容に組立モードの入口が含まれることも、B7 を塞いでいるものを変えない——入口は基地を操作対象にしたときの戦闘ビューへ開閉を足すことであり、既存の `BaseView` にも `ViewManager` のビューの追加にも依存しない。

その後の順序は依存に従う。F10 は F9 の直後に置き、通れば F13 と B16 が開き、B1 の段を跨ぐ燃焼の判定も足せる。B9（配管）と B10（与圧）は B1 と F10 の両方が済んでから続けて通す。B2（ドッキング）は B1 の逆向きの計算であり、B1 の直後が最も安い。

## 7. 段階ごとの表

### 7-1. 第1巻の残り

| 段階 | 内容 | 依存 | 検証 |
|---|---|---|---|
| **F8** | 接触形状のカプセル化（§11-1）、ツリー・カプセルの開発用線画（§13 後段）、投影面積への外装要素と横並びエッジの算入（T-10） | なし | `tests/physics/capsule-contact.test.ts`（掃引カプセルが球同士の場合に掃引球と一致すること、細長い機体の貫通が捕まること）。`tests/unit/mass-properties.test.ts` に、展開した放熱板が投影面積に寄与することと、横へずれて並ぶ平行なエッジの射影が和になることを足す |
| **F9** | 姿勢制御系の分離（§14-1〜14-3、§14-9）、磁気トルカの配線、`planExecution` のメソッド化（T-7） | なし | `tests/unit/attitude-control.test.ts`（減衰最小二乗の配分が要求トルクを満たすこと、対向するスラスタが同時に噴かないこと、飽和と排出が別々の閾値で往復に落ちないこと）。`tests/unit/magnetorquer.test.ts`（磁場のない領域でトルクが0、磁場に平行な軸のトルクが0） |
| **F10** | 推進剤の物性表とエンジン型式（§15-1〜15-4）、ツィオルコフスキーによる燃焼計算（§15-5）、絞り率スロットル（§14-8） | F9 | `tests/physics/tsiolkovsky.test.ts`（質量比から Δv、Δv から推進剤質量の往復）、`tests/physics/plan-executor-math.test.ts` の拡張（燃焼時間が質量変化を含む式になること）。実行時検証（同じキーで機体設計により出る加速度が変わること） |
| **F12** | ロフトメッシュの生成と自機モデルの置き換え（§13）、敵対機の質量特性の扱いの決定（T-12） | なし | 目視確認（既定の設計の外皮が現在の自機と同じ大きさに見えること、断面をまたぐ頂点の対応で外皮がねじれないこと）。`WebGPURenderer` のジオメトリ差し替えを行わないことを実装で守る |
| **F13** | 推力軸と重心のずれ、ジンバル速度を含む補償（§14-7）、推進系HUDパネル（§14-10）、推進剤とエンジンの劣化（§15-6）、消費電力と廃熱の熱収支への接続（T-13） | F10 | `tests/physics/thrust-alignment.test.ts`（重心を外れた推力が生むトルク、ジンバル速度で追随しきれない構成の検出）。実行時検証（推進系パネルが電力収支と放熱余裕を表示し、`totalPowerDraw` が `ThermalSystem` の入力に現れること） |

### 7-2. 第2巻の残り

| 段階 | 内容 | 依存 | 検証 |
|---|---|---|---|
| **B1** | `splitStages` と分離の物理（§7）、分離後のツリーと質量特性の再計算、アブレータ減少分の質量への反映（T-11） | なし（段を跨ぐ燃焼の判定のみ F10） | `tests/unit/stage-split.test.ts`（連結成分の分割、運動量保存）。実行時検証（分離して2つの物体が別々の軌道に乗ること、アブレータが尽きた機体の質量が減っていること） |
| **B6** | 生産と生産可能性の統合（§5）、建造費の表（T-4）、白金族の質量（T-5）、設計ファイル入出力の共通化（T-15） | なし | `tests/unit/production.test.ts`（要求の集計、在庫の消費、生産時間係数0での即時完成、建造費を持たない要素が存在しないこと）。実行時検証（設計ファイルを読み込んで実機が得られ、在庫が減ること） |
| **B7** | 組立モードの入口と骨格モード（§6-1、§6-2、§6-4）と右レールの性能パネル（§6-3） | F12 | 手動確認（基地を操作対象にすると組立モードの開閉が現れ、開くと戦闘ビューの左右のレールへ編集パネルが加わり、閉じると取り除かれること。作業領域の枠が線として見え、接続口のクリックでノードが生え、断面の指定が外皮の形として見え、枠を超える設計が枠を突き抜けて表示されること）。**外皮として見えなければ操作を確かめられないため F12 が先に要る** |
| **B2** | ドッキングポートによる結合（§8） | B1 | `tests/unit/docking-capture.test.ts`（捕獲条件が条件ごとに判定されること、補正が運動量と角運動量を保存すること、分割が結合前の運動量に戻ること）。実行時検証（2機を結合して1つの剛体として飛び、切り離せること） |
| **B4** | ペイロード倉庫と放出（§9）、弾薬の搭載要素化と段による制約（§12） | B1 | 実行時検証（無人ブースターで弾薬を軌道に投入し、艦艇が既存の接近回収で取れること、圏外では放出の指令が実行されないこと） |
| **B8** | 搭載モードと塗装モード（§6-5、§6-7） | B6, B7 | 手動確認（生産できない要素が淡色で不足を示すこと、トラス上の任意位置と `hull` 外表面に取り付けられること、塗った色がドックの照明の下で見えること） |
| **B9** | 配管の搭載要素化、`autoRouteFeed` と `resolveFeed`（§10） | B1 | `tests/unit/feed-network.test.ts`（自動敷設が繋がった構成を作ること、手動の上書きがやり直しで保持されること、流量不足で期待どおりの `EngineFeedResult` が返ること、分離で流路が切れること） |
| **B10** | `physics/pressurant.ts` と与圧低下による推力低下（§11） | B9, F10 | `tests/physics/pressurant.test.ts`（加圧式とポンプ式で同じ Δv に要る加圧ガス質量が差を持つこと、自己加圧で蒸発損失が増えること、与圧低下で加圧式は比例して、ポンプ式は下限で急に推力を失うこと） |
| **B11** | 通信パネル（先行して単独で置ける）、配管モード、右レールの段一覧、飛行中の段パネルとペイロードパネル、熱シールドのアブレータ残量表示（§6-6、§6-8、§6-11） | 通信パネルはなし／他は B1〜B10 | 実行時検証（圏外の無人機で通信パネルが圏外と最寄りの中継点を示すこと、大気に入る軌道の機体でアブレータ残量が常時見えること） |
| **B12** | 基地モジュールの区画（§14）。コンテナをツリーを持たない `Vessel` として実装する | B6, B8 | `tests/unit/habitat.test.ts`（閉ループ率ごとの補給量、廃熱から求まる要求放熱面積、水タンクの経路長からの遮蔽面密度）。コンテナの切り離しと回収は実行時検証 |
| **B13** | 敵のドロップ、生産設備の稼働、送電網と月面の電力（§16-1、§16-2） | B12 | `tests/unit/facility.test.ts`（電力不足時の停止順、変換の収支、アパタイトを持たない状態で電解が動かないこと）、`tests/unit/power-grid.test.ts`（距離と送電量に対する導体質量と損失） |
| **B14** | 熱シールドと大気掠め（§15-3）、大気捕集による産地（`access: 'atmosphere'`）、永久影の放熱（§16-3）。**磁気トルカは F9 に含む** | B13 | `tests/unit/aerobraking.test.ts`（大気掠めのアブレータ消耗と減速量）、`tests/unit/radiator.test.ts`（背景温度による実効性能の差）。実行時検証（火星と木星の大気捕集でキセノンが得られること） |
| **B15** | 軌道上での結合（§6-9）。2つのツリーを1つにし、質量特性・配管・段を再計算する | B2, B9, B12 | 実行時検証（ドックの最大寸法を超える構造物が組め、1つの設計として保存できること） |
| **B17** | 戦闘ビューでのモジュールのプロパティーウィンドウ（§19）。`src/game/vessel/module-pick.ts` の `pickModule`、`src/game/hud/module-property-rows.ts` の種別ごとのプロパティ行と操作項目、操作モジュールのウィンドウが機体全体のウィンドウであること、ツリーを持たない機体と操作モジュールを持たない機体の扱い | 判定の純関数はなし／配線と実行時検証は F12／分離機構の操作項目は B1 | `tests/unit/module-pick.test.ts`（外装要素が背後の `hull` エッジより優先されること、見かけの直径が閾値を下回るモジュールが候補から外れること、どこにも当たらないクリックが操作モジュールを返すこと、操作モジュールの候補が複数あるとき序列どおりに1つへ定まること）。実行時検証（主機を右クリックして推力と供給状態が出ること、操作モジュールを右クリックすると機体全体のウィンドウが出ること、既存の `PropertyWindow` がそのまま用いられ並行する実装が増えていないこと） |
| **B16** | 核熱推進・電気推進・核融合推進のエンジン型式、ヘリウム3の同位体分離（§17-3〜§17-5） | F10, B14 | `tests/physics/tsiolkovsky.test.ts` の拡張、`tests/unit/jupiter-escape.test.ts`（木星脱出の質量比が有限に収まること） |

### 7-3. 段階に属さない作業

| 作業 | 内容 | 検証 |
|---|---|---|
| **T-14** | 通信圏の判定をフレーム内で記憶し、`CommNetwork.update` の間引きを実時刻で判定する | 実行時検証（時間加速中に通信圏の判定が毎フレーム全再構築にならないこと）。`[F3]` の負荷表示で確かめる |
| **T-8** | `CLAUDE.md` に残る経緯の記述を一掃する。文が担っている理由の説明を失わないよう、1件ずつ現在の姿の記述へ置き換える | `npm run typecheck` は無関係。文書の読み直しによる |
| **T-9** | `DEVELOP/CONTROL_SYSTEM_REDESIGN_PROPOSAL.md` の識別子を現在の実装の名前へ読み替える。提案を実施すると決めた時点で、着手の前に行う | 同上 |
