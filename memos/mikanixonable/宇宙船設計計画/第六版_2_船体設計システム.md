# 船体設計システム — 実装計画

この文書は、プレイヤーが宇宙船の形状を設計し、保存し、生産できるようにするための計画である。

対象は以下の6つである。

1. 断面の定義と、その面積・断面二次モーメントの計算（§3、§5）
2. ノードとエッジからなる背骨と、そこから生成される外皮（§4、§6）
3. 接触形状と空力特性（§7）
4. 搭載要素の配置と内容積の割り当て（§9）
5. 設計の保存・検証・生産（§10）
6. 設計エディタ（§11）

**他の巻との関係** — この巻は『推進系と推進剤』の巻で実装する `Vessel` への統合に依存する。艦艇と軌道基地が単一のエンティティ `Vessel` になっており、搭載要素の型と性能値が定義済みであることを前提とする。ただし §3 と §5（断面の幾何）は `src/physics/` に閉じた純関数のみであり、いかなる依存も持たない。この2節は他のすべてに先行して着手できる。

配管の敷設、タンクの与圧、分離機構による多段化、ペイロードの投入は『配管・多段・ペイロード』の巻の対象である。この文書では、エッジの種別として分離機構が存在することと、内容積に配管を収められることのみを扱い、その動作には踏み込まない。生産に必要な資源と設備、およびそれらが広がっていく進行は『資源と推進の進化』の巻の対象である。

---

## 1. 設計判断

### 1-1. 船体は背骨と断面で定義する

宇宙船の形状は、**ノードとエッジからなる背骨**と、**各ノードが持つ断面**で定義する。外皮は、エッジの両端にある2つの断面をつないだ立体（ロフト）として生成する。

エッジから外皮までの距離は定数ではなく、両端のノードの断面寸法によって決まる。したがって、細い部分と太い部分を持つ機体、先細りの機体、段ごとに直径の異なるロケットが、追加の機構なしに作れる。

### 1-2. 断面が分岐方向を決める

各ノードは、断面の表裏に向かう2つの軸方向の接続口と、断面形状ごとに決まる側面の接続口を持つ。側面の口の方向は断面が決め、各ノードが持つ回転位相ぶん回る。

- **多角形の断面** — 各辺の外向き法線の方向に口を持つ。三角形なら3方向、八角形なら8方向である。
- **円の断面** — プレイヤーが分岐数 n を 2・3・4・5・6 から選び、360度を n 等分した方向に口を持つ。分岐数2なら0度と180度、分岐数3なら0度と120度と240度である。
- **楕円の断面** — 長軸の正負2方向に口を持つ。

分岐の角度をプレイヤーが数値で指定する操作は存在しない。角度は断面形状と分岐数と回転位相から決まる。

### 1-3. 結合はすべて平面同士の突き合わせとする

エッジと断面の結合、側面分岐の結合、および断面を組み合わせて複合断面を作るときの結合は、いずれも平面同士の突き合わせとする。曲面同士を滑らかに融合させる処理も、立体同士の重なりを差し引く処理も行わない。

この判断により、外皮の体積・表面積・重心・慣性テンソルがすべて解析式で求まり、部分同士が重なることもない。数値積分も、等値面の三角形分割も不要である。実際のロケットも、段間部と側面ブースターの取り付けはフランジ結合であり、工学的にも自然である。

円の断面に側面の口を設ける場合のみ、その方向の円弧を弦で切り落として平面を作る。切り落としによる断面積と断面二次モーメントの減少は、弓形の解析式を差し引いて厳密に計算する。

### 1-4. 寸法は離散値とする

エッジの長さは 0.5 メートル単位で指定する。断面の外接円半径は、**2.0 メートル未満では 0.25 メートル単位、2.0 メートル以上では 0.5 メートル単位**とする。

半径の刻みを長さより細かくするのは、半径が体積に二乗で効くためである。半径 1.0 メートルのタンクを 1.5 メートルにすると体積は 2.25 倍になり、小型の機体では 0.5 メートル刻みは粗すぎる。大型の機体では相対的な粒度が十分に細かいため 0.5 メートルで足りる。

連続値にしないのは、対称な機体や、断面の揃った段を組むことが困難になるためである。

### 1-5. 物理量は形状から導出する

質量・重心・慣性テンソル・投影面積は定数として持たず、背骨と断面と搭載物から導出する。扁平な機体は実際に回りにくく、非対称な機体は実際に重心がずれる。

### 1-6. 接触形状はカプセルの集合とする

剛体接触と被弾の判定は、機体を1つの球で表すのではなく、**背骨の各エッジに対応するカプセルの集合**で行う。全長数十メートルの細長い機体や扁平な機体を1つの球で表すと、当たり判定が形状とまったく合わない。

カプセルの集合であれば、既存の空間格子による近傍探索をそのまま使え、掃引球の判定をカプセルに拡張するだけで済む。被弾したカプセルから、損傷する搭載要素も自然に決まる。

### 1-7. 設計と生産を分ける

プレイヤーは、資源を消費せずに設計を作り、名前を付けて保存し、読み込める。実機を得るには、保存した設計を指定して生産する。生産には時間がかかる仕組みを持つが、初期状態ではすべての設計の生産時間を0とする。

---

## 2. 前提

この巻は、以下がすでに実装されていることを前提とする。

- **`Vessel`** — 艦艇と軌道基地を統合した単一のエンティティクラス。背骨と搭載要素を保持し、質量特性・推進剤・電力・熱・推進系・姿勢制御を持つ。背骨を持たない機体（敵艦）のために、質量特性を直接与える経路も持つ。
- **搭載要素の型と性能値** — 武器、ドッキングポート、太陽電池パドル、放熱板、エンジン、RCSスラスタ（以上は外装）、酸化剤タンク、還元剤タンク、球状イナートガスタンク、RCS推進剤タンク、バッテリー、コックピット、自動操縦装置、弾薬庫、配管、ペイロード倉庫、フライホイール、磁気トルカ、基地モジュール（以上は内装）。それぞれの質量・性能値・資源コストが定義済みであること。
- **推進剤の物性表とエンジンの型式** — 推進剤ごとの比推力・密度・凝固点・貯蔵損失・タンク材料の要求、エンジンのサイクルごとの性能係数と全長。設計エディタが Δv と機体の全長を求めるために用いる。
- **姿勢制御アクチュエータの配分** — RCSスラスタが任意の位置と向きに配置されうることを前提とした、トルクと並進推力の配分。
- **能力の判定** — `hasCockpit` / `hasAutopilot` / `hasBaseModule` / `hasEngine` / `isOperable`。設計の検証と、設計エディタを開ける条件の判定に用いる。

---

## 3. 断面

### 3-1. 定義

断面は、1つ以上の**基本断面**を面で貼り合わせた複合体である。

```ts
// src/game/vessel/cross-section.ts
export type PrimitiveShape =
  | { kind: 'circle';   radius: number; branchCount: 2 | 3 | 4 | 5 | 6 }
  | { kind: 'ellipse';  majorRadius: number; minorRadius: number }
  | { kind: 'polygon';  sides: 3 | 4 | 5 | 6 | 8; radius: number }
  | { kind: 'notched';  sides: 6 | 8; radius: number };  // 辺長が 1:2 で交互する切り欠き型

export interface SectionPrimitive {
  readonly id: string;
  readonly shape: PrimitiveShape;
  readonly phaseAngle: number;                      // この基本断面の回転位相 [rad]
  readonly attachment: PrimitiveAttachment | null;  // null は複合体の根
}

export interface PrimitiveAttachment {
  readonly parentId: string;            // 貼り付け先の基本断面
  readonly parentFaceIndex: number;     // 貼り付け先のどの辺に付けるか
  readonly childFaceIndex: number;      // 自分のどの辺を貼り付けるか
}

export interface CrossSection {
  readonly primitives: readonly SectionPrimitive[];
}
```

- 複合体は木構造である。各基本断面は、親の1つの辺に、自分の1つの辺を重ねて貼り付ける。
- 貼り付ける2つの辺は長さが等しくなければならない。等しくない場合は設計として不正とし、エディタが理由を表示する。
- 貼り合わせ面を共有するため、基本断面同士は重ならない。したがって複合断面の面積は基本断面の面積の和であり、断面二次モーメントは平行軸の定理で合成した和である。
- 円と楕円は辺を持たないため、複合体の根としてのみ使える。子として貼り付けることはできない。

例として、四角形断面の一辺に三角形断面を貼り付けると、五角形の家型の断面になる。四角形の対辺2つに三角形を貼り付ければ、六角形の紡錘型になる。八角形の複数の辺に四角形を貼り付ければ、突起を持つ断面になる。

### 3-2. 基本断面の形状

| 形状 | 寸法パラメータ | 側面の口の数 | 口の方向 |
|---|---|---|---|
| 円 | 半径 | 2・3・4・5・6 から選ぶ | 360度を分岐数で等分した方向 |
| 楕円 | 長半径・短半径 | 2 | 長軸の正負方向 |
| 正三角形 | 外接円半径 | 3 | 各辺の外向き法線 |
| 正方形 | 外接円半径 | 4 | 各辺の外向き法線 |
| 正五角形 | 外接円半径 | 5 | 各辺の外向き法線 |
| 正六角形 | 外接円半径 | 6 | 各辺の外向き法線 |
| 正八角形 | 外接円半径 | 8 | 各辺の外向き法線 |
| 切り欠き六角形 | 外接円半径 | 6 | 各辺の外向き法線 |
| 切り欠き八角形 | 外接円半径 | 8 | 各辺の外向き法線 |

**切り欠き型** — 正多角形の辺の長さがすべて等しいのに対し、切り欠き型は長い辺と短い辺が1対2の比で交互に並ぶ。

- **切り欠き八角形** — 正方形の4つの角を切り落とした形である。長さ2の辺が4つ（元の正方形の辺の残り）と、長さ1の辺が4つ（切り落とした角の断面）を持つ。長い辺の中点に大きな面を確保しつつ、角の張り出しを抑えられる。
- **切り欠き六角形** — 正三角形の3つの角を切り落とした形である。長さ2の辺が3つと、長さ1の辺が3つを持つ。

切り欠き型は、正多角形より広い平面を確保しながら外接円半径を抑えられるため、太陽電池パドルや放熱板のような広い取り付け面を要する部品を並べるのに適する。

### 3-3. 側面の口の大きさ

側面の口は、辺の方向（周方向）と軸方向が張る矩形である。

- **周方向の寸法** — その辺の長さそのもの。
- **軸方向の寸法** — 母断面の外接円半径に `PORT_WIDTH_RATIO`（既定 1.0）を掛けた値。大きな断面には大きな分岐が付けられる。

分岐するエッジの相手側の断面は、その外接円の直径がこの矩形の両方の寸法以下でなければならない。収まらない場合は設計として不正とする。

隣接する辺の口同士は干渉してはならない。多角形では辺が離れているため通常は干渉しないが、円の分岐数が多く口の幅が広い場合には、切り落とす弓形が重なりうる。これも設計として不正とする。

---

## 4. 背骨

```ts
// src/game/vessel/skeleton.ts
export interface SkeletonNode {
  readonly id: string;
  readonly pos: Vec3;              // 船体ローカル座標 [m]
  readonly section: CrossSection;
  readonly axis: Vec3;             // 断面の法線（背骨の進行方向）
  readonly phaseAngle: number;     // 断面全体の回転位相 [rad]
}

export type EdgeKind =
  | { kind: 'hull' }                                   // 外皮を作る。内側が内容積になる
  | { kind: 'strut'; radius: number }                  // 外皮を作らない構造材
  | { kind: 'decoupler'; separationImpulse: number };   // 外皮を作らず、切り離せる

export interface SkeletonEdge {
  readonly id: string;
  readonly a: string;
  readonly b: string;
  readonly portA: PortRef;
  readonly portB: PortRef;
  readonly length: number;         // 0.5 m 単位
  readonly kind: EdgeKind;
}

export type PortRef =
  | { kind: 'axial'; sign: 1 | -1 }
  | { kind: 'lateral'; primitiveId: string; faceIndex: number };

export interface VesselSkeleton {
  readonly nodes: readonly SkeletonNode[];
  readonly edges: readonly SkeletonEdge[];
}
```

背骨はグラフであり、閉路を含んでよい。三角形や梯子状に組めば扁平な機体になる。

`decoupler` エッジは、分離可能な境界を表す。この巻ではエッジの種別として存在し、外皮を作らないことのみを扱う。分離の動作は『配管・多段・ペイロード』の巻の対象である。

### 4-1. 接続口のローカル座標系

各接続口は、その口に固有のローカル座標系を持つ。

- **原点** — 口の平面の中心
- **+Z 軸** — 口の平面の外向き法線
- **+Y 軸** — 口の基準ロール方向。軸方向の口では断面の回転位相が決め、側面の口ではその辺に沿う方向とする
- **+X 軸** — 右手系で決まる

外装の搭載要素は、この座標系に取り付く。**可動部を持つ要素（放熱板の折り畳み、太陽電池パドルの回転、遮蔽の展開、武装の俯仰）は、すべてこの座標系を基準に動く。**取り付け位置を固定座標で持つ要素は存在しない。

弾薬ベルトは武装に付属する内装要素であり、武装を取り付けたノードの接続口の座標系から垂れる。

### 4-2. 内容積の連続性

**軸方向に連なるエッジ同士は断面を共有するため、内容積が連続する。**同じ推進剤を収める限り、軸方向に連なる複数のエッジを1つのタンクとして扱える。

**側面から分岐するエッジの内容積は、母体の内容積と連続しない。**分岐部の付け根は母体の外皮の表面であり、分岐エッジは母体の外側から始まる。母体の内容積は分岐によって欠けることなく、そのまま残る。したがって、母体と分岐にまたがる1つのタンクは作れない。

ノード自身は面であって体積を持たない。3本以上のエッジが集まるノードでも、体積が二重に数えられることはない。

---

## 5. 断面の面積と断面二次モーメント

基本断面が多角形（正多角形・切り欠き型・複合体の構成要素）である場合、頂点列から以下で厳密に求める。頂点を反時計回りに `(x_i, y_i)`、`i` の次を `i+1` として、

```
cross_i = x_i·y_{i+1} − x_{i+1}·y_i
A   = (1/2)    · Σ cross_i
Cx  = (1/(6A)) · Σ (x_i + x_{i+1}) · cross_i
Cy  = (1/(6A)) · Σ (y_i + y_{i+1}) · cross_i
Ix  = (1/12)   · Σ (y_i² + y_i·y_{i+1} + y_{i+1}²) · cross_i
Iy  = (1/12)   · Σ (x_i² + x_i·x_{i+1} + x_{i+1}²) · cross_i
Ixy = (1/24)   · Σ (x_i·y_{i+1} + 2·x_i·y_i + 2·x_{i+1}·y_{i+1} + x_{i+1}·y_i) · cross_i
```

を求め、平行軸の定理で重心まわりに移す。この式は任意の単純多角形に対して厳密であり、形状ごとの個別の式を持つ必要がない。切り欠き型も、複合断面も、この1つの式で扱える。

**相乗モーメント `Ixy` は0とは限らない。** 複合断面が対称でない場合、断面の主軸は座標軸から回転する。慣性テンソルの計算では `Ixy` を必ず含める。正多角形と円だけであれば `Ixy` は常に0になるが、複合断面を導入する以上これは成り立たない。

円と楕円は解析式で求める。

```
円（半径 r）      A = πr²,   Ix = Iy = πr⁴/4,   Ixy = 0
楕円（半径 a, b）  A = πab,   Ix = πab³/4,        Iy = πa³b/4,   Ixy = 0
```

**円に側面の口を設ける場合** — その方向の円弧を弦で切り落とし、平面を作る。切り落とされる弓形の中心角を `θ`、半径を `r` として、

```
弓形の面積        A_seg = (r²/2)·(θ − sin θ)
弓形の重心距離     d_seg = (4r·sin³(θ/2)) / (3·(θ − sin θ))
弓形の断面二次モーメント は θ の閉形式で求まる
```

を円の値から差し引く。分岐数ぶんの弓形を、それぞれの方向で差し引く。切り落とし量は §3-3 の口の幅から決まる。

```
src/physics/section-moments.ts   (純関数)
    polygonArea(vertices) -> number
    polygonCentroid(vertices) -> { x, y }
    polygonSecondMoments(vertices) -> { ix, iy, ixy }          // 重心まわり
    circleSegmentMoments(radius, halfAngle) -> { area, centroidDist, ix, iy }
    sectionMoments(section) -> { area, centroid, ix, iy, ixy }  // 複合断面全体
    sectionOutline(section, sampleCount) -> readonly Vec2[]     // ロフト用の輪郭
```

検証は `tests/physics/section-moments.test.ts` に置く。正n角形の面積と断面二次モーメントが既知の閉形式と一致すること、辺数を増やすと円の値に収束すること、切り欠き八角形が「正方形から4つの直角三角形を引いた値」と一致すること、複合断面の面積が構成要素の和と一致すること、対称な複合断面の `Ixy` が0になり非対称なものは0でないことを固定する。

---

## 6. 外皮の生成と立体の物理量

### 6-1. 外皮の生成

`hull` エッジは、両端のノードの断面をつないだ立体を作る。

- 両断面の輪郭を、それぞれ**120点**に等分してサンプリングする。120は3・4・5・6・8のいずれでも割り切れるため、辺数の異なる断面同士でも頂点が一対一に対応する。円と楕円も同じ120点で表す。
- **等分は弧長で行う。**円に側面の口を設けた断面は、円弧と直線が混じった輪郭になる。角度で等分すると直線部の点が疎になり、ロフトの帯の面積が不均一になる。弧長で等分すれば帯の面積が均等になる。
- 対応する頂点同士を直線で結び、側面を四角形の帯として張る。
- 両断面の回転位相が異なる場合、頂点の対応は位相差を考慮して、対応する頂点間の距離の総和が最小になる割り当てを選ぶ。これにより外皮がねじれない。
- 複合断面の場合、輪郭は複合体全体の外周である。貼り合わせ面の内側は輪郭に含まれない。

側面の口から分岐するエッジは、口の平面の上に相手側の断面を置き、そこから同じ規則でロフトする。分岐部は平面同士の突き合わせであり、本体の外皮と交差しない。

### 6-2. 立体の物理量

外皮は「断面が軸方向に線形に変化する一般化角錐台」の集まりである。

**エッジ1本の体積** — 長さ `h`、両端の断面積 `A₁` / `A₂`、中間断面の面積 `Am` として

```
V = (h/6)·(A₁ + 4·Am + A₂)
```

輪郭を線形補間するため中間断面は両端の輪郭の平均であり、`Am` はその平均輪郭の面積である。

**慣性テンソル** — 軸に沿った位置 `t ∈ [0, h]` における断面積 `A(t)`、断面二次モーメント `Ix(t)` / `Iy(t)` / `Ixy(t)`、断面重心 `C(t)` は、いずれも `t` の多項式である。したがって

```
I_axial      = ρ ∫ (Ix(t) + Iy(t)) dt
I_transverse = ρ ∫ (I(t) + A(t)·|C(t) − C_ref|²) dt
I_product    = ρ ∫ (Ixy(t) + A(t)·(...)) dt
```

はいずれも多項式の積分として閉形式で求まる。各エッジの慣性テンソルを平行軸の定理で船体重心まわりに移して合成する。

**表面積** — 側面の帯の面積の和に、露出した端面（他のエッジが接続していない断面）を加える。

**主軸3方向の投影面積** — 空力と接触に用いる（§7）。

```
src/physics/hull-loft.ts   (純関数)
    loftVolume(sectionA, sectionB, length) -> number
    loftInertia(sectionA, sectionB, length, density) -> InertiaTensor
    loftCentroid(sectionA, sectionB, length) -> number
    loftSurfaceArea(sectionA, sectionB, length) -> number
    loftProjectedArea(sectionA, sectionB, length, axis) -> number
```

検証は `tests/physics/hull-loft.test.ts` に置く。円断面の等断面ロフトが円柱の解析値と一致すること、両端の断面積が等しい角錐台の体積が `A·h` になること、L字に組んだ骨格の慣性主軸が3値とも異なること（すなわちジャニベコフ効果が発生する形状であること）を固定する。

### 6-3. 外皮の肉厚と構造質量

構造の質量は、外皮の表面積と肉厚から求める。肉厚は区画ごとに決まる。

**内圧を持つ区画（推進剤タンク、イナートガスタンク、コックピット）** — 薄肉圧力容器の式による。内圧 `p`、断面の外接円半径 `r`、材料の許容応力 `σ`、溶接効率 `e`、安全率 `k` として

```
t = k · p · r / (σ · e)
```

**内圧を持たない区画（機器区画、貨物区画、ペイロード倉庫）** — 座屈に対する最小肉厚と、製造上の下限肉厚の大きいほうを取る。

この扱いにより、**タンクを大きくすると質量が半径に比例して増える。**内圧の高い加圧式エンジン向けのタンクは、ポンプ式向けのタンクより厚く重い。これが加圧式の単純さと引き換えになる代償として、質量の形で現れる。

材料は資源進行によって選べる範囲が広がる。材料ごとに比強度（許容応力を密度で割った値）が異なり、アルミ合金・チタン合金・炭素繊維複合材の順に軽くなる。推進剤ごとに使える材料が限られる（液体水素は水素脆化に耐える材料、四酸化二窒素はチタン不可、過酸化水素は限られた材料のみ）ため、**推進剤の選択が材料の選択を通じて質量に効く。**

### 6-4. 質量特性の合成

```ts
// src/game/vessel/mass-properties.ts
export interface MassProperties {
  readonly dryMass: number;          // 構造 + 搭載要素の乾燥質量 [kg]
  readonly loadedMass: number;       // 推進剤と貨物を含む総質量 [kg]
  readonly centerOfMass: Vec3;       // 船体ローカル座標
  readonly inertia: InertiaTensor;   // 重心まわり
  readonly principalAreas: Vec3;     // 主軸3方向の投影面積 [m²]
}
export function deriveMassProperties(
  skeleton: VesselSkeleton,
  placements: readonly PartPlacement[],
  propellant: PropellantStore,
): MassProperties;
```

搭載要素はそれぞれの取り付け位置に質点として置く。推進剤はそれを収めるエッジの内容積の重心に置く。推進剤が減れば重心と慣性テンソルが変わるため、質量が一定量変化したときに再計算する。

---

## 7. 接触形状と空力特性

### 7-1. 接触形状

機体の剛体接触と被弾は、**背骨の各エッジに対応するカプセルの集合**で判定する。

```ts
// src/game/vessel/collision-shape.ts
export interface HullCapsule {
  readonly edgeId: string;
  readonly a: Vec3;        // 船体ローカル座標
  readonly b: Vec3;
  readonly radius: number; // 両端の断面の外接円半径の平均
}
export function deriveCapsules(skeleton: VesselSkeleton): readonly HullCapsule[];
```

- `hull` エッジと `strut` エッジがカプセルを持つ。`decoupler` エッジは分離機構そのものであり、両端のカプセルが接するため独自のカプセルを持たない。
- `GameEntity.radius` は**外接球の半径**として残す。空間格子による広域の絞り込みと、天体表面への到達判定はこの球で行う。狭域の判定だけがカプセルになる。
- 判定は、カプセル対球（弾丸、破片、薬莢）とカプセル対カプセル（機体同士）の2種である。既存の掃引球の判定を掃引カプセルに拡張する。

```
src/physics/capsule-contact.ts   (純関数)
    sweptCapsuleSphereToi(capsule, sphereStart, sphereEnd, sphereRadius) -> number | null
    sweptCapsuleCapsuleToi(a0, a1, b0, b1) -> number | null
    closestPointsOnSegments(p0, p1, q0, q1) -> { s: number; t: number }
```

**被弾したカプセルから損傷する搭載要素が決まる。**そのエッジの内容積に収めた内装要素と、そのエッジの両端のノードに取り付けた外装要素が候補になる。機体のどこに当たったかが、何が壊れるかに直結する。

### 7-2. 空力特性

抗力は自由分子流域（低軌道の高度）で働く。この領域では**抗力係数は形状にほとんど依存せず、`Cd ≈ 2.2` である。**形状の差は投影面積として現れる。

したがって `Cd` は 2.2 の定数とし、投影面積のみを形状と姿勢から求める。速度方向の単位ベクトルを機体座標系に移した `v̂` に対して、

```
A(v̂) = |v̂·x̂|·A_x + |v̂·ŷ|·A_y + |v̂·ẑ|·A_z
bcInv = Cd · A(v̂) / m
```

`A_x` / `A_y` / `A_z` は主軸3方向の投影面積であり、設計時に一度求める。この式は直方体に対して厳密であり、それ以外の形状に対しても十分な近似である。

**`bcInv` は姿勢に依存する値になる。**細長い機体を横に向ければ抗力が数倍になり、進行方向に向ければ最小になる。これは再突入と軌道の減衰に直接効き、姿勢を保つことに意味を与える。

同じ投影面積が太陽輻射圧の係数 `srpCoeff = C_R · A(ŝ) / m` にも用いられる。`ŝ` は太陽方向であり、`C_R` は表面の反射特性から決まる定数とする。

---

## 8. 描画

```
src/render/hull/loft-mesh.ts
    buildLoftMesh(skeleton, lod) -> THREE.BufferGeometry
```

エッジごとに120分割のロフト帯を生成し、露出した端面を蓋で閉じ、全体を1つのジオメトリに結合する。生成は設計が変わったときに1度だけ行う。遠距離では分割数を下げた別のジオメトリを用いる。

`WebGPURenderer` は `Mesh` のジオメトリを最初の描画時にキャッシュするため、生成済みメッシュのジオメトリを差し替えてはならない。設計変更時は `Mesh` ごと作り直す。

外装の搭載要素は、取り付けた接続口のローカル座標系（§4-1）に置く。これらは個別のメッシュとして外皮のメッシュに子として付ける。可動部はその座標系を基準に動く。

エンジンは性能値として全長を持ち、その長さぶんノズルが機体後端から突き出す。比推力の高いエンジンほどノズルが長い。

---

## 9. 搭載要素の配置と内容積

### 9-1. 配置

搭載要素は、背骨のノードに取り付ける**外装要素**と、エッジの内容積に収める**内装要素**に分かれる。

- **外装要素** — 武器、ドッキングポート、太陽電池パドル、放熱板、エンジン、RCSスラスタ。ノードの空いている接続口に取り付ける。接続口を1つ占有するため、そこからエッジを伸ばすことはできない。
- **内装要素** — 酸化剤タンク、還元剤タンク、球状イナートガスタンク、RCS推進剤タンク、バッテリー、コックピット、自動操縦装置、弾薬庫、配管、ペイロード倉庫、フライホイール、磁気トルカ、基地モジュール。`hull` エッジの内容積に収める。

```ts
// src/game/vessel/assembly.ts
export type PartPlacement =
  | { kind: 'external'; partId: string; nodeId: string; port: PortRef }
  | { kind: 'internal'; partId: string; edgeIds: readonly string[] };

export interface VesselAssembly {
  readonly skeleton: VesselSkeleton;
  readonly placements: readonly PartPlacement[];
}
```

内装要素の `edgeIds` が配列であるのは、軸方向に連なる複数のエッジをまたぐ1つのタンクを作れるためである（§4-2）。連なっていないエッジをまたぐ配置は不正とする。

### 9-2. 内容積の割り当て

`hull` エッジそれぞれの内容積に、プレイヤーが何を収めるかを指定する。そのエッジのロフト体積が上限である。1つのエッジに複数の内装要素を収めてよく、その体積の和が上限を超えないこと。

| 収容物 | 実効容積の係数 | 備考 |
|---|---|---|
| 極低温タンク（液体水素・液体酸素・液体メタン） | 0.85 | 断熱材のぶん減る |
| 常温貯蔵タンク（ヒドラジン・四酸化二窒素・過酸化水素） | 0.95 | |
| イナートガスタンク（球状） | 0.55 | 球をエッジの内容積に詰めるため、球以外の空間が死ぬ |
| RCS推進剤タンク | 0.95 | |
| ペイロード倉庫・弾薬庫 | 0.90 | 容積と最大積載質量の両方で制限される |
| コックピット | 0.80 | 与圧に必要な構造のぶん減る |
| バッテリー・自動操縦装置・フライホイール・磁気トルカ | 1.00 | |
| 配管 | 口径から決まる | エッジの長さと口径の断面積の積 |

固体モーターはタンクではなく、エッジそのものである。ケースが構造材を兼ねるため、固体モーターを割り当てたエッジは `hull` エッジ1本がモーター1基に対応する。

**イナートガスタンクが球状である理由** — 内圧に対して最も効率のよい圧力容器は球である。同じ耐圧なら球は円筒より薄い壁で済み、タンク自体の質量が小さい。一方、球は角柱状の内容積に詰めたときの充填率が悪い。この対立が実効容積の係数0.55として現れる。

---

## 10. 設計の保存・生産

### 10-1. 設計データ

```ts
// src/game/vessel/blueprint.ts
export interface VesselBlueprint {
  readonly id: string;
  name: string;
  readonly skeleton: VesselSkeleton;
  readonly placements: readonly PartPlacement[];
  readonly feedNetwork: FeedNetwork;    // 配管の敷設
  readonly paint: PaintScheme;
  readonly stageOrder: readonly string[];
  readonly createdAt: number;
  readonly updatedAt: number;
}
export interface PaintScheme {
  primaryColor: string;
  secondaryColor: string;
  accentColor: string;
  metalness: number;
  roughness: number;
}
```

設計は `localStorage` の `tepui.blueprints` に保存する。保存・読み込み・複製・削除・改名ができる。既存のセーブデータの入出力と同じ方式でファイルへの書き出しと読み込みも行う。

`feedNetwork` と `stageOrder` は、この巻では空の値として保持するのみである。内容の解決は『配管・多段・ペイロード』の巻が担う。

### 10-2. 検証

設計の検証は独立した純関数とし、エディタと生産の両方が同じ関数を呼ぶ。

```ts
// src/game/vessel/blueprint-validation.ts
export interface BlueprintIssue {
  readonly severity: 'error' | 'warning';
  readonly targetId: string;      // ノード／エッジ／搭載要素の id
  readonly message: string;
}
export function validateBlueprint(bp: VesselBlueprint): readonly BlueprintIssue[];
```

この巻で検証する内容:

- 背骨が連結していること
- 複合断面の貼り合わせ面の長さが一致すること
- 側面の口から出るエッジの断面が、口の周方向・軸方向の両方の寸法に収まること
- 隣接する側面の口が干渉しないこと
- 1つの接続口に、エッジと外装要素が同時に割り当てられていないこと
- 内容積の割り当てがロフト体積を超えないこと
- 内装要素がまたぐエッジが軸方向に連なっていること
- 推進剤とタンク材料が適合すること
- コックピットまたは自動操縦装置を1つ以上持つこと
- 推力軸と重心のずれが規定内であること
- ノード数・総質量・最大寸法・複合断面の構成要素数が上限以内であること

**生産に必要な資源と設備が揃っているかは、この検証では扱わない。**設計そのものの成立と、それを作れるかどうかは別の問いであり、後者は生産可能性の判定（§10-4）が答える。持っていない要素を使った設計も、設計としては正当であり、保存できる。

配管の連結、イナートガスの供給、弾薬庫の有無に関する検証は『配管・多段・ペイロード』の巻で追加する。

### 10-3. 生産

生産は、保存した設計を指定し、**資源と設備と電力**を消費して実機を得る操作である。通貨は使わない。

```ts
export interface ProductionTask {
  readonly id: string;
  readonly blueprintId: string;
  readonly startedAt: number;      // simTime
  readonly finishAt: number;       // simTime
}
```

- 消費する資源は、搭載要素ごとの資源コストと、外皮の質量に対応する構造材の和とする。
- 生産時間は搭載要素の合計に**生産時間係数**を掛けて求める。この係数は**セーブデータに含め、ステージごとの既定値を持つ**。既定値は0であり、すべての設計が即座に完成する。係数を後から有効にしても、既存のセーブに影響しない。
- 生産キューは基地モジュールを持つ機体が保持する。完成した機体はその基地の格納庫に配備される。

### 10-4. 生産可能性

**技術ツリーは存在しない。**研究点も、前提技術の木も、解禁の状態も実装しない。ある搭載要素を使った設計を生産できるかどうかは、その時点で持っている**資源・設備・電力**だけで決まる。

```ts
// src/game/economy/producibility.ts
export interface Requirement {
  readonly kind: 'resource' | 'facility' | 'power';
  readonly id: string;
  readonly needed: number;
  readonly available: number;
}
export function producibility(
  bp: VesselBlueprint,
  ledger: ResourceLedger,
  facilities: readonly string[],
  powerAvailable: number,
): readonly Requirement[];   // 空配列なら生産できる
```

- **設計は自由、生産だけが制約を受ける。**持っていない推進剤やエンジンを使った設計を作ることも保存することもできる。生産のときにだけ拒否される。
- 設計エディタは、生産できない設計に対して不足しているものを表示し、それがどこで手に入るか、何から作れるかを辿れる形で示す。
- 資源・設備・産地の定義と、そこから生まれる推進の世代の進行は『資源と推進の進化』の巻が定める。

---

## 11. 設計エディタ

基地モジュールを持つ機体の基地ビューに「設計」タブを置く。既存のウィジェット（`src/game/hud/widgets/`）のみで構成し、生の `<button>` および `<input>` を直接書かない。

### 11-1. 画面構成

```
+--------------------------------------------------------------------------------------+
| [設計名] ▾  [新規] [複製] [保存] [名前を変更] [削除]          [検証: 2件の警告]  [×]  |
+--------------------------------------------------------------------------------------+
| モード: [ 骨格 ] [ 搭載 ] [ 塗装 ]                                                     |
+---------------------------+--------------------------------+-------------------------+
| 左パネル                  | 中央: 3Dビューポート            | 右パネル                 |
| （モードごとに内容が変わる）|                                | （常に同じ内容）         |
|                           |   ノード／エッジをクリックで選択 |                         |
|                           |   接続口をクリックで新規ノード   | 乾燥質量        12.4 t  |
|                           |   ドラッグで視点回転             | 総質量          48.1 t  |
|                           |   ホイールでズーム               | 全長            34.5 m  |
|                           |                                | Δv           4,820 m/s  |
|                           |   表示: [骨格][外皮][断面][段]  | 推力重量比(月)     1.8  |
|                           |                                | 慣性主軸  1.2/3.4/3.5   |
|                           |                                | 推力軸ずれ      0.03 m  |
|                           |                                | 電力収支      +1.2 kW   |
|                           |                                | 放熱余裕         12 %   |
|                           |                                | ─── 段 ───────────── |
|                           |                                | 3: 上段  Δv 2,100 ▲▼  |
|                           |                                | 2: 中段  Δv 1,800 ▲▼  |
|                           |                                | 1: 下段  Δv   920 ▲▼  |
|                           |                                | ─── 生産 ─────────── |
|                           |                                | 不足: 炭素 1.2 t       |
+---------------------------+--------------------------------+-------------------------+
| [ 元に戻す ] [ やり直す ]                              [ この設計で生産 ]              |
+--------------------------------------------------------------------------------------+
```

**モードは3つである。**断面の編集は骨格モードの中で行い、独立したモードにしない。断面はノードの属性であり、ノードを選んだときに左パネルへ編集欄が出れば足りる。段の一覧も独立したモードにせず、右パネルの常設表示に置く。モードが増えるほど、プレイヤーが「今どのモードにいるか」を把握する負担が増える。

『配管・多段・ペイロード』の巻で、モードに「配管」が1つ加わって4つになり、3Dビューポートの表示切り替えに「配管」が加わり、右パネルに「加圧ガス余裕」が加わる。

右パネルの性能表示は、要素を選択またはホバーしている間、その要素を追加した場合の差分を数値の横に併記する。

### 11-2. 骨格モード

左パネルの内容:

| 区画 | ウィジェット | 内容 |
|---|---|---|
| **選択中のノード** | | |
| 断面の構成 | ツリー表示（折り畳み可） | 基本断面の木構造。行を選ぶとその基本断面を編集する。単一の基本断面なら折り畳んだまま使える |
| 基本断面の形状 | `SegmentedControl<PrimitiveShape>` | 円／楕円／三角／四角／五角／六角／八角／切り欠き六角／切り欠き八角 |
| 外接円半径 | `ValueInput` | 2.0 m 未満は 0.25 m 刻み、以上は 0.5 m 刻み |
| 楕円の短半径 | `ValueInput` | 楕円選択時のみ |
| 円の分岐数 | `SegmentedControl<2\|3\|4\|5\|6>` | 円選択時のみ |
| 基本断面の位相 | `Slider` | 選択中の基本断面の回転位相 |
| 断面全体の位相 | `Slider` | 15度刻みのスナップあり |
| 貼り付け | `Button` | 選択中の辺に新しい基本断面を貼り付ける。辺の長さが一致しない場合は警告 |
| 接続口 | 一覧 | 使用中／空きを表示し、空きをクリックすると新規ノードが生える |
| 断面の性能 | 表示 | 面積、内容積への寄与、断面二次モーメント、主軸の角度 |
| **選択中のエッジ** | | |
| 種別 | `SegmentedControl<EdgeKind>` | 外皮／構造材／分離機構 |
| 長さ | `ValueInput` | 0.5 m 刻み |
| 付加値 | `ValueInput` | 構造材の半径、分離機構の分離力積 |
| **全体** | | |
| ミラー編集 | `ToggleSwitch` | YZ平面 |
| 削除 | `Button` | 選択中のノード以下を削除 |

3Dビューポートは、断面の編集中に**選択中のノードの断面を正面から見た2D表示に切り替えられる**。辺をクリックすると、その辺が「側面の口の候補」か「基本断面の貼り付け先」かを選べる。

操作:

- **ノードの追加** — 選択中のノードの空いている接続口をクリックする。既定の長さのエッジと、同じ断面のノードが生える。生えた先が新しい選択対象になる。3Dビューポート上の接続口を直接クリックしても同じ操作になる。
- **ノードの移動** — エッジの長さを変えることで行う。ノードの座標を直接ドラッグする操作は持たない。位置は接続関係と長さから一意に決まる。
- **削除** — 選択中のノードとその先につながる部分をまとめて削除する。削除の前に、削除される範囲を3Dビューポート上で強調表示する。
- **ミラー編集** — 有効な間、YZ平面の片側で行った操作が反対側にも同時に適用される。
- **元に戻す／やり直す** — 設計データ全体のスナップショットを操作ごとに保持する。上限は50手とする。

### 11-3. 搭載モード

左パネルの内容:

| 区画 | ウィジェット | 内容 |
|---|---|---|
| 要素パレット | 種別ごとのアコーディオン | 外装要素と内装要素の一覧。生産に必要な資源または設備が揃っていないものは淡色で表示し、何が不足しているかを示す。選択して設計に含めることはできる |
| 選択中の要素 | 表示 | 質量・性能値・資源コスト。エンジンは全長・比推力・絞り範囲・要求与圧・点火あたりの損失も表示する |
| 選択中のエッジの内容 | 一覧 | そのエッジに収めた内装要素。追加・削除ができる。軸方向に連なるエッジへ拡張するボタンを持つ |
| | 表示 | ロフト体積、実効容積の合計、残容積、収容質量、タンク材料の適合 |

操作:

- 要素パレットで外装要素を選び、3Dビューポート上のノードの空いている接続口をクリックすると装着する。装着できない口はクリックできず、理由をヒントで示す。
- ホバー中は、装着した場合の外観を半透明で表示し、右パネルの性能に差分を表示する。
- エッジをクリックすると、その内容積の割り当てを編集する。

### 11-4. 塗装モード

左パネルに、基本色・副色・アクセント色のカラー選択、金属感と粗さのスライダー、および既定の配色プリセットを置く。3Dビューポートに即座に反映する。

### 11-5. データの流れ

```
DesignEditorState (エディタの編集中データ、Undo/Redo のスタックを持つ)
    │  編集操作ごとに
    ▼
VesselBlueprint
    ├─► validateBlueprint()        → 左パネルと3Dビューポートの問題表示
    ├─► sectionMoments()           → 断面の性能表示
    ├─► deriveMassProperties()     → 右パネルの質量・重心・慣性・投影面積
    ├─► deriveCapsules()           → 接触形状の表示（デバッグ表示）
    ├─► deriveStages()             → 右パネルの段の一覧と Δv
    ├─► producibility()            → 右パネルの不足の表示
    └─► buildLoftMesh()            → 3Dビューポートの外皮表示
```

性能計算は編集操作のたびに実行する。計算はすべて解析式であり、ノードとエッジの数に対して線形であるため、毎操作の実行で問題にならない。外皮メッシュの生成のみ、編集が一定時間止まってから実行する。

---

## 12. 段階的な実装計画

| 段階 | 内容 | 依存 | 検証 |
|---|---|---|---|
| **S1** | 断面の面積・重心・断面二次モーメント（§3、§5）。多角形の一般式、切り欠き型、円の弓形差し引き、複合断面の合成。`src/physics/` に閉じ、ゲームには繋がない | なし | `section-moments.test.ts` |
| **S2** | ロフトの体積・慣性テンソル・表面積・投影面積（§6-1、§6-2）。同じく `src/physics/` に閉じる | S1 | `hull-loft.test.ts` |
| **S3a** | 背骨のデータ構造（§4）、肉厚と構造質量（§6-3）、質量特性の導出（§6-4）、既定の設計の定義。**導出した値を機体には適用せず、既定の設計の導出値が現在の定数と一致することをテストで確かめるだけにする** | S2 | `mass-properties.test.ts`（既定設計の質量・慣性・投影面積が現在の定数と一致すること） |
| **S3b** | 導出した質量特性を機体に適用し、`const.ts` の質量・慣性・参照断面積の定数を廃止する | S3a | 既存の全ステージが動作し、姿勢運動が形状由来の慣性で動くこと |
| **S4** | 接触形状のカプセル化（§7-1）と姿勢依存の空力（§7-2） | S3b | `capsule-contact.test.ts`（掃引カプセルが掃引球と球同士の場合に一致すること、細長い機体の貫通が捕まること） |
| **S5** | ロフトメッシュの生成（§8）と内容積の割り当て（§9） | S3b | 目視確認 |
| **S6** | 設計データの保存・読み込み・検証（§10-1、§10-2）。UI は持たず、設計ファイルを手で書けば新しい機体が作れる状態にする | S3a | `blueprint-validation.test.ts` |
| **S7** | 設計エディタの骨格モード（§11-1、§11-2） | S5, S6 | 手動確認 |
| **S8** | 搭載モード（§11-3） | S7 | 手動確認 |
| **S9** | 生産と生産可能性の判定（§10-3、§10-4） | S6 | `producibility.test.ts`（不足の列挙が正しいこと） |
| **S10** | 塗装モード（§11-4） | S7 | 目視確認 |

**S1 から着手する。** これは `src/physics/` に閉じた純関数とテストのみであり、ゲームには一切繋がない。この巻の最大の未知は「複合断面と一般化角錐台の物理量が本当に閉形式で求まるか」であり、S1 と S2 でそれを確かめてから統合の費用を払う。

**S3a と S3b を分けるのは、失敗の切り分けのためである。**S3a で導出値が現在の定数と一致することを確かめてから適用すれば、姿勢運動が変わった原因が導出の誤りなのか適用の誤りなのかを判別できる。両方を1段階で行うと、どちらが原因か分からないまま挙動だけが変わる。

S3b で止めても、慣性が形状由来になった状態で遊べる。S6 で止めても、設計ファイルを手で書けば新しい機体を生産できる。

---

## 13. 既存実装に対する変更点

| 対象 | 変更内容 |
|---|---|
| `game/vessel/` | 断面・背骨・アセンブリ・質量特性・接触形状・設計データ・検証を新設する |
| `physics/section-moments.ts` / `physics/hull-loft.ts` | 新設する。純関数のみ |
| `physics/capsule-contact.ts` | 新設する。掃引カプセルの判定 |
| `game/game-entity/vessel.ts` | 背骨とアセンブリを保持し、質量特性と接触形状をそこから導出する。`bcInv` と `srpCoeff` を姿勢依存の導出値にする |
| `game/simulation/contact.ts` | 狭域の判定をカプセル対応にする。広域の絞り込みは外接球のまま |
| `game/const.ts` | 機体の質量・慣性・参照断面積・接触半径の定数を廃止し、設計から導出される値に置き換える。`PORT_WIDTH_RATIO` と半径の刻みの閾値を追加する |
| `game/hud/base-view.ts` | 設計・生産のタブを置く |
| `game/hud/design-editor/` | 新設する。骨格・搭載・塗装の各モード |
| `game/unlock-manager.ts` | ステージの解禁のみを扱う。搭載要素の解禁には関与しない |
| `game/save/save-data.ts` | 保存形式を `VesselBlueprint` と `VesselAssembly` を中心とした構造に置き換え、生産時間係数を含め、保存形式の版を更新する。旧形式は既定の設計へ読み替えるか、読み替え不能な場合は明示的に拒否する |
| `render/hull/loft-mesh.ts` | 新設する |
| `render/ships.ts` | 自機のモデルを事前生成アセットからロフト生成に置き換える。敵・破片は当面そのままとする |
| `tools/export-models.mjs` | 自機のベイクが不要になる。敵・破片・基地のベイクは残す |
| `CLAUDE.md` / `DEVELOP/OWNERSHIP.md` / `DEVELOP/CALLSTACK.md` / `DEVELOP/SPEC.md` | 各段階と同じ変更セットで更新する |

---

## 14. 技術的リスク

1. **複合断面の輪郭抽出** — 複合断面の外周は、基本断面の輪郭から貼り合わせ面を除いて連結したものである。貼り合わせ面が完全に一致していることが前提であり、浮動小数点の誤差で隙間や重なりが生じると輪郭が破綻する。辺の長さの一致は設計の検証で保証し、輪郭の連結は頂点の同一性ではなく、貼り合わせの木構造をたどって構成する。
2. **断面をまたぐ頂点の対応** — 辺数の異なる断面や、回転位相の異なる断面をつなぐとき、頂点の対応を誤ると外皮がねじれる。弧長による120点への等分と、対応距離の総和を最小にする割り当てで解決するが、両端の位相差が大きい場合や、複合断面と単一断面をつなぐ場合の見た目は確認が必要である。
3. **カプセル判定の費用** — 機体1機がエッジの数だけカプセルを持つため、機体同士の判定は組み合わせになる。外接球による広域の絞り込みを必ず先に通し、球が交わる場合のみカプセル同士を総当たりする。エッジ数の上限（設計の検証で保証する）が判定の上限を決める。
4. **姿勢依存の `bcInv`** — 毎フレーム速度方向と機体姿勢から投影面積を求めることになる。計算自体は内積3回で済むが、予測軌道の積分でも同じ値が要る。予測は未来の姿勢を持たないため、**予測では現在の姿勢の値を固定して使う**。これは姿勢を保っている機体では正確であり、回転している機体では平均から外れる。再突入の予測が姿勢に依存することを、UI で示す必要がある。
5. **描画メッシュの生成コスト** — 背骨から生成するメッシュは実行時生成であり、`WebGPURenderer` はジオメトリを最初の描画時にキャッシュする。生成は設計変更時のみとし、既存メッシュのジオメトリを差し替えない運用を守る必要がある。
6. **設計の自由度と性能の破綻** — 断面寸法とエッジ長に上限を設けないと、極端に大きな機体が作れてしまう。ノード数・エッジ数・総質量・最大寸法・複合断面の構成要素数に上限を設け、検証で拒否する。
7. **保存形式の互換性** — 機体の表現が部品の配列から背骨とアセンブリの構造へ変わるため、旧形式の保存データは既定の設計への読み替え以上のことはできない。読み替え不能な保存データは明示的に拒否し、破損した機体を生成しないこと。
8. **UIの総量** — 設計エディタは3つのモードを持ち、配管のモードが加われば4つになる。既存のパネルへ統合できないかを追加のたびに確認し、置き場の規約に従うこと。
