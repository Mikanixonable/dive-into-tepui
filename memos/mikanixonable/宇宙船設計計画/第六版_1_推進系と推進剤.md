# 推進系と推進剤 — 実装計画

この文書は、以下の4つをこのリポジトリに追加するための計画である。

1. 艦艇と軌道基地を単一の実体 `Vessel` に統合する（§3）
2. 搭載要素の型と性能値を定義する（§4）
3. 主機・並進RCS・フライホイール・磁気トルカを分離した推進系と姿勢制御系（§5）
4. 推進剤の種別と、実データに基づくエンジン性能、質量変化を含む燃焼計算（§6）

**この文書は他のどの計画にも依存しない。** 機体の形状は既存の実装のまま扱い、質量・慣性テンソル・推力は搭載要素の合計として求める。ここで定義した搭載要素の型と推進剤の物性は、そのまま後続の2巻が土台として使う。

**後続の2巻との関係** — 第2巻は機体形状を背骨と断面で定義し、質量・重心・慣性テンソル・断面積を形状の帰結として導出する。本文書が定義する搭載要素を「背骨のどのノードに取り付け、どのエッジの内容積に収めるか」は第2巻の担当であり、本文書は取り付け位置を一切扱わない。ただし本文書の姿勢トルク配分は、取り付け位置と噴射方向が設計ごとに変わることを前提として、任意配置のアクチュエータ集合に対して成立するように定義する（§5-2）。第3巻は推進剤の配管、タンクの与圧、多段分離、ペイロードを扱う。したがって、エンジンのサイクルごとに定める要求与圧（§6-2）が実際の設計上の制約として意味を持つのは、第3巻でイナートガスと配管が実装されてからである。本文書の段階では、要求与圧はエンジンの型式が持つ性能値として定義されるにとどまる。

---

## 1. 対象外

機体形状の定義、外皮の生成、設計エディタ、設計の保存と生産、資源と生産可能性、推進剤の配管、タンクの与圧、多段分離、ペイロードは本文書の対象外である。月面着陸、月面基地、資源採掘、ドッキングポートによる機体の結合も対象外である。

推進剤ごとに定めるタンク材料の要求（§6-1）は物性として登録するにとどめ、その材料が入手できるかどうかの判定は行わない。

---

## 2. 設計判断

### 2-1. 実体は `Vessel` 一種類のみとする

艦艇と軌道基地を別のクラスとして持たない。両者はいずれも「部品が載った物体」であり、物理的な差は積んでいる部品だけである。「これは基地か」という判定は存在せず、部品の有無から導かれる能力だけを問う。

### 2-2. 質量特性は2つの経路から与えられる

`Vessel` は「搭載要素と形状から質量特性を導出する機体」と「`MassProperties` を直接与えられた機体」の両方を扱う。前者は自勢力の設計した機体であり、後者は敵艦である。敵艦は背骨を持たず、種別ごとに定めた質量・重心・慣性テンソルをそのまま受け取る。`Vessel` の側から見れば、質量特性は「導出された値か、与えられた値か」の別なく同じ1つのフィールドである。

### 2-3. 推進と姿勢制御は4つの系統に分ける

軌道変更のための大きな Δv、近接機動と姿勢制御の補助、推進剤を消費しない姿勢制御、推進剤も反作用も要らない姿勢制御は、それぞれ推力・比推力・応答性・消費資源・使える領域が異なる。単一のスラスタとして扱わず、主機・並進RCS・フライホイール・磁気トルカの4系統に分ける。姿勢トルクをどのアクチュエータが出すかは、要求を受けた側が決める。

### 2-4. アクチュエータの配置は設計ごとに変わる

RCSスラスタの取り付け位置と噴射方向は、機体ごとに異なる。船体の6方向に1基ずつという配置を前提にしない。姿勢トルクの配分は、任意個・任意配置のスラスタ集合に対して成立する形で定義する。

### 2-5. 推進剤は登録されたペアからのみ選ぶ

比推力は実測値であり、任意の酸化剤と還元剤の組み合わせに対して定義できない。推進剤は「酸化剤と還元剤のペア」「単推進剤」「固体グレイン」「ハイブリッドの組」として表に登録されたものからのみ選ぶ。表は §6-1 に置く。

### 2-6. 性能値は搭載要素が持つ

推力・比推力・トルク・発電量・放熱量・冷却能力を、機体ごとの定数として持たない。すべて搭載要素の性能値の合計として求める。部品を積み替えれば、他に何も書き換えることなく機体の性能が変わる。

### 2-7. 燃焼は質量変化を含めて計算する

燃焼中は推進剤の消費により質量が減り、加速度が上がる。一定加速度を仮定すると、質量比の大きい機体で点火が遅れてマニューバノードを外す。燃焼時間と点火時刻は、必ず質量変化を含む式で求める。

---

## 3. 艦艇と基地の統合

### 3-1. 能力は搭載要素から導く

- **軌道基地** = 基地モジュール部品（格納庫・倉庫・生産設備）を積んだ `Vessel`。
- **艦艇** = コックピットと主機を積んだ `Vessel`。
- **無人ブースター** = 自動操縦装置と主機を積み、コックピットを積まない `Vessel`。

```ts
// src/game/vessel/capabilities.ts
export function hasCockpit(v: Vessel): boolean;      // 人が操作できる
export function hasAutopilot(v: Vessel): boolean;    // 無人で計画を実行できる
export function hasBaseModule(v: Vessel): boolean;   // 格納・倉庫・生産・設計ができる
export function hasEngine(v: Vessel): boolean;       // 主機を持つ
export function isOperable(v: Vessel): boolean;      // hasCockpit || hasAutopilot
```

操作対象に選べるのは `isOperable` を満たす機体である。基地ビューを開けるのは `hasBaseModule` を満たす機体である。

### 3-2. `Vessel` が持つもの

| フィールド | 内容 |
|---|---|
| `massProperties: MassProperties` | 乾燥質量・搭載質量・重心・慣性テンソル。搭載物と推進剤残量から導出するか、直接与えられる（§2-2）。変化時のみ再計算する |
| `propellant: PropellantStore` | 推進剤の種別ごとの残量 |
| `power: PowerSystem` | 発電・蓄電・消費 |
| `thermal: ThermalSystem` | 熱収支 |
| `propulsion: PropulsionSystem` | 主機・並進RCS（§5） |
| `attitudeControl: AttitudeControlSystem` | 姿勢トルクの配分（§5-2） |
| `parts: AnyPart[]` | 搭載要素。HPと性能の唯一の源 |
| `plan: Plan` / `planExecutor: PlanExecutor` | 軌道計画とその自動実行 |

`EntityManager` は `vessels: Vessel[]` という単一の配列を持つ。操作対象は `ActivePlayerController` が `Vessel | null` として保持する。

### 3-3. 敵艦の質量特性

敵艦は背骨を持たず、設計データも持たない。種別ごとに定めた `MassProperties`（質量・重心・慣性テンソル）をコンストラクタで直接受け取り、それを `massProperties` にそのまま置く。慣性テンソルの種別ごとの値と表示倍率・当たり半径は、現在の実装の値をそのまま引き継ぐ。

この経路は暫定ではなく、`Vessel` の恒久的な2つ目の入口である。敵艦に設計を持たせるかどうかは本文書の対象外であり、持たせない限りこの経路が使われ続ける。

### 3-4. 基地の格納機構

現在 `Base` が持つハッチ位置・ハッチ法線・格納スロット・最大格納数は、基地モジュール部品の性能値として移す。

**ドッキングポートによる機体の結合は実装しない。** したがって格納の判定は現在のまま「一定距離内かつ相対速度が小さい」とし、その距離と相対速度の閾値、およびハッチの位置と法線を、機体の定数からではなく基地モジュール部品から読む形に置き換える。判定の内容そのものは変えない。統合の途中で格納が動かなくなることのないよう、この置き換えは `Vessel` 統合と同じ変更セットで行う。

### 3-5. 表示種別の導出

マップ上の対象種別は、`hasBaseModule`（基地モジュールを持つか）と所属勢力の2軸から導く。

| 条件 | 種別 |
|---|---|
| 基地モジュールを持つ | `'base'` |
| 基地モジュールを持たず、自勢力 | `'player'` |
| 敵対勢力 | `'ship'` |

弾薬ピックアップは `Vessel` ではないため、`'ammo'` はこの導出の外側にそのまま残る。表示ポリシーが持つ自艦・敵・弾薬・基地の4トグルは、この導出の結果に対してそのまま適用でき、変更を要さない。

### 3-6. 配列統合の波及範囲

`vessels` 1本への統合は、配列構成に依存する以下すべてに波及する。段階 S1 の作業量はこの一覧が示す範囲である。

**`EntityManager`**

`all()` / `otherEntities()` / `attractors()` / `sync()` / `syncPlayers()` / `updatePlayers()` / `cleanup()` / `prune()` / `applyVisibility()` / `perfCounts()` / `findPlayer()` / `findEnemy()` / `parkPlayer()` / `removePlayer()` / `addPlayer()` / `addEnemy()` / `addBase()` / `updateBaseEquatorNodes()` / `syncEquatorNodes()`

**その他**

| 対象 | 波及の内容 |
|---|---|
| `Simulator.stepAttitudes` | 5配列の走査を `vessels` を含む構成に改める |
| `ActivePlayerController` | 保持する型と、`vessels` に対する生存掃引・引き継ぎ |
| `Targeter` の `CombatTarget` | 2クラスの合併型を `Vessel` 単一に改める |
| `NavTarget.resolveEntity` | 敵・自艦・基地の順に引く3段の探索を単一の探索に改める |
| `CreativeStage.placeObject` | 4種の作り分けを、積む部品の違いに改める |

---

## 4. 搭載要素

### 4-1. 一覧

搭載要素は、機体の外側に露出する**外装要素**と、機体の内容積を占める**内装要素**に分かれる。

**外装要素**

| 要素 | 主な性能値 |
|---|---|
| 武器 | 発射速度、1発あたりの威力、初速、給弾の要求量 |
| ドッキングポート | 適合クラス、移送速度 |
| 太陽電池パドル | 面積、発電効率、展開の可否 |
| 放熱板 | 面積、放熱効率、展開の可否 |
| エンジン | 対応推進剤、サイクル、推力、比推力、全長、ジンバル可動角、ジンバル速度、絞り範囲、再着火回数、点火1回あたりの推進剤損失、連続燃焼時間の上限 |
| RCSスラスタ | 推力、対応推進剤、比推力 |

**内装要素**

| 要素 | 主な性能値 |
|---|---|
| 酸化剤タンク | 対応推進剤、容積、断熱等級、要求与圧、材料 |
| 還元剤タンク | 対応推進剤、容積、断熱等級、要求与圧、材料 |
| イナートガスタンク（球状） | 容積、耐圧、充填ガス種別 |
| RCS推進剤タンク | 対応推進剤、容積、材料 |
| バッテリー | 蓄電容量、最大出力 |
| コックピット | 搭乗人数、必要与圧容積 |
| 自動操縦装置 | 計画実行の可否、消費電力 |
| 弾薬庫 | 収納マガジン数 |
| 配管 | 口径、対応推進剤、最大流量 |
| ペイロード倉庫 | 内容積、最大積載質量、開閉の可否 |
| フライホイール | 最大トルク、最大蓄積角運動量、消費電力 |
| 磁気トルカ | 最大磁気モーメント、消費電力 |
| 基地モジュール | 格納数、倉庫容量、生産設備、ハッチ位置、ハッチ法線、格納スロット |

**要件の一覧に対する追加** — 上の一覧には、明示的に挙げられていない4つの要素が含まれる。いずれも実装対象とする機能の成立に必要である。

- **RCSスラスタ** — RCS推進剤タンクを積む以上、それを噴射する推進器が必要である。タンクは内装、スラスタは外装であり、別の要素として扱う。
- **フライホイール** — 「RCSガスを消費しない姿勢制御」という機能そのものである（§5）。
- **磁気トルカ** — フライホイールの角運動量を推進剤ゼロで捨てる手段である（§5-3）。使える領域が限られるため、フライホイールの代替ではなく、フライホイールと対になる要素である。
- **分離機構** — 多段ブースターシステムの成立に必要である。ただしこれは搭載要素ではなく、機体の構造を切断する辺の種別として扱う。分離は接続の切断であり、取り付ける物ではないためである。詳細は第3巻が扱う。

### 4-2. 性能値の合成

機体の性能は、積んでいる搭載要素の性能値から求める。

| 機体の性能 | 求め方 |
|---|---|
| 総推力 | 主機の推力の合計 |
| 総トルク | 姿勢トルクの配分（§5-2）が、要求方向ごとに実際に出せる最大値として答える |
| 推進剤容量 | 推進剤の種別ごとに、対応するタンクの容積と密度の積の合計 |
| 発電量 | 太陽電池パドルの面積・効率・日射から求めた値の合計 |
| 蓄電容量 | バッテリーの容量の合計 |
| 放熱能力 | 放熱板の面積と効率から求めた値の合計 |
| 装甲 | 各要素のHPの合計。コックピットまたは主要構造を失った時点で0とする |

総トルクを単純な合計としないのは、アクチュエータの配置が設計ごとに変わるためである。同じ推力のスラスタでも、モーメントアームの向きによって出せるトルクの方向が異なり、方向によっては打ち消し合う。したがって「この機体のトルクはいくつか」という問いには、方向を指定しない限り答えがない。

推進剤の残量は `PropellantStore` が推進剤の種別ごとに保持し、タンクの容積の合計が上限である。どのタンクから減るかは区別しない。

---

## 5. 推進系と姿勢制御系

### 5-1. 4つのシステムの役割

| システム | 役割 | 消費 | 比推力 | 特性 |
|---|---|---|---|---|
| **主機（EngineBank）** | 軌道変更のための大きな Δv | 液体・固体・ハイブリッド推進剤 | 260〜450 s | 推力大。始動と停止にコストがある。固体は停止も再着火もできない |
| **並進RCS（TranslationRcs）** | 近接機動・姿勢制御の補助 | 単推進剤または冷ガス | 70〜230 s | 推力小。応答は即時。出せる方向はスラスタの配置で決まる |
| **フライホイール（ReactionWheels）** | 姿勢制御のみ | 電力のみ | — | トルク小・連続。**角運動量が飽和する** |
| **磁気トルカ（MagneticTorquers）** | 姿勢制御とアンローディング | 電力のみ（微小） | — | トルク極小。**地球磁場のある領域でしか使えない。磁場に平行な軸まわりのトルクは出せない** |

### 5-2. 姿勢トルクの配分

姿勢トルクを要求する主体は、手動操作と自動操縦の2つである。両者は `AttitudeControlSystem` に要求を出し、どのアクチュエータが出すかはそこが決める。

配分は「推進剤を消費しないものを先に使い、足りないぶんを推進剤で埋める」という3段の優先順位で行う。

1. **磁気トルカ** — 推進剤も、ごくわずかな電力しか消費しない。ただし出せるトルクは τ = m × B（m は磁気モーメント、B は地球磁場）であり、B に平行な成分は原理的に出せない。地球磁場のない領域では何も出せない。
2. **フライホイール** — 電力のみを消費する。出せる条件は、そのホイールの最大トルクを超えないことと、蓄積角運動量 h が上限 hMax に達していないことの両方である。ホイールが τ_wheel を出すと、反作用として角運動量が `h += -τ_wheel · dt` だけ溜まる。
3. **RCSスラスタ** — 上の2つで出しきれなかったぶんを出す。推進剤を消費する。

`|h|` が hMax の既定割合を超えると**アンローディング**を行う。外部トルクを与えながらホイールを減速し、h を外部に捨てる。外部トルクは磁気トルカを優先し、磁場が足りないぶんだけRCSが補う。自動実行を既定とし、手動実行のボタンをHUDに置く。

この規則により、地球低軌道では姿勢制御がほぼ無料で成立し、月周辺やラグランジュ点ではアンローディングのたびにRCS推進剤を消費する。活動する領域によって運用の性格が変わる。

### 5-3. 任意配置のアクチュエータに対する配分

RCSスラスタの取り付け位置 r_k と噴射方向 e_k は設計ごとに決まる。推力 F_k で噴いたとき、そのスラスタが機体に与える力は −F_k·e_k、重心まわりのトルクは τ_k = r_k × (−F_k·e_k) である。

要求トルクと要求並進推力を同時に満たす推力の組み合わせ (F_1, …, F_n) は、一般に**劣決定**である。スラスタが6基を超える機体では解が無数にあり、6基未満または配置が偏った機体では厳密解が存在しない。

したがって配分は、**減衰最小二乗**（Tikhonov 正則化した擬似逆行列）で解く。各スラスタの寄与を列に持つ 6×n の行列 A に対し、要求ベクトル b を満たす F を

```
F = Aᵀ (A Aᵀ + λI)⁻¹ b
```

として求める。正則化項 λ は、要求を厳密に満たすことより推力の総量を抑えることを優先する重みであり、そのまま推進剤消費を抑える働きをする。厳密解が存在しない配置でも、最も近い解が得られる。

得られた F_k は 0 以上・そのスラスタの最大推力以下にクランプする。クランプによって要求との差が残る場合、その差はそのまま出せなかったぶんとして扱う（再配分は行わない）。

```ts
// src/physics/attitude-control.ts   (純関数)
export interface TorqueRequest {
  readonly torque: Vec3;          // 要求トルク（機体座標系）
  readonly force: Vec3;           // 要求並進推力（機体座標系）
}
export interface ThrusterSpec {
  readonly position: Vec3;        // 重心からの取り付け位置 r_k
  readonly exhaust: Vec3;         // 噴射方向の単位ベクトル e_k
  readonly maxThrust: number;     // [N]
}
export interface ActuatorSet {
  readonly thrusters: readonly ThrusterSpec[];
  readonly wheels: readonly WheelSpec[];
  readonly torquers: readonly TorquerSpec[];
}
export interface Allocation {
  readonly torquerMoment: Vec3;           // 磁気トルカに与える磁気モーメント
  readonly wheelTorque: Vec3;             // フライホイールが出すトルク
  readonly thrusterLevels: readonly number[];  // 各スラスタの推力 [N]
  readonly achievedTorque: Vec3;          // 実際に出るトルクの合計
  readonly achievedForce: Vec3;           // 実際に出る並進推力の合計
  readonly powerDraw: number;             // [W]
}

export function allocateTorque(
  request: TorqueRequest,
  actuators: ActuatorSet,
  wheelState: WheelState,
  magneticField: Vec3 | null,   // 機体座標系での地球磁場。磁場のない領域では null
): Allocation;

export function desaturationRequest(
  wheelState: WheelState,
  hMax: number,
): TorqueRequest | null;
```

`allocateTorque` は3段の優先順位（§5-2）を順に適用し、各段で残った要求を次の段に渡す。磁気トルカの段は τ = m × B を m について解くが、B に平行な成分は解を持たないため、要求トルクの B に垂直な成分だけを扱う。

### 5-4. 磁場の評価

地球磁場は双極子近似で求める。

```ts
// src/physics/geomagnetic.ts   (純関数)
export function geomagneticField(r: Vec3): Vec3;   // ECI 位置 → 磁束密度 [T]
```

双極子の軸は地球の自転軸から約 11 度傾いているが、本文書ではこの傾きを含める。地球から離れるほど磁場は距離の3乗に反比例して弱まり、月周辺やラグランジュ点では磁気トルカが実質的に使えなくなる。この減衰は近似式がそのまま与えるため、「使える領域」を別途定義する必要はない。磁気モーメントと磁場の外積が要求トルクに対して無視できる大きさになった時点で、配分は自動的に次の段へ落ちる。

### 5-5. 重力傾斜トルク

全機体に重力傾斜トルクを適用する。

```ts
// src/physics/gravity-gradient.ts   (純関数)
export function gravityGradientTorque(
  rRel: Vec3,        // 重力源から機体への位置ベクトル
  mu: number,        // 重力源の重力定数
  inertia: Inertia,  // 機体座標系の慣性テンソル
  q: Quat,           // 機体の姿勢
): Vec3;
```

トルクは τ_gg = 3·(μ/r³)·(r̂ × (I·r̂)) である。r̂ は重力源方向の単位ベクトルを機体座標系に回したものであり、I は機体座標系の慣性テンソルである。慣性主軸が等しい機体ではトルクは 0 になり、細長い機体ほど強く、長軸を天底方向に向けようとする。

`Simulator.stepAttitudes` の姿勢積分に外部トルクとして加える。重力源はその位置で最も強く引く天体1つとする。

慣性テンソルを形状から導出する以上、この効果は形状の帰結として現れる。細長い機体を設計すれば、放っておくと天底を向く。姿勢を保つには姿勢制御系が常に働き続けることになり、その消費が低軌道での運用に効く。

### 5-6. 並進推力の選択

- 要求方向が主機の推力軸と一致し、要求の大きさがRCSの最大推力を超える場合は主機を使う。
- それ以外はRCSを使う。
- 要求の大きさが主機の絞り下限を下回る場合もRCSを使う（§5-8）。
- プレイヤーはHUDで「主機を使わない」「主機のみ」を明示的に選べる。

### 5-7. 推力軸と重心のずれ

主機の推力軸が機体重心を通らない場合、その差からトルクが生じる。この効果は正しく計算する。

ジンバル機構を持つ主機は、ずれを打ち消す方向に首を振る。ただしジンバルには可動角に加えて**ジンバル速度**の上限があり、速い姿勢変化には追従できない。追従しきれないぶんと、可動角で打ち消しきれないぶんは `AttitudeControlSystem` が補償する。ジンバルを持たない主機のずれは補償されず、機体が回る。

### 5-8. スロットル操作

主機の出力は**絞り率 0〜100% の連続値**で指定する。加速度による段階指定は持たない。機体の質量と積んだエンジンによって同じ絞り率でも加速度は変わり、燃焼中は質量が減るぶん加速度が上がる。

キー `[1]` 〜 `[4]` は絞り率のプリセットに割り当てる。

| キー | 絞り率 |
|---|---|
| `[1]` | 25% |
| `[2]` | 50% |
| `[3]` | 75% |
| `[4]` | 100% |

エンジンのサイクルごとの絞り下限（§6-2）を下回る指定は下限にクランプし、HUDにクランプされている旨を表示する。下限より低い出力が要るときは、主機ではなく並進RCSを使う。

複数のサイクルのエンジンを混載した機体では、絞り下限はエンジンごとに異なる。下限を下回る絞り率を指定されたエンジンは、下限で噴くのではなく停止する。これにより、絞り率を下げていくとエンジンが1基ずつ落ちていく挙動になる。

### 5-9. モジュール

```
src/physics/attitude-control.ts        (純関数)
    allocateTorque(request, actuators, wheelState, magneticField) -> Allocation
    desaturationRequest(wheelState, hMax) -> TorqueRequest | null
src/physics/geomagnetic.ts             (純関数)
    geomagneticField(r) -> Vec3
src/physics/gravity-gradient.ts        (純関数)
    gravityGradientTorque(rRel, mu, inertia, q) -> Vec3
src/physics/thrust-alignment.ts        (純関数)
    resultantThrust(engines, throttles) -> { force: Vec3; torqueAboutCom: Vec3 }
    gimbalCorrection(engines, desiredForce, com, dt) -> readonly GimbalAngle[]
src/game/vessel/attitude-control-system.ts
src/game/vessel/propulsion-system.ts
```

`gimbalCorrection` が `dt` を取るのは、ジンバル速度の上限によって1フレームで到達できる角度が制限されるためである。

### 5-10. 飛行中のHUD

飛行中に**推進系**パネルを置く。個別に折り畳める。主機・並進RCS・フライホイール・磁気トルカの4行を持ち、主機は絞り率と推進剤の種別ごとの残量とイナートガス残量、RCSは残量、フライホイールは蓄積角運動量と上限の比、磁気トルカは現在の磁場強度に対して出せるトルクの目安をメーターで表示する。フライホイールは飽和が近い場合に警告色にする。アンローディングの手動実行ボタンをこのパネルに置く。

**フライホイールが飽和してRCSに切り替わる瞬間に、短い通知音とヒントを出す。** メーターは飽和が近いことを示せるが、切り替わった瞬間そのものは見ていなければ気づけない。切り替わりは推進剤の消費が始まる瞬間であり、プレイヤーが知るべき出来事である。逆に、アンローディングが終わってフライホイールに戻る瞬間には通知しない。資源の消費が始まるほうだけを知らせる。

既存のウィジェット（`src/game/hud/widgets/`）のみで構成し、生の `<button>` および `<input>` を直接書かない。既存のパネルへ統合できないかを追加のたびに確認し、置き場の規約に従う。

---

## 6. 推進剤とエンジン

### 6-1. 推進剤の物性表

比推力は真空値、密度は貯蔵状態の値である。凝固点は還元剤と酸化剤のうち高いほうを示す。日あたり損失率は日射のある軌道での標準的な断熱を前提とした蒸発損失であり、断熱等級と遮蔽で低減できる。

**液体（酸化剤と還元剤のペア）**

| ペア | 真空比推力 [s] | 混合比 O/F | 平均密度 [kg/m³] | 凝固点 [°C] | 日あたり損失 [%/day] | タンク材料の要求 | 毒性 |
|---|---|---|---|---|---|---|---|
| 液体酸素 / 液体水素 | 450 | 5.5 | 360 | −253 | 1.5 | 水素脆化に耐える材料。アルミ合金または特定のステンレス | なし |
| 液体酸素 / 液体メタン | 370 | 3.5 | 830 | −183 | 0.4 | 制約なし | なし |
| 液体酸素 / シラン | 265 | 3.0 | 900 | −183 | 0.4 | 制約なし | シランは自然発火性 |
| 四酸化二窒素 / モノメチルヒドラジン | 320 | 1.65 | 1200 | −11 | 0 | **チタン不可**（応力腐食割れ）。ステンレスまたはアルミ合金 | 強い。専用設備を要する |
| 過酸化水素 / ケロシン | 310 | 7.0 | 1290 | −0.4 | 0 | 高純度アルミまたは特定のステンレスのみ | 弱い |

**単推進剤（RCS向け）**

| 推進剤 | 比推力 [s] | 密度 [kg/m³] | 凝固点 [°C] | 日あたり損失 [%/day] | タンク材料の要求 | 毒性 | 特性 |
|---|---|---|---|---|---|---|---|
| ヒドラジン（触媒分解） | 230 | 1010 | 2 | 0 | 制約なし | 強い。専用設備を要する | 触媒床が累積噴射時間で劣化する |
| 過酸化水素（触媒分解） | 160 | 1400 | −0.4 | 0 | 高純度アルミまたは特定のステンレスのみ | 弱い | 無毒に近い |
| ヘリウム冷ガス | 165 | 高圧貯蔵 | — | 0 | 高圧に耐える材料 | なし | イナートガスをそのまま使える。電力も点火も不要 |
| 窒素冷ガス | 70 | 高圧貯蔵 | — | 0 | 高圧に耐える材料 | なし | 最も単純。安価だが性能が低い |

**固体・ハイブリッド**

| 種別 | 比推力 [s] | 密度 [kg/m³] | 凝固点 [°C] | 日あたり損失 [%/day] | 毒性 | 特性 |
|---|---|---|---|---|---|---|
| 固体（過塩素酸アンモニウム + 末端水酸基ポリブタジエン + アルミ粉末） | 265 | 1800 | — | 0 | 燃焼生成物に塩化水素 | 点火後は停止も絞りも再点火もできない |
| ハイブリッド（ABS樹脂 + 亜酸化窒素） | 260 | 1100 | −91 | 0 | なし | **自己加圧できる**（蒸気圧が高く、イナートガスタンクが不要）。絞れる・停止できる・再点火できる |
| ハイブリッド（ABS樹脂 + 液体酸素） | 305 | 1000 | −183 | 0.4 | なし | 絞れる・停止できる・再点火できる。イナートガスによる与圧を要する |

**イナートガス**

| ガス | 密度（20 MPa, 常温） | 特性 |
|---|---|---|
| ヘリウム | 33 kg/m³ | 軽く、極低温でも凝縮しない。必要質量が小さい。冷ガススラスタにも使える |
| 窒素 | 230 kg/m³ | 安価だが重い。極低温推進剤には使えない |

固体推進剤の燃料成分はアルミ粉末であり、酸化アルミニウムはその燃焼生成物である。ABS樹脂はハイブリッドロケットの固体燃料側であり、亜酸化窒素または液体酸素の酸化剤と組み合わせて使う。

**凍結とヒータ電力** — 凝固点を上回る温度を保てない機体では、推進剤が凍る。タンク・バルブ・配管はヒータで加熱し、そのぶんの電力を消費し続ける。常温貯蔵性推進剤ほど凝固点が高く、日陰の長い軌道では維持に電力が要る。極低温推進剤は逆に、日射で蒸発するが凍らない。この対比が、推進剤の選択を電力系の設計と結び付ける。

**シランの位置づけ** — 液体酸素／シランの比推力が液体酸素／メタンより大きく劣るのは、燃焼生成物である二酸化ケイ素が燃焼室とノズルの温度で凝縮相であり、膨張過程で仕事に寄与しないためである。固体推進剤のアルミナと同じ機構であり、比推力が固体モーター並みに落ちる理由もそこにある。同じ理由で二酸化ケイ素がノズルスロートに堆積するため、**連続燃焼時間に上限がある**（§6-2）。この推進剤は、月のレゴリスと水氷だけで生産できる唯一の液体推進剤である。性能は固体モーター並みだが、絞れて停止でき再点火できる。

**選択の意味** — 固体は安価で高密度、ただし一度きりであり、使い捨ての帰投用ブースターに適する。液体酸素／液体水素は比推力が最も高いが密度が低く巨大なタンクを要し、蒸発損失が大きく、点火のたびの損失も大きいため、長期の待機と多数回の点火のどちらにも向かない。ヒドラジン系は性能が中位だが常温で長期間待機でき、軌道上に置いておくブースターに適する。ただし凍結を防ぐ電力と、毒性を扱う設備が要る。

### 6-2. エンジンの型式

エンジンは「サイクル（供給方式）」と「対応推進剤」の組で定義する。同じ推進剤でもサイクルが違えば性能が変わる。

| サイクル | 比推力係数 | 推力重量比 | 要求与圧 [MPa] | 絞り範囲 | 再着火 | 連続燃焼時間の上限 | 特徴 |
|---|---|---|---|---|---|---|---|
| 加圧式 | 0.95 | 低 | 2〜3 | 40〜100% | 無制限 | あり（アブレーティブ冷却） | 構造が単純で信頼性が高い。タンクを高圧に耐える厚さにする必要があり、タンク質量とイナートガスの消費が増える |
| 電気ポンプ式 | 0.98 | 中 | 0.2〜0.4 | 30〜100% | 無制限 | なし | 電力を消費する。バッテリー容量が燃焼時間を制限する |
| ガス発生器サイクル | 1.00 | 高 | 0.2〜0.4 | 60〜100% | 数回 | なし | 標準的な性能 |
| 段階燃焼サイクル | 1.04 | 高 | 0.2〜0.4 | 50〜100% | 数回 | なし | 比推力が最も高い。エンジン質量とコストが大きい |
| エキスパンダーサイクル | 1.03 | 低 | 0.2〜0.4 | 30〜100% | 無制限 | なし | **対応推進剤は極低温のみ。**再生冷却で受け取れる熱量が推力の上限を決め、おおよそ 300 kN を超えられない |
| 固体モーター | 1.00 | 最高 | 不要 | 不可 | 不可 | あり（グレイン量で決まる） | 点火後は停止できない。配管も与圧も要らない |
| ハイブリッドモーター | 1.00 | 中 | 中 | 20〜100% | 無制限 | なし | 酸化剤のみタンクに持つ |

要求与圧の 2〜3 MPa と 0.2〜0.4 MPa の10倍の差が、そのままイナートガスの消費量の差になる。加圧式は構造が単純だが、その単純さの代償をイナートガスタンクの容積と質量で払う。

**エンジンの追加の性能値**

| 性能値 | 内容 |
|---|---|
| 全長 | 真空専用エンジンは膨張比を大きく取るためノズルが長く重い。比推力の高いエンジンほど長い。設計上は機体後端から突き出す長さとして現れる |
| 点火1回あたりの推進剤損失 | 極低温エンジンは点火のたびに配管とポンプを冷却する必要があり（チルダウン）、数キログラムから数十キログラムを捨てる。常温貯蔵性はほぼ0。細かい軌道修正を何度も行う運用では積み上がる |
| 連続燃焼時間の上限 | アブレーティブ冷却の加圧式、および凝縮相を生む推進剤（固体、液体酸素／シラン）に上限がある。再生冷却のポンプ式は上限なし。上限に達したエンジンは強制的に停止し、冷却されるまで再着火できない |
| ジンバル速度 [deg/s] | 可動角に加えて速度を持つ。速い姿勢変化には追従できず、追従しきれないぶんは姿勢制御系が補償する |

エンジン部品の比推力は `推進剤ペアの比推力 × サイクルの比推力係数` とする。推力と質量と全長はサイクルとサイズから決める。ジンバル可動角とジンバル速度もサイクルごとに定める。

### 6-3. ツィオルコフスキーによる燃焼計算

燃焼時間と点火時刻はツィオルコフスキーの式に基づいて求める。

```
src/physics/tsiolkovsky.ts   (純関数)
    dvFor(m0, mf, isp)               // Δv = Isp·g0·ln(m0/mf)
    massAfterBurn(m0, dv, isp)
    burnTimeFor(dv, m0, thrust, isp) // 推進剤流量 = thrust/(Isp·g0)
    propellantForDv(dv, mDry, isp)
```

一定加速度を仮定すると質量比の大きい機体で点火が遅れてマニューバノードを外すため、質量変化を必ず含める。

点火1回あたりの推進剤損失は、燃焼に必要な推進剤とは別に、点火の時点で一括して差し引く。したがって Δv がごく小さいノードでは、損失のほうが燃焼で使う量を上回ることがある。

### 6-4. 推進剤とエンジンの劣化

劣化には性質の異なる3つの形がある。

| 対象 | 劣化の形 | 回復 |
|---|---|---|
| 極低温推進剤 | 日射に応じて蒸発し、**残量が減る**。断熱材の等級と遮蔽で低減できる | しない（補給のみ） |
| ヒドラジン単推進剤スラスタ | 触媒床の活性が累積噴射時間に応じて低下し、**比推力が落ちる**。推進剤自体は密閉容器内で10年以上安定であり、量は減らない | 整備で回復する |
| 固体推進剤・イナートガス・常温貯蔵性の二液推進剤 | 劣化しない | — |

推進剤が減る劣化と、性能が落ちる劣化は別のものである。前者は補給でしか埋まらず、後者は基地での整備で回復する。

劣化の計算は毎フレームでは行わず、残量または比推力を読む時点で前回の評価時刻からの経過時間（または累積噴射時間）に基づいて一括で計算する。

---

## 7. 段階的な実装計画

各段階は、それ単体で遊べる状態で区切る。

| 段階 | 内容 | 依存 | 検証 |
|---|---|---|---|
| **S1** | `Vessel` への統合（§3）。艦艇と軌道基地を単一クラスにし、能力を搭載要素から導出する。敵艦の質量特性の直接指定経路（§3-3）、基地の格納機構の移設（§3-4）、表示種別の導出（§3-5）、配列統合の波及（§3-6）を含む。推進系はこの段階では変更しない | なし | 既存の全ステージが動作すること。格納・ドックビュー・マップ表示トグルが同じく動くこと |
| **S2** | 搭載要素の一覧と性能値の合成（§4）。`parts.ts` を §4-1 の一覧に置き換え、機体の性能定数を要素の合計に差し替える | S1 | 既定の構成の総推力・総容量が `const.ts` の定数と一致すること |
| **S3** | 姿勢制御の外部トルク（§5-5）と磁場（§5-4）。重力傾斜トルクを姿勢積分に加え、地球磁場の純関数を置く。この段階では磁気トルカはまだ姿勢制御に使わない | なし（S1・S2 と並行できる） | `gravity-gradient.test.ts`、`geomagnetic.test.ts` |
| **S4** | 姿勢制御系の分離（§5-1〜5-3、§5-9）。任意配置のアクチュエータに対する減衰最小二乗の配分、フライホイールの飽和、磁気トルカとRCSによるアンローディング | S2, S3 | `attitude-control.test.ts` |
| **S5** | 推進剤の物性表とエンジンの型式（§6-1〜6-2）、ツィオルコフスキーによる燃焼計算（§6-3）、絞り率によるスロットル操作（§5-8） | S4 | `tsiolkovsky.test.ts`、既存 `plan-executor-math.test.ts` の拡張、スロットル操作の手動確認 |
| **S6** | 推力軸と重心のずれ、ジンバル速度を含む補償（§5-7）、推進系HUDパネルと飽和通知（§5-10）、推進剤とエンジンの劣化（§6-4） | S5 | `thrust-alignment.test.ts`、手動確認 |

S1 と S2 が構造的な山場である。S1 で実体が1種類になり、S2 で機体の性能が搭載要素の帰結になる。S3 は純関数の追加のみで既存の構造に触れないため、S1・S2 と並行して進められる。S4 以降は、その上で推進と姿勢制御の中身を差し替えていく作業になる。

S3 を S4 より前に置くのは、重力傾斜トルクが姿勢制御系の負荷そのものだからである。配分を書き換える前に外部トルクを入れておけば、新しい配分が実際に何を打ち消しているのかが観測できる。

---

## 8. 既存実装に対する変更点

| 対象 | 変更内容 |
|---|---|
| `game/game-entity/ship.ts` / `player.ts` / `base.ts` / `enemy.ts` | `Vessel` に統合する。基地固有の状態は基地モジュールが持つ。敵艦は `MassProperties` の直接指定経路を使う |
| `game/game-entity/parts.ts` | 搭載要素を §4-1 の一覧に置き換える。エンジンをサイクルと推進剤の組で定義し、全長・点火損失・燃焼時間上限・ジンバル速度を性能値に加える |
| `game/simulation/entity-manager.ts` | 艦艇と基地の配列を `vessels` に一本化する。§3-6 の一覧が波及範囲である |
| `game/simulation/simulator.ts` | `stepAttitudes` の配列走査を改め、重力傾斜トルクを外部トルクとして姿勢積分に加える |
| `game/active-player-controller.ts` / `game/targeter.ts` / `game/nav-target.ts` | 保持・参照する型を `Vessel` に改める |
| `game/map-pickable.ts` / `game/celestial/map-visibility.ts` | 種別を基地モジュールの有無と勢力から導出する（§3-5）。表示トグルの4種はそのまま使う |
| `game/docking.ts` | 格納判定の閾値とハッチ位置を基地モジュール部品から読む（§3-4） |
| `game/stages/creative-stage.ts` | `placeObject` の4種の作り分けを、積む部品の違いに改める |
| `game/player/player-throttle.ts` | 姿勢トルクを直接書かず `AttitudeControlSystem` に要求する。並進推力は主機とRCSの選択を含む。スロットルを絞り率の連続値に改める |
| `render/rcs-nozzles.ts` / `game/player/rcs-effects.ts` | ノズルのテーブルを機体ごとのアクチュエータ集合から取り、噴くノズルの選択を配分の結果から読む |
| `game/plan/plan-executor-math.ts` | 燃焼時間と点火時刻を質量変化を含む式に置き換え、点火1回あたりの損失を差し引く |
| `game/plan/plan-executor.ts` | 燃焼中の質量変化に伴い加速度を毎ステップ再評価する |
| `game/const.ts` | 機体の質量・慣性・推力・角加速度の定数と `THROTTLE_LEVELS` を廃止し、搭載要素から導出される値と絞り率に置き換える |
| `game/save/save-data.ts` | 保存形式を `Vessel` と搭載要素を中心とした構造に置き換え、保存形式の版を更新する。旧形式は既定の構成へ読み替えるか、読み替え不能な場合は明示的に拒否する |
| `CLAUDE.md` / `DEVELOP/OWNERSHIP.md` / `DEVELOP/CALLSTACK.md` / `DEVELOP/SPEC.md` | 各段階と同じ変更セットで更新する |

---

## 9. 主要な技術的リスク

1. **減衰最小二乗の正則化重み** — λ が小さすぎると、要求をわずかに良く満たすために対向するスラスタを同時に噴かせ、推進剤を無駄に消費する解が選ばれる。大きすぎると要求トルクを十分に出せない。λ はアクチュエータの規模に依存するため固定値では足りず、行列 A の特異値の大きさに対する相対値として決めること。

2. **アンローディングの発振** — フライホイールの飽和と外部トルクによる角運動量の排出は互いを打ち消す方向に働くため、閾値と排出量の取り方によっては、飽和と排出を短周期で繰り返す状態に落ち込む。排出の開始と終了に別々の閾値を設け、開始した排出は下側の閾値に達するまで続けること。磁気トルカによる排出は極めて遅いため、この往復の周期は軌道周期の程度になる。

3. **磁気トルカで出せない軸** — τ = m × B は B に平行なトルクを出せないため、その軸の要求は原理的に磁気トルカでは満たせない。配分が「磁気トルカで出せる」と誤って判断すると、その軸の姿勢が制御されないまま放置される。B に垂直な成分だけを扱い、平行成分は必ず次の段へ落とすこと。

4. **姿勢制御の要求元の一元化** — 現在は手動操作の経路と自動操縦の経路が、それぞれ機体のトルクを直接書いている。両方を `AttitudeControlSystem` への要求に置き換えないと、片方がフライホイールの角運動量を更新せずにトルクを出すことになり、飽和の計算が合わなくなる。トルクを書く箇所を1つに絞ること。

5. **重力傾斜トルクと時間加速** — 重力傾斜トルクは軌道周期の程度の時定数を持つため、時間加速下では1ステップの姿勢変化が大きくなる。姿勢積分の運動エネルギー射影が発散しないことを、高倍率での長時間の待機で確かめること。

6. **時間加速中の燃焼** — 質量変化を含む燃焼計算は、1ステップが数十秒に達する時間加速下では、ステップ内での質量変化が無視できない。燃焼中のステップは閉形式で質量を進めるか、燃焼の開始と終了の時刻でステップを区切ること。

7. **推力軸のずれによる制御不能** — 主機を非対称に積むと、推力軸が重心を大きく外れる。ジンバル補償で打ち消しきれない構成は、警告を出したうえで、それでも機体が回ることを許すか拒否するかを決めておくこと。ジンバル速度の上限があるため、可動角が足りていても速い外乱には追従できない。

8. **保存形式の互換性** — 機体の表現が変わるため、旧形式の保存データは既定の構成への読み替え以上のことはできない。読み替え不能な保存データは明示的に拒否し、破損した機体を生成しないこと。

9. **UIの総量** — 推進系パネルが飛行中のHUDに加わる。既存のパネルへ統合できないかを確認し、置き場の規約に従うこと。飽和の通知音は頻繁に鳴りうるため、連続して鳴らないよう最短間隔を設けること。
