# プロンプトテンプレート

複雑な判断を伴う作業以外はSonnetをサブエージェントとして実装させあなたは監督、レビューに徹して。
実装が確認できればそれぞれコミットして
最後にコードレビューをして

実装に入って。実装計画で依存関係になく複雑な価値判断の伴わないタスクは全て切り出してlunaエージェントを用いて並列的に実装し、実装完了後はレビューし必要なら差し戻しを行い監督して。
実装完了後コミットして

計画的に実行したい。別ファイルに修正の実装計画書を書き出して。単体で文脈が読めるように相対的な表現を避け絶対的な表現で記述して。また、修正内容だけでなく、その修正を行うことで何が解消され、それがどのように良いのかを文脈を含めて明言して。
最後に修正計画をレビューして。この際修正は、既存の実装の歴史的経緯を引きずらないよう注意して。その機能を最初から実装し直したときに同じような実装になるものが、歴史的経緯に引きずられない筋の良い実装である。
以降は複雑な判断を伴わない作業はSonnetをサブエージェントとして用い最大限並列実行して。あなたはレビュー、監督を行い、実装できたと判断すればコミットして。
最後にコードレビューを行なって。

その機能を最初から実行したときになるようなものになっているか？経緯に引っ張られていないか

1の実装に入って。作業は別worktreeで行い、実装計画で依存関係になく複雑な価値判断の伴わないタスクは全て切り出してlunaエージェントを用いて可能ならば並列実行し、実装完了後はレビューし必要なら差し戻しを行い監督して。
実装完了後はmainに統合しコミットして



軌道表示、予測軌道、計画軌道とその周辺のロジックおよびUIのバグ、リファクタリング対象を調査して。また機能追加、機能廃止、修正、変数や関数の改名を提案して。必要ならば周辺領域に対象を広げたり深掘りをしても構わない。あなた自身が目標に専念するためにサブエージェントの使用、並列化も歓迎する。モジュール、関数は疎結合で、一般性が高く、一つの責務に対して一つのファイル、オブジェクト、関数が対応するカスケーディングで整理された構造となるように大胆な変更も厭わないで。またUIについては一般的に流通するUIのデザイン言語に従った、よりシンプルに直感的なUIを目指したい

過剰な複雑さをなくす

軌道表示、予測軌道、計画軌道とその周辺のロジックおよびUIのバグ、リファクタリング対象を調査して。また機能追加、機能廃止、修正、変数や関数の改名を提案して。必要ならば周辺領域に対象を広げたり深掘りをしても構わない。あなた自身が目標に専念するためにサブエージェントの使用、並列化も歓迎する。モジュール、関数は疎結合で、一般性が高く、一つの責務に対して一つのファイル、オブジェクト、関数が対応するカスケーディングで整理された構造となるように大胆な変更も厭わないで。またUIについては一般的に流通するUIのデザイン言語に従った、よりシンプルに直感的なUIを目指したい

過剰な複雑さをなくす
- 作業し完了後mainに統合しコミット。実装はSonnetをサブエージェントとして用い可能ならば並列実行して

- 別worktreeで実装し、完了すればmainに統合。あなたは設計、レビュー、差し戻し、修正方針の判断、実装完了の可否、方針の変更など複雑な判断に集中し単純作業はサブエージェントに切り出して行なって。
- 各フェーズを別worktreeで実装し、完了すればmainに統合。あなたは設計、レビュー、差し戻し、修正方針の判断、実装完了の可否、方針の変更など複雑な判断に集中し単純作業はサブエージェントに切り出して並列的に行なって。最後にコードレビューして
- 全て着手。別worktreeで作業し完了後mainでコミット。依存関係にない作業はサブエージェントで並列化。最後にコードレビューを行なって。必要なら周辺の実装を調べたり深追いしても良い

この現状の二つの課題に対して、コードベースを調査し原因を考え、計画書をレビューし、要件や実装アプローチの修正、追加、廃止、優先度の変更などを行って。この際、必要な文脈はそれだけを読んでわかるよう相対的な表現を避けて絶対的な表現で入れて。コンテキストを継承しない別のAIに実装させるつもりで必要十分な情報を載せること。必要ならば周辺のコードに手を広げても、一つの物事に対して深追いしても良い。あなたがおーけすとウェーションに集中できるよう無制限にサブエージェントの使用を推奨する。
作業は別worktreeで行い、完了すれば再度レビューし、mainにコミット。


- そのためでもあるが、UIの仕組みを一貫性のある疎結合な、シンプルなコードに書き換える。歴史的経緯による歪んだ実装を廃止し、ファイル、クラス、メソッド、変数が必ず一つの責務ですっきりと表現できるようにする。
- それに合わせて、ファイル名、クラス名、メソッド名、変数名も、想定的な用言、文脈に依存する表現、現在の実装からはずれている歴史的な経緯を持つ命名、実装が想像しづらい命名、複数通りの解釈が取れてしまう命名は廃止し、より責務を表現するに相応しい名前がある場合は積極的に改名する。
- 以上のことは実装量は問わないので、理想的状態をまず考え、それに合わせて根本的解決を図る。現在の姿、実装の延長ではなく、要件に合わせてコードを書き換える。それがゲームの操作性、楽しさ、わかりやすさを上げると判断するなら大胆な変更もかまわない。
以前一度行ったが、しばらく行っていないためUIが崩れてきた。
これらの方針に従って、UIとデザインの改善計画の第一版を別ファイルで書き出たい。
コードベースを調査し、計画書をレビューし、要件や実装アプローチの修正、追加、廃止、優先度の変更などを行って。この際、必要な文脈はそれだけを読んでわかるよう相対的な表現を避けて絶対的な表現で入れて。コンテキストを継承しない別のAIに実装させるつもりで必要十分な情報を載せること。必要ならば周辺のコードに手を広げても、一つの物事に対して深追いしても良い。あなたがおーけすとウェーションに集中できるよう無制限にサブエージェントの使用を推奨する。

軌道オブジェクト一覧ウィンドウから「近傍」ボタンを削除
LOGISTICSを設定に改称。敵の波状攻撃を発生させるか否かのトグルを追加。


設定ビューを追加。ESCメニューから飛ぶことができ、
以降

マップビュー表示ウィンドウは収納できるようにして。


- Sonnetをサブエージェントとして用いてあなたは監督して。
- 作業は別worktreeで行い、完了すればレビューし、mainにコミットして



ういう機能を追加したい、現状の実装だとどこに配線する案が考えうるか、それらによって連鎖的に変わる挙動は何になるか、複数の案を出してください

コード調査し、その結果を自分でも読んで検証して、線を付け替える、付け足し型のプロンプトを書いて計画書を作らせ、それを直して実装

あらかじめコードを調べて線を付け替える候補を提案する
