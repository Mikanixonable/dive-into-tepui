# プロンプトテンプレート

複雑な判断を伴う作業以外はSonnetをサブエージェントとして実装させあなたは監督、レビューに徹して。
実装が確認できればそれぞれコミットして
最後にコードレビューをして

実装に入って。実装計画で依存関係になく複雑な価値判断の伴わないタスクは全て切り出してlunaエージェントを用いて並列的に実装し、実装完了後はレビューし必要なら差し戻しを行い監督して。
実装完了後コミットして

計画的に実行したい。別ファイルに修正の実装計画書を書き出して。単体で文脈が読めるように相対的な表現を避け絶対的な表現で記述して。また、修正内容だけでなく、その修正を行うことで何が解消され、それがどのように良いのかを文脈を含めて明言して。
最後に修正計画をレビューして。この際修正は、既存の実装の歴史的経緯を引きずらないよう注意して。その機能を最初から実装し直したときに同じような実装になるものが、歴史的経緯に引きずられない筋の良い実装である。
以降は複雑な判断を伴わない作業はSonnetをサブエージェントとして用い最大限並列実行して。あなたはレビュー、監督を行い、実装できたと判断すればコミットして。
最後にコードレビューを行なって。

その機能を最初から実行したときになるようなものになっているか？経緯に引っ張られていないか

1の実装に入って。作業は別worktreeで行い、実装計画で依存関係になく複雑な価値判断の伴わないタスクは全て切り出してlunaエージェントを用いて可能ならば並列実行し、実装完了後はレビューし必要なら差し戻しを行い監督して。
実装完了後はmainに統合しコミットして


作業し完了後mainに統合しコミット
実装はSonnetをサブエージェントとして用い可能ならば並列実行して


軌道表示、予測軌道、計画軌道とその周辺のロジックおよびUIのバグ、リファクタリング対象を調査して。また機能追加、機能廃止、修正、変数や関数の改名を提案して。必要ならば周辺領域に対象を広げたり深掘りをしても構わない。あなた自身が目標に専念するためにサブエージェントの使用、並列化も歓迎する。モジュール、関数は疎結合で、一般性が高く、一つの責務に対して一つのファイル、オブジェクト、関数が対応するカスケーディングで整理された構造となるように大胆な変更も厭わないで。またUIについては一般的に流通するUIのデザイン言語に従った、よりシンプルに直感的なUIを目指したい

過剰な複雑さをなくす

