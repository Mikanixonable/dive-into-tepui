
1. レンダリング技術: WebGLベースなら Three.js か Babylon.js(いずれもローポリ表現・ポストプロセス〈オーロラのようなグロー表現〉が得意)、もっと作り込むなら WebGPU も視野。フレームワーク色を薄くしたいなら素のWebGL+自前エンジンも選択肢だが工数が跳ねる。
2. 言語/ビルド: TypeScript + Vite が現状の定番(型安全性、軌道計算のような数値コードでバグを減らせる)。
3. 物理・軌道計算の実装方針: 自前でN体シミュレーション(RK4/RKF45などの数値積分器)を書くか、既存の天体力学ライブラリ(例: satellite.js は地球周回限定で三体問題には力不足)を使うか。三体問題・ラグランジュ点・ハロー軌道まで扱うなら、ほぼ自前実装が必須になりそう。
4. 物理エンジン(衝突・破片): 見た目フ効果込み)なら剛体回転を自前で解く必要があり、汎用物理エンジン(Rapier/Cannon-esque)だけでは不十分な可能性。                       5. ゲームループ/アーキテクチャ: ECSを採用するか、シンプルなオブジェクト指向にするか。宇宙船・弾・破片・地球大気などエンティティ種が多いのでECSと相性は良さそう。       6. 状態管理・UI層: HUD やマニューバM/Canvas overlay か React/Vueを被せるか)。                                                                                   7. マルチプレイの有無:シューティングがCPU対戦のみか対人戦もあるか(あるならネットコード同期の設計が変わる)。           8. 配信形態: 静的ホスティングのみか、マッチメイキング等)が要るか。
                                                                                                最初に決めるべき優先度が高いのは (3)ECS採用可否 です。ここが後の全設計(データ構造、レンダリングとの結合方法)を左右します。個人的な推奨は「TypeScript + Vite + Three.js + 自前ECS + 自前RK4積分器」という組 形態(シングルプレイ想定か)を確定させたいです。どこから詰めますか？

                                                                            
---
# Geminiによる実現可能性調査
結論から言うと、**十分に作成可能**です。

現在のブラウザ環境（特にJavaScriptエンジンとWebGL/WebGPU）は非常に強力であり、ハードSF的なアプローチが求められる複雑な軌道力学のシミュレーションをブラウザ上でリアルタイムに動かすことができます。月面開発のロジスティクスや、L1/L2ラグランジュ点を利用した物資輸送シミュレーションなどを表現するには非常に面白いテーマです。

実現に向けてクリアすべき、3つの技術的な柱とその実装方針を解説します。

## 1. 軌道計算（物理エンジン）の要件

L1、L2のようなラグランジュ点周辺の軌道（ハロー軌道やリサジュー軌道）は力学的に非常に不安定（カオス的）です。わずかな誤差があっという間に拡大するため、自作の物理エンジンの精度がゲームのコアになります。

* **太陽の摂動（N体問題）:**
地球、月、宇宙船の制限3体問題に、太陽の重力を加えた「4体問題」として計算します。各天体からの重力加速度は以下の式で合成します。

$$\mathbf{a}_i = \sum_{j \neq i} G \frac{m_j}{\vert{}\mathbf{r}_{ij}\vert{}^3} \mathbf{r}_{ij}$$


* **自転の潮汐力（非球形重力場）:**
自転による天体の歪み（赤道ふくらみ）が軌道に与える影響は、「J2摂動」として実装します。極軌道や低軌道（LEO）での軌道面歳差運動をシミュレートするのに不可欠です。
* **大気抵抗（Atmospheric Drag）:**
L1やL2（地球から約38万km）の距離では完全に無視できますが、地球や月の近傍でのエアロキャプチャーや打ち上げを想定するなら必須です。高度に応じた大気密度モデル（単純な指数関数モデルなど）を用意し、以下の式を組み込みます。

$$F_D = \frac{1}{2} \rho v^2 C_D A$$



**【計算手法の選択】**
単純なオイラー法ではすぐに軌道が破綻します。最低でも**4次のルンゲ＝クッタ法（RK4）**、より長期の安定性を求めるなら**シンプレクティック積分**（Verlet法など）を採用する必要があります。JavaScriptは標準で64ビット浮動小数点数（Double精度）をサポートしているため、位置座標の計算自体の精度はブラウザでも十分に確保できます。

## 2. 推奨される技術スタック

UIの構築や状態管理の観点から、モダンなWebフロントエンド技術を活用するのが最もスムーズです。

| コンポーネント | 推奨技術 | 理由 |
| --- | --- | --- |
| **UI・状態管理** | React + React Three Fiber | 複雑な計器類のUIと、3D空間上のオブジェクトの状態同期が容易なため。 |
| **3D描画** | Three.js (WebGL/WebGPU) | 宇宙空間のライティングや地球の大気シェーダーなどの実装実績が豊富。 |
| **物理演算** | Web Workers (JS/TS) | 重い積分計算を別スレッドに逃がし、UI描画（60FPS）のブロックを防ぐため。 |

※さらに計算速度が必要になった場合は、物理演算部分のみをRust等で記述し、**WebAssembly (Wasm)** としてブラウザに読み込ませる拡張も可能です。

## 3. 開発における最大の壁

ゲームとして成立させる場合、以下の2つの問題に直面します。

1. **タイムワープ機能の実装:**
月まで数日かかるため、ゲーム内時間を1000倍などに加速させる必要があります。単純に1フレームあたりの時間ステップ（$\Delta t$）を大きくすると計算が爆発して宇宙船が地球を突き抜けます。Dormand-Prince法などの「刻み幅制御付きルンゲ＝クッタ法」を用いて、天体に近づいた時だけ自動的に計算の解像度を上げる工夫が必要です。
2. **極端なスケール差（Zファイティング）:**
地球（半径約6,400km）から宇宙船（数メートル）までを描画しようとすると、通常の深度バッファでは精度が足りず、テクスチャがチラつく「Zファイティング」が発生します。Three.jsで `logarithmicDepthBuffer: true` を有効に設定することが必須となります。


---

# Geminiによる要約
Listed directory dive-into-tepui
Viewed README.md:1-95
Viewed package.json:1-24
Listed directory src
Listed directory physics
Listed directory render
Listed directory game
Listed directory types
Viewed CLAUDE.md:1-68

このプロジェクト **`dive-into-tepui`** は、**地球低軌道（LEO）を舞台にした、リアルな軌道力学に基づく3Dシューティングゲーム**です。

技術的には、**TypeScript + Three.js（WebGPUレンダラー）**で構築されており、本格的な宇宙シミュレーター（Kerbal Space Programのようなシステム）とシューティングゲームの要素を融合させています。

以下にプロジェクトの主要な特徴、アーキテクチャ、およびコード構成について解説します。

---

### 1. 主な機能と特徴

*   **リアルな軌道力学のシミュレーション**
    *   RK4（ルンゲ・クッタ4次）数値積分を用いて、自機や敵機、薬莢、デブリ（破片）の軌道を計算しています。
    *   単なる二体問題だけでなく、**地球の扁平率による影響（J2摂動）**、**太陽や月による潮汐力（第三体摂動）**、**大気抗力（高度による密度変化と減速）**などをシミュレートしています。
*   **本格的な宇宙船の操作とナビゲーション**
    *   **RCS（姿勢制御システム）**による3軸姿勢制御（ピッチ・ヨー・ロール）および並進移動（前進・後退・左右・上下）。
    *   `[C]` キーによる**進行方向（Prograde）の自動維持（オートパイロット）**。
    *   画面下部中央に、宇宙船の姿勢を示す **ナビボール（Navball）** を描画。
*   **軌道計画マップモード (`[M]`キー)**
    *   地球を俯瞰するマップ画面に切り替えることができ、プレイヤーの軌道をクリックして**マニューバーノード（軌道変更計画）**を作成できます。
    *   WASDQEキーで必要速度変化（$\Delta v$）を調整し、軌道遷移の計画を立ててタイムワープで自動移動する機能があります。
*   **大気圏突入と熱シミュレーション**
    *   高度が下がると大気密度が上がって軌道が減衰します。
    *   大気摩擦によって機体が加熱され（サットン・グレイブス式）、放射冷却とのバランスで熱が計算されます。耐熱限界を超えると大気圏で燃え尽きてゲームオーバーになります。
*   **物理ベースのディテール**
    *   発砲すると薬莢が排出され、慣性で漂います。宇宙船が破壊されると破片が散らばり、非対称コマの特有の回転運動である **ジャニベコフ効果（テニスラケットの定理）** に基づいてスピンします。
    *   弾薬ベルトが宇宙船の挙動（推進加速度、回転時の遠心力・コリオリ力など）に応じてリアルに揺れる物理シミュレーション（Position-Based Dynamics）を搭載しています。
*   **WebAudioによるBGMと効果音**
    *   音声ファイルを読み込むのではなく、シンセサイザーの波形生成によってBGM（ミニマル・ミュージック風）やCIWSの射撃音・モーター音などをリアルタイム合成しています。

---

### 2. ディレクトリ・コード構成

主要なコードは `src/` ディレクトリ配下に分類されています。

*   **`src/`**
    *   [main.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/main.ts) — エントリーポイント。WebGPU の初期化、描画ループ（`requestAnimationFrame`）と `Game.update` の呼び出し。
    *   **[game/](file:///Users/pandeaconica/lab/dive-into-tepui/src/game/)** (ゲームロジック)
        *   [game.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/game/game.ts) — ゲームの司令塔。エンティティ（自機、敵機、弾、デブリ）の更新、入力処理、衝突判定、オートパイロット制御。
        *   [hud.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/game/hud.ts) — 画面上に表示される HUD（高度、速度、ターゲット情報など）の描画。
        *   [navball.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/game/navball.ts) — 2D Canvasを使ったナビボール計器の描画。
        *   [audio.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/game/audio.ts) — WebAudioによる動的サウンド合成。
    *   **[physics/](file:///Users/pandeaconica/lab/dive-into-tepui/src/physics/)** (物理・軌道計算)
        *   [orbital.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/physics/orbital.ts) — 万有引力、J2摂動、潮汐摂動、マニューバー軌道予測計算。
        *   [attitude.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/physics/attitude.ts) — 剛体の姿勢角（クォータニオン）と角速度の統合計算（ジャニベコフ効果を維持するアルゴリズム）。
        *   [atmosphere.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/physics/atmosphere.ts) — 高度ごとの大気密度モデル。
        *   [ephemeris.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/physics/ephemeris.ts) — 太陽と月の軌道位置（暦）の計算。
    *   **[render/](file:///Users/pandeaconica/lab/dive-into-tepui/src/render/)** (3D描画)
        *   [earth.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/render/earth.ts) — 地球の 3D メッシュ（fBmノイズによる自動生成）、大気のグラデーションシェル、オーロラ。
        *   [ships.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/render/ships.ts) — 自機や敵機、薬莢、デブリ、弾薬ベルトなどのローポリメッシュ構築。
        *   [orbitline.ts](file:///Users/pandeaconica/lab/dive-into-tepui/src/render/orbitline.ts) — 自機やターゲットの軌道予測線の描画。

---

### 3. 特殊な描画アプローチ：「フローティング・オリジン (Floating Origin)」

広大な宇宙空間（地球低軌道）を float32（単精度浮動小数点数）で描画しようとすると、座標値が大きくなりすぎてジッター（グラフィックスのがたつき）が発生します。
このゲームでは、**プレイヤー宇宙船を常にワールド座標の原点 `(0, 0, 0)` に固定**し、地球や敵機などのすべてのオブジェクトをプレイヤーからの相対位置に毎フレーム配置し直す「フローティング・オリジン」アプローチを採用しています。これにより、カメラ付近の描画精度を極限まで保っています。

---

### 4. 実行コマンド

開発や動作確認には以下のコマンドを使用します（[package.json](file:///Users/pandeaconica/lab/dive-into-tepui/package.json) より）：

*   **開発サーバー起動**: `npm run dev` (Webpack デブサーバーが `http://localhost:8080` で起動します)
*   **プロダクションビルド**: `npm run build` (`dist/` ディレクトリに最適化されたJSとHTMLが出力されます)
*   **型チェック**: `npm run typecheck` (`tsc` による型エラーの静的検証)

※ WebGPUを使用しているため、動作確認には **Chrome や Edge などの WebGPU 対応ブラウザ** が必要です。

より詳細なゲーム仕様については [README.md](file:///Users/pandeaconica/lab/dive-into-tepui/README.md) または開発上のガイダンスが書かれた [CLAUDE.md](file:///Users/pandeaconica/lab/dive-into-tepui/CLAUDE.md) もあわせてご参照ください。


---


軌道計画モード(マップモード)の現状実装
概要
[M] キーでトグルする第2の操作モード。時間・物理シミュレーションは通常どおり進み続けたまま(simulate() は止まらない)、カメラと入力の解釈だけを「地球中心の俯瞰ビュー」に切り替え、将来のマニューバ(軌道変更)を視覚的に計画できるようにする機能です。実装は全部 game.ts 内に集約されています。

状態(フィールド)
mapMode: boolean — トグル状態。toggleMap()(case 'KeyM')で切替。
mapYaw/mapPitch/mapDist — 俯瞰カメラの球面座標(ドラッグ・ホイールで操作)。
mapCamera: THREE.PerspectiveCamera — 戦闘用カメラとは別インスタンス。near=1e4, far=6e8(モルニヤ級の高楕円軌道全体が収まる遠方)。
editNu: number | null — 編集中ノードの位置(自機軌道上の真近点角)。nullなら未配置。
editDv: Vec3 — 編集中のΔv(x=プログレード、y=ノーマル、z=ラジアルアウト成分)。
node: ManeuverNode | null — 確定済みの実行計画(nodeTime・rNode・vPlanned・targetEl・dvTotal)。
autoWarp: boolean — [N] によるノードへの自動タイムワープ中フラグ。
フロー
[M] でモード開始(toggleMap()) — mapMode=true、ヒントを表示。マップに入っても時間・物理は継続。
updateMapPlanning(dt)(mapMode中は毎フレーム呼ばれる)
現在軌道の要素を計算し、離心率0.98以上や非有限周期(双曲線軌道など)なら「計画できません」と表示して以降をスキップ。
クリックは pickOrbitNu() で処理: 自機軌道を離心近点角で512分割サンプルし、画面投影して最も近い点の真近点角を返す → editNu に設定。
editNu が設定済みなら、通常の推進キー(W/S/A/D/Q/E)をΔv調整に転用([V]微調整で感度をNODE_DV_RATE_FINEに切替)。
計画パネル(HUD左下)に Δv 内訳・到達までの時間・計画後の軌道要素(遠地点/近地点/傾斜角/周期)をリアルタイム表示(planHtml())。近地点が120km未満なら警告。
カメラ: mapYaw/mapPitch/mapDist をマウス/矢印キーで操作し、mapCamera を地球中心(フローティングオリジンなので -o)を注視する球面軌道カメラとして毎フレーム再配置。
再度 [M] で確定(finalizeNode()) — editNu・editDv から計画軌道要素を計算して this.node に凍結し、マップを抜ける。ΔvがNODE_MIN_DV未満なら何も確定しない。
[X] — マップ中なら編集中のノード(editNu/editDv)を破棄。戦闘中なら確定済みnodeを破棄。
[N] — nodeがあれば自動ワープをトグル。simulate()内で残り時間に応じて WARP_LEVELS から適切な段を自動選択し、AUTOWARP_STOP(20秒)前で解除。
戦闘ビューでの誘導(updateNodeGuide()) — ノード位置マーカー(残り時間カウントダウン付き)と、目標速度との差分方向を示す「BURN」マーカーを表示。
達成判定(orbitClose()) — 長半径の相対誤差・離心率差・軌道面角度差がすべて閾値内(NODE_TOL_SMA/ECC/PLANE_DEG)なら「マニューバ達成」としてノードを消去・ヒント表示。
表示面での特記事項
マップ中は方向系マーカー(プログレード/レトログレード/ボアサイト/リード)を隠し、自機位置マーカー(▷)のみ表示。
計画軌道線(白、plannedOrbitLine)は、マップ中はeditNuに基づくプレビュー、戦闘中は確定済みnode.targetElを表示。
マップ中のみ、生存中の全敵の軌道線(enemyOrbitLines)をターゲット以外も含めて表示し比較可能に。
既知の設計上の制約
双曲線軌道・離心率0.98以上・非有限周期の状態では計画不可(エラーメッセージのみ)。
ノード配置はスクリーン投影ベースのピクセル距離判定(NODE_PICK_PX)なので、軌道線が画面上で密集/重なる配置だと誤ってクリックしにくい。
finalizeNode() は現在の軌道要素を再計算するため、マップを開いてから閉じるまでの間に(自動ワープ等で)軌道が変化していても、確定時点の実軌道を基準に計画される(マップ表示中に見ていた軌道とズレる可能性はある設計)。



---

敵機のポリゴン削減 / LOD — 第零ステージで敵50機が5km圏内に密集する設計なので、遠距離の敵は低ポリ版に差し替える(または非表示化)のが最も効きそうです。ENEMY_SCALE=10の巨大メッシュが50体同時描画されるのは、モバイルGPUには重い可能性があります。
地球テクスチャの解像度を端末に応じて縮小 — 現状 2048×1024 の PNG を焼き出していますが、モバイル向けに1024×512版も書き出しておき、navigatorの画面サイズやメモリ推定(navigator.deviceMemory)で出し分ける、というのが手軽です。
パーティクル・デブリ・薬莢の上限を端末別に下げる — MAX_DEBRIS/MAX_CASINGS/MAX_BULLETS を定数でなく動的値にし、タッチデバイス検出時(TouchControls.isTouchDevice()が既にあります)は半分程度に絞る。
物理衝突判定のO(N²)の間引き — 現状は「N<200」前提のコメントがありますが、第零ステージ50機+ベルト12+薬莢260+デブリ160だと数百体規模になり得ます。距離の粗いバケット分割(グリッド)を入れるか、モバイルでは判定対象を間引く。
devicePixelRatioのキャップ — scene.ts で renderer.setPixelRatio(window.devicePixelRatio) をそのまま使っていますが、高密度スマホ(dpr 3〜4)はレンダリング解像度が跳ね上がるので Math.min(devicePixelRatio, 1.5〜2) 程度にキャップするのが、労力対効果が一番高いと思います。
まず着手するなら 5(dprキャップ) が数行の変更で最も費用対効果が高く、次に 1(敵のLOD/間引き) が第零ステージの体感負荷に直結すると思います。方向性が決まれば実装します。


---
#done 構造化
## done

これまでに実装・完了した機能や修正事項です。（gitのコミット履歴や過去のタスクリストを元に構造化しています）

### 軌道力学・宇宙環境 (Orbital Mechanics & Environment)
- 大気抵抗による減速、断熱圧縮による加熱・オーバーヒートの再現（熱飽和や空力的な破壊によるゲームオーバー）
- 地球の潮汐力による軌道傾斜角変化、J2摂動の物理学的に正確な再現
- 太陽と月の追加、およびそれらによる摂動の再現
- 地球のローポリからリアル調への修正（オーロラ、雲、雲の陰、薄明のグラデーション、連続な濃度の大気）
- 地球の影に入ると宇宙船が暗くなるライティングの実装

### 自機デザイン・武器・3Dモデル (Ship Design, Weapons & 3D Models)
- 宇宙船の基本デザイン変更（直方体ベース、後部4エンジン、左右太陽電池パドル、前面2門の機関砲）
- マガジンの実装。右側から取り込まれる直方体形状の弾倉。マガジンチェーンの物理演算によるたわみ表現
- マガジンチェーンの挙動修正（根本の固定方向延長、消費時に空のマガジン枠を破片として放出など）
- リロードシステムの追加。排出される砲身モデルの追加と0.7倍へのリサイズ
- 宇宙船モデルへのRCSノズル追加、マガジン取り込み口/排出口追加、ソーラーパドルと骨格の位置ズレ統一
- 薬莢のモデル軽量化（ポリゴン数半減、直径0.7倍、全長拡大など）、艦砲CMS風の形状へリッチ化
- 敵の破片モデルの複雑化。機体種別（mech/crystal/ring/spike等）に応じた固有メッシュの実装とジャニベコフ効果による回転演出

### サウンド・エフェクト (Sound & Effects)
- 戦闘BGMの追加（空気感が感じられるレトロシンセ風のアンビエントなミニマルミュージック）
- 機銃起動音、発砲音を艦砲CMS風（レールを滑るような摩擦音、胴鳴りとクラック）に変更
- 爆発音を廃止し、自機の戦闘システムが知らせる電子音（複数音の撃破通知）に変更
- デブリ衝突音群の追加（「ことっ」という紙コップに当たるような音、敵との距離に応じた遅延計算付き）
- 軌道マニューバ時の噴射エフェクト追加、RCSの噴射エフェクト方向の修正
- ズーム時のマズルフラッシュ強度を1/5に弱める調整

### UI・HUD (User Interface & HUD)
- UIをダークテーマ（モノトーン＋オレンジのアクセントカラー）に変更
- ウィンドウの重なり修正（TARGETS、CONTACTS、SHIPSTATUS、ORBITの配置調整）
- Navballの実装（後に廃止し、軌道要素のアイコンと推奨キー操作を直接ビューに表示する方針へ移行）
- ズーム機能とズームウィンドウの実装。右上に配置し、敵ターゲット枠とLEAD表示を追加
- 弾道予測やLEAD表示の改善（敵集団のテーマカラーへ統一、過去20秒表示など）
- 視界外にある敵集団の方角を示す三角マーカーの追加
- 視野角の近い位置に敵機が大量にある場合のマーカーグループ化

### 操作・ゲームシステム (Controls & Game System)
- エンジン出力の3段階制御と、微調整モードの追加（姿勢制御感度を0.2倍に）
- 並進移動（WSADQE）と回転移動（IKJLUO）のキー操作割り当て。CTRL/SHIFTでの加減速、P(ESC)でのメニュー表示
- 軌道計画モード（マップモード）の実装。ノード（マニューバ）の追加、時間ワープ、ドラッグでの視点回転や月中心フォーカス機能
- ステージ0およびステージ00の追加（波状攻撃、時間制限ではなくスコア制、近距離でのRCS・ロケット操作練習）
- 弾薬補給ロジック。開始時にマガジンが連結されており、軌道上のマガジンに近づいて補給する仕組み
- 敵の当たり判定、空力的な分解・再突入でのデスポーン時は撃破カウントしないように修正
- モバイル用UIの追加（時間加速/減速ボタン、微動モード切り替えボタン）
- npm run dev で既にポートが使用されている場合は別ポートを使用し、CTRL+Cで即時停止する機能

### バグ修正・リファクタリング (Bug Fixes & Refactoring)
- 軌道表示と地球テクスチャのジッタリング・チラつきの軽減
- syncRenderのmapMode分岐整理、MapView / MapPlannerへのコード抽出（Refactoring）


---
# セーブ機能の実装提案

現在のアーキテクチャに基づき、ゲーム状態を保存・復元するセーブ機能の実装アプローチを提案します。

## 1. セーブデータの構造（SaveData）
ゲームの状態を JSON 形式でシリアライズし、ブラウザの `localStorage` または `IndexedDB` に保存します。
まずは `localStorage` を使用する想定で、以下のようなデータ構造を定義します。

```typescript
interface GameSaveData {
  version: number;          // セーブデータのスキーマバージョン（将来の仕様変更対応）
  stageId: string;          // 'creative', 'stage1' などのステージID
  simTime: number;          // 現在のシミュレーション時刻 [s]
  
  // エンティティ群の保存
  player: PlayerSaveData | null;
  enemies: EnemySaveData[];
  ammos: AmmoSaveData[];
  
  // ※ Debris（薬莢や破片）やエフェクト（爆発など）は見た目専用であるため保存対象外とするのが一般的です。
}

interface EntitySaveData {
  id: string;
  kind: 'player' | 'enemy' | 'ammo';
  position: { x: number, y: number, z: number };
  velocity: { x: number, y: number, z: number };
  attitude: { q: { x: number, y: number, z: number, w: number }, omega: { x: number, y: number, z: number } };
}

interface PlayerSaveData extends EntitySaveData {
  mags: number;       // マガジン残量
  heat: number;       // 現在の熱量
  // plan (軌道計画ノード) の保存
  planNodes: { time: number, dv: { x: number, y: number, z: number } }[]; 
}

interface EnemySaveData extends EntitySaveData {
  enemyType: string;
  alive: boolean;
  health: number;
}
```

## 2. 実装方針と各クラスへの影響

### A. セーブ・ロード管理クラスの新規作成 (`src/game/save-manager.ts`)
`Game` インスタンスから必要な状態を抽出し、また逆に `Game` へ状態を流し込むための `SaveManager` クラスを作成します。

- **`save(game: Game): void`**:
  `game.entities`、`game.simTime`、`game.activeStage` の状態を取得し、JSON 文字列化して `localStorage.setItem('tepui.save', json)` で保存。
- **`load(game: Game): boolean`**:
  JSONをパースし、以下のアクションを実行します。
  1. `game.entities` にある既存のオブジェクトを全て `dispose()` してクリアする。
  2. `game.simTime` と `ephemeris` の時刻を復元する。
  3. JSON内の `PlayerSaveData` から `new Player(...)` を生成し、位置・速度・姿勢を上書きして `game.entities.addPlayer(...)` を呼ぶ。
  4. 同様に `Enemy` や `Ammo` を再生成する。
  5. プレイヤーの軌道計画 (`Plan`) に保存されたノードを再追加する。

### B. エンティティのシリアライズ対応
各クラス（`Player`, `Enemy`, `Ammo` 等）に自身の状態を出力する `serialize()` メソッドと、生データから状態を上書き復元する `restore(data: SaveData)` メソッド（またはファクトリ関数）を実装します。

### C. UI との結合 (`SettingsPanel` / `Input`)
- **ポーズメニュー (ESC)**
  `SettingsPanel` に「SAVE」「LOAD」ボタンを追加します。
- **クイックセーブ / ロード**
  必要に応じて `Input` クラスで `[F5]` (クイックセーブ) / `[F9]` (クイックロード) などのショートカットをリッスンし、`SaveManager` を呼び出せるようにします。

## 3. 実装上の課題・注意点

> [!WARNING]
> 以下の点について、実装時に設計の検討が必要です。

1. **軌道計画 (`Plan`) の復元**
   プレイヤーが設定したマニューバノード（時刻と $\Delta V$）を保存・復元する必要があります。ノードを復元した後、`PlanEditor` に再計算させてセグメント（`PlanSegment`）を正しく再構築する仕組みが必要です。
2. **ステージ遷移との兼ね合い**
   ステージ1とクリエイティブモードではゲームの進行ロジック（勝敗判定など）が異なります。セーブデータをロードする際は、まず保存されていた `stageId` のステージを正しく初期化（`initStage`）した上で、エンティティを上書きする必要があります。
3. **物理シミュレーションの連続性**
   セーブデータから復元した直後に、RK4 積分器のステップが急激に飛んだり、当たり判定の不整合が生じないよう、ロード直後に一度 `simulate(0)` や `entities.sync()` を手動で回して状態を安定させる必要があります。

## 4. 進行ステップの提案

もしこの実装方針でよろしければ、以下のフェーズに分けて実装を進めることができます。

- **Phase 1**: `SaveManager` のベースと、各エンティティの `serialize()` / `restore()` の実装。
- **Phase 2**: クリエイティブモードにおける F5/F9 キーによるクイックセーブ/ロード機能の組み込み（状態の書き出し・読み込みテスト）。
- **Phase 3**: ESCメニュー（UI）への統合と、他ステージでの動作確認。
- **Phase 4**: マニューバノード（軌道計画）のシリアライズ・復元対応。

いかがでしょうか？内容を調整したい点や、先に着手したい機能があればお知らせください。

---
# 基地・ドックビュー・部品システム 実装計画

ご要望いただいた「基地機能」「宇宙船の部品化と内部状態」「ドックビューでの換装・修理」について、現在のアーキテクチャ（物理シミュレーション中心・リアルスケール・UIコンポーネント化）に自然に馴染むよう仕様を具体化し、実装ステップを策定しました。

## 1. 具体化されたゲーム仕様（ゲームデザイン案）

### 1-1. 宇宙船の「部品（Parts）」と内部状態
現在の `Ship` や `Player` クラスのパラメータ（HP、熱、電力など）を、個別の「部品オブジェクト」に持たせ、それらの総和や状態によって船の性能が決定する設計にします。

*   **部品の種類と役割:**
    *   **本体 (Hull):** 船の基礎。破壊されると船体が分解（轟沈）。
    *   **コックピット (Cockpit):** パイロットまたは制御コア。破壊されると操作不能（ゲームオーバー）。
    *   **装甲 (Armor):** 各部位を覆う。衝突や被弾時のダメージを肩代わりする。
    *   **RCS (Thruster):** 姿勢制御力（トルク）を提供する。損耗すると旋回性能が落ちる。
    *   **RCSタンク (RCS Tank):** RCS推進剤の残量（プロペラント）を持つ。枯渇すると姿勢制御ができなくなる。
    *   **放熱板 (Radiator):** 冷却能力。損耗すると廃熱効率が下がり、オーバーヒートしやすくなる。
    *   **太陽電池パドル (Solar Panel):** 電力生成能力。損耗すると電力供給が落ちる。
    *   **武装 (Weapon):** ガトリング、キャノン、ミサイルなど。種類によって発射レートや威力が変わる。
*   **損耗（ダメージ）モデル:**
    *   被弾時やデブリ衝突時、単純な総HPの減少ではなく、確率または被弾位置（部位判定）によって特定の部品の「損耗度（HP）」が低下する。
    *   部品が損傷・機能停止することで、船の挙動が徐々に制限される（部位破壊の緊迫感）。

### 1-2. 基地（Base）エンティティと軌道配置
宇宙空間に浮かぶ「基地」を、プレイヤーが利用可能な恒久的な補給・整備拠点として定義します。

*   **軌道配置 (Orbit Placement):**
    *   マップビューの「軌道オブジェクト追加」UI を拡張し、「基地」を選択可能にする。
    *   配置先として、地球や月の周回軌道だけでなく、**ラグランジュ点（L1/L2）** や **ハロー軌道** などの特殊軌道も指定できるようにする。（既存の `halo.ts` などの物理モデルを活用）。
*   **基地の機能:**
    *   他のオブジェクト同様に物理シミュレーション上で軌道を移動し、固有のメッシュ（宇宙ステーション等）を持つ。
    *   内部に「格納庫（保有する船のリスト）」「倉庫（保有する部品のリスト）」「資金（ゲーム内通貨）」のステータスを持つ。
*   **収容と発進 (Docking & Launch):**
    *   **収容:** 基地に対して右クリックメニューから「ランデブー対象にする」を選び、基地から一定距離以内（例: 100m以内）かつ相対速度が極小（例: 1m/s以下）になった状態で「収容（Dock）」コマンドを実行すると、船が宇宙空間から消え、基地の格納庫に入る。
    *   **発進:** ドックビューから格納庫にある船を選び、「発進（Launch）」すると基地の近傍に船がスポーンする。

### 1-3. ドックビュー（Dock View）と経済システム
基地がアクティブ状態（または自身がドック入りしている状態）のときに開ける、専用の全画面（または大きなモーダル）UIです。

*   **整備（修理と換装）:**
    *   **修理:** 損耗した部品（放熱板、太陽電池パドル、装甲など）を選択し、通貨を消費して損耗度を100%に回復させる。
    *   **換装:** 武装をガトリングからキャノンに付け替えたり、大容量のRCSタンクに積み替えたりできる。
*   **ショップ機能:**
    *   基地の「ショップ」タブで、新しい部品や別の船体を通貨を消費して購入し、基地の倉庫に入れることができる。
*   **通貨システムとクリエイティブモード:**
    *   通常モードでは、敵の撃破やミッション達成で通貨を獲得する。
    *   クリエイティブモードでは、通貨の表示が「∞ (無限)」となり、購入や修理にかかるコストがすべて無視される（実質無料）。

---

## 2. 実装計画 (フェーズ分け)

非常に大規模な機能追加となるため、4つのフェーズに分割して段階的に実装し、各フェーズごとに動作確認を行えるようにします。

### Phase 1: 部品システムの基礎組み込み（Ship / Player のリファクタリング）
まずは「部品」という概念をコードベースに導入し、既存の船の挙動を部品のステータス依存に切り替えます。
*   [ ] `Part` (部品) インターフェースと各種部品クラス（Hull, Cockpit, RcsTank, Radiator 等）の定義。
*   [ ] `Ship` および `Player` クラスが `parts: Part[]` を持つように拡張。
*   [ ] 被弾・衝突ダメージ処理 (`applyCollisionDamage` 等) を、総HPの減少から「ランダムな部品へのダメージ割り振り」へ変更。
*   [ ] RCS燃料の概念追加と、姿勢制御時 (`RcsEffects` 等) での燃料消費ロジックの組み込み。
*   [ ] 熱管理 (`ThermalSystem`) と電力管理 (`PowerSystem`) を、部品の現在HP（機能状態）から性能を算出して動作するように修正。

### Phase 2: 基地エンティティの追加とラグランジュ点/ハロー軌道配置
物理空間上に拠点となるオブジェクトを配置できるようにします。
*   [ ] `GameEntity` を継承した `Base` クラスの作成（単純なステーションモデルを表示）。
*   [ ] 基地の内部状態（所持金、保有パーツリスト、格納されている船リスト）のデータ構造定義。
*   [ ] `OrbitObjectPlacer` (クリエイティブ用の配置UI) を拡張し、ターゲットに「L1」「L2」「Halo軌道」などを選べるようにし、エンティティとして「基地」をスポーン可能にする。
*   [ ] 基地の右クリックメニューの実装（「アクティブにする」「収容する」等のコマンド追加）。

### Phase 3: ランデブー収容とドックビューUIの土台構築
ゲームプレイとして「ドックに入ってUIを開き、また宇宙へ出る」ループを完成させます。
*   [ ] 距離と相対速度を判定し、条件を満たした場合に自機を消去して基地の格納庫データに格納するロジックの実装。
*   [ ] 基地アクティブ時に開ける `DockView` UIコンポーネント (HTML/CSS) の作成。
*   [ ] ドックビュー上での「格納されている船のリスト表示」と、「船の宇宙空間への発進（Spawn）」機能の実装。

### Phase 4: ドックビューでの修理・換装・購入機能の実装
本格的なカスタマイズ要素と経済システム（クリエイティブ無敵対応）を組み込みます。
*   [ ] ドックビュー内に「船の装備スロット」と「倉庫の部品一覧」を表示し、ドラッグ＆ドロップ（またはクリック）で部品を入れ替える機能の実装。
*   [ ] 損傷した部品の「修理」ボタンと、修理コスト計算ロジックの実装。
*   [ ] 新しい部品を買える「ショップ」機能の実装。
*   [ ] クリエイティブモード時はすべての消費コストを無視（または所持金を無限大に）するフラグ処理の実装。

---

## ユーザーへの確認事項
*   **仕様の方向性:** 上記の部品分割やドックでの換装の流れは、思い描かれているゲームイメージと合致していますでしょうか？
*   **RCS燃料の補給について:** 今回の仕様では基地に戻ればタンクを新品に換装（または燃料補充）できますが、戦闘中に弾薬のようにアイテムを拾って回復するようなアーケードライクな要素も持たせますか？それとも「補給は基地でのみ可能」というハードな作りにしますか？
*   **優先順位:** このまま Phase 1 (宇宙船の部品システム化) からコードの実装を進めてよろしいでしょうか？（もし並行して、マップビューでの概要表示やカメラ画角などの細かな要望を先に片付けたい場合はおっしゃってください）
