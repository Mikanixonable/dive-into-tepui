# DEVELOP/ — 仕様と規約の一次情報

**コードの現状はコードだけが語る。** ここに置くのは、コードからは読み取れないもの — 「どう振舞う
べきか」(仕様)と「どう書くべきか」(規約)だけである。コードを追認するだけの文書は置かない。

| 文書 | 何が書いてあるか | 対応する問い |
| --- | --- | --- |
| [SPEC/](SPEC/) | ゲームがどう振舞うべきか(分野別) | 「本来どうあるべきなのか」 |
| [CODING-RULE.md](CODING-RULE.md) | 設計方針・命名規則・コメント規約・テスト | 「何と名付け、どう書くのか」 |

## SPEC/ の位置づけ

SPEC/ は **これから作るものの意図** を書く場であり、**常にコードより先行する。**
未実装の記述が残っているのは正常な状態で、仕様と実装が一致することを目標にしない。

**何をどう書くかの規則は [SPEC/README.md](SPEC/README.md) が正本。** ここには書かない。

## コードの構造を知りたいとき

保持関係(誰が何を持っているか)と呼び出し関係は、維持された文書としては持たない。
必要になった時点でコードから調べる:

- `/ownership` — インスタンスの保持関係と状態の正本
- `/callstack` — その関数が何を・どの順で・どんな条件で呼ぶか(per-frame の実行順)
- `/inv-callstack` — その関数を誰が呼んでいるか(依存の遡り)
- `/overview` — 全体像の掴み方(どこから読み始めるか)

調べた結果は、**保存先を指定されたとき**に限り commit hash を添えて残せる(計画書・検討メモの
md へ)。ここ `DEVELOP/` には置かない — 現状の写しであって、「どう振舞うべきか」ではない。

## その他

- 進行中の検討・やることリストは `../memos/` 以下。**指示があったときだけ書き換える**(指示が
  なければ読むだけ)。
