# outdated　必要になったら以下のプロンプトで再生成

main.tsからgame.tsにper frameで呼び出している処理がupdateとrenderに分かれていて、そこからサブルーチン的に他のprivate関数を呼んでいるわけですが、これらの依存関係について調べて、木構造として教えてください。例えばmain/fooがgame.barとgame.braをこの順で呼び、game.barがgame.brabraを呼んでいるときは以下のような木構造として把握したい。

- main/foo()
   - game.bar()
     - game.brabra()
   - game.bra()
 
原則、perframeでちょうど一度だけ呼ぶもの、複数個所から参照されていない関数についての調査ですが、mapmode、phase、pausedなどによって分岐する部分があれば（つまりper frameの呼び出し回数が0回から1回になるものがあれば）、呼び出し条件を備考として追記してください。多少簡略化されていても構いません。以下のように

- main/foo()
   - game.bar() // pausedがfalseの時のみ実行
     - game.brabra()
   - game.bra()

調査対象はmain.tsとgame.tsだけでなく、そこから呼び出されているplayerやmapやcameraなど各所の更新関数を含みます。典型的にはupdate~~やsync~~みたいな名前であることが期待されますが、そうでないものもあります。

いまは呼び出し順（コールスタック）の流れだけを追いたいので、引数の情報は省略してください。また、呼び出し順に対する影響が小さいもの（つまり、何らかの値を計算して返却することが主な責務になっている副作用のない関数）も除外します。

結果はCALLSTACK.mdに出してください


# per-frame 呼び出し依存木

`main.ts` の `requestAnimationFrame` ループが毎フレーム `game.update(dt)` → `game.render(dt)` の順に呼ぶところを起点に、そこから辿れる per-frame 呼び出し(副作用のあるサブルーチン呼び出しのみ)を木構造にまとめたもの。値を計算して返すだけの純粋関数は省略している。

条件分岐で 0 回になり得る呼び出しには `// 条件` を付けている。条件が無いものは毎フレーム必ず1回呼ばれる。

対象: `main.ts`, `game/game.ts`, `game/player/*`, `game/camera/*`, `game/map-mode/*`, `game/plan/*`, `game/orbit-entity/*`, `game/stages/*`, `hud/*`, `game/effects-system.ts`, `game/flash-effect-manager.ts`, `game/pip-renderer.ts`, `game/ephemeris.ts`, `game/targeter.ts`, `game/sim-speed-manager.ts`, `game/input.ts`, `render/environment-scene.ts`, `render/orbitline.ts`。

---

## 全体像

- main/animate(dt) // rAF毎フレーム
  - [game.update(dt)](#gameupdatedt)
  - [game.render(dt)](#gamerenderdt)

---

## 要約

- game.update(dt)
  - input.update() // pending キュー(キー/クリック/マウス)をこのフレーム分に確定し、次フレーム用にクリア
  - handleEdgeInput()
    - input.presses()
    - player.handleEdgeInput(presses)
    - handleEdgePress(code) // player側で未消費のキー1つにつき0〜1回
  - planSystem.updateMapModeWithPhase(isPlaying, touchControls, mapMode) → cameraSystem.mapMode に反映
    - hud.setPlanPanel(null) + hud.setMapToolbarVisible(false) + editor.closeMenu() + touchControls?.setMapMode(false) // !isPlaying && mapMode の場合のみ(ポーズ/ゲームオーバー中に地図を強制で閉じる)
  - handleFrame(dt)
    - handlePausedFrame() // activeStage.isPlaying && paused の場合のみ、以降は下のupdateFrameを実行せずreturn
    - simulator.integrateSimulation(simTime, dt, simSpeed, player, activeStage, hardCollision=false, doSubstep=false) // !activeStage.isPlaying の場合のみ(ゲームオーバー後の簡略物理)、以降は下のupdateFrameを実行せずreturn
    - updateFrame(dt) // activeStage.isPlaying && !paused の場合のみ
  - cameraSystem.updateActiveCamera(player, sunAzimuth, focusRel, input, dt, playerR)
    - mapCamera.update(mouse, keyYaw, keyPitch, dt, focusRel, sunAz) // cameraSystem.mapMode===true の場合のみ
    - chaseCamera.update(mouse, keyYaw, keyPitch, dt, origin, player, zoomActive) // cameraSystem.mapMode===false の場合のみ
- game.render(dt)
  - sync(dt)
    - environment.sync({dt, origin, displayTime, camera, sunPhase0, moonPhase0, mapMode, mapCameraFar, lit})
    - syncDynamicObjects(o, pv)
    - effects.syncFlashEffects(dt, lastSimDt, o, activeCamera)
    - syncHud(dt, o, pv)
  - pipRenderer.renderFrame(renderer, {firing, mapMode, camera, playerShipObj, setMuzzleFlashesVisible, updateOverlay})
    - [!firing || mapMode] 通常フルスクリーン描画
    - renderCombatWithPip(renderer, ctx) // firing && !mapMode の場合のみ

---

## game.update(dt)

- game.update(dt)
  - input.update() // pending キュー(キー/クリック/マウス)をこのフレーム分に確定し、次フレーム用にクリア
  - handleEdgeInput()
    - input.presses()
    - player.handleEdgeInput(presses)
      - throttle.toggleRcsDamp() // KeyT
      - throttle.enableProgradeReset() // KeyF
      - player.toggleFineAttitude() // KeyV
      - throttle.toggleProgradeHold() // KeyC
      - throttle.setThrottlePreset(0|1|2) // Digit1/2/3
      - fire.manualReload() // KeyR。リロード成功時のみキーを消費(戻り値true)
    - handleEdgePress(code) // player側で未消費のキー1つにつき0〜1回
      - cameraSystem.chaseCamera.toggleFollowAttitude() // KeyG
        - hud.hint(...)
      - simSpeedManager.shift(-1|1) // Comma/Period
        - simSpeedManager.cancelAutoWarp() // 常に(手動シフトで自動ワープを解除)
        - sfx.warp() // 上限/下限を超えない場合のみ
        - hud.hint('TIME WARP ×...') // 同上
      - planSystem.toggleMap(isPlaying, touchControls, mapMode) // KeyM
        - [開くとき: !mapMode]
          - editor.selectedNodeIdx = null (直接代入)
          - plan.markDirty()
          - hud.setMapToolbarVisible(true)
          - touchControls?.setMapMode(true) // タッチデバイスのみ
          - hud.hint(...)
        - [閉じるとき: mapMode]
          - editor.onMapClosed(plan)
            - plan.pruneNearZeroDv(C.NODE_MIN_DV)
          - hud.setMapToolbarVisible(false)
          - hud.setPlanPanel(null)
          - editor.closeMenu()
            - mapGizmo.closeMenu()
          - touchControls?.setMapMode(false) // タッチデバイスのみ
          - hud.hint(...) // plan.nodes.length > 0 の場合のみ
        - planGuide.clearActiveTarget() // toggleMap呼び出し直後、閉じた(mapMode===false)場合のみ game.ts 側から追加で呼ぶ
      - planSystem.toggleAutoWarpToFirstNode(isPlaying, mapMode) // KeyN。mapMode中は即return
        - hud.hint('マニューバノードがありません…') // ノード無し or !isPlaying
        - simSpeedManager.cancelAutoWarp() + hud.hint('自動ワープ解除') // 既に自動ワープ中の場合
        - simSpeedManager.startAutoWarpTo(node.time) + hud.hint('ノードへ自動ワープ開始') // 未開始の場合
      - planSystem.clearPlanByKey(mapMode) // KeyX
        - [mapMode: 選択中ノードのみ削除]
          - editor.deleteSelected(plan) → plan.removeNode(idx) + editor.closeMenu() // 選択がある場合のみ
          - simSpeedManager.cancelAutoWarp() + hud.hint('ノードを削除') // 実際に削除できた場合のみ
        - [!mapMode: 計画全体を破棄]
          - plan.clear() // plan.nodes.length > 0 の場合のみ
          - planGuide.clearActiveTarget() // 同上(onPlanCleared経由)
          - simSpeedManager.cancelAutoWarp() + hud.hint('マニューバ計画を破棄') // 同上
      - hud.toggleHelp() // KeyH
      - hud.toggleSettings() // Escape
      - location.reload() // KeyR。!activeStage.isPlaying の場合のみ
  - planSystem.updateMapModeWithPhase(isPlaying, touchControls, mapMode) → cameraSystem.mapMode に反映
    - hud.setPlanPanel(null) + hud.setMapToolbarVisible(false) + editor.closeMenu() + touchControls?.setMapMode(false) // !isPlaying && mapMode の場合のみ(ポーズ/ゲームオーバー中に地図を強制で閉じる)
  - handleFrame(dt)
    - handlePausedFrame() // activeStage.isPlaying && paused の場合のみ、以降は下のupdateFrameを実行せずreturn
      - sfx.setThrust(false)
      - player.pause()
        - throttle.clearTransientState()
        - fire.stopFiring()
    - simulator.integrateSimulation(simTime, dt, simSpeed, player, activeStage, hardCollision=false, doSubstep=false) // !activeStage.isPlaying の場合のみ(ゲームオーバー後の簡略物理)、以降は下のupdateFrameを実行せずreturn
      - simulationSubStep() ×1
        - ephemeris.update(simTime)
        - stepWorldOrbits()
          - stepEntities(enemies) → stepOrbitRK4() // 生存中の敵ごと
          - stepEntities(bullets) → stepOrbitRK4() // 弾ごと
          - stepEntities(casings) → stepOrbitRK4() // 薬莢ごと
          - stepEntities(debris) → stepOrbitRK4() // デブリごと
          - stepEntities(ammos) → stepOrbitRK4() // 弾薬ピックアップごと
      - stepCoastingAttitudes(simDt) // hardCollision=falseなのでここで呼ばれる
        - stepAttitude() // 敵・薬莢・デブリ・弾薬ピックアップそれぞれ
    - updateFrame(dt) // activeStage.isPlaying && !paused の場合のみ
      - simSpeedManager.update(simTime)
        - simSpeedManager.autoWarpUntil = null + hud.hint(...) // 自動ワープの目標時刻に到達した場合のみ
      - player.behave(ctx)
        - player.hpRegen(dt) // 0 < hp < maxHp の場合のみ実質意味あり
        - fire.tickMapMode(dt) → tickReloadTimer(dt) // mapMode の場合、以降スキップしてreturn
        - fire.updateFireState(dt, input, scoreCounter, simTime, simSpeed, zoomActive, addBullet) // !mapMode の場合のみ
          - tickReloadTimer(dt)
          - sfx.emptyClick() + hud.hint('弾薬切れ…') // 発射キーが押されているが弾切れの初回フレームのみ
          - fireCycle(...) // 発射キー押下 かつ simSpeed.canPlayerFire かつ弾薬ありの場合のみ
            - sfx.spinUp() // 発射開始直後の1フレームのみ(このフレームはfireGunまで進まない)
            - fireGun(...) // スピンアップ完了後、発射間隔(FIRE_INTERVAL)を満たした場合のみ
              - spawnBullet(...)
              - dropCasing(...)
              - spawnMuzzleFlash(...)
              - scoreCounter.recordShot()
              - sfx.fire()
            - consumeRound(player) // fireGunの直後
              - spawnEjectedMagazineFrame(player) // 弾倉を撃ち切った直後(magsLeft > 0)のみ
              - sfx.magFeed() // 弾倉を撃ち切ったがバレル内に残弾倉ありの場合のみ
              - dropBarrel(player) + sfx.playReload() // バレル内の最後の弾倉を撃ち切った場合のみ
                - fx.spawnBarrel(...)
        - throttle.updateThrustState(input, simSpeed, att, state) // !mapMode の場合のみ
          - sfx.setThrust(false) // 推力入力なし or !canPlayerThrust
          - sfx.setThrust(true) // 推力入力あり かつ canPlayerThrust
      - simulator.buildPlayerAccel(action.thrustFn) // 副作用のあるクロージャ生成のみ(実際の加速度計算はintegrateSimulation内のRK4評価時)
      - simulator.integrateSimulation(simTime, dt, simSpeed, player, activeStage, hardCollision=true, doSubstep=true, playerAccel)
        - simulationSubStep() ×1〜64 // ワープ段階(simSpeed > MAX_PHYS_SIM_SPEED)に応じてサブステップ数が増える
          - ephemeris.update(simTime)
          - stepOrbitRK4(player.state, dt, playerAccel) // player.alive の場合のみ
          - player.thermal.updateThermal(dt, r, v) // player.alive の場合のみ
          - stepWorldOrbits() // 上記と同じ5種のstepEntities
        - hit.checkBulletHits(nextSimTime, player, activeStage, simulator) // hardCollision=true なのでサブステップごとに呼ばれる
          - target.attacked(bullet, simTime, activeStage) // 弾がターゲットに命中した場合のみ(敵 or player)
            - [Enemy.attacked()]
              - activeStage.scoreCounter.recordHit()
              - hitEffect(bullet) // 被弾後もhp>0の場合
                - sfx.hit()
                - fx.spawnPlasmaFlash(...) // bullet.type==='plasma'
                - fx.spawnBulletFlash(...) // bullet.type==='normal'
                - fx.scatterFragments(...)
              - activeStage.recordEnemyDeath(enemy, simTime, true) // hp<=0の場合
                - scoreCounter.recordKill()
                - hud.hint(...)
                - unlockManager.reportClear(index, hud) // checkWin()がtrueになった場合のみ
                - onWin(simTime) // 同上(Stage0/00はonWinを空実装でオーバーライド)
                  - showWinScreen(...)
              - destroyEffect() // hp<=0の場合
                - sfx.explosion()
                - fx.spawnShipDestroyEffect(...)
            - [Player.attacked()]
              - hitEffect(bullet) // 被弾後もhp>0の場合
              - activeStage.recordPlayerLost(reason) // hp<=0の場合
                - showResultScreen(hud, sfx, false, ...)
              - destroyEffect() // hp<=0の場合
        - targeter.markBoardCrossings(player, simulator) // hardCollision=true なのでサブステップごとに呼ばれる
          - boardMarks.push(...) // 通常弾がターゲットのボード面を通過した場合のみ
      - player.checkLoss(dt, simTime, activeStage) // player.alive でない場合は即return
        - thermal.updateAltitudeAlarm(dt, alive, altitude)
          - hud.hint(...) + sfx.altAlarm() // 高度警告の閾値を新規に下回った場合のみ
          - checkThermalLimits()
            - hud.hint(...) // 熱/動圧が危険域に入った初回フレームのみ
        - destroyEffect() // 熱/動圧超過 or 高度がPLAYER_MIN_ALT未満の場合のみ
          - sfx.explosion()
          - fx.spawnShipDestroyEffect(...)
        - activeStage.recordPlayerLost(reason) // 同上
      - collisionPhysics.resolve(dt, player, allEntities, onClank) // simSpeedManager.canResolvePhysicalCollisions の場合のみ(高ワープ時はスキップ)
        - player.belt.collisionSections(dt, r, v, q) // player.alive && dt>1e-6 の場合のみ
        - resolveCollisionPairs()
          - resolveCollisionPair(...) // 貫入している衝突ペアごと
          - onPlayerCasingImpact() // player-薬莢ペアが衝突した場合のみ → sfx.clank()
        - player.belt.applyCollisionSections(dt, r, v, q) // player.alive && dt>1e-6 の場合のみ
      - updateAttitudes(min(simDt, 0.12))
        - player.updateAttitude(input, mapMode, attDt, onCancelHint)
          - throttle.updateAttitude(...) // player.alive でない場合は即return
            - onCancelHint() // 進行方向ホールド中に手動回転入力があった場合のみ(ホールド解除と同時)
            - autoAlignTorque(...) // ホールド中 かつ 手動回転入力なしの場合のみ
            - stepAttitude(att, torque, attDt) // 常に(姿勢積分)
        - simulator.stepCoastingAttitudes(attDt)
          - stepAttitude() // 敵・薬莢・デブリ・弾薬ピックアップそれぞれ
      - simulator.cleanup(dt, simTime, activeStage)
        - e.checkLoss(...) → Enemy.checkLoss() // 敵ごと
          - destroyEffect() // 自然再突入で失われた場合のみ
            - sfx.explosion() + fx.spawnShipDestroyEffect(...)
          - activeStage.recordEnemyDeath(enemy, simTime, false) // 同上
            - scoreCounter.recordEnemyLoss() + hud.hint(...)
        - b.checkLoss(...) // 弾ごと(副作用は alive=false 代入のみ)
        - cs.checkLoss(...) // 薬莢ごと(同上)
        - d.checkLoss(...) // デブリごと(同上)
        - ammo.checkLoss(...) // 弾薬ピックアップごと(同上)
        - prune(enemies) → e.dispose() // 死亡した敵ごと(メッシュ・軌道線をsceneから除去)
        - prune(bullets) → dispose() // 同上(弾)
        - prune(casings) → dispose() // 同上(薬莢、geometry/materialまで破棄)
        - prune(debris) → dispose() // 同上(デブリ)
        - prune(ammos) → dispose() // 同上(弾薬ピックアップ)
      - player.update(dt)
        - belt.update(dt, magsLeft, roundsInMag, att, thrustAccelVec)
          - physics.shiftBeltNodes() // リロードでフィード量が急に戻った場合のみ
          - physics.update(dt, att, thrustAccelVec, feed)
            - initNodesOnce() // 初回呼び出し時のみ
            - estimateAngularAccel(...)
            - integrateVerlet(...)
            - pinRootToAnchor(...)
            - relaxDistanceConstraints() // 内部で6回反復
            - resetIfFolded() // ベルトが折れ込んでいる場合のみ内部でリセット
            - advanceOrientationConstraints(dt, att) // リンクごとに角度クランプ・ツイスト更新
      - planSystem.updateEditing(dt, input) // mapMode の場合のみ
        - editor.updateEditing(plan, dt, simTime, origin, toDisplayFrame, input, project, opts)
          - handleMapClick(...) // input.clicks() の各クリックごと
            - mapGizmo.closeMenu()
            - editor.selectedNodeIdx = idx // 既存ノード or 予測軌道近傍をヒットした場合
            - plan.addNode({time, dv}) // 予測軌道上をクリックした場合のみ
            - sfx.warp() // ノード選択/追加時
          - handleMapRightClick(...) // input.rightClicks() の各右クリックごと
            - editor.selectedNodeIdx = idx + mapGizmo.openMenu(...) // ノード/ラベルをヒットした場合
            - mapGizmo.closeMenu() // 何もヒットしなかった場合
          - plan.adjustNodeDv(selectedNodeIdx, dvx, dvy, dvz) // ノード選択中 かつ WASDQE入力がある場合のみ意味を持つ
          - hud.setPlanPanel(...)
          - hud.setMapToolbarState(...)
      - targeter.updateCombatTargeting(player, enemies, input, activeCamera, project) // !mapMode の場合のみ
        - handleTargetLockByRightClick(...) // input.rightClicks().length>0 && player.alive の場合のみ
          - toggleLockedTarget(hit) // 右クリックが敵に命中した場合のみ
            - hud.hint('ターゲット固定解除') // 既存ロック解除時
            - hud.hint('ターゲット固定: ...') // 新規ロック時
          - hud.hint('ターゲット固定解除') // 右クリックが外れ、かつ既にロックがあった場合(ロック解除)
        - (this.autoTarget を再計算して代入。計算自体は純粋関数)
      - activeStage.update(dt, player, simulator, simTime, simSpeedManager) // 具体ステージへディスパッチ
        - behaveAllEnemies(dt, player, simulator, simTime, simSpeed) // 全ステージ共通の先頭処理
          - enemy.behave(dt, simTime, player, simulator, simSpeed) // 生存中の敵ごと(simSpeed.canEnemyFire の判定は behave 内部)
            - firePlasma(simTime, player, simulator) // バースト継続中でburstDelayが0を切った場合、または新規攻撃判定が成立した場合
              - new Bullet(...) + simulator.addBullet(pb)
        - [Stage0(訓練スコアアタック)] timer.update(dt, onTimeUp)
          - onTimeUp('timeup') → setPhase('timeup') // 制限時間到達フレームのみ
          - showScoreAttackResultScreen(...) // 同上
        - [Stage00(無限サバイバル)] waveManager.update(dt, player, enemies, addEnemyCb, simTime, logistics)
          - logistics.updateLogistics(simTime, player, respawnOnDespawn=true)
            - absorbNearbyAmmo(player) // player.alive の場合のみ
              - player.onPickup(...) + sfx.pickup() + hud.hint(...) // 範囲内の弾薬ピックアップごと
            - despawnFarAmmo(player, true)
              - spawnForPlayer(...) // 遠方化して消えた弾薬ピックアップの数だけ再スポーン(respawnOnDespawn=trueのため)
                - new Ammo(...) + addAmmo(...) + sfx.warp() + hud.hint(...)
            - spawnForPlayer(player) // LOGISTICS_CHECK_INTERVALごと、かつ低弾薬・上限未満の場合のみ
          - updateWaitingForAmmoPhase(player) // phase==='waiting_for_ammo' の場合のみ
            - hud.toast(...) // 弾薬取得後、フェーズ遷移時
          - updateSpawningEnemiesPhase(dt, player, addEnemy) // phase==='spawning_enemies' の場合のみ
            - spawnWave(player, addEnemy) // spawnTimerが0になった場合のみ
              - addEnemy(enemy) → Stage.addEnemy() → simulator.addEnemy(...) + scoreCounter.recordSpawnEnemy() // 生成された敵ごと
          - updateActiveCombatPhase(dt, enemies, player, addEnemy) // phase==='active_combat' の場合のみ
            - despawnOutOfRangeEnemies(...) // 射程外の敵ごと(alive=false + dispose())
            - spawnWave(player, addEnemy) // spawnTimerが0、かつグループ数/波数上限未満の場合のみ
        - [Stage1/Stage2(キャンペーン)] logistics.updateLogistics(simTime, player) // respawnOnDespawn=false
          - absorbNearbyAmmo(player) // player.alive の場合のみ
          - despawnFarAmmo(player, false) // 再スポーンなし
          - spawnForPlayer(player) // LOGISTICS_CHECK_INTERVALごと、かつ低弾薬・上限未満の場合のみ
  - cameraSystem.updateActiveCamera(player, sunAzimuth, focusRel, input, dt, playerR)
    - mapCamera.update(mouse, keyYaw, keyPitch, dt, focusRel, sunAz) // cameraSystem.mapMode===true の場合のみ
    - chaseCamera.update(mouse, keyYaw, keyPitch, dt, origin, player, zoomActive) // cameraSystem.mapMode===false の場合のみ
      - updateChaseView(...) // !player.alive の場合のみ(死亡時はオービタルフレーム追従)
      - updateGunsightView(...) // player.alive && zoomActive の場合のみ(KeyZズーム)
      - updateChaseView(...) // player.alive && !zoomActive && camFollowAttitude の場合のみ(姿勢追従三人称、デフォルト)
      - updateChaseView(...) // player.alive && !zoomActive && !camFollowAttitude の場合のみ(オービタルフレーム追従)

---

## game.render(dt)

- game.render(dt)
  - sync(dt)
    - environment.sync({dt, origin, displayTime, camera, sunPhase0, moonPhase0, mapMode, mapCameraFar, lit})
      - syncEarth(dt, origin, displayTime)
        - earth.setRotation(phase)
        - earth.tick(dt, displayTime)
      - syncSkyBodies(displayTime, origin, camera, sunPhase0, moonPhase0, mapMode, mapCameraFar)
        - earth.setSunDir(...)
        - sun.billboard.sync(...)
        - placeCombatMoon(...) // !mapMode の場合のみ
        - moonMesh.position.set(...) + scale.setScalar(...) // mapMode の場合のみ(マップスケール配置)
        - moonMesh.lookAt(...) // 常に
      - syncLighting(lit)
    - syncDynamicObjects(o, pv)
      - player.sync(input, cameraSystem, isPlaying, paused)
        - thrustEffects.sync(thrustVizDir, throttleIdx, alive, camera)
          - core.hide() + outer.hide() // 非表示条件(推力なし/死亡/ズーム中)の場合のみ
          - core.sync(...) + outer.sync(...) // 表示条件を満たす場合のみ
        - rcsEffects.sync(input, rcsDamp, att, alive, phasePlaying, paused, camera)
          - puff.hide() ×4 // 回転入力/減衰なし、またはズーム中の場合
          - puff.hide() // 個別スラスタの噴射力が閾値未満の場合(回転中の分岐内で)
          - puff.sync(...) // 個別スラスタの噴射力が閾値以上の場合
        - belt.sync(alive) // 各リンクの position/quaternion を直接設定(サブ呼び出しなし)
      - e.syncBulletTransform(o, pv) // simulator.allEntities() のうち Bullet のみ
      - e.syncTransform(o) // Bullet以外の全エンティティ(敵/薬莢/デブリ/弾薬ピックアップ)
    - effects.syncFlashEffects(dt, lastSimDt, o, activeCamera)
      - flashEffectManager.syncFlashEffects(...)
        - fx.billboard.sync(...) // 有効なフラッシュエフェクトごと
        - scene.remove(fx.billboard.mesh) + fx.billboard.dispose() // 寿命切れのフラッシュエフェクトごと
    - syncHud(dt, o, pv)
      - planSystem.updateDisplay(mapMode)
        - display.hide() + editor.hideGizmo() // !mapMode の場合のみ、以降スキップしてreturn
          - line.setVisible(false) + markers.hide('ghost')
          - mapGizmo.update([], null)
        - display.update(plan, state, origin, frameRotating, sliderT, project) // mapMode の場合のみ
          - line.setVisible(true) + line.setOrigin(origin)
          - plan.maybeRefresh(player, ephemeris, simTime, predictDurationSec) // 内部でスロットル(dirty時~5Hz/通常2秒間隔)、大半のフレームはno-op
          - rebuildGeometry(...) → line.refresh(segments) // trajSamplesの参照が変わった(=実際に再計算された)フレームのみ
          - markers.set('ghost', ...) // ゴーストスライダーがアクティブな場合のみ
          - markers.hide('ghost') // 同上でない場合
        - editor.updateGizmo(plan, origin, toDisplayFrame, project, mapDist) // mapMode の場合のみ
          - mapGizmo.update(nodeSpecs, axisSpecs) // 常に(ノード/軸ハンドルDOMの再構築)
        - mapHud.updateLabels(...) // mapMode の場合のみ
          - hud.markers.set(id, 'poi', ...) // 地球/月/太陽/ラグランジュ点の各ラベルごと(画面内/画面外で表示形態が変わる)
      - syncEntityOrbitLines(o, pv, mapMode)
        - player.orbitLine.update(playerEl|null, o, thrustVizDir!==null, true)
          - regenerate(el, focusE) // 要素がしきい値以上ドリフトした場合、または初回のみ
        - enemy.orbitLine.update(...) // 敵ごと。mapMode && enemy.alive && enemy!==target の場合のみグレー表示、それ以外は非表示
      - targeter.updateOrbitLine(o)
        - orbitLine.update(tgtEl, origin)
          - regenerate(el, focusE) // 有効なターゲット軌道があり、要素ドリフト/初回条件を満たす場合のみ
      - ephemeris.updateReferenceLines(simTime, o, mapMode)
        - geoLine.update(null, origin) + moonLine.update(null, origin) // !mapMode の場合のみ(非表示化)
        - geoLine.update(GEO_ELEMENTS, origin, false, false) + moonLine.update(moonOrbitElements(simTime), origin, false, false) // mapMode の場合のみ
      - planGuide.updatePlannedLine(plan, ephemerisCtx, o, mapMode)
        - plan.maybeRefresh(...) // !mapMode の場合のみ(この後planGuide.updateからも同一引数で呼ばれるが、スロットルにより実質2回目はno-op)
        - plannedLine.update(elements|null, origin) // 常に(白い計画軌道ラインの表示/非表示)
      - markersSystem.updateMarkers(markerCtx, pv, project)
        - updateMapModeMarkers(mapMode, mapLabelIds, project)
          - markers.hide(方向系マーカー各種) + markers.set('self', ...) // mapMode の場合のみ
          - markers.hide('self') + markers.hide(mapLabelIds各id) // !mapMode の場合のみ
        - updateOrbitalDirectionMarkers(target, o, pv, project) // !mapMode の場合のみ
          - markers.set(pro/retro/nrm/anm/radout/radin) // 常に(このブランチ内)
          - markers.set(tgtdir/atgdir) // ターゲットが存在する場合のみ
          - markers.hide(tgtdir/atgdir) // ターゲットが存在しない場合
        - updateBoresightMarker(player, mapMode, project)
          - markers.set('bore', ...) // player.alive && !mapMode の場合のみ
          - markers.hide('bore') // それ以外
        - updateEnemyMarkers(enemies, target, o, project)
          - markers.hide('e'+i) // 非生存の敵ごと
          - markers.set('e'+i, ...) // 生存中の敵ごと(画面上で近接する敵はクラスタ化しラベル省略)
        - updateAmmoMarkers(ammos, o, project)
          - markers.hide('mg'+i) // 消費/非生存の弾薬ピックアップごと
          - markers.set('mg'+i, ...) // 生存中の弾薬ピックアップごと
        - updateLeadAndDirMarkers(ctx, o, pv, project)
          - [!mapMode && player.alive] 敵ごとに:
            - markers.hide('lead-'+name) + markers.hide('dir-'+name) // 敵が非生存の場合
            - updateOffscreenDirMarker(...) → markers.set('dir-'+name, ...) // 画面外の場合のみ、画面内ならhide
            - updateLeadMarker(...) → markers.set('lead-'+name, ...) // 直近ロック中 かつ 有効なリード解がある場合のみ、それ以外はhide
          - [mapMode || !player.alive] 敵ごとに markers.hide('lead-'+name) + markers.hide('dir-'+name)
        - markers.hide('lead') // 常に(旧単一リードマーカーキーの後始末)
        - markers.resolveCollisions() // 常に(ラベル衝突緩和 + SVGリーダーライン再描画)
      - markersSystem.updateNodeMarkers(player, playerEl, tgtEl, project)
        - markers.hide('an') + markers.hide('dn') // playerElかtgtElがnull、または軌道面がほぼ一致してノードが定義できない場合
        - markers.set('an', ...) + markers.set('dn', ...) // 有効な昇交点/降交点が計算できた場合のみ
      - targeter.updateBoardMarkers(player, dt, project)
        - hud.markers.hide('bh'+i) // ターゲット無し、または該当スロットに有効なマークが無い場合
        - hud.markers.set('bh'+i, 'mk-boardhit', ...) // ターゲットが存在し、有効期限内のマークがあるスロットのみ
      - hud.markers.hide('burn') // mapMode の場合のみ
      - planGuide.update(plan, ephemerisCtx, o, pv, playerEl, player.alive, project) // !mapMode の場合のみ、戻り値 {achieved}
        - hud.markers.hide('nd') + hud.markers.hide('burn') + hud.setPlanPanel(null) // ノード無し or !player.alive の場合のみ、以降スキップしてreturn
        - plan.maybeRefresh(...) // 実質スロットルによりno-op(updatePlannedLineと同一フレーム2回目の呼び出し)
        - hud.markers.hide('nd') + hud.markers.hide('burn') // 解決可能なターゲットが無い場合のみ、以降スキップしてreturn
        - [達成時 achieved===true]
          - plan.consumeFirstNode()
          - hud.markers.hide('nd') + hud.markers.hide('burn')
          - hud.setPlanPanel(null) + hud.hint('✓ マニューバ達成…') // 全ノード消化時
          - hud.hint('✓ ノード達成…') // ノードが残っている場合
          - sfx.warp()
        - [未達成時]
          - hud.markers.set('nd', 'mk-mnode', '◆', ...)
          - hud.markers.set('burn', 'mk-burn', '⬢', ...)
        - simSpeedManager.cancelAutoWarp() // achieved===true の場合のみ(呼び出し元のgame.tsから)
      - hud.panels.update(this, dt, playerEl, tgtEl)
        - touchControls?.setActive('KeyT'/'KeyV'/'KeyC', ...) // touchControls が存在する場合のみ、呼び出し自体は毎フレーム
        - setStats(d) // hudTimer<=0 の場合のみ(約10Hzにスロットル)
          - DOM各要素の textContent/classList 更新(met/simSpeed/rcs/throttle/fine/camfollow/prohold/ammo/alt/spd/ap/pe/inc/prd/q/t/count)
          - stage0El関連の表示切替 // hudSubStatus !== null の場合のみ(Stage0特有のサブステータス)
        - setTarget(data|null) // hudTimer<=0 の場合のみ(setStatsと同じスロットル)
        - setEnemyList(rows) // listTimer<=0 の場合のみ(約4Hzにスロットル)
      - hud.tick()
        - hint.style.opacity='0' // ヒントのタイムアウトが切れたフレームのみ
        - toast.style.opacity='0' // トーストのタイムアウトが切れたフレームのみ
  - pipRenderer.renderFrame(renderer, {firing, mapMode, camera, playerShipObj, setMuzzleFlashesVisible, updateOverlay})
    - [!firing || mapMode] 通常フルスクリーン描画
      - updateOverlay(null) → markersSystem.updatePipOverlay(target, player, activeCamera, null)
        - markers.hide('pip-tgt') + markers.hide('pip-lead') // 常に(rectがnullのため)
      - renderer.render(scene, camera)
    - renderCombatWithPip(renderer, ctx) // firing && !mapMode の場合のみ
      - renderer.render(scene, cam) // 通常カメラでのベースパス
      - カメラをプレイヤー機首方向・ZOOM_FOVへ再配置
      - playerShipObj.visible = false
      - setMuzzleFlashesVisible(false) → effects.setMuzzleFlashesVisible(false)
        - fx.billboard.mesh.visible = false // muzzleフラグ付きフラッシュエフェクトごと
      - renderer.render(scene, cam) // PIPビューポートへのシザー描画パス
      - updateOverlay(rect) → markersSystem.updatePipOverlay(target, player, activeCamera, rect)
        - markers.hide('pip-tgt') + markers.hide('pip-lead') // rect/ターゲット無し、またはプレイヤー非生存の場合
        - markers.set('pip-tgt', 'mk-target', '◇', ...) // 有効なターゲットがある場合のみ
        - markers.set('pip-lead', 'mk-lead', '✛', ...) // 有効なリード解(t<25s)がある場合のみ
        - markers.hide('pip-lead') // 有効なリード解が無い場合
      - (finally) カメラ姿勢・playerShipObj.visible を元に戻す + setMuzzleFlashesVisible(true) → effects.setMuzzleFlashesVisible(true)
        - fx.billboard.mesh.visible = true // muzzleフラグ付きフラッシュエフェクトごと

---

## 補足

- `plan.maybeRefresh(...)` は `planGuide.updatePlannedLine()` と `planGuide.update()` の両方から同一引数で呼ばれるが、`Plan` 内部の壁時計スロットル(`PREDICT_DIRTY_THROTTLE_MS`/`PREDICT_REFRESH_INTERVAL_MS`)により、実際に `predictTrajectory()` が走るのは1フレームにつき最大1回。mapMode中は `planSystem.updateDisplay()` → `PlanDisplay.update()` からのみ(別のdurationポリシーで)呼ばれる。
- `simulator.integrateSimulation()` は毎フレーム呼ばれるが、`activeStage.isPlaying` の真偽で `hardCollision`/`doSubstep`/`playerAccel` の有無が切り替わる2つの呼び出しサイトがある(上記ツリーではどちらも展開して記載)。
- 高ワープ時(`simSpeed > C.MAX_PHYS_SIM_SPEED`)は `simulationSubStep()` が1フレームに最大64回まで実行される。
- `TouchControls` は per-frame の update メソッドを持たず、DOMイベント(`pointerdown`/`pointerup`)経由で `input.setVirtualKey()` を呼ぶだけなので、このツリーには独立ノードとして登場しない。
